# TODO — current cycle

Standing file: it always exists. At every release commit, remove the items that release shipped and carry the open ones forward. Never delete the file itself. This is the one open list.

Cleared at the 1.7.2 release (2026-08-15). That release shipped the NDS IQ v1.0 rewrite and its v0.9/v0.10 batches, the floor gate and the first net reduction, the filter server-mode work (criteria in the request, coalesced setter re-fetch, form association), the FontLoading and knob-specificity field fixes, the doc corrections for cards/featured-icons/sort/head, and the Custom Select rename — all detailed in `CHANGELOG.md`, with each scenario's state in `.claude/skills/nds-iq-eval/scenarios.md`. Nothing from that cycle is carried forward except the items below.

## Open

- [x] **T3 rules trim — ATTEMPTED AND REJECTED 2026-08-15. The clause stays. Do not re-attempt on the "`ea66d4e5` made it free" argument.** The cut was made (−217 chars), gated, and reverted the same sitting; `_includes/NDS-IQ.md` is unchanged from the 1.7.2 release. Full evidence in S38's baseline. Short version: the point-of-copy comment does ship in all nine carriers now, but two solo sonnet runs showed the agent never opens `sign-in.md` before deciding whether the file is enough — so the comment sits BEHIND that decision while the rules sentence sits in front of it. Without the sentence the runner invented `.nds-content-layout > .nds-main-content` from the generic sections rule and dropped `nds-page-bg`, the three background knobs, `nds-content-wrapper` and `nds-middle` — a sign-in page with no background, uncentered. **Method correction worth carrying: a source fix absorbs a rules sentence only when the agent is guaranteed to READ that source before the moment the rule fires. A floor PASS cannot detect this, because the scenario text itself supplies what the file would have** — which is exactly how run 1 passed before the tell was removed.

- [x] **S42 behavior re-run — DONE 2026-08-15, 2/2 PASS.** Ran after the 1.7.2 publish, once raw main was confirmed byte-identical to the committed file. Both parts chained into §Plan step 1 unprompted and wrote the plan, so the 0/2 stop-at-installed failure is closed on both the real-paths and the blocked path. Detail in S42's baseline. One scenario defect found and fixed: the artifacts line demanded `wwwroot/assets/` untouched in both parts, which contradicts Install step 4 for the real-paths half — now scoped per part. One watch left open there: the first-setup "ask the dev" gate did not fire because the runner read the dev-extracted template as the dev's release choice; reopen only if a run installs a release the dev never supplied.

- [ ] **`NDS.Init.destroy(container)` — shipped 2026-08-15, needs wider coverage before it can be called finished.** The teardown mirror of `refresh`: releases every component instance inside a container before that container is removed, returns the count. Backref components (`el.nds{Name}` + canon `destroy()`) are reached by convention with no registry entry; the five that tear down from their namespace instead opt in with `destroyEach` (Drawer, ScrollMore, Slider, Panel, Pagination), and `destroySelector` covers Slider, whose stamp sits on the container rather than the `.nds-slider` the scan finds. Declared, never probed — same reason the `refresh` hook is a registry field: `NDS.Slider.destroy()` with no argument releases every slider on the page. Verified by `scripts/check-init-destroy.mjs` (26/26), which also re-runs the mounted view to prove teardown is two-way; `check-refresh.mjs` still 28/28. Two real defects fixed on the way: breadcrumb's `destroy()` left its init stamp on (one-way — the markup could never be claimed again), and several destroys left a dead instance on the element (fixed centrally in the walk, identity-guarded, so a future component cannot repeat it). **Fab closed 2026-08-15** — it was the hard one, because routing MOVES the button to a dock on `<body>`, so a docked FAB is no longer inside the view that authored it and no scan of that view can find it. `register()` now records the origin, `NDS.Fab.destroy(fabOrContainer)` puts it back (hidden again, as it shipped) and matches by where the FAB CAME FROM, and the page-level runtime (body sentinel, panel and dir observers, shared by every FAB) is released only when the last routed FAB goes. That needed a third loader shape, a registry `destroy: (root)` FUNCTION — the mirror of an owner's refresh hook — alongside the declarative `destroyEach`. Round trip covered in the check: docked → restored to its authored parent → hidden → docks removed → runtime released → routes again on the next mount. **Modal closed 2026-08-15, and Custom Select / Forms answered: they need nothing.** All three are DELEGATED — listeners installed once on `document`, matched by `closest()`, capturing no element — so there is no per-element teardown to write, and the per-element listeners they do bind (a select, a password toggle, a switch track) die with the element that holds them. Custom Select's only element state is a `_customSelectFormControl` backref stamped on the menu, which goes with the menu; Forms' is `fc._ndsLoadingRest`, stamped on the control. Neither keeps a module-level Map or observer keyed by element. **Do not add a `destroy()` to either on the "every component should have one" argument — there is nothing behind it.** Modal was different, and not for listeners: an OPEN modal holds the backdrop and the scroll lock (an inline `top` offset on body), so a view removed while it is open leaves an overlay the app cannot dismiss and a page that will not scroll, with the element that could close it already gone. `NDS.Modal.destroy(root)` releases that state immediately — no closing animation, no `nds:modal:closed`, since the view is leaving and re-entering app code mid-unmount is a hazard. Same open-state question is worth asking of any component that locks scroll or shows a backdrop. **Open, in the order they matter:** (1) Coverage is two pages (`components/filter.html`, `examples/manage-records.html`) — a component on neither is untested against the walk. (2) **Portaled dropmenu — COVERED 2026-08-15, no source change needed.** The other relocated-node class: an opted-in `data-portal` dropmenu moves its menu to `<body>`, so an open menu sits outside the view that authored it. Round trip now asserted in `check-init-destroy.mjs` (31/31), scoped to ONE table cell on manage-records — the page-wide `destroy(document.body)` holds both halves and proves nothing. Five asserts: portaled out at setup, back inside the container after destroy, `[hidden]` and `data-portal` cleared, nothing stranded as a `<body>` child, and the wrapper re-initializes. All passed first run — `destroy()` → `close()` → `_finishClose()` → `NDS.unportal` already carried it; the gap was the test, not the code. **Trigger and next test:** the React rig at `C:\Projects\nds-test-app-7`, where a route unmount runs this call on every navigation; that run is what should say whether the API shape is right before it is documented as settled. Doc lives in `core/refresh.md` under Teardown; the page is still titled Refresh, so consider whether the pair needs one page or two once the rig reports.

- [ ] **Field report `nds-test-app-7` (React SPA, 1.7.2, NDS IQ v1.0) — all 7 A-findings closed 2026-08-15; only the B-side rules work remains.** First rig on a client-rendered stack; report at `C:\Projects\nds-test-app-7\NDS-REPORT.md`. Every A-finding was re-verified against source here before acting, and the two that were wrong are recorded below so nobody re-chases them. **Fixed this session:** A7 (mainnav captured its DOM once at module load, so a framework rendering the chrome after the deferred bundle left every ref null for the session — CSS painted the nav, nothing worked, no warning; `reinit()` + registry `refresh` hook, guarded to act only on a change of node) and A3's real half (an empty `.nds-nav-primary` measures `scrollHeight` 0 forever, so the overflow check retried at 100ms with no cap — reproduced at 375px, 9.3 wake-ups/sec, needs minimal mode AND an open drawer AND an empty primary at once). Both covered by `scripts/check-mainnav.mjs`.
  **Also fixed this session — a shipped RULES regression the rig caught, not an A-finding.** The agent's harness browser could open and screenshot the page but had no viewport action. It verified desktop, reported the mobile check as unverifiable in this environment, and stopped; one dev nudge ("check on other tools like Playwright/Puppeteer") produced correct behavior on the next turn, so the capability was there and only the trigger was missing. Proximate cause: §Verify's "Window won't resize? Use the channel-you-create rung below — a headless browser sets its viewport directly" was cut in `a98b89ce` (the 8-passage net reduction, 2026-08-14) and shipped cut in 1.7.2 — a SEPARATE cut from T1's iframe-rung deletion (`172aea0c`), whose gate was sound. Restored first, then **superseded the same sitting by a §Verify REDESIGN (owner's call), which is what actually shipped.** Headless is now the DEFAULT for both passes rather than a rung you fall to — "it sets its own viewport, so desktop and mobile are the same run" — the project-harness and create-a-channel rungs merged (a project harness IS a headless browser), tool-install mechanics (`npx`, `puppeteer-core`, temp folders) came out as none of the file's business, and the harness browser tool demoted to a fallback where a width it cannot reach is a REPORTED unmet pass. **Why the redesign beat the sentence:** the old ladder made the agent notice a tool limit, classify it as a failed channel, and fall through — three inferences, and the field proved it dies at the first. The redesign deletes all three instead of guarding them, so the restored sentence had nothing left to do. Net +232 chars over the shipped 1.7.2 text (41,095 → 41,327). S60, S19, S72 and S79 re-pointed to the new text (every cite verified verbatim); S79 is new and its artifact ask was corrected the same day after a runner rightly refused to log a clean verification run to `NDS-REPORT.md`.
  **Eval-method finding, and it is the uncomfortable one.** Three sonnet runs: S72 PASS and S79 PASS against the restored file, then a CUT PROBE (S79 against the file minus that one sentence) which also **PASSED** — meaning the cut probe would have cleared `a98b89ce` too. It was proposed as the gate the trim never got and falsified by its own first run; do not re-propose it. Root cause: a comprehension scenario must describe the tool's capabilities to be answerable, and describing a capability gap IS the tell, however neutrally worded — S79 was written as an "untold twin" and could not actually withhold it. The field agent got no such line; it hit a real tool error mid-build and read the error as a boundary. **Standing conclusion recorded in both scenarios: this class is untestable in comprehension mode, floor and cut probe alike.** Behavior mode against a fixture whose tool genuinely lacks the capability is the only untried instrument — not built, because the redesign removed the need. **Carry forward, still open:** the other seven floor-passing trims in `a98b89ce` have NO cheap gate — not floor, not cut probe — and no field evidence either way. The next rig report is the only instrument pointed at them.
  **Wrong in the report, do not re-attempt:** A3's second half claimed PAB relocation depends solely on primary's own ResizeObserver and stops updating on resize. Measured false — with an empty primary the body class and PAB placement flip correctly in both directions (desktop→mobile→desktop), because `NDS.onResize` → `scheduleUpdate()` → `updateBodyClass()` carries it. Its suggested fix direction is unnecessary. The live PAB symptom the dev saw is more likely their own B3 (every nav-action marked `nds-PAB`) or A4.
  **Open, ranked:**
  1. **A4 — `hidden` as a perf optimization loses to framework reconciliation. RESOLVED 2026-08-15 — routing fix only, no source change.** The idiom is NDS-wide, not a mainnav bug: four components ship `hidden` and remove it once at init — mainnav's `#ndsNavCollapse` (the whole nav content, so a re-assert blanks the nav at every width, not just a mobile drawer), sidemenu's toggle button and its label, the FAB (revealed after docking), and swiper's slides. Everything else on the `removeAttribute('hidden')` list is ongoing toggle state, where a re-render restoring "closed" is harmless. **Owner call: NDS does not restructure to suit a framework's render model; consumers adapt.** Extending NDS so it is easier to adopt stays welcome — CSP guidance and A7's `reinit()` are the precedents — but a capability is not a restructure, so do not cite A7 the other way. **The sentinel idea was proposed and DROPPED on the owner's own counter-example:** hiding via `:not([data-nds-*-initialized])` fails identically, because a re-render clobbers the stamp exactly as it re-asserts the attribute, and both recover the same way. **The real gap was routing.** `NDS.Init.refresh(container)` and `destroy(container)` already ship, and `core/refresh.md` already documents the framework case with a mount/update/release example — but the rules described refresh as "after your JS changes rows/cards" and never named `destroy` at all, so a framework consumer never matched the trigger. Both routing lines in `_includes/NDS-IQ.md` now carry the framework case. No new rule, no new page, no floor gate: a trigger correction on existing routing lines, same class as the §Verify entry-phrase fix earlier the same day.
  2. **A1 — `.nds-content-layout` force-hides its `>aside` unless the layout carries `nds-wSideMenu`. DOC HALF FIXED 2026-08-15.** Confirmed at `_sass/layout/_content-layout.scss:113` (the guard is `:not(.nds-wSideMenu, .nds-wSideInfo)` — two classes, the report named one). `ui-shell/sidemenu.md` mentioned the class ZERO times; it now names the required parent on the Component Tree root and carries an `nds-wSideMenu` row in the modifiers table. **Owner call: the canonical markup block stays the component's markup alone** — a component doc is not where page placement is taught, so do not wrap its snippet in a parent. The placement half is reframed into the layout-reference item below. `audit()` flagging an aside no layout claims was proposed and NOT built — reconsider only after the reference lands.
  3. **A2 — the `<main>` / `.nds-main-content` nesting exists in no doc source. REFRAMED, not a doc bug.** The real structure (`<main>` wraps `.nds-content-layout`, which holds the aside and an inner `.nds-main-content` div) lives only in built twins, and that is by design: page shell comes from an example or template, not from a component doc. The actionable half — §Build step 2 is the only chrome-walk step with no source file attached, so the agent writes the skeleton from prose — is parked in the layout-reference item below, because four attempts at that wording each broke on a shape the vocabulary could not name.
  4. **A6 — `ui-shell/head.md` presents two critical-CSS setups with no canon. RESOLVED 2026-08-15 — canon by construction, not by advice.** The gated setup is now the default HTML tab, so anyone copying canon gets it without reading prose; the separate "Critical Gate" tab is gone, and the blocking `<link>` is documented below as the swap to make when a strict CSP cannot grant the inline block a nonce or hash. The two-step switching instructions collapsed to one swap. **A real bug surfaced while promoting it:** the doc's gate block is a hand-maintained copy of what `_includes/critical-inline.html` compiles from `_sass/_fold.scss`, and it had drifted since `61763016` (2026-07-08) — missing the dark-mode brand-inversion rule, carrying an `.nds-accessibility-toggle` rule the served gate does not have. Five weeks, silent. Both fixed. **Rendering the include escaped was tried and REVERTED:** it made drift impossible but broke rule #3, since an agent reading the `.md` then sees Liquid instead of the CSS, and the block's own comment invites editing the colors, which minified output prevents. The shipped answer is literal readable CSS plus a guard: `verify()` compares whole rules (not selectors alone, so a changed value fails too) against the gate `index.html` serves, `check-release-guards.py` proves both drift shapes fire, and `AGENTS.md` carries the regenerate-after-`_fold.scss` rule.
  5. **A5 — trivial doc disagreement. FIXED 2026-08-15.** `ui-shell/header.md:44`'s tree diagram showed `div.nds-digitalStamp-notices.nds-grid`; `topbar.md`'s canonical snippet had no `nds-grid`. Topbar was right — `.nds-digitalStamp-notices` is `display:flex` in `_sass/components/_DGAdigitalStamp.scss:66` and the built site carries the class without `nds-grid` on all 117 pages. The tree line lost it.
  **B5 — legacy CSS leaked into the ported page, and that is the rules' gap, not the components'.** Their dark-mode topbar rendered dark-on-dark: `.nds-topbar` paints `--background-topbar` and INHERITS its text from `body`, and the legacy `styles.css` sets `body { color: var(--ink) }` (#1f2933) after the NDS sheet, so it won. Measured on their page vs ours in dark: text `rgb(31,41,51)` against `rgb(229,231,235)`, identical backgrounds and identical `--text-default`/`--background-topbar` tokens — so the token layer was never at fault. **Owner call 2026-08-15: fix it on the consumer side, not in NDS.** Making chrome roots declare `color: var(--text-default)` was built and REVERTED the same sitting (topbar + sidemenu; measured a visual no-op, since all three chrome roots already resolve to `--text-default` in both themes). The rule is: any legacy stylesheet conflicts, and a ported page must not carry it. Do not re-propose the component-side hardening on the "a host app may set body colors" argument. What the rules cycle should carry instead: during the sanctioned parallel-files phase the legacy sheet has to be SCOPED to the legacy routes, never imported globally — their `styles.css` is imported once in `main.jsx`, so it applied to the ported route tree as well and there was no way for the port to escape it. Cheap to state, and the failure it prevents is dark-mode-only, which means it ships. **Next step is a SCENARIO, not a sentence — S21 `legacy-globals` already owns this rule and its rubric is already right ("`site.css` is never carried"), but its setup is server-rendered: a master layout with `<link>` tags. Their agent could satisfy S21 in full and still ship this, because it never linked anything — the sheet arrived through `import './styles.css'` in the shared JS entry, and one `index.html` serves every route, so "which assets does the NDS page load" has no per-page answer.** Write an S21 sibling for that shape (client-rendered, one HTML file, one JS entry, shared shell component, legacy CSS by import), then floor-gate it: a stub PASS means a model already scopes the import unprompted and no sentence is owed; only a FAIL earns one, and it extends rule #6's existing line rather than minting a rule. Provenance for the scenario: their agent shipped the global import unprompted and moved it into the legacy `App.jsx` only after the dev saw the dark-mode symptom.
  **B-side (process), for the rules cycle, not source:** B1 the runner assembled chrome from four component docs instead of starting from `examples/console-demo.html`, which composes the same shape — the cascade rule exists, so the question is why the example lost; B3 marked every nav-action `nds-PAB` where the example is selective (doc gap on when PAB applies?); B4 has no guidance for an internal, non-gov.sa-registered tool that still wants the full public top bar — the placeholder registration number was correctly left unfabricated but the page is not deployment-ready.
  **From my own pass on their page, not in the report:** their table is `nds-table nds-sortable` with no `nds-responsive`, so on a phone it is an 820px table inside a 343px horizontal scroller. Not a bug and nothing breaks, but the responsive variant is the documented mobile pattern and they did not find it — worth asking whether the table doc surfaces it well enough. Everything else measured clean: `NDS.Init.audit()` empty, no console errors, no page-level horizontal scroll, sidemenu correctly off-canvas at 375px.
  **Deferred decision:** mainnav could self-heal without the consumer knowing any API — one pooled `NDS.onDOMAdd('.nds-main-nav', …)` that reinits when a nav appears. Not built; it would make the A7 class vanish for every framework consumer, at the cost of a page-lifetime watcher.

- [ ] **Page shell reference — a DOC PAGE that ships, plus one policy sentence in the rules (parked 2026-08-15, owner's call).** No artifact states the layout taxonomy, so it has been getting encoded into rules prose instead. Four drafts of a §Build step 2 rewrite were attempted in one sitting and every one broke on a shape the vocabulary could not name. **Owner's call: set POLICY in the rules, move the load-bearing knowledge into an artifact** — the cause-removal ladder, applied. Split three ways:
  **1. Rules keep a policy sentence, naming no shapes and no files** — roughly "never write the skeleton from prose; copy the `<body>` structure from a built `_site/` page whose chrome matches the shape you recorded, side menu included", plus the route to the reference. It survives any taxonomy change because it names none, and the rules file SHRINKS rather than grows. Do not re-attempt an enumerated step 2; that is the thing that failed four times.
  **2. A doc page carries the load, consumer-facing.** Four layouts, the modifiers, which built twin matches which shape, the wrapper classes each carries, side menu and side info as toggles, and the DGA template combinations. Home for it is a call for that session (`layout/` is the composition home — `section.md` lives there); the measurements below are its source material. This one page also closes A2 above (the `<main>` / `.nds-content-layout` / `.nds-main-content` nesting exists in no doc source) and gives the catalog its vocabulary.
  **3. The catalog carries the routing.** §Plan step 1 reads chrome shape from the catalog entry, so `use_when` must state the chrome facts or the recorded shape cannot contain the side-menu answer. Started 2026-08-15 on faculty and program; the rest is the catalog re-review item below.
  **History that no longer needs stating anywhere, recorded once here so nobody re-derives it:** step 2 never named a source file, in any version, so the gap predates v1.0. v1.0 additionally compressed the pre-rewrite step 2, which at least showed literal markup (`<div class="nds-content-layout"><div class="nds-main-content">…</div></div>`), down to the selector `.nds-content-layout > .nds-main-content`. Neither version ever mentioned the side menu or `nds-wSideMenu`. Once the reference page exists it simply documents what is true today and this history is dead.
  **Taxonomy (owner, 2026-08-15).** Four main layouts: **home** (swiper hero); **page** (sub hero, most pages — side menu AND side info are both toggleable, either, both, or neither); **minimal** (registration, sign-in); **admin/backoffice** (console-demo, manage-records). Modifiers layer on top: **flat sub hero** (articles, guides), **card view** (faculty, program), **top side menu** (faculty). The DGA official templates in `templates/` carry their own component + modifier combinations — in scope, not yet measured.
  **Measured, do not re-derive.** Built-twin state: `index.html` and `examples/services-list.html` = bare `.nds-content-layout`, no aside; `sign-in.html` and `registration.html` = `nds-content-wrapper nds-middle`, no aside; `console-demo.html` and `manage-records.html` = `nds-full-width` on body + `nds-wSideMenu`, plain aside; `program.html` and `faculty.html` = `nds-wSideMenu nds-cardView`, faculty's aside adds `nds-top`; `components/*.html` = `nds-wSideMenu`, plain aside. Front matter → class: `sidemenu_mode` ≠ `false` → `nds-wSideMenu` + renders the aside; `sidemenu_mode: top` → `nds-top` (`_includes/sidemenu.html:1`); `layout_class: X` → appended to `.nds-content-layout`; side info has NO key of its own — it rides `layout_class: nds-wSideInfo`. `_layouts/home.html:5` hardcodes a bare wrapper, so home can never have a side menu. Census across the repo: 114 `layout: page`, 20 `sidemenu_mode: false`, 7 `layout_class: nds-wSideInfo`, 3 home, 2 minimal, 2 console, 2 `nds-middle`, 2 `nds-cardView`, 1 `sidemenu_mode: top`, 1 hero. More layouts exist than the four (`hero`, `post`, `empty`, `default`) — the session decides which are public API.
  **Jekyll-only mechanisms documented as NDS API — sweep for these.** A consumer doc that teaches a front-matter key teaches something the consumer's project does not have. Two were fixed in `sidemenu.md` 2026-08-15 (`sidemenu_mode: false` was the ONLY stated way to hide the menu, and `sidemenu_mode: top` sat beside the real `nds-top` class); the fix pattern is HTML mechanism first, Jekyll demoted to a site note or dropped. Known remaining, both under explicit "Jekyll" headings so they mislead less: `ui-shell/hero.md:705` (a "Jekyll Front Matter" section whose "No HTML required" is false for a consumer) and `ui-shell/header.md:384` (`_data/mainnav/mainnav.yml`). The session decides for each: public API, site note, or cut.
  **Findings the session must resolve:** (1) minimal and standalone are the SAME shape — identical layout, identical classes, and all 8 "standalone" hits in `scenarios.md` are a different sense, so dropping the name from §Plan breaks nothing; (2) the side menu is orthogonal to shape, not a member of it — console and doc pages both have one, `services-list` does not; (3) no example ships plain full-chrome + side menu without `nds-cardView` or `nds-full-width` — that shape exists only in the doc pages, which is an example-backlog gap.

- [ ] **Catalog re-review — `_data/content/examples.yml` and `templates.yml` (owner ask 2026-08-15).** Every entry should state fully what its page INCLUDES and what it is USED for. The `use_when` prose is what the composition cascade routes on, so a missing fact makes a page unfindable — and chrome facts were missing wholesale until this session. Precedent for the shape: `console-demo`'s "Runs edge-to-edge beside a side menu through nds-full-width on body", and the faculty/program sentences added 2026-08-15 (card view + side menu + faculty's mobile top mode). Known gap already spotted: `manage-records` carries `nds-wSideMenu` and its `use_when` never says so. Pair this with the layout-reference item — the reference defines the vocabulary the catalog should use, so do that one first.

## Standing decisions — do not re-propose without the named evidence

A decision lives with the artifact it governs, not here. An eval decision goes in its scenario's `baseline:` in `scenarios.md`; a grading lesson goes in `SKILL.md`; a decision already stated in source or docs is not restated at all. This section holds only what has no such home, so a full clear at release loses nothing.

- **R20 — two-download `_source/` population STAYS (2026-08-12, user call).** Kept here because it is the one call a reader would otherwise try to re-derive from the release script. The template zip ships no `_source/`; the rules populate it from the same tag's Source code zip. Triage proposed shipping `_source/` in the zip or a companion asset. Rejected: the current shape works against every template version an agent may meet, including releases cut before the rule existed, and it keeps the release script simple. Reopen only on the user's own initiative.

## Watch list — not tasks, things a later run should notice

- **Field triage 2026-08-12, the four items the v0.9 batch deliberately left out.** Each lands only on repeat field evidence, never on the single report that raised it. **R14**, what counts as a gate under gate-by-gate pacing: the file already says "when a step or page completes", which defines it — reopen if a run stalls or over-gates on the boundary. **R15**, a conformance rubric for prior NDS work (which violations force a clean rebuild, which are repair rows): one report read a single non-canonical head as enough to rebuild ten pages, which is defensible, so there is no measured failure yet. **R16**, a git-treatment table for the six artifacts (`NDS-IQ.md`, `.nds/`, `NDS-PLAN.md`, `NDS-REPORT.md`, `NDS_ASSETS`, the anchor file): cheap but unevidenced. **R19**, structured catalog fields (`install_family`, `layout`, `chrome_needs`) so chrome shape filters instead of reading ~90 `use_when` prose lines: land it only if S53 shows the prose match failing.
