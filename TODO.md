# TODO — current cycle

Standing file: it always exists. At every release commit, remove the items that release shipped and carry the open ones forward. Never delete the file itself. This is the one open list.

Cleared at the 1.8.1 release (2026-08-18). That release shipped the CSP inline-style fixes, Filter's target-id reclaim, the field-status cut to error and help, the `location.hash` fix, the `lang`/`dir` audit check, the framework-view lifecycle contract, the Page Shell framework-wrapper and modifier-timing notes, and NDS IQ v2.2 — plus all three field reports (`nds-test-app-7` cycles 2 and 3, `sso-rig` cycle 2) worked end to end. Detail is in `CHANGELOG.md`, and each scenario's state is in `.claude/skills/nds-iq-eval/scenarios.md`.

## Open

- [x] **Eval suite rebuild — small-fixture composite rigs — DONE 2026-08-20 (`e6edb1ce`, `fe7736be`).** 7 composite rigs runnable (R1 install, R2 plan, R3 build, R4 verify, R5 upgrade, R6 lifecycle, R7 legacy-port), mechanical graders in `fixtures/tools/grade/`, every state leak-audited, and evidence classes stamped in the index as `ev:` flags. The full pass ran against the candidate the same day; two field rigs followed on 2026-08-21. Original direction, kept for the reasoning: convert behavior-gradable scenarios to composite runs on the SMALLEST fixture that fires them, graded by mechanical reporters (`fixtures/tools/grade/` style), one run → many verdicts. Comprehension mode demotes to flashlight: a comprehension fail alone never mandates a rules edit — it triggers a rig or field check first. Policy-class scenarios (no field anchor: S24, S61 shape) become non-gating; stamp every scenario's evidence class (field / protocol / policy) in the index. Token-efficiency hard rules landed in SKILL.md 2026-08-20. **Re-sequenced 2026-08-20 (owner):** rebuild comes FIRST — the v3 candidate is frozen as the starting point, the rebuilt suite re-tests it, and improvements land from those results.

- [ ] **NDS IQ v3 — candidate is the base; the suite rebuild and both field rigs are DONE, and improvements now land from their results (owner call 2026-08-20; rework executed same day).** `_includes/NDS-IQ-v3.0-trimmed.md` (24,193 chars as of 2026-08-21, −45% vs the live file) is v3's base. It was validated by a full sweep against the trim (64 comprehension scenarios + 2 behavior rigs, ~1.2M subagent tokens): every failure mapped to deleted text, none to kept text. Two independent authors converged at ~24.1K carrying the full evidence manifest — the measured floor; under 20K means dropping evidence-backed content. **Clause status after the 2026-08-20 full pass and the 2026-08-20/21 field rigs — TWO of the original five are still pending**, numbering kept so scenario cites resolve (owner's agent words them). **(1) LANDED in the structured variant 2026-08-21** — P5 mismatch diagnosis "hand-assembled runtime or interrupted upgrade" (S55); only the trimmed base still reads "inconsistent runtime". **(2) LANDED in the structured variant 2026-08-21** — bannerless-JS fallback = doc source + file (S25), in §Install's older-release bullet; still absent from the trimmed base. **(3) NOT ADDED** — stylesheet-sweep load locations, `<link>` in master layout / `import` in shared JS entry (S83 rig); counter-evidence, R2's S83 PASSED without it. **(4) LANDED `0f298f18`** — body-copy regions enumeration incl. accessibility panel + cookie popup (S84 rig). **(5) RE-AIMED, not dropped** — §Verify headless own-tooling license + tab-not-a-skip-reason (S79/S86). The exhortation halves are field-disproven and must not be restored: v2.3 carries them in full and the runner still never reached headless. But the DEFECT they aimed at is the most reproducible failure in the cycle (mobile pass failed in all three runs), so the clause splits into two live sub-items below — the viewport-mechanism rule (the actual fix) and the UNMET-vs-waived blocker. At adoption: re-point every scenario cite to the new wording, run the rebuilt suite against the candidate, then the field rig. **Publishing is a separate owner call** — the candidate does not replace `_includes/NDS-IQ.md` (stamp v3.0, publishes on push) until the owner explicitly says so.
  - [ ] **Structured v3.0 test variant — CREATED 2026-08-21, NOT VALIDATED.** `_includes/NDS-IQ-v3.0-structured.md` derives from the trimmed baseline. It adds work-mode routing, frontend/backend authority, phase gates, viewport proof, and an unmet-status block. The live file and trimmed baseline remain unchanged. Run the required floor checks for new clauses, then the rebuilt suite and field rig before adoption.
  - [x] **Trim decision — v3.0 STANDS; the full file does not justify its size (field evidence 2026-08-20/21).** Rig 5 run three times: v3.0 (24,193), v2.4-trimmed (23,302, ABANDONED — it is the OLDER trim; v3.0 = v2.4 + the `0f298f18` restores, and its §Verify keeps 52% of v2.3's against v3.0's 79%, carrying no twin rule at all), and v2.3 (44,149; §Verify byte-identical to published v2.2, so Verify results are clean signal about the trim). SIX failures reproduce identically on the 44K file and the 24K trim — filter scope collapses to legacy parity, featured icons missing, `data-state="current"` missing, card structure wrong, mobile viewport unmet, source/twin never compared. The extra ~20K bought ZERO unique catches; the v2.3 run verified LESS (first pass never clicked a control, never toggled dark mode) and churned MORE (hero fixed three times, 8 owner corrections across 2 pages).
  - [x] **Clause #5's EXHORTATION half is proven inert — do not restore it.** v2.3 carries "a browser tab your session already holds is not a reason to skip the attempt" AND "not a fallback you have to reach" in full, and the rig 5 runner tried `resize_window` three times and never ran headless Chrome; the v3.0 run at least reached it after owner pushback. Why it failed: the runner DID attempt, and the tool returned a false success — an exhortation cannot fix a lying tool. The clause is re-aimed, not dropped: the two live replacements are the viewport-mechanism rule and the UNMET blocker, both below.
  - [x] **DONE 2026-08-21 — landed in the structured candidate, then strengthened same day after the rig 5 retest** (the runner refused the false pass but reached CDP only on owner nudge, so §Verify now also names scratch-folder `puppeteer-core`/Playwright as the DEFAULT mobile path and requires a failed CDP attempt before `unmet`). Validation rides the candidate's suite run + next rig. Original spec: **Write the viewport-mechanism clause — this is what replaces dead clause #5, and it is the real fix for the mobile pass.** The mobile visual pass failed in ALL THREE runs; the exhortation wording failed because the runner DID attempt — it called `resize_window` three times and got a false success, so "do not skip the attempt" addresses a laziness that was never the cause. Root cause: §Verify lists "project e2e harness, Playwright, Puppeteer, or headless Chrome" as four equal options, and two of them silently produce a wrong viewport — an extension-driven resize no-ops while reporting success, and `chrome --window-size` clamps at the OS minimum (~500px on Windows) while `--screenshot` still crops to the REQUESTED width, which reads as an RTL overflow bug that does not exist (rig 5 chased exactly that phantom). Name the mechanism instead of the menu. Owner-drafted text, one register fix applied, replaces the four-tool menu so net growth is ~zero: *"**Set the viewport, never the window.** OS window minimums (~500px) can silently clamp smaller sizes while screenshots still crop to the requested dimensions — a false responsive failure. Use `page.setViewport()` (Puppeteer), a context `viewport` (Playwright), or CDP `Emulation.setDeviceMetricsOverride` — never `--window-size` or browser-window resize. Before trusting a screenshot, verify `window.innerWidth`; if it differs from the target, the viewport is wrong, not the page."* Floor gate: BOTH halves fail — the rigs default to window sizing unprompted (stub fails) and the real file points at the broken option (real fails), which is the both-fail condition for proposing. Admission test: ends in an artifact check the agent runs alone (`window.innerWidth` readback). No source surface owns "how to drive a browser", so this genuinely belongs in the rules file.
  - [x] **DONE 2026-08-21 — landed in the structured candidate** (Plan: `Awaiting Verification` requires every agent-owned check passed; Verify exit gate: an unmet item keeps the row `In Progress`), **and field-exercised on the rig 5 retest: the row correctly held `In Progress` with mobile unmet.** Floor gate skipped — direct field evidence stood in for it; suite run still pending. Original spec: **Write the one clause the field earned: an unmet pass must BLOCK the status.** Both rigs moved rows to `Awaiting Verification` with a declared-unmet viewport, and rig 5 on v2.3 quoted "unmet, not waived" verbatim while doing it. No version says unmet blocks anything. Floor gate before writing.
  - [x] **DECIDED 2026-08-21 — phase gates, in the structured candidate:** entry/exit gates per phase, a phase-boundary reread rule in §How-to-use, and dual-homing of the three stranded items (icon sweep at Build exit AND Verify; built twin at Build entry AND Verify entry; archetype/shape recorded at Build entry). Rig 5 retest ran under them clean. Original spec: **Decide the fix for phase-stranded knowledge — sectioning, not wording; three instances.** Icon sweep homed in §Build, needed at §Verify (S65, three consecutive misses). Built-twin rule homed in §Verify, needed at §Build (rig 5). Home shape homed in `page-shell.md` / §Build step 2, needed by the per-page cascade (rig 5's hero). Options: duplicate the statement at both moments, give the per-page cascade an explicit re-entry list, or something else — the S65 placement fix alone was not enough.
  - [x] **ADOPTED 2026-08-21 in the structured candidate** — as the Authority-by-concern table plus the Port work-mode row ("preserve requirements and project contracts; replace UI structure with NDS canon"); held on the rig 5 retest (no parity trims, removals named). Floor gate not run — rides the suite. Original spec: **Adopt the principle the field wrote:** "content follows the legacy app, but structure follows NDS canon" — rig 5's own plan named it, and it covers hero, featured icons, `data-state="current"`, card anatomy and section-action in one line. Floor gate first.
  - [ ] **Restore-#2 (porting-principle defaults enumeration) — one test now, positive but not clean.** Rig 7 ran without it; rig 5 run 1 ran with it and still shipped one filter facet. The rig 5 RETEST (2026-08-21) ran with the structured candidate's reworded version ("NDS UI defaults, not questions to ask") and mostly held — search, sort, export, pagination and two facets applied unprompted — but the year facet still needed an owner ask, and it is the same app and pages as run 1. Still needs an R7-shape run (different legacy app) before validated-or-cut, and note any verdict covers the structured wording, not the v3.0-trimmed text verbatim.
  - [x] **"Dev corrections log" — RESOLVED as owner practice, NOT a standing rule (owner call 2026-08-21; a same-day `SKILL.md` landing was reverted — do not re-land).** The log has nothing to do with the eval skill's loop: the owner runs the rigs, asks the rig agent for a corrections log when they want one, and feeds the artifacts here for analysis — confirmed findings land in TODO. The grading caution survives on its own: rig prose is self-reported, so without a log, ask the owner who caught what (rig 5's first run was scoreable only because its plan carried one).

- [x] **Guard scenario for the layout-modifier clause — AUTHORED 2026-08-20 as S88** (`route-modifier-before-mount`), blind per the spec: console-route variant of S87's `mini-spa` in prose, mixed-shape app so the route-dependent branch is actually exercised, setup/prompt name neither the class nor timing. Floor pair and baseline NOT run — ride the v3 full run.

- [x] **Cooldown Button — the two fake-state APIs removed (owner call 2026-08-18). BREAKING.** `data-sent-title`/`data-sent-message` claimed *sent* on the click; `data-cooldown-loading` held a spinner for a fixed count that matched no real response, and with the documented wiring it DELAYED the send (the request goes out from `nds:cooldown:triggered`, which fired after the timer). Both gone, and with them `nds:cooldown:loading`, `loadingTimer`, `cfg.loading` and `ctx.triggered` — the last existed only to tell a mid-loading abort from a real one. The component now owns the throttle and the label; the page owns the request, `data-state="loading"` and the confirmation, all off `nds:cooldown:triggered`. `[data-state~="loading"]` hides the label, so the countdown ticks under the spinner and reads correctly when the state comes off. Ownership is total: the component never sets or clears `loading`, so a request outliving the countdown keeps its spinner. Doc demo, JS API tab, both examples and `form-template` all rewired. **Reasoning correction worth keeping:** the first case against both APIs leaned on "only demos use it" — rejected, the whole site is demos and NDS ships for cases it does not host. What justified removal was that neither could be correct in any flow. **Needs a `### Migrating from v1.8.1` entry at release**, and this is a stable-surface break — the version line was deliberately left at `1.8.x-dev` (owner call), so the release commit decides the real number.

- [x] **ALL FIXED 2026-08-21 — Field findings 2026-08-20/21 — NDS 1.8.1 / NDS IQ v3.0 and v2.3 (rig 5, rig 7).** Eight findings, all source: the examples/templates home-page gap (landed as `templates/home-template.md` on a new `shell` layout, plus catalog, sidemenu and `page-shell.md` routing), `cards.md`, `hero.md`, `core/refresh.md`, `mainnav.md`, the `nds-filter.js` banner, and the `NDS.Init.audit()` current-page nav check with its audit-bundle split. All source, none rules text — the attribution default holding. Each verified against source before landing here.
  - [x] **DONE 2026-08-21 — a home/landing page now exists and the cascade can route it. Landed as a TEMPLATE, not an example (owner call): `templates/home-template.md` + `_data/content/templates.yml` + `_data/hero/home-template.yml` + the sidemenu.** Built from the official DGA home composition the owner supplied — main hero slider, About with four counting figures, a Services card deck, a three-across news row, a partner logo strip, and the last-modified line. Structure mirrors the site's own `index.md` (`layout: home`, hero from a data file). `use_when` names home page, landing page, portal home and main page; templates outrank examples in the cascade, so this is now the top match. Also tightened the About-Entity entry, which claimed "a home page feature row or feature grid" and would otherwise have competed for the same request — it now says a feature row *inside a larger page*. Two build lessons: the featured icons must use the HGI FONT form (`<i class="hgi hgi-stroke hgi-x">`), not the inline mask set (`nds-icon nds-hgi-x`), which carries only a handful of names and paints a solid square for the rest; and `data-src` lazy loading is swiper-only — outside a swiper nothing swaps it, so the news images silently never loaded until they moved to `src` + `loading="lazy"`. Verified headless: counters reach 1.5M, all three swipers init, every image resolves, zero page errors. Original finding: The catalogs cover Services List, Registration, Sign In, Faculty Profile, Faculty CV, Academic Program, Manage Records and Console (admin landing only); `templates.yml:53` names "a home page feature row or feature grid" — one section, flat sub-hero, not the page. So the cascade cannot route a public home page and falls through to step 3, custom scaffold — exactly what rig 5 did before hand-building the main hero without `nds-full-width` and without the entire `.nds-swiper-navigation` block, a hard rule #3 copy violation with the canonical markup sitting at `_site/index.html:793`. `page-shell.md`'s shape table DOES carry a Home row ("Copy from: Home page"), so the reference exists and only the catalog route is missing. `use_when` should match organization home page, hero slider, stat tiles, featured cards. **Confirmed again on the rig 5 retest (2026-08-21)** — the cascade fell to custom scaffold for the same page shape a second time; owner call: build a real `examples/` home/landing page, not just the catalog entry.
  - [x] **DONE 2026-08-21 — `components/cards.md` — state when `.nds-card-meta` is required.** Best-practice bullet added: the wrapper is only for grouping a second meta row (a rating) beside the tags; tags alone go straight inside `.nds-card-content`, since `.nds-card-tags` is styled by the card root, not by the wrapper. Original finding: The canonical anatomy shows `.nds-card-tags` only inside `.nds-card-meta` (2 markup instances, both wrapped), but the built twin uses bare `.nds-card-tags` at `_site/index.html:1296,1339,1382`. The wrapper groups tags WITH a rating; tags alone sit directly under `.nds-card-content`.
  - [x] **DONE 2026-08-21 — `ui-shell/hero.md` — document the colored, photo-free MAIN hero.** Best-practice bullet (empty `.nds-hero-image-wrapper`, `--overlay: 1`, `--img-overlay-color`, keep `nds-oncolor`, don't reach for `nds-flat`), a `--img-overlay-color` row in the CSS properties table, and the `nds-flat` modifier row now says the rule is scoped to `.nds-sub` and warns off `nds-oncolor` inside a flat hero. Original finding: `nds-flat` and `--hero_image` are documented under the SUB hero only, and the main/slider hero is described as always photo-backed. The working pattern is an empty `.nds-hero-image-wrapper` at `--overlay: 1` with `--img-overlay-color`. Also note `nds-flat` is sub-hero-only: applying it to the main hero renders `nds-oncolor` text white-on-white.
  - [x] **DONE 2026-08-21 — `core/refresh.md` — Counter Animation is absent from "What It Updates".** Named as covered: a Numbers row was added to the table. Verified in source — Numbers is a scanner (no `refresh` hook), so `refreshContainer` calls `NDS.Numbers.init()` when the container matches `.nds-counter-value`, which re-arms it. The never-animates case is the already-documented one, a namespace still stubbed because the page had no numbers at load. The row also names the `data-animated` no-replay stamp. Original finding: The table names Filter, Selection, Pagination, Empty and Sort. A `.nds-counter-value` injected after an async fetch never animates, with no console warning and no error from `refresh()`. Name it either way, covered or not.
  - [x] **DONE 2026-08-21 — `ui-shell/mainnav.md` data-attributes table — separate the three `data-state` meanings.** All three verified in source. The table now carries two rows: `current` (author-owned, the only thing that highlights the current page, `aria-current="page"` drives no styling so set both, `NDS.Init.audit()` reports a miss) and `active` (component-owned and transient, never author it, stripped on the next dropdown close). The side menu's `<li data-state="active">` is named inside the `active` row with a link to `ui-shell/sidemenu`, rather than a separate note. Original finding:, because two of them share a word.** Rig 5 marked the current nav link `data-state="active"`, which looks identical (the `_buttons.scss:36` indicator groups `selected`/`open`/`active`/`current`) but is the WRONG attribute, and the owner had to catch it. Three distinct things are in play: **(a)** `<li data-state="active">` in the SIDE MENU — author-written, current page AND a structural instruction, since `nds-drawer.js:21` "opens every ancestor branch at init" off it (`querySelectorAll` at :257) and `nds-sidemenu.js:172` takes the FIRST match's label for the mobile toggle bar; **(b)** `<a data-state="active">` in the MAIN NAV — component-owned and transient, `nds-mainnav.js:553-554` adds it when a dropdown opens and REMOVES it when it closes; **(c)** `<a data-state="current">` in the main nav — author-owned current page, `_mainnav.scss:405,644`, nothing writes over it. The table must say (b) is component-owned and transient, that (c) is the current page, and that the side menu's (a) is a different element doing a different job. **Unifying the spelling is rejected both directions:** mainnav cannot adopt `active` (the component deletes it on the next dropdown close, so the highlight silently dies mid-session — the latent half of the rig-5 bug), and the side menu cannot cheaply adopt `current` (it would touch `nds-drawer.js` logic + banner, `nds-sidemenu.js`, `_includes/sidemenu.html`, `sidemenu.md`, and every consumer's existing side-menu markup, breaking the markup-is-a-production-contract rule for a cosmetic win).
  - [x] **DONE 2026-08-21 — `NDS.Init.audit()` current-page nav check shipped, plus the audit split.** Audits moved out of the loader into `_js/nds-audit.js`, its own build-owned bundle (`nds-audit.min.js`, never auto-injected — loaded on the `enableLogging` sweep or the first `NDS.Init.audit()` call via the `NDS.Audit` lazy stub; production pages ship zero audit bytes). Two new checks landed: the href-aware current-page nav check (warns on a nav link whose href resolves to the current page, or carrying `aria-current="page"`, without `data-state~="current"` — adaptive message when the wrong `active` token is present) and a submit-typed `data-stepper-control` check (the form-template collision shape; implicit-submit `<button>` with no type is caught too). Original spec kept below for the record.
  - [x] ~~**`NDS.Init.audit()` — flag a nav link with `aria-current="page"` and no `data-state="current"`.**~~ Exactly the shape rig 5 shipped, mechanically detectable, and audit is the one instruction that runs in every rig. **×2: recurred on the rig 5 retest (2026-08-21) in exactly the specified shape** — `aria-current="page"` set, `data-state="current"` missing, zero visual highlight until the owner caught it. Priority up: the check as specified would have caught both field misses mechanically. Silent today: the two states style identically through the shared `_buttons.scss:36` indicator group, so nothing surfaces until an unrelated dropdown close wipes the highlight.
  - [x] **DONE 2026-08-21 — `_js/nds-filter.js` banner — add the `getByTarget()` race to Gotchas.** Root cause found in source and written as the mechanism, not the symptom: `nds-filter.js` rides the delegated bundle, so pre-bundle `NDS.Filter` is the loader's lazy Proxy stub and every call returns a Promise. `getInstance()`/`getByTarget()` are synchronous reads the stub cannot bridge, so they hand back a truthy Promise with every method undefined — which is what the report read as "an instance with no prototype methods". `whenReady(el, cb)` is fire-and-forget and survives the gap. Bundles rebuilt. Original finding: It can return a real instance with populated internal state and no prototype methods when called right after an async load inside a deferred module script. `whenReady()` is the race-free path and sits in Methods, but the existing Gotcha covers a different case — an instance that never registers at all.

- [x] **ALL FIXED 2026-08-21 — Field findings 2026-08-21 — rig 5 retest (NDS 1.8.1 / NDS IQ v3.0-structured candidate).** Six source fixes shipped: `topbar.md` sample + modifier row, `_tables.scss` zebra, `_mainnav.scss` orphan border, `form-template.md` submit wiring (which grew into the stepper hand-off), `_cards.scss` `.nds-center`, `footer.md` `nds-brand`. Each verified in a real browser — the form's four gates click-tested end to end, zebra proved old-vs-new on the same DOM (`WWGGWG…` → `WGWGWG…`), card centering measured (`left:0 right:106` → `53/53`), and the nav border driven through a real 375x812 CDP viewport by `scripts/check-nav-actions-empty.mjs`. All verified against source this session. Run overall good (owner): CSP close-out worked, the unmet-blocker held the row `In Progress`, no legacy-parity trims. Mobile scored PARTIAL — the runner correctly refused the false viewport but reached CDP tooling only on owner nudge; fixed same day in the candidate (§Verify now names scratch-folder `puppeteer-core`/Playwright as the default mobile path and requires a failed CDP attempt before `unmet`). Dev corrections this run: 5 (topbar gap, booking sub-hero modifiers, footer `nds-brand`, year-slider add, missing mainnav `current` state — noted post-run). Report finding #3 (a root-class diff exit check) stays a floor-gate candidate only — the footer doc fix below is the source-first answer.
  - [x] **DONE 2026-08-21 — `ui-shell/topbar.md` code sample — drop `nds-topbar-info` from the digital-stamp button.** Dropped from the sample. The modifier table's description was wrong too ("Groups the DGA tab contents and the right-side widget cluster") and now says widget cluster only, plus why it must not go on the button: `_topbar.scss:22` makes it a flex row at `--spacing-xl`. Original finding: The sample (line 49) puts it on the button; the built twin (`_includes/topbar.html:2`) does not — the class belongs only on the sibling widgets div, and sharing it forces equal flex sizing that visibly stretches the button's contents apart when the widget cluster is small. Doc fix; the doc's own modifier table already describes it as the container class.
  - [x] **DONE 2026-08-21 — `_sass/components/_tables.scss` — zebra striping counts hidden rows.** Pagination's hide mechanism confirmed first: `item.hidden = true` (`nds-pagination.js:1197`), i.e. the `[hidden]` attribute. The `of :not(...)` list now mirrors the canonical hide set in `_fold.scss:151` rather than patching just the two reported components — `.nds-sub, .nds-hidden, [hidden], [data-state~="hidden"], [data-filtered]`. `:nth-child(even of S)` indexes only siblings matching S, so alternation now counts visible rows. It was the only zebra rule in `_sass/` (checked). Original finding: `:nth-child(even of :not(.nds-sub))` counts DOM position; Filter hides rows via `[data-filtered]` + `display:none` (`_fold.scss:151`) and Pagination leaves hidden rows in the DOM too, so visible-row alternation breaks after every hidden row (rig repro: 8 consecutive visible search results, one stripe color). Candidate fix: extend the `of :not(...)` list with the hidden states; confirm pagination's exact hide mechanism first.
  - [x] **DONE 2026-08-21 — `_sass/components/_mainnav.scss` — `.nds-nav-actions` paints an orphan 1px border when every item is `nds-PAB`.** No new traversal: `checkTogglerVisibility()` already ran `hasItems(DOM.secondary)` at exactly the right moments (after `managePABPlacement()` at init, and on `modeChanged || _navChanged`), so it was renamed `checkNavComposition()` and now also stamps `data-nav-empty` on `.nds-nav-actions` when it holds nothing real. SCSS drops the whole row from layout on the stamp (`display: none`, owner call — an empty sticky flex row still costs a sticky containing block and paints a background band, so unsetting the border alone was the narrower fix). Reverse path checked: `managePABPlacement()` reinserts through the `[data-pab-ph]` placeholders while the container is hidden, then `checkNavComposition()` clears the stamp — no lock-in. No `:has()`. Covers the consumer-authored-empty case for free. **Verified end to end** by `scripts/check-nav-actions-empty.mjs` (puppeteer-core, real 375x812 CDP viewport): 10/10 action items relocated, `data-nav-empty` stamped, `display:none` with the stamp vs `display:flex` + `border-top:1px` on a 0px-high row without it. Two harness traps worth keeping: the Chrome extension's `resize_window` reports success and does NOT resize (`innerWidth` stayed 2048) — always assert `window.innerWidth`; and `NDS.Mainnav.reinit()` early-returns while the nav element is unchanged, so class-adding after init never relocates — author `nds-PAB` during parsing via a document-start MutationObserver on `document` (not `documentElement`, which is null that early). Original finding: Below the PAB breakpoint the relocation JS moves all items out, leaving only `display:none` placeholders (`data-pab-ph`), and the unconditional `border-top` still paints as a stray line. Fix via a class the PAB relocation JS stamps on the emptied container — no `:has()` (standing rule).
  - [x] **DONE 2026-08-21 — `templates/form-template.md` — the final "Submit Application" button can never validate. Reframed TWICE (owner): first to a template bug, then — correctly — to a stepper OVERREACH.** The first fix just deleted `data-stepper-control` from the button; owner rejected that ("it's an API, fix instead of delete"), and rejected adding validation to the stepper too: **the stepper is a progress display, it must not prevent anything — the submitter owns the flow.** Landed instead: (1) **`_js/nds-stepper.js` stops cancelling form submits.** The delegated handler ran an unconditional `e.preventDefault()`; it now hands off a submit-typed control inside a form (`if (btn.form && btn.type === 'submit') return;`) — no cancel, no move, the form owns that click. Scoped on `btn.form` because a typeless `<button>` reports `type === 'submit'`, and every doc demo control is a typeless button OUTSIDE a form. Swept the built site: only 3 in-form controls exist, all `type="button"`, so zero demos changed. (2) **`components/stepper.md` — new "Advancing the Stepper" block** naming all three paths and the one question that picks between them (can anything refuse the move?): `data-stepper-control` = unconditional (Back, demos, walkthroughs, `goto` restart); `NDS.Stepper.next()` = gated, **every form step**; `data-current` = the step number lives in your own state. With a worked HTML+JS pair, an updated attributes-table row, and a Best Practices bullet. (3) **Template rewired to demonstrate it** — both Continue buttons now validate via `NDS.Forms.validateForm()` then `NDS.Stepper.next()`, so the empty-form path dies at step 1 instead of sliding to the success screen; the final Submit advances from `nds:formValid` + the request's outcome. Back keeps `data-stepper-control="previous"` — nothing can refuse going back. (4) `NDS.Init.audit()` message rewritten: the shape is now inert rather than a silent submit-swallow. Original finding: Reframed from the rig report: a TEMPLATE bug, not a stepper bug (owner call).** The button (line 313) is `type="submit"` + `data-stepper-control="next"` on one element, and `nds-stepper.js:149` preventDefaults every stepper-control click — so no `submit` event fires, `data-ajax` validation never runs, `nds:formValid` (which the template's own comment says sends the request) can never fire, yet the stepper advances: the shipped page reaches "Application submitted" from a fully empty form. The stepper is navigation by design; the intermediate `type="button"` Continues never claimed validation. Fix in the template (rewire the final step per its own comment) + a stepper banner Gotcha: never `data-stepper-control` on a `type="submit"` button.
  - [x] **DONE 2026-08-21 — `_sass/components/_cards.scss` — `.nds-center` never centers `.nds-card-text` itself.** Took the `margin-inline: auto` option, not `align-items: center` on `.nds-card-content`: a `<form>` inside a card is `display: contents`, so its children ARE the content box's flex items and `align-items: center` would shrink-wrap the form fields. `.nds-card-text` is the only child with a `max-width` (checked — line 261 is the sole one), so the auto margin is the whole fix. A `ponytail:` comment names the ceiling and the upgrade path if a second capped child ever appears. Original finding: The `:is()` rule centers the box's children; nothing centers the box once `max-width: var(--paragraph-max-width)` (line 261) engages — rig measured a 94px/0px side split at 848px card width. Fix: `align-items: center` on `.nds-card-content` under the `.nds-center`/`.nds-statistic` variants, or `margin-inline: auto` on `.nds-card-text`.
  - [x] **DONE 2026-08-21 — `ui-shell/footer.md` — say the canonical footer SHIPS with `nds-brand`.** All three spots reframed (feature card, best-practice bullet, modifier table row): the canonical markup carries it, removing it is a choice rather than a reset to a default, and the light neutral is what you get instead. Original finding: The prose (lines 374, 409) frames `nds-brand` as an opt-in over a light-neutral default, but the canonical code block (line 108) and the built twin (`_includes/footer.html:2`) both carry it — a reader trimming the block plausibly "reverts to the documented default" and loses the shipped look, which is exactly what the rig did.
  - [x] **`data-state="current"` missed on the mainnav AGAIN — ×2 field recurrence (owner catch). Recorded; the mechanical catch (audit check) shipped 2026-08-21, the `mainnav.md` table split stays open above.** First run set the wrong attribute (`active`); the retest set `aria-current="page"` ONLY — accessibility-only, drives no CSS, so the current page rendered with zero highlight until the owner caught it and the rig added `data-state="current"` alongside on all 4 pages. This is EXACTLY the shape the 2026-08-20 `NDS.Init.audit()` check specifies (`aria-current="page"` present, `data-state="current"` missing) — the check is field-validated twice and would have caught both misses mechanically. Also hardens the `ui-shell/mainnav.md` three-meanings table split. No new item; both live under the 2026-08-20/21 block.

- [x] **ALL CLOSED 2026-08-18/21 — Field findings 2026-08-18 — NDS 1.8.1 / NDS IQ v2.2.** Two feed the v3 rework above; four are NDS code/doc. Kind: **gap** = API hole; **papercut** = works as documented but silently traps a reasonable reader; **doc-trap** = behavior correct, banner misleads.
  - [x] **papercut — DONE 2026-08-18, but NOT as filed.** The report asked for `%s` to be accepted alongside `{s}`. **Rejected (user call): `{s}` stays the only token.** Two spellings for one value doubles the API surface, and `%s` implies a printf contract the component does not honour — `%d` would still fail, so the next report is the same report. The real defect the report described was **silence** ("silent until the cooldown first fires in prod"), which `%s` acceptance does not fix and a diagnostic does. Shipped instead:
    - `wire()` warns on a `data-cooldown-label` naming no `{s}` — `%s`, `%d`, `{seconds}`, `{ s }`, a typo — at page load rather than first click. Canonical `NDS CooldownButton: ` prefix; the component had zero diagnostics before, alone among 28 `_js/` files.
    - `TOKEN = /\{s\}/g` in `render()` + `stampSizers()`, fixing a second bug the report missed: the old string `.replace('{s}', …)` swapped only the FIRST occurrence, so a bilingual label naming `{s}` once per language filled the first and left the second showing literal `{s}`.
    - Implementation note worth keeping: the warn uses `search()`, not `test()`. `TOKEN` is `/g`, so `test()` carries `lastIndex` between buttons and would warn at random.
    - Not generalized to other `data-*-label` attributes: no other component substitutes a token, so there is nothing to share yet.
  - [x] **gap — DONE 2026-08-18.** `NDS.CooldownButton.start(btn)` always dispatched `nds:cooldown:triggered`, so the flow the doc already advertised — "start the cooldown from a flow other than the button's own click (e.g. after a form submit)" — sent a second code, because `triggered` is where the request lives. Now `start(btn, opts)`: `silent` skips only `triggered` (`tick`/`end` still fire, a resumed cooldown still has to render its seconds and re-enable), and `seconds` overrides `data-cooldown` so a cooldown can be RESUMED across a page load at its remainder. `seconds` is its own opt-in, so a button with no `data-cooldown` can be driven entirely from JS. Non-breaking: `start(btn)` is byte-for-byte the old behaviour. Edge the build nearly shipped: `stampSizers` reserved label width from `cfg.cooldown`, so a `{ seconds: 120 }` override on a `data-cooldown="9"` button would have resized mid-countdown — it now takes the effective total. 7-case check in the scratchpad covers both options, the fallback on a garbage `seconds`, and that width case.
  - [x] **SUPERSEDED — see the Cooldown Button entry above.** The click-vs-outcome coupling on `data-sent-title` / `data-sent-message` was first answered with a doc stopgap; the attributes were then removed outright, so `data-sent-on` / `markSent()` are moot, not parked.
  - [x] **SUPERSEDED — see the Cooldown Button entry above.** The `nds:cooldown:loading` / `data-cooldown-loading=0` coupling was first answered by naming it in the banner; the event and the attribute were then removed outright.
  - [x] **gap — FIXED 2026-08-20, as specified.** §Plan tick-box rule rewritten in place: "Open items are" → "Plan progress is", added "Never delete a resolved item: the ticked lines are part of the plan's memory", and the conversion sentence now reads "reformat every item, resolved ones included, as boxes in place" — the two-style drift source is gone. No sentence added. No scenario cites the old wording (checked; S1/S27 baselines only require `- [ ]` lines, which stand). Eval rides the v3 full run.
  - [x] **gap — FIXED 2026-08-20, as specified.** New mechanical §Build paragraph after the parts-inventory one: "The parts check runs again before page JS" — list the behaviors page JS adds, match against `components.yml` + the page's component banners, wire through the component, never rebuild; runs as a step, not on doubt. Cross-refs §JS wiring for the helper twin. Additive — no cited §Build sentence touched. Floor formality: the stub half of the floor pair was not run pre-write (field evidence already showed the REAL file failing — three reinventions in one session, which is the both-fail condition's hard half); the v3 full run validates the landed text. Revision stamp bumped v2.2 → v2.3 with this edit (first edit after the v2.2 publish); v3 stamps after the rework (owner call 2026-08-20).

## Standing decisions — do not re-propose without the named evidence

A decision lives with the artifact it governs, not here. An eval decision goes in its scenario's `baseline:` in `scenarios.md`; a grading lesson goes in `SKILL.md`; a decision already stated in source or docs is not restated at all. This section holds only what has no such home, so a full clear at release loses nothing.

- **R20 — two-download `_source/` population STAYS (2026-08-12, user call).** Kept here because it is the one call a reader would otherwise try to re-derive from the release script. The template zip ships no `_source/`; the rules populate it from the same tag's Source code zip. Triage proposed shipping `_source/` in the zip or a companion asset. Rejected: the current shape works against every template version an agent may meet, including releases cut before the rule existed, and it keeps the release script simple. Reopen only on the user's own initiative.

## Watch list — not tasks, things a later run should notice

- **Field triage 2026-08-12, the four items the v0.9 batch deliberately left out.** Each lands only on repeat field evidence, never on the single report that raised it. **R14**, what counts as a gate under gate-by-gate pacing: the file already says "when a step or page completes", which defines it — reopen if a run stalls or over-gates on the boundary. **R15**, a conformance rubric for prior NDS work (which violations force a clean rebuild, which are repair rows): one report read a single non-canonical head as enough to rebuild ten pages, which is defensible, so there is no measured failure yet. **R16**, a git-treatment table for the six artifacts (`NDS-IQ.md`, `.nds/`, `NDS-PLAN.md`, `NDS-REPORT.md`, `NDS_ASSETS`, the anchor file): cheap but unevidenced. **R19**, structured catalog fields (`install_family`, `layout`, `chrome_needs`) so chrome shape filters instead of reading ~90 `use_when` prose lines: land it only if S53 shows the prose match failing.

- **The docs-site homepage audit warning (nav `current`) is EXPECTED — do not "fix" `_includes/mainnav.html`.** This repo's mainnav items are dummies, not real site navigation, so no link legitimately marks a current page (owner, 2026-08-21, retracting a same-day proposal). The canon for the marker lives in `ui-shell/mainnav.md` + the audit check; a later audit run on this site that warns on the Home link is noise, not a finding.

- **Two rig-7 findings were WRONG — do not re-chase them if a later report raises them again.** "The mainnav silent-hidden failure is not a Gotcha anywhere reachable from the component's docs or banner" — it is, verbatim, in `_js/nds-mainnav.js`, and `core/refresh.md` states the mount/unmount contract outright. "CSP checks are threaded per page with no way to close out a project-wide no-CSP decision" — the bold "**Strict `style-src`:**" lead-in already is the conditional, in both §Build and §Verify. Both were filed confidently; both dissolved on a first read of the source. **Correction 2026-08-21: the CSP half was HALF right.** Build/Verify are conditional, but §Plan was not — "plan the required nonce or hash" assumed a CSP exists, and "CSP grant" sat unconditionally in the one review message, so agents asked about CSP on projects that have none (owner field report). Fixed in the v3.0-structured candidate: the sweep records `no CSP` and closes the question; the review-message ask is conditional. The mainnav half stays dismissed.

- **The block sweep script that wrapped everything.** The 1.8.0 cycle unwrapped 82 stray `.nds-block` divs it had left across doc pages — around definition items, around demo cards, and inside `<table>`. All known instances are gone and the built site scans clean. If a future sweep touches markup in bulk, check its output against the parents it wraps, not just the count of files changed.
