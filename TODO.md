# TODO — current cycle

Standing file: it always exists. At every release commit, remove the items that release shipped and carry the open ones forward. Never delete the file itself. This is the one open list.

Cleared at the 1.8.0 release (2026-08-16). That release shipped `NDS.Init.destroy(container)` and the five component `destroy()` methods behind it, Main Navigation's `reinit()` and its own doc page, the Page Shell reference, NDS IQ v2.0 with the built-page shell copy and the headless-first §Verify, and the `nds-test-app-7` field report closed end to end — all detailed in `CHANGELOG.md`, with each scenario's state in `.claude/skills/nds-iq-eval/scenarios.md`. Nothing from that cycle is carried forward.

## Open

- [ ] **Field report `nds-test-app-7` cycle 2 (React SPA, 1.8.0, NDS IQ v2.0) — 7 findings TRIAGED 2026-08-16, every claim verified against source before entering this list; report at `C:\Projects\nds-test-app-7\NDS-REPORT.md`, plan reviewed and conforming.** Work these later, in this order:
  - [x] **F7 CONFIRMED — Filter's stale-id registry blocks remounts; the report's root cause is exact.** `_js/nds-filter.js:2979` skips any `data-filter-target` id already in `_instancesByTarget`, the entry is deleted only in `destroy()` (`:2924`), and NO staleness check exists — a framework view unmounted without `NDS.Init.destroy()` leaves a detached instance squatting the id, so the remount stays skeleton-held forever (the crit `data-nds-filter-initialized` hold). The doc's destroy-on-unmount pattern covers the consumer side (the agent admitted skipping it), but a source hardening is on the table: at claim time, release-and-reclaim when the registered instance's `filterContainer.isConnected` is false — the A7 `reinit()` precedent (a capability, not a restructure), and it would ALSO kill the "intermittent first-load race" the report filed separately (React StrictMode double-invoked effects = mount/unmount/remount = this same stale-id shape, not timing). Sweep other id-keyed registries for the same hole while there. Second candidate from the same finding: an `NDS`-prefixed console warning when a refresh claim skips a held id — today only `audit()` sees it after the fact. **DONE** — one `liveInstance(id)` helper in `nds-filter.js` replaces every `_instancesByTarget.get/has` read (`initializeFilters`, `create`, `getInstance`, `getByTarget`, and the `refresh()` walk): a registered instance whose `filterContainer` is detached is destroyed (releasing its pooled `NDS.onDOMAdd` subscribers — the reap also stops `refresh()` re-scanning orphans after a route change), the id is freed, and one `NDS Filter: reclaiming target "…"` warning names the missing `NDS.Init.destroy()`. Registry sweep found no second hole: stepper's id map is keyed off an element-level init stamp and overwrites on remount, copy's aborts the previous controller — Filter's was the only registry GATING creation on a `has(id)`. StrictMode's double-invoked effects are the same mount/unmount/remount shape, so the report's separate "intermittent first-load race" should be gone too (verify when F2 is worked). Regression check: two new cases in `scripts/check-init-destroy.mjs` (remount claimed by a fresh instance, remount stamped so the crit hold releases) — both FAIL on the pre-fix bundle, 36/36 pass with it.
  - [x] **F5 REAL, and it is the S80/B5 REOPEN CONDITION MET — the legacy-globals leak shipped again in the field.** The agent inventoried `styles.css` as rule #6's named legacy dependency and still never checked it for the bare-element selectors the same rule names; the dev caught it as the dark-mode symptom (again). The report's ask fits the admission test: a §Plan inventory step — like the CSP sweep — that greps the legacy stylesheet for bare-element selectors when the strategy is parallel-files/shared-build. T3's lesson applies: the knowledge sits behind a read nothing prompts; a prompted sweep sits in front. Eval-gated (S80/S21 exist; floor the sentence before landing). **Scenario landed 2026-08-16 — `S83 legacy-globals-caught-at-inventory`**, the mandated first step: it grades the INVENTORY moment (is the sheet opened and its bare-element rules named in `NDS-PLAN.md` before any page is built), which neither sibling covers — S80 grades noticing/remedy while building, S21 what a server-rendered NDS layout carries. Its `floor:` is PENDING: **run the pair (stub + real v2.0) before drafting any sentence**, and propose text only if BOTH fail. A stub FAIL alone is not a licence, and a stub PASS sends this to source — but there is no NDS doc that owns the consumer's own legacy CSS, so the cause-removal ladder lands on block text if the pair fails. Also fixed in passing: S81's `baseline:` line had drifted to the bottom of S82's block. **DONE — floor pair run 2026-08-16/17 on a junction-backed fixture, BOTH cells FAIL** (two real-v2.0 runs reproduced F5 exactly — one plan asserted the sheet "is not loaded on any NDS page" while its own strategy kept every NDS route under the importing entry; the 35-call stub run EDITED `styles.css` and still built under its globals). The sentence cleared the gate and landed as **v2.1**: a step-1 stylesheet sweep beside the CSP one — open the globally-loaded sheets, grep element-level selectors, record hits in the plan, rule-#7 strategy names how NDS pages escape them. Results, harness notes, and S80's reopen rider recorded in `scenarios.md`. Confirm re-run on v2.1: PASS — the plan's first finding is the stylesheet hazard with the sheet's real selectors and the shared-entry reach, pre-build, strategy-attached.
  - [x] **F3 REAL doc gap — framework wrapper elements break the shell's layout, mechanism source-verified.** `.nds-main-content` is `flex-direction: column; flex-wrap: wrap; align-content: flex-start` (`_content-layout.scss:36`), so an un-widthed wrapper div (JSX single-root reflex) shrink-wraps and everything inside loses the edge-to-edge layout; `.nds-content-section` survives only via crit's explicit `width: 100%`. Striping's `nth-of-type` and the `>aside` guard break under wrappers too. Same class, one level up, verified 2026-08-16 on the owner's ask: the framework MOUNT ROOT (`<div id="root">` between `body` and the chrome). No `body >` selector exists anywhere in the SCSS (chrome styling survives), but the viewport-fill chain snaps — `body { min-height: 100dvh; display: flex; flex-direction: column }` + `main { flex: 1 }` (`_reset.scss:29`, `_content-layout.scss:6`) stretches `main` inside a content-sized `#root`, so the footer rides up on short pages (the rig's long tables masked it). Consumer fix is one line: `#root { display: contents }`, or mirror the flex chain onto it. Fix per the ladder: ONE framework note in `layout/page-shell.md` covering BOTH levels — the mount root (`display: contents` or flex pass-through) and per-component wrappers (return a Fragment, never a wrapper, into `body`/`.nds-content-layout`/`.nds-main-content`) — the routed read at exactly the right moment; rules text only if a scenario proves the doc alone insufficient. **DONE** — `layout/page-shell.md` gained a `Framework Wrappers` block in Usage Guidelines (before Modifier Classes) covering both levels: the mount root (`display: contents`, or `flex: 1` + column pass-through) and per-component wrappers (return a fragment; never a plain `div` inside `body`/`nds-content-layout`/`nds-main-content`). Re-verified while writing, and ONE triage claim above is wrong: the section's survival is NOT crit's `width: 100%` — it is `_sass/layout/_section-layout.scss:26`, `:is(.nds-hero-section, .nds-content-section) { width: 100% }` in the MAIN bundle. The rest holds: `.nds-main-content` at `_content-layout.scss:35-39`, `main { flex: 1 }` at `:6`, `body` flex column at `_reset.scss:22-35`, the `>aside` direct-child hide at `:113`, striping's `nth-of-type` at `:220`, and no `body >` structural selector anywhere in `_sass/`. Doc-only, so `last_edit` moved and `updated` did not. No rules text — the doc is the routed read. **Then MEASURED, on the owner's ask (the item was raised to confirm NDS itself does not conflict with a wrapper between `body` and the page content), not left at source reading — `scripts/check-mount-wrapper.mjs`, three wrapper positions × four shapes (side menu, console, striped-no-menu, minimal sign-in), before/after on chrome width, content width, section width, side-menu visibility, striping parity, layout box and footer:**
    - **Mount root (`body` → wrapper → chrome): NO conflict on any shape, every metric identical.** The single exception is the sticky footer on a page whose content is SHORTER than the viewport — `body`'s flex column no longer reaches `main`, so the footer stops at the content (2000 → 1509 on the side-menu shape, → 1127 console, → 1100 striped). The minimal shape is immune: it ships no footer and centers on the layout's own `min-height`. This is the whole conflict surface, and it is invisible on any page taller than the viewport — which is why it survived the rig's own testing.
    - **Wrapper inside `.nds-content-layout`: badly broken, but only under `nds-wSideMenu`** — the wrapper takes the side menu's grid column, so the entire page collapses to `--nds-sidemenu-width` (1020 → 260 with a menu, 1140 → 260 console) and doc height roughly doubles. Without a side menu the layout is a single column and nothing moves.
    - **Wrapper inside `.nds-main-content`: shrink-wraps only when the content's max-content width is under the container** — no change on any normal page, 1336 → 360 on sign-in's narrow form. So the failure is real but content-dependent, which is why it reads as intermittent.
    - **BOTH remedies the doc offers were then measured too, not just the breakage** (owner's ask — a recommended fix that is never run is a guess). Each is injected AFTER the wrapper exists, on all four shapes, long and short: `#root { display: contents }` and `#root { flex: 1; display: flex; flex-direction: column }`. **16 fix runs, zero changed values, zero page errors** — every metric back to its unwrapped baseline and the footer re-pinned to the viewport bottom on every short page. Neither remedy costs anything elsewhere in the shell.
    - **Striping never broke, in any position** — `nth-of-type` counts inside the wrapper, and one wrapper holding every section preserves the parity exactly. The first draft of the doc note claimed it did; **the claim was measured false and removed**, along with the `>aside` framing (the real mechanism is the grid column, not the direct-child hide). The `>aside` hide at `_content-layout.scss:113` is a no-side-menu guard, not what breaks here.
  - [x] **F4 RE-FRAMED 2026-08-18 on the owner's field data — it is not about `nds-full-width`, it is STRUCTURAL MODIFIER CLASSES AS A CLASS OF THING.** Owner reports 4/4 runs on rig7 stamped the class from JS instead of writing it in the markup; the original entry read it as one FOUC-sensitive class with theme-only precedent (`ui-shell/head.md:113`). The table proves the general case: `page-shell.md`'s Modifier Classes carries NINE across four targets — `nds-wSideMenu` / `nds-wSideInfo` / `nds-cardView` / `nds-content-wrapper` / `nds-middle` / `nds-post` on the layout, `nds-full-width` on `body`, `nds-top` on `aside.nds-sidemenu`, `nds-flat` on the hero — and every one changes layout, so every one costs a frame at the wrong shape when JS stamps it after mount. Both surfaces name the TARGET and neither names the TIMING (`_includes/NDS-IQ.md:183` "on `<body>`"; the table's Applies-to column), which is why the JS reflex wins 4/4 — nothing pushes back. **DOC HALF DONE 2026-08-18**: a lead paragraph above the Modifier Classes table states the rule for all nine at once — markup the browser paints first; server-rendered writes it in the template; client-routed and route-dependent stamps it in a synchronous inline script as `body`'s first child, before the framework mounts, never a mount effect. Doc-only, so `last_edit` moved and `updated` did not. **IQ CLAUSE LANDED 2026-08-18 on the owner's call** — §Build step 2, immediately after the mount-root sentence it extends (same family: a client-rendered app breaking a body-level thing at first paint), component-agnostic per the name-a-component-only-on-recurrence rule, naming three of the nine as examples. Rides the unpublished v2.2; guide revision row extended; guards 9/9; file 44,842 chars. **The floor pair was NOT run** — the same deliberate skip as the mount-root sentence, on the same grounds: 4/4 field runs outrank a harness that has never reproduced the JS reflex, per the S84/R5 field-outranks-harness precedent. Recorded, not hidden. **STILL OWED: the guard scenario** — a console-route variant of S87's `mini-spa` fixture (already the right shape), authored under the no-leak rule: the setup states the route shapes and nothing about class timing, stamping, or first paint. Until it exists, this clause has no regression cover and a later trim would be unmeasurable.
  - [x] **F6 — composition-source members silently dropped (search submit + filter dropmenu from manage-records); the agent's own miss, rule #3 already bans it.** The ask is a before-done self-check shaped like the icon sweep: re-open the named composition source and diff the shipped page's controls against it. Scenario-first (this is toolbar-adjacent recurrence #3 across rigs); no rules text before the floor pair. **DONE 2026-08-17 — S84 landed, floor pair run: stub PASS + real PASS = LATENT, no sentence owed.** Both cells shipped both field-dropped members and flagged every cut (real run rule-anchored each one and ran the icon sweep unprompted); the field incident was the rig agent's own admitted miss on text that already bans it. The before-done self-check is NOT admitted; S84 is the standing guard — a rig shipping a silent member trim on current text reopens it. Full detail, harness notes (junctions → real-copy fixtures after a second traversal void; composition scenarios must not sit behind an unresolved chrome gate), and a two-datapoint annotated-Status-cell watch recorded in S84.
  - [x] **F1 MISREAD as stated — do not act on its ask without a local repro.** The claim "refresh() only invokes refresh hooks, so Tables is skipped" is wrong: scanners (no `refresh` field) get their idempotent `init()` re-run by the walk (`nds-loader.js:1028`), and `core/refresh.md:54` already documents the bundle-skip. The observed stuck-Tables symptom is most plausibly F7's Filter hold or the stub-skip window; reproduce locally before touching `core/refresh.md`'s "no component can be forgotten" sentence. **F2 PARTIAL** — the race framing likely reduces to F7 + StrictMode, but the underlying ask stands as an owner capability call: no public "NDS fully initialized" primitive exists (`data-nds-loaded` = reveal, not delegated bundles; watching it is DIY). Decide: documented promise/event, or a doc note that the arrival scan self-heals and refresh calls need no awaiting. **DONE 2026-08-17 — F2 DECIDED: doc note, no API; measured, not assumed.** `scripts/check-mount-timing.mjs` (new, puppeteer, holds/aborts bundle responses to make each timing window real): 13/13 — view mounted before `nds-main` runs → startup detection inits it; guarded `refresh` in the pre-NDS window → clean no-op; mounted after detection but before the delegated bundle → the arrival scan inits it with zero consumer calls; post-load → one `refresh`; never-arrived bundle → `refresh` skips the stubs without force-loading, and `loadBundle` recovers even after a failed download (retry path measured). The rig's poll-and-retry helper was medicating F7 (fixed) plus windows that self-heal. `core/refresh.md` gained a "Framework Views" block: the whole contract is guarded `refresh` on mount + `destroy` on unmount, no readiness check, `loadBundle` only for a component type the first paint never had. F1's mechanism half confirmed by the same harness (case 3: scanners re-init via the walk); its stuck-Tables symptom stands explained by F7 + the stub-skip window, and the "no component can be forgotten" sentence stays untouched. Also corrected en route: `data-nds-loaded` means critical-inited + main CSS applied (`nds-loader.js:762-764`) — more than reveal cosmetics, less than all-ready. Lifecycle scenario on `mini-spa` guards the contract (S85).
  - [x] **Watch (S72/S79 field data):** the rig verified in a driven Chrome tab whose resize did not take, and reported mobile as an honest UNMET check instead of switching to a viewport-setting headless run — the degradation behaved (no false "verified"), but the headless-first default did not reach mobile. Second occurrence gets a scenario look. Cause disambiguated in-session (owner, 2026-08-17): headless WAS available — one dev nudge and the run switched and passed mobile — so the gap is entry-into-fallback without a headless attempt, not tooling. Candidate fix when #2 fires: extend P1's gate to the ladder entry (any fallback rung names the failed headless attempt). **CLOSED 2026-08-17 by owner call — not waiting for #2** (the nudge confirmed the cause; the nudge is the cost the fix removes). The clause landed in §Verify's ladder opener ("Cannot drive one? That takes the same named failure as the claim … a browser tab your session already holds is not a reason to skip the attempt"), guarded by S86 (grades the ORDER, respecting S79's tool-limits-are-unsimulatable lesson), rides v2.1.

- [x] **Harness-fidelity audit — scenarios that leak answers or work in the runner's favor are not real tests (owner call 2026-08-17, from cycle 3 landing on v2.1 the same day its gates passed).** The proof case: S84's floor pair PASSED both cells while the field shipped the exact failure on the same text — because the fixture's seeded plan row enumerated the members under test ("manage-records.md — console shape, table + filter + pagination + export") while the rig's real plan row said only "Example match: Manage Records", no list. The runner was handed in-context what the field withholds; the pass measured the leak, not the file. Audit every scenario's setup, prompt, and fixture artifacts for three leak classes: **(1) seeded artifacts carrying the graded answer** (S84's plan row — the proven case); **(2) setup facts that foreground the tested capability instead of leaving it discoverable** (e.g. S86's "a scriptable headless Chrome is present" — world-state, but the field agent must FIND that out; check every same-sitting gate run from 2026-08-17: S74/S77/S44/S85/S86 all passed on setups written by the same hand that wrote the sentences); **(3) prompts naming the graded surface** (the S72/S79 tell — policed, sweep for residue). Deliverable: per-scenario leak classification in `scenarios.md` notes, de-leaked setups, and re-runs for every pass the audit marks suspect — a suspect pass reverts to UNMEASURED, not FAIL. Then one standing harness rule in the skill (a `SKILL.md`/preamble sentence): the setup states only what the field state would show; an enumeration that restates the answer is a leak. **Sequencing: R5's S84 re-run happens AFTER its fixture is de-leaked (strip the member list from the seeded row to match the field plan), or the re-run proves nothing — same-sitting gate runs for future rule edits should be authored blind where possible (setup written before the sentence, or by re-using field artifacts verbatim).** **DONE 2026-08-17** — all 86 scenarios classified: audit block + 16 `leak:` labels in `scenarios.md`, standing no-leak rule in `SKILL.md` (leak class 4 — fresh-context runs cannot measure long-session decay — named there for sso F1), assembly pointer in `fixtures/README.md`. Two passes voided to UNMEASURED and re-run de-leaked the same day: **S86 PASS clean** (the ladder-entry clause fires with no headless tool named; R1's field condition stays open) and **the S84 pair, on the field plan's own row text — the leak WAS the result**: stub cell trimmed the bar to legacy parity (flagged, not silent; nothing like the leaked ship-everything), real cell shipped 6/7 members incl. both field-dropped ones but **silently dropped column visibility** and justified its named cuts with the parity rationale ("legacy never had them"). Soft FAIL on current text — the harness now agrees with the field, and R5's floor evidence is in hand. Residue labels (C2/C3) bound what 11 older passes may claim; S20/S70's floor-PASS trim candidacies weakened by their leaks; S74/S77/S85 audited clean, passes stand.
- [ ] **Field report `nds-test-app-7` cycle 3 (React SPA, 1.8.0, NDS IQ v2.1 mid-evolution copy) — 6 findings TRIAGED 2026-08-17, every claim verified against source before entering this list; report at `C:\Projects\nds-test-app-7\NDS-REPORT.md`, plan reviewed (chrome + Application list built, tick-box format and chrome-as-is defaults visibly working; hero fix and full-pattern rebuild recorded in the build log).** Work in this order:
  - [x] **R5 REAL, and it is the S84 REOPEN CONDITION MET — the silent member trim shipped on v2.1 text.** The rig matched Manage Records, then cut filter, column-visibility, export, and pagination down to legacy parity ("legacy didn't have them, dataset is 12 rows") — corrected only after the dev named the missing controls. The sentence it contradicts is `_includes/NDS-IQ.md:197` ("defaults to apply, not questions to ask") and `components.yml`'s Tables entry names the pairing — no information was missing; caution-over-rule was chosen. S84's floor pair PASSED both cells (2026-08-17), so the harness could not reproduce the temptation the field applies (the fixture's plan cited the scope; the rig's context had dataset-size + parity pressure) — **field outranks the harness per S84's own reopen clause.** The before-done self-check question (F6's original ask: re-open the cited composition source, diff the shipped members) reopens with field-grade evidence; the report's proposed extension ("a matched example that pairs components by default ships all of them; small dataset and legacy minimalism are not valid reasons — only a concrete domain reason is") extends the existing `:197` sentence, fits the admission test (ends in the catalog-pairing check), and aligns with the deliver-extra principle. Eval-gate: land text, re-run S84 against the edited file, and note the harness-vs-field gap in S84's baseline. **DONE 2026-08-18 — the ban row at `_includes/NDS-IQ.md:98` gained "A small dataset or a minimal legacy page is not a constraint: a matched source ships all its members, and a member you drop for a real domain reason is named to the dev." Placed there rather than at `:197` as the report proposed: dropping copied members already had a home in rule #3, and one concern gets one statement. S84 behavior re-run on the edited text PASSES — 7/7 members (was 6/7, column visibility silently dropped), and its Fee/Status and sub-row drops are named in the plan with a domain reason. R6's IQ half rides this; its doc-side anatomy marker still ships with 1.8.1.** Prior evidence: **floor 2026-08-17 (harness audit): the de-leaked S84 pair ran — stub = parity trim (flagged), real = one SILENT member drop (column visibility) + the "legacy never had them" rationale unchallenged. Soft FAIL on current text; the harness-vs-field gap is closed and the sentence has its gate evidence — decide and land at this item's sitting.**
  - [x] **R6 — the missing hero is R5's family, not a doc-blocked failure (re-framed 2026-08-17 on the owner's adapt-principle).** The built page the rig copied from CARRIES the hero as `.nds-main-content`'s first child — the rig confirmed that itself while fixing — so the omission is incomplete COPYING of the source, the same shape as R5's member trim: shipped less than the source carries, silently, across two gates. IQ-side: R5's before-done self-check (re-open the copy source, diff what shipped) catches a missing hero for free — fold R6 into R5's scenario/sentence work rather than treating it separately. **DONE 2026-08-18 — proven by S84's behavior run on the edited rule, no separate work needed.** The runner shipped `nds-hero-section nds-sub` with breadcrumbs in `RecordsList.js` and ZERO hero in its `Shell.js`: the hero was neither dropped (R6's failure) nor mislocated into the chrome (the ambiguity the proposed marker addressed). R5's ban row covers the IQ half as predicted. Doc-side anatomy marker DEMOTED to optional polish for 1.8.1 — the tree does still group the hero under "chrome regions", but a runner reading it placed the hero per page anyway; reopen only if a rig puts a hero in the shell.
  - [x] **R4 CONFIRMED doc gap — Filter's deferred option-build is missing from its banner Gotchas.** Verified: hidden, non-URL-active auto-filters defer their option-list build until first open (`_js/nds-filter.js` — `_deferredFilters` at `:223/:1325`, `data-delay="500"` at `:1358`), and the banner's Gotchas (`:40`) carries no line for it — the one place §JS wiring routes agents to. The rig's verify pass read an empty option list as a broken filter. **DONE 2026-08-18** — Gotcha landed in the filter banner: a hidden auto-populated filter defers its option build to first open (`data-delay="500"`, options built on `nds:dropmenu:prepare`), so an empty list right after load has not run yet rather than being broken. Bundles rebuilt; the banner ships via `_source/`, not the minified bundle, which is the copy §JS wiring routes to.
  - [ ] **R1 — §Verify fallback entered without a headless attempt: clause LANDED, fix NOT proven.** `14dcfab2` put P1's named-failure gate on the ladder opener, but its only validation is S86 — a same-sitting gate run whose setup foregrounds headless availability (leak class 2, in the audit's suspect pool). The report's Playwright re-run is the dev-nudge that confirmed the cause, not evidence the clause works unprompted. Closes on FIELD evidence: the next rig verify pass attempts headless first (or names its failed attempt) with no nudge — plus S86 re-run on a de-leaked setup. **The S86 half is DONE 2026-08-17 (harness audit): de-leaked re-run PASS — the clause fires with no headless tool named in the setup. Only the field half remains.** Second supporting datapoint 2026-08-18 (S84 behavior run, unprompted): drove headless Chrome for the visual pass, and when its resize did not take, named that specific failure and reported mobile UNMET rather than waiving it. Harness, not field — R1 still closes on a rig. The report's extra ask (name a concrete probe command like `npx playwright --version`) stays NOT admitted: how to probe an environment is judgment, and a named command goes stale per stack.
  - [x] **R2 — `#root` breaks the body>header/main/footer chain: CLOSED as the adapt-principle working (owner re-frame 2026-08-17).** The rig's tag predates `b542839f`'s Framework Wrappers doc, and the system behaved exactly as designed without it: the verify pass caught the floating footer (the oversized gap in the mobile screenshot), the agent root-caused it against `_reset.scss`/`_content-layout.scss` itself, fixed it consumer-side (`#root { display: contents }` — the same remedy the unseen doc names), and recorded it in plan + report. Routed docs are enrichment, never load-bearing — a missing doc that still ends in a correct consumer-side fix is a pass, not a gap. The doc ships with 1.8.1 as enrichment; nothing else owed.
  - Cycle observations, no action: the v2.1 defaults visibly worked (stamp kept with its placeholder boxed, demo-labeled theme switcher dropped with reasons, boxed subtract-later items throughout); the "still asked for release" the owner saw traces to the rig's mid-evolution v2.1 copy predating the first-setup-latest edit, plus the `-dev`-banner P5 row which legitimately asks. **Zero runtime issues** — and each unshipped runtime fix has a named reason it went unexercised: the filter reclaim protects destroy-skippers and this rig wired the lifecycle correctly; the forms status cut needs the form gate (next); the CSP fixes need a strict CSP and deep pagination the rig lacks. Cycle 3's bleeding was all doc-layer (R2 layout break, R6 two missed hero gates, R4 debug pass).

- [ ] **Field report `sso-rig` cycle 2 (server-rendered .NET, ChangePassword port, 1.8.0, NDS IQ v2.0) — 2 findings TRIAGED 2026-08-17, every claim verified against source before entering this list; report at `D:\Projects\IU-SSO\NDS-REPORT.md`.** Work in this order:
  - [ ] **F1 PRIMARY — NDS-IQ triggers that require the agent to NOTICE a claim don't survive long sessions; the rebuilt Password is the exhibit, not the finding.** The report's own framing: rules whose firing moment is a mental event ("am I about to claim NDS has no X?") work at turn 1 and decay across a session through three converging failure modes — (a) **attention decay**, P1's specific phrasing fades from active foreground as ports/messages/code push it out; (b) **context compaction** summarizes hooks like "grep `components.yml` before coding" as gist not habit; (c) **momentum bias**, after N turns of "just build the thing" the pause-and-check reflex weakens turn on turn. The rule survives as concept, the reflex does not. So a rule bolted to a mental hook is fragile against session length by construction, and a rule bolted to a mechanical moment (about to write page JS) fires regardless of turn count. This is a cross-cutting durability class, not one component's miss — `nds-test-app-7` R5 (silent member trim on v2.1 text) reads the same way: the agent knew, would have checked at turn 1, did not at turn 60 under legacy-parity pressure. **Exhibit (this report):** NDS.Password ships every capability the rig hand-rolled — `data-rule="length|upper|lower|digit|special|match"` chips (`_js/nds-password.js:72-79`), submit-gating via `setCustomValidity` (`:224`), live match wiring via `data-password-match` (`:136-137`, `:174-176`), custom-rule extension via `NDS.Password.addRule` (`components/password.md:632-635`), catalogued in `_data/content/components.yml` — and the agent still hand-rolled the whole page JS block because P1's trigger never fired. The report's ask is a mechanical §Build trigger ("before you write an `input`/`change`/`submit` handler on any `.nds-form-*` element, do the catalog check first"). **Work with R5 as one evolution, not two** — same class, same gate, both eval-gated the same way. **Harness implication (blocks the eval design, not just the sentence):** fresh-agent floor runs cannot reproduce what this finding is ABOUT. A one-turn scenario runs at turn 1 where the reflex is strongest; the field failure lives at turn 60. BOTH cells passing may only mean the harness never entered the state the field agent was in — leak class 4 for the harness-fidelity audit above (fresh-context runs undermeasure attention/compaction/momentum drift). The eval design must catch this before the sentence runs: needs a long-context variant (padded scenario or a re-used field transcript prefix), and the scenario's `baseline:` notes the caveat explicitly. **Sequencing: this waits on the harness-fidelity audit** — a durability finding measured on a harness that cannot enter the failure state is worth nothing, and the audit is exactly where that gap gets named and fixed. Then floor pair (stub + real) on the long-context variant, S72/S79 discipline: propose only if BOTH fail; a stub PASS sends it back to source (banners/catalog cues at the point of copy). **PARTIAL 2026-08-17 — the decay MECHANISM landed, the rest deferred (owner call: no spend on the multi-turn driver).** Landed in working tree (uncommitted): the anchor gained the compaction sentence ("A compacted or summarized context starts a new session: read the file again before more NDS work") — the one NDS surface harnesses re-inject every turn, so it cannot decay — plus a dateless content-check Anchor-update note in §Install for existing anchors; heading took **v2.2**; `check-release-guards.py` 9/9. The multi-turn decay driver (harness #4) is PARKED — the decay class is field-validated-only (leak class 4 caveat), revisit on a second field recurrence. **REMAINING for the batch (do later, then ONE scoped eval + commit):** (1) F1's moment-bolted trigger sentence, drafted and awaiting the owner's OK — §JS wiring: "Before you write an `input`, `change`, or `submit` handler on any `.nds-form-*` element, run the catalog check first: the component that owns that surface usually already ships, and its banner carries the wiring you were about to hand-roll."; (2) R5's member-trim sentence (floor evidence in hand); (3) F2's doc paragraph below; (4) the guide's dev-tell line — **DONE 2026-08-18**, one paragraph after the resume block in `guides/get-started.md`: long sessions drift, and "follow the IQ" makes the agent re-read and pick the workflow back up. Doc-only, `last_edit` moved.
  - [x] **F2 REAL doc gap — `password.md` § Confirm Password only names the chip pattern.** Verified: `components/password.md:159-160` frames the match chip as how confirm mode works and both confirm demos ship it; `_js/nds-password.js:145` proves match-only init is legal (requires chips OR match, not both) and `:188-224` proves `data-password-match` alone gates submit via `setCustomValidity` with no chip present. So a reader following rule #3 verbatim ships the chip regardless of role, when a chipless "check on submit" confirm field is equally canonical. Fix per the ladder + attribution default: source fix — add a two-pattern paragraph to `password.md` § Confirm Password, name each pattern, keep both canonical blocks. **DONE 2026-08-18** — the section description no longer frames the chip as the mechanism ("The submit stays blocked until the two values are equal. There are two patterns for showing the result, and both are canonical"), and a `Two Patterns` block names each: with a chip when confirm sits beside strength rules, without when confirm is the only check on the field, plus "Do not add a chip only because the demo has one." Re-verified while writing: `_js/nds-password.js:145` returns only when there are NEITHER chips nor a match target, and the mismatch path calls `setCustomValidity` off the match test alone — so chipless confirm gates the submit with no chip present, as claimed. Doc-only, so `last_edit` moved and `updated` did not.

- [x] **Framework mount fix becomes IQ standard — LANDED 2026-08-17 on the owner's own field test.** Rides the unpublished v2.2 (no bump; the sso-F1 anchor patch already took the number and has not been pushed), placed in §Build step 2 after "what the shape's page lacks stays out, without asking."; `integration-quality.md` gained the v2.2 revision row covering both v2.2 changes; `check-release-guards.py` 9/9. **The floor pair was NOT run, deliberately** — R2 below is a field-grade stub PASS (a rig with no Page Shell doc self-fixed it), so the harness would have argued against the sentence and the owner's field test says otherwise. Same call shape as R5: field outranks the harness. The gate skipped is on the record here, not hidden. **Guard landed 2026-08-18 — `S87 mount-root-layout-chain`**, authored under the no-leak rule on the `mini-spa` fixture verbatim (prompt is "port the Contact page to NDS"; nothing names the footer, the layout chain, or the mount element, and the rubric grades the shipped CSS rule rather than a citation). **S87 PASSES on a de-leaked pair 2026-08-18 — stub FAIL, real PASS — after two voided pairs the same sitting.** Method record, because the voids are the reusable part: pair 1 INCONCLUSIVE (the setup seeded prior built rows, putting both cells past the master-layout moment the sentence governs); pair 2 self-voided under the harness audit's own rule (leak class 2 — the setup named the mount container and `src/styles.css`, the graded element and its destination, plus a post-hoc setup edit and an off-canon prompt). Pair 3 named neither and the real cell still reached "mount the React root inside it with `display: contents`" unprompted, while the stub cell missed the shell entirely for the third time. **The leak's fingerprint is the diff between pairs 2 and 3:** the leaked run named `src/styles.css` back, the clean one names only the React root. A `mini-spa` behavior run is still owed — comprehension grades a described action, the rubric wants shipped CSS. What the pair showed: Real cell built chrome first, then gave the mount container `display: contents` in the project's own stylesheet unprompted, quoting the §Build sentence; the stub cell never reached the shell. The pre-run expectation (latent, like S82, on S83's stub datapoint) was WRONG and is retired: both stub cells here were lazy (3 calls, then 1) and so never wandered into `page-shell.md`, where S83's 35-call stub had found the fix — doc and rule differ in reliability, not content. The harness now agrees with the owner's field test. One tail: a first pair the same day was VOIDED (setup seeded prior built rows, putting both cells past the master-layout moment — INCONCLUSIVE, not FAIL), and a `mini-spa` behavior run is still owed before the guard is proven end to end, since comprehension grades a described action and the rubric wants shipped CSS. Sentence as landed: *"A client-rendered app mounts this copy inside a container element (`<div id="root">` or the stack's equivalent). Give that element `display: contents` in the project's own stylesheet: `body`'s layout chain must reach `header`/`main`/`footer` directly, and the mount node otherwise breaks it — the footer rides up on short pages."* Doc side already shipped: `layout/page-shell.md`'s Framework Wrappers block, whose snippet comment now names `#root`/`#app`/`app-root` so a non-React stack does not copy a dead selector.

- [x] **URL writers drop `location.hash` — breaks every hash-routed consumer (found 2026-08-18 by the S84 behavior runner, then verified wider than it reported).** Three sites rebuild the URL from `pathname` + `search` alone: `_js/nds-filter.js:899-902` (`updateUrlParams`), `:692` (`_rollbackApplied`), and `_js/nds-pagination.js:912` (`_writePageParam`). `grep location.hash` across filter/pagination/tables returns nothing, so nothing preserves it. On a hash-routed SPA (`#/records`) the first filter change or page click silently wipes the route — the runner hit it live and shimmed it project-side, which is the tell that NDS owes the fix. Not a regression: the shape predates this cycle. **DONE 2026-08-18** — all three sites append `window.location.hash`; `scripts/check-url-hash.mjs` is the new guard (pins the three writers AND fails any other `replaceState` that rebuilds from `pathname` without the hash — proven by reverting one site). Bundles rebuilt. Rides 1.8.1 as a `### Fixed` entry.

- [ ] **NDS IQ v3 — full rework, weekend (owner call 2026-08-18).** A mid-cycle §Build consolidation was written and reverted the same sitting (the policy holds rewrites to a cycle boundary); its one correctness fix and F4's modifier clause were kept. One fact worth carrying: five scenarios cite §Build sentences by quote (S44, S53, S77, S81, S87), so any rewrite of that section re-points those `cite:` lines and takes a `full` run — not a sitting.

- [ ] **Release 1.8.1 — routine shipping, NOT the fix (demoted 2026-08-17 on the owner's adapt-principle: IQ must work on any template; routed docs are enrichment, never load-bearing — cycle 3 proved it by self-fixing R2 without the doc).** Fifteen commits sit above the v1.8.0 tag; ship them when convenient. Batch the small open doc fixes in first so one release carries everything — R4 (filter banner Gotcha), R6's anatomy marker, F4 (pre-paint body-modifier note) — then the release flow: version sweep, changelog with its Migration section (the forms error/help status cut and the feedback-slot markup change need entries; the CSP `style=""` fixes and filter reclaim ride as fixes), `mkrelease`, tag, publish. Nothing waits on it: a cycle-4 form build on the old tag's "always include the slot" ships a harmless inert div (deliver-extra), and the status cut renders neutral — cosmetic. The IQ track (harness audit → R5) leads and publishes on push.

- [x] **Field report `sso-rig` cycle 1 (server-rendered .NET, sign-in family, 1.8.0, NDS IQ v2.0) — 4 findings TRIAGED 2026-08-16, every claim verified against source before entering this list; report at `D:\Projects\IU-SSO\NDS-REPORT.md`.** Work these later, in this order:
  - [x] **F3 CONFIRMED — Alert's toast progress knob is stamped via `innerHTML` `style="…"`, blocked under strict CSP.** `_js/nds-alert.js:118` builds the close-button HTML with a literal `style="--progress-duration: ${Number(duration)}ms;"` that lands via `innerHTML` (`:136`). Rule #5's promise to strict-CSP consumers ("NDS's own JS sets styles through the CSSOM, which no policy governs") is broken in exactly this one place. Functional impact: nil (auto-dismiss runs on `setTimeout`, `:224-228`); cosmetic: the progress ring never animates because the custom property is never set; signal: every toast fires a console error under strict CSP. Fix per the ladder: cut the `style` off the HTML string, write `alert.querySelector('.nds-alert-close').style.setProperty('--progress-duration', ...)` after `innerHTML` insertion — same effect, CSP-clean. Then sweep `_js/*.js` for `style="` in template strings — any other HTML-templated knob is at the same risk, and the sweep makes the promise checkable across every future `innerHTML`-authored subtree. **DONE b2823ea1** — the sweep found three more shipped sites, two of them worse than F3: pagination's virtual-scroll spacers collapsed to 0px (page-number menu could never scroll past its first window), the editor's remove-menu lost its indent (depths 1/2/3 render as identical destructive rows — the label prefix is one dash at any depth), and upload's file-item template. All four now write via the CSSOM; `nds-showcase.js` and `nds-theme-foundation-day.js` still hold `style="` but build to `docs-assets/`, which no consumer hosts. Upload's 24px moved to `_upload.scss` instead — a constant belongs in CSS — which also lets a consumer strip the inline style from copied canonical markup and still render right. Follow-on from the same reading: `_upload.scss`/`_buttons.scss`/`_accessibility.scss` now set `--_progress-size: var(--progress-size, default)` so a consumer override wins from `:root` or a wrapper instead of losing to inheritance proximity; stepper excluded (sets `--progress-size` on its root and reads it back in four calcs — that swap is a knob-architecture pass, not a CSP fix).
  - [x] **F4 CONFIRMED — OTP fieldset `data-status` never paints the digit borders.** `_forms.scss:63,72,81` scope the error/success/warning border rules to `.nds-form-container[data-status] > .nds-form-control` — the container that DIRECTLY wraps the control. `_otp.scss` has no `data-status` selector (verified all 143 lines), and `components/otp.md:300,359,444,607` all show the canonical pattern as `setStatus({element: otpGroup, ...})`. So the feedback text renders (via the fieldset's `data-feedback-target`) but the digit boxes stay neutral — the "invalid what" affordance every other field gives for free is missing. Fix per the ladder: three lines in `_otp.scss` cascading `.nds-otp-group[data-status="{error,success,warning}"] .nds-otp-container > .nds-form-control { border-color: … }` — no JS change, same `setStatus` call, matches the visual contract of every other form field. The alternative (teach `setStatus` to propagate group→containers in JS) is larger scope for identical visible behavior. **DONE** — shipped as the three-line cascade, but the owner then cut field status down to `error` + `help` system-wide (owner call, same session): success/warning/info no longer tint the outline OR the message. Root cause sat in JS, not CSS — `setStatus` now folds any other status to `neutral` on both the container and the feedback it creates, because `_feedback-icons.scss` colours by ANCESTOR (`[data-status="success"] .nds-feedback`) and `Feedback.create` stamps the status on the message element, so a CSS-only cut left two paths still painting. A hand-authored `data-status` bypasses the JS guard entirely, so `_forms.scss` also resets the feedback knobs to `initial` for both `.nds-form-container` and `.nds-form-group`. Two traps found on the way: `_forms.scss`'s old `>.nds-feedback` status rules never fired in the common case (a `data-feedback-target` puts the message a level down, so `_feedback-icons.scss` was doing all the work), and `help` was already a full status in the feedback component — the first guard flattened it and killed its "?" icon. `.nds-feedback` also gained a default `color: var(--text-primary-paragraph)`. Docs: `forms.md` (demo buttons now Error/Help/Neutral/Clear), `otp.md`, `filter.md`, `feedback-icons.md` (its Feedback Messages dropmenu was missing `help`). Release note is a `### Changed` entry, NOT a migration step — consumer calls still run, they just render neutral.
  - [x] **F2 REAL gap, but a DOC gap — no API added (owner call 2026-08-17).** `_js/nds-forms.js:700` — `validateForm` runs `acc.firstInvalidInput.focus()` when `focusFirst: true` (default); `setStatus` (`:206-294`) paints `data-status` and inserts feedback but never touches focus. Both proposed APIs were rejected on reading: (1) an opt-in `focus` option saves the consumer nothing — `setStatus` is per-FIELD, so "which error is first" stays the caller's decision either way, and it would cost a `delete` guard because `:271` spreads `options` straight into `NDS.Feedback.create`; (2) `focusFirstInvalid(container)` is one native line the consumer can write, and they already hold the element at call time. The message announces on its own regardless — `_js/nds-feedback.js:115` sets `aria-live` at insert. **DONE** — `components/forms.md` gained a `Server-rendered Errors` block in the Form Status API section (prose + standalone `nds-block` snippet: loop, `setStatus` per field, focus the first), plus one comment line in the JavaScript API reference dump so a reader who only greps the API surface sees it. The snippet's focus line handles the group case the report never named: a `<fieldset>` (OTP, radio group) cannot take focus, so `firstError.querySelector('input, textarea, select') || firstError`. The page had NO server-error guidance at all before this — grepped, zero hits. Doc-only, so `last_edit` moved and `updated` did not. Reopen only if a rig writes the loop wrong WITH the doc in front of it.
  - [x] **F1 REAL doc/IQ gap — canonical markup mixes STRUCTURAL and SLOT hooks; a reader following rule #3 verbatim ships opt-in scaffold as required markup.** `_js/nds-forms.js:232-252` — when `setStatus` runs without any `data-feedback-target`, `target` stays as the container itself and the feedback inserts there. So `<div class="nds-form-footer" data-feedback-target hidden>` is opt-in for explicit placement, not required for empty-field feedback to work. Doc-page examples ship the slot inline without calling out its opt-in status; the JS source's `setStatus`/`_ensureX` helpers are where the structural-vs-slot discriminator lives. Failure mode is bloated ports that read wrong to a reviewer who knows the API, not broken pages. Fix per the attribution default (2026-08-14): SOURCE first — the forms/OTP doc-page examples that ship the slot inline don't mark it opt-in, and neither does the forms banner. Eval-gated: floor the sentence before proposing rule #3 text; the doc/banner fix likely absorbs it. Same class as `nds-test-app-7`'s F3 (routed read at the right moment > rules text). **PHASE 1 DONE 2026-08-17 (owner call: feedback is API-created; inline slot only for a permanent position).** Classification sweep found it worse than triaged: `forms.md` MANDATED the slot ("Always include a `data-feedback-target` element in the form footer… The validation system needs this target" — false per `nds-forms.js:232-259`) and the empty-hidden slot ships in the primary markup of 21 files (~100 of 141 hits; the rest are populated/permanent slots that stay, plus `form-header` placements — intentional, stay). Fixed: the false bullet and the stale four-status Status Feedback description rewritten (API-created default, error/help only), the Permanent Feedback section gained the canonical placement paragraph (any element in the container; header above, footer below; hidden targets auto-show/hide), radio/checkbox "Place a…" bullets reworded opt-in, forms banner gained the no-slot-needed Gotcha, bundles rebuilt. `nds-form-footer` confirmed zero-CSS (bare hook, not a designed area). **CLOSED at phase 1 (owner call 2026-08-17): phases 2–3 dropped.** The empty-hidden slot is inert (zero CSS, no paint, and as the container's last child it renders feedback where the fallback would anyway) — harmless extra per the deliver-extra principle, so the ~100-slot sweep is churn with no measured gain (and bulk-sweep risk, see the watch list). No rules sentence is proposed, so the floor gate has nothing to license — no scenario, no runs. Reopen only if a rig report shows the slot causing real harm, not reviewer taste.

- [x] **`NDS-PLAN.md` format is not dev-readable — fix in the next IQ revision (owner ask 2026-08-16).** Exhibit: `C:\Projects\nds-test-app-7\NDS-PLAN.md` — the table row carries the status, but everything a dev must act on (open items, unmet checks, deferred decisions) sits buried mid-paragraph in dense build-note prose; the dev cannot scan what is done vs waiting on them. Direction: the plan's open/actionable items get tick boxes (`- [ ]`) like this file, so tracking is a glance. Design constraint to reconcile: §Plan's own rule that status lives in the Status column ONLY ("a second status system drifts, and the stale one wins on resume") — checkboxes must mark ITEMS (questions, unmet checks, fixes owed), never duplicate page status. Rules-text change → eval-gated: the plan-artifact scenarios (S1, S27, S42) grade `NDS-PLAN.md`'s shape and their `artifacts:` lines re-point with the format. **DONE 2026-08-17, rides v2.1** — one bullet in §Plan's memory list after the statuses bullet: open items are `- [ ]` lines (question/unmet check/fix owed/deferred decision), boxes never carry page status, and a pre-rule plan converts at its next update in place, rewriting nothing else (the conversion sentence covers the live mid-migration case the owner raised). S1/S27/S42 `artifacts:` re-pointed with dated notes. Gated by one solo run on the S83 fixture seeded with the old-format plan: PASS clean — answered questions became a Decisions section, the two unresolved items became boxes in place, statuses and inventory untouched, no build, both sentences quoted (runner self-stamped "Claude Fable 5" over model ID claude-sonnet-5 — contradictory self-report, ID credible). The conversion clause has ONE measured run and no standing scenario; add one at the next evolve if it matters. Full behavior re-runs of S27/S42 on the new artifacts lines ride the pre-release sweep.

## Standing decisions — do not re-propose without the named evidence

A decision lives with the artifact it governs, not here. An eval decision goes in its scenario's `baseline:` in `scenarios.md`; a grading lesson goes in `SKILL.md`; a decision already stated in source or docs is not restated at all. This section holds only what has no such home, so a full clear at release loses nothing.

- **R20 — two-download `_source/` population STAYS (2026-08-12, user call).** Kept here because it is the one call a reader would otherwise try to re-derive from the release script. The template zip ships no `_source/`; the rules populate it from the same tag's Source code zip. Triage proposed shipping `_source/` in the zip or a companion asset. Rejected: the current shape works against every template version an agent may meet, including releases cut before the rule existed, and it keeps the release script simple. Reopen only on the user's own initiative.

## Watch list — not tasks, things a later run should notice

- **Field triage 2026-08-12, the four items the v0.9 batch deliberately left out.** Each lands only on repeat field evidence, never on the single report that raised it. **R14**, what counts as a gate under gate-by-gate pacing: the file already says "when a step or page completes", which defines it — reopen if a run stalls or over-gates on the boundary. **R15**, a conformance rubric for prior NDS work (which violations force a clean rebuild, which are repair rows): one report read a single non-canonical head as enough to rebuild ten pages, which is defensible, so there is no measured failure yet. **R16**, a git-treatment table for the six artifacts (`NDS-IQ.md`, `.nds/`, `NDS-PLAN.md`, `NDS-REPORT.md`, `NDS_ASSETS`, the anchor file): cheap but unevidenced. **R19**, structured catalog fields (`install_family`, `layout`, `chrome_needs`) so chrome shape filters instead of reading ~90 `use_when` prose lines: land it only if S53 shows the prose match failing.

- **The block sweep script that wrapped everything.** The 1.8.0 cycle unwrapped 82 stray `.nds-block` divs it had left across doc pages — around definition items, around demo cards, and inside `<table>`. All known instances are gone and the built site scans clean. If a future sweep touches markup in bulk, check its output against the parents it wraps, not just the count of files changed.
