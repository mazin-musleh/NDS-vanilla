# TODO — current cycle

Standing file: it always exists. At every release commit, remove the items that release shipped and carry the open ones forward. Never delete the file itself. This is the one open list.

Cleared at the 1.8.0 release (2026-08-16). That release shipped `NDS.Init.destroy(container)` and the five component `destroy()` methods behind it, Main Navigation's `reinit()` and its own doc page, the Page Shell reference, NDS IQ v2.0 with the built-page shell copy and the headless-first §Verify, and the `nds-test-app-7` field report closed end to end — all detailed in `CHANGELOG.md`, with each scenario's state in `.claude/skills/nds-iq-eval/scenarios.md`. Nothing from that cycle is carried forward.

## Open

- [ ] **Field report `nds-test-app-7` cycle 2 (React SPA, 1.8.0, NDS IQ v2.0) — 7 findings TRIAGED 2026-08-16, every claim verified against source before entering this list; report at `C:\Projects\nds-test-app-7\NDS-REPORT.md`, plan reviewed and conforming.** Work these later, in this order:
  - [x] **F7 CONFIRMED — Filter's stale-id registry blocks remounts; the report's root cause is exact.** `_js/nds-filter.js:2979` skips any `data-filter-target` id already in `_instancesByTarget`, the entry is deleted only in `destroy()` (`:2924`), and NO staleness check exists — a framework view unmounted without `NDS.Init.destroy()` leaves a detached instance squatting the id, so the remount stays skeleton-held forever (the crit `data-nds-filter-initialized` hold). The doc's destroy-on-unmount pattern covers the consumer side (the agent admitted skipping it), but a source hardening is on the table: at claim time, release-and-reclaim when the registered instance's `filterContainer.isConnected` is false — the A7 `reinit()` precedent (a capability, not a restructure), and it would ALSO kill the "intermittent first-load race" the report filed separately (React StrictMode double-invoked effects = mount/unmount/remount = this same stale-id shape, not timing). Sweep other id-keyed registries for the same hole while there. Second candidate from the same finding: an `NDS`-prefixed console warning when a refresh claim skips a held id — today only `audit()` sees it after the fact. **DONE** — one `liveInstance(id)` helper in `nds-filter.js` replaces every `_instancesByTarget.get/has` read (`initializeFilters`, `create`, `getInstance`, `getByTarget`, and the `refresh()` walk): a registered instance whose `filterContainer` is detached is destroyed (releasing its pooled `NDS.onDOMAdd` subscribers — the reap also stops `refresh()` re-scanning orphans after a route change), the id is freed, and one `NDS Filter: reclaiming target "…"` warning names the missing `NDS.Init.destroy()`. Registry sweep found no second hole: stepper's id map is keyed off an element-level init stamp and overwrites on remount, copy's aborts the previous controller — Filter's was the only registry GATING creation on a `has(id)`. StrictMode's double-invoked effects are the same mount/unmount/remount shape, so the report's separate "intermittent first-load race" should be gone too (verify when F2 is worked). Regression check: two new cases in `scripts/check-init-destroy.mjs` (remount claimed by a fresh instance, remount stamped so the crit hold releases) — both FAIL on the pre-fix bundle, 36/36 pass with it.
  - [x] **F5 REAL, and it is the S80/B5 REOPEN CONDITION MET — the legacy-globals leak shipped again in the field.** The agent inventoried `styles.css` as rule #6's named legacy dependency and still never checked it for the bare-element selectors the same rule names; the dev caught it as the dark-mode symptom (again). The report's ask fits the admission test: a §Plan inventory step — like the CSP sweep — that greps the legacy stylesheet for bare-element selectors when the strategy is parallel-files/shared-build. T3's lesson applies: the knowledge sits behind a read nothing prompts; a prompted sweep sits in front. Eval-gated (S80/S21 exist; floor the sentence before landing). **Scenario landed 2026-08-16 — `S83 legacy-globals-caught-at-inventory`**, the mandated first step: it grades the INVENTORY moment (is the sheet opened and its bare-element rules named in `NDS-PLAN.md` before any page is built), which neither sibling covers — S80 grades noticing/remedy while building, S21 what a server-rendered NDS layout carries. Its `floor:` is PENDING: **run the pair (stub + real v2.0) before drafting any sentence**, and propose text only if BOTH fail. A stub FAIL alone is not a licence, and a stub PASS sends this to source — but there is no NDS doc that owns the consumer's own legacy CSS, so the cause-removal ladder lands on block text if the pair fails. Also fixed in passing: S81's `baseline:` line had drifted to the bottom of S82's block. **DONE — floor pair run 2026-08-16/17 on a junction-backed fixture, BOTH cells FAIL** (two real-v2.0 runs reproduced F5 exactly — one plan asserted the sheet "is not loaded on any NDS page" while its own strategy kept every NDS route under the importing entry; the 35-call stub run EDITED `styles.css` and still built under its globals). The sentence cleared the gate and landed as **v2.1**: a step-1 stylesheet sweep beside the CSP one — open the globally-loaded sheets, grep element-level selectors, record hits in the plan, rule-#7 strategy names how NDS pages escape them. Results, harness notes, and S80's reopen rider recorded in `scenarios.md`. Confirm re-run on v2.1: PASS — the plan's first finding is the stylesheet hazard with the sheet's real selectors and the shared-entry reach, pre-build, strategy-attached.
  - [x] **F3 REAL doc gap — framework wrapper elements break the shell's layout, mechanism source-verified.** `.nds-main-content` is `flex-direction: column; flex-wrap: wrap; align-content: flex-start` (`_content-layout.scss:36`), so an un-widthed wrapper div (JSX single-root reflex) shrink-wraps and everything inside loses the edge-to-edge layout; `.nds-content-section` survives only via crit's explicit `width: 100%`. Striping's `nth-of-type` and the `>aside` guard break under wrappers too. Same class, one level up, verified 2026-08-16 on the owner's ask: the framework MOUNT ROOT (`<div id="root">` between `body` and the chrome). No `body >` selector exists anywhere in the SCSS (chrome styling survives), but the viewport-fill chain snaps — `body { min-height: 100dvh; display: flex; flex-direction: column }` + `main { flex: 1 }` (`_reset.scss:29`, `_content-layout.scss:6`) stretches `main` inside a content-sized `#root`, so the footer rides up on short pages (the rig's long tables masked it). Consumer fix is one line: `#root { display: contents }`, or mirror the flex chain onto it. Fix per the ladder: ONE framework note in `layout/page-shell.md` covering BOTH levels — the mount root (`display: contents` or flex pass-through) and per-component wrappers (return a Fragment, never a wrapper, into `body`/`.nds-content-layout`/`.nds-main-content`) — the routed read at exactly the right moment; rules text only if a scenario proves the doc alone insufficient. **DONE** — `layout/page-shell.md` gained a `Framework Wrappers` block in Usage Guidelines (before Modifier Classes) covering both levels: the mount root (`display: contents`, or `flex: 1` + column pass-through) and per-component wrappers (return a fragment; never a plain `div` inside `body`/`nds-content-layout`/`nds-main-content`). Re-verified while writing, and ONE triage claim above is wrong: the section's survival is NOT crit's `width: 100%` — it is `_sass/layout/_section-layout.scss:26`, `:is(.nds-hero-section, .nds-content-section) { width: 100% }` in the MAIN bundle. The rest holds: `.nds-main-content` at `_content-layout.scss:35-39`, `main { flex: 1 }` at `:6`, `body` flex column at `_reset.scss:22-35`, the `>aside` direct-child hide at `:113`, striping's `nth-of-type` at `:220`, and no `body >` structural selector anywhere in `_sass/`. Doc-only, so `last_edit` moved and `updated` did not. No rules text — the doc is the routed read. **Then MEASURED, on the owner's ask (the item was raised to confirm NDS itself does not conflict with a wrapper between `body` and the page content), not left at source reading — `scripts/check-mount-wrapper.mjs`, three wrapper positions × four shapes (side menu, console, striped-no-menu, minimal sign-in), before/after on chrome width, content width, section width, side-menu visibility, striping parity, layout box and footer:**
    - **Mount root (`body` → wrapper → chrome): NO conflict on any shape, every metric identical.** The single exception is the sticky footer on a page whose content is SHORTER than the viewport — `body`'s flex column no longer reaches `main`, so the footer stops at the content (2000 → 1509 on the side-menu shape, → 1127 console, → 1100 striped). The minimal shape is immune: it ships no footer and centers on the layout's own `min-height`. This is the whole conflict surface, and it is invisible on any page taller than the viewport — which is why it survived the rig's own testing.
    - **Wrapper inside `.nds-content-layout`: badly broken, but only under `nds-wSideMenu`** — the wrapper takes the side menu's grid column, so the entire page collapses to `--nds-sidemenu-width` (1020 → 260 with a menu, 1140 → 260 console) and doc height roughly doubles. Without a side menu the layout is a single column and nothing moves.
    - **Wrapper inside `.nds-main-content`: shrink-wraps only when the content's max-content width is under the container** — no change on any normal page, 1336 → 360 on sign-in's narrow form. So the failure is real but content-dependent, which is why it reads as intermittent.
    - **BOTH remedies the doc offers were then measured too, not just the breakage** (owner's ask — a recommended fix that is never run is a guess). Each is injected AFTER the wrapper exists, on all four shapes, long and short: `#root { display: contents }` and `#root { flex: 1; display: flex; flex-direction: column }`. **16 fix runs, zero changed values, zero page errors** — every metric back to its unwrapped baseline and the footer re-pinned to the viewport bottom on every short page. Neither remedy costs anything elsewhere in the shell.
    - **Striping never broke, in any position** — `nth-of-type` counts inside the wrapper, and one wrapper holding every section preserves the parity exactly. The first draft of the doc note claimed it did; **the claim was measured false and removed**, along with the `>aside` framing (the real mechanism is the grid column, not the direct-child hide). The `>aside` hide at `_content-layout.scss:113` is a no-side-menu guard, not what breaks here.
  - [ ] **F4 REAL doc gap — `nds-full-width` is FOUC-sensitive and only theme has pre-paint guidance** (`ui-shell/head.md:113` is theme-only). Document body-level paint-affecting classes alongside the theme guard (stamp before first paint; the rig's `index.html` route-guard + `useLayoutEffect` pattern is a working reference).
  - [x] **F6 — composition-source members silently dropped (search submit + filter dropmenu from manage-records); the agent's own miss, rule #3 already bans it.** The ask is a before-done self-check shaped like the icon sweep: re-open the named composition source and diff the shipped page's controls against it. Scenario-first (this is toolbar-adjacent recurrence #3 across rigs); no rules text before the floor pair. **DONE 2026-08-17 — S84 landed, floor pair run: stub PASS + real PASS = LATENT, no sentence owed.** Both cells shipped both field-dropped members and flagged every cut (real run rule-anchored each one and ran the icon sweep unprompted); the field incident was the rig agent's own admitted miss on text that already bans it. The before-done self-check is NOT admitted; S84 is the standing guard — a rig shipping a silent member trim on current text reopens it. Full detail, harness notes (junctions → real-copy fixtures after a second traversal void; composition scenarios must not sit behind an unresolved chrome gate), and a two-datapoint annotated-Status-cell watch recorded in S84.
  - [ ] **F1 MISREAD as stated — do not act on its ask without a local repro.** The claim "refresh() only invokes refresh hooks, so Tables is skipped" is wrong: scanners (no `refresh` field) get their idempotent `init()` re-run by the walk (`nds-loader.js:1028`), and `core/refresh.md:54` already documents the bundle-skip. The observed stuck-Tables symptom is most plausibly F7's Filter hold or the stub-skip window; reproduce locally before touching `core/refresh.md`'s "no component can be forgotten" sentence. **F2 PARTIAL** — the race framing likely reduces to F7 + StrictMode, but the underlying ask stands as an owner capability call: no public "NDS fully initialized" primitive exists (`data-nds-loaded` = reveal, not delegated bundles; watching it is DIY). Decide: documented promise/event, or a doc note that the arrival scan self-heals and refresh calls need no awaiting.
  - [ ] **Watch (S72/S79 field data):** the rig verified in a driven Chrome tab whose resize did not take, and reported mobile as an honest UNMET check instead of switching to a viewport-setting headless run — the degradation behaved (no false "verified"), but the headless-first default did not reach mobile. Second occurrence gets a scenario look.

- [ ] **Field report `sso-rig` cycle 1 (server-rendered .NET, sign-in family, 1.8.0, NDS IQ v2.0) — 4 findings TRIAGED 2026-08-16, every claim verified against source before entering this list; report at `D:\Projects\IU-SSO\NDS-REPORT.md`.** Work these later, in this order:
  - [x] **F3 CONFIRMED — Alert's toast progress knob is stamped via `innerHTML` `style="…"`, blocked under strict CSP.** `_js/nds-alert.js:118` builds the close-button HTML with a literal `style="--progress-duration: ${Number(duration)}ms;"` that lands via `innerHTML` (`:136`). Rule #5's promise to strict-CSP consumers ("NDS's own JS sets styles through the CSSOM, which no policy governs") is broken in exactly this one place. Functional impact: nil (auto-dismiss runs on `setTimeout`, `:224-228`); cosmetic: the progress ring never animates because the custom property is never set; signal: every toast fires a console error under strict CSP. Fix per the ladder: cut the `style` off the HTML string, write `alert.querySelector('.nds-alert-close').style.setProperty('--progress-duration', ...)` after `innerHTML` insertion — same effect, CSP-clean. Then sweep `_js/*.js` for `style="` in template strings — any other HTML-templated knob is at the same risk, and the sweep makes the promise checkable across every future `innerHTML`-authored subtree. **DONE b2823ea1** — the sweep found three more shipped sites, two of them worse than F3: pagination's virtual-scroll spacers collapsed to 0px (page-number menu could never scroll past its first window), the editor's remove-menu lost its indent (depths 1/2/3 render as identical destructive rows — the label prefix is one dash at any depth), and upload's file-item template. All four now write via the CSSOM; `nds-showcase.js` and `nds-theme-foundation-day.js` still hold `style="` but build to `docs-assets/`, which no consumer hosts. Upload's 24px moved to `_upload.scss` instead — a constant belongs in CSS — which also lets a consumer strip the inline style from copied canonical markup and still render right. Follow-on from the same reading: `_upload.scss`/`_buttons.scss`/`_accessibility.scss` now set `--_progress-size: var(--progress-size, default)` so a consumer override wins from `:root` or a wrapper instead of losing to inheritance proximity; stepper excluded (sets `--progress-size` on its root and reads it back in four calcs — that swap is a knob-architecture pass, not a CSP fix).
  - [x] **F4 CONFIRMED — OTP fieldset `data-status` never paints the digit borders.** `_forms.scss:63,72,81` scope the error/success/warning border rules to `.nds-form-container[data-status] > .nds-form-control` — the container that DIRECTLY wraps the control. `_otp.scss` has no `data-status` selector (verified all 143 lines), and `components/otp.md:300,359,444,607` all show the canonical pattern as `setStatus({element: otpGroup, ...})`. So the feedback text renders (via the fieldset's `data-feedback-target`) but the digit boxes stay neutral — the "invalid what" affordance every other field gives for free is missing. Fix per the ladder: three lines in `_otp.scss` cascading `.nds-otp-group[data-status="{error,success,warning}"] .nds-otp-container > .nds-form-control { border-color: … }` — no JS change, same `setStatus` call, matches the visual contract of every other form field. The alternative (teach `setStatus` to propagate group→containers in JS) is larger scope for identical visible behavior. **DONE** — shipped as the three-line cascade, but the owner then cut field status down to `error` + `help` system-wide (owner call, same session): success/warning/info no longer tint the outline OR the message. Root cause sat in JS, not CSS — `setStatus` now folds any other status to `neutral` on both the container and the feedback it creates, because `_feedback-icons.scss` colours by ANCESTOR (`[data-status="success"] .nds-feedback`) and `Feedback.create` stamps the status on the message element, so a CSS-only cut left two paths still painting. A hand-authored `data-status` bypasses the JS guard entirely, so `_forms.scss` also resets the feedback knobs to `initial` for both `.nds-form-container` and `.nds-form-group`. Two traps found on the way: `_forms.scss`'s old `>.nds-feedback` status rules never fired in the common case (a `data-feedback-target` puts the message a level down, so `_feedback-icons.scss` was doing all the work), and `help` was already a full status in the feedback component — the first guard flattened it and killed its "?" icon. `.nds-feedback` also gained a default `color: var(--text-primary-paragraph)`. Docs: `forms.md` (demo buttons now Error/Help/Neutral/Clear), `otp.md`, `filter.md`, `feedback-icons.md` (its Feedback Messages dropmenu was missing `help`). Release note is a `### Changed` entry, NOT a migration step — consumer calls still run, they just render neutral.
  - [ ] **F2 REAL API gap — `NDS.Forms.setStatus` has no focus semantics, so server-error hydration silently skips the first-invalid focus consumers expect.** `_js/nds-forms.js:687` — `validateForm` runs `acc.firstInvalidInput.focus()` when `focusFirst: true` (default); `setStatus` (`:225-273`) paints `data-status` and inserts feedback but never touches focus. The split is defensible (state-only primitive vs submit lifecycle) but every stack-rendered consumer hydrating server `ModelState` errors traverses only `setStatus` and ends up re-writing the same query-and-focus loop, and a long form with the first invalid field below the fold renders an error the user cannot see. Fix per the ladder: add an opt-in `focus` option to `setStatus` (default `false` for back-compat) that focuses the container's first `input, textarea, select` after the feedback lands; and/or a `NDS.Forms.focusFirstInvalid(container)` helper for many-error hydration. Doc-only is second-best — the loop is small but every server-rendered consumer writes it.
  - [x] **F1 REAL doc/IQ gap — canonical markup mixes STRUCTURAL and SLOT hooks; a reader following rule #3 verbatim ships opt-in scaffold as required markup.** `_js/nds-forms.js:232-252` — when `setStatus` runs without any `data-feedback-target`, `target` stays as the container itself and the feedback inserts there. So `<div class="nds-form-footer" data-feedback-target hidden>` is opt-in for explicit placement, not required for empty-field feedback to work. Doc-page examples ship the slot inline without calling out its opt-in status; the JS source's `setStatus`/`_ensureX` helpers are where the structural-vs-slot discriminator lives. Failure mode is bloated ports that read wrong to a reviewer who knows the API, not broken pages. Fix per the attribution default (2026-08-14): SOURCE first — the forms/OTP doc-page examples that ship the slot inline don't mark it opt-in, and neither does the forms banner. Eval-gated: floor the sentence before proposing rule #3 text; the doc/banner fix likely absorbs it. Same class as `nds-test-app-7`'s F3 (routed read at the right moment > rules text). **PHASE 1 DONE 2026-08-17 (owner call: feedback is API-created; inline slot only for a permanent position).** Classification sweep found it worse than triaged: `forms.md` MANDATED the slot ("Always include a `data-feedback-target` element in the form footer… The validation system needs this target" — false per `nds-forms.js:232-259`) and the empty-hidden slot ships in the primary markup of 21 files (~100 of 141 hits; the rest are populated/permanent slots that stay, plus `form-header` placements — intentional, stay). Fixed: the false bullet and the stale four-status Status Feedback description rewritten (API-created default, error/help only), the Permanent Feedback section gained the canonical placement paragraph (any element in the container; header above, footer below; hidden targets auto-show/hide), radio/checkbox "Place a…" bullets reworded opt-in, forms banner gained the no-slot-needed Gotcha, bundles rebuilt. `nds-form-footer` confirmed zero-CSS (bare hook, not a designed area). **CLOSED at phase 1 (owner call 2026-08-17): phases 2–3 dropped.** The empty-hidden slot is inert (zero CSS, no paint, and as the container's last child it renders feedback where the fallback would anyway) — harmless extra per the deliver-extra principle, so the ~100-slot sweep is churn with no measured gain (and bulk-sweep risk, see the watch list). No rules sentence is proposed, so the floor gate has nothing to license — no scenario, no runs. Reopen only if a rig report shows the slot causing real harm, not reviewer taste.

- [x] **`NDS-PLAN.md` format is not dev-readable — fix in the next IQ revision (owner ask 2026-08-16).** Exhibit: `C:\Projects\nds-test-app-7\NDS-PLAN.md` — the table row carries the status, but everything a dev must act on (open items, unmet checks, deferred decisions) sits buried mid-paragraph in dense build-note prose; the dev cannot scan what is done vs waiting on them. Direction: the plan's open/actionable items get tick boxes (`- [ ]`) like this file, so tracking is a glance. Design constraint to reconcile: §Plan's own rule that status lives in the Status column ONLY ("a second status system drifts, and the stale one wins on resume") — checkboxes must mark ITEMS (questions, unmet checks, fixes owed), never duplicate page status. Rules-text change → eval-gated: the plan-artifact scenarios (S1, S27, S42) grade `NDS-PLAN.md`'s shape and their `artifacts:` lines re-point with the format. **DONE 2026-08-17, rides v2.1** — one bullet in §Plan's memory list after the statuses bullet: open items are `- [ ]` lines (question/unmet check/fix owed/deferred decision), boxes never carry page status, and a pre-rule plan converts at its next update in place, rewriting nothing else (the conversion sentence covers the live mid-migration case the owner raised). S1/S27/S42 `artifacts:` re-pointed with dated notes. Gated by one solo run on the S83 fixture seeded with the old-format plan: PASS clean — answered questions became a Decisions section, the two unresolved items became boxes in place, statuses and inventory untouched, no build, both sentences quoted (runner self-stamped "Claude Fable 5" over model ID claude-sonnet-5 — contradictory self-report, ID credible). The conversion clause has ONE measured run and no standing scenario; add one at the next evolve if it matters. Full behavior re-runs of S27/S42 on the new artifacts lines ride the pre-release sweep.

## Standing decisions — do not re-propose without the named evidence

A decision lives with the artifact it governs, not here. An eval decision goes in its scenario's `baseline:` in `scenarios.md`; a grading lesson goes in `SKILL.md`; a decision already stated in source or docs is not restated at all. This section holds only what has no such home, so a full clear at release loses nothing.

- **R20 — two-download `_source/` population STAYS (2026-08-12, user call).** Kept here because it is the one call a reader would otherwise try to re-derive from the release script. The template zip ships no `_source/`; the rules populate it from the same tag's Source code zip. Triage proposed shipping `_source/` in the zip or a companion asset. Rejected: the current shape works against every template version an agent may meet, including releases cut before the rule existed, and it keeps the release script simple. Reopen only on the user's own initiative.

## Watch list — not tasks, things a later run should notice

- **Field triage 2026-08-12, the four items the v0.9 batch deliberately left out.** Each lands only on repeat field evidence, never on the single report that raised it. **R14**, what counts as a gate under gate-by-gate pacing: the file already says "when a step or page completes", which defines it — reopen if a run stalls or over-gates on the boundary. **R15**, a conformance rubric for prior NDS work (which violations force a clean rebuild, which are repair rows): one report read a single non-canonical head as enough to rebuild ten pages, which is defensible, so there is no measured failure yet. **R16**, a git-treatment table for the six artifacts (`NDS-IQ.md`, `.nds/`, `NDS-PLAN.md`, `NDS-REPORT.md`, `NDS_ASSETS`, the anchor file): cheap but unevidenced. **R19**, structured catalog fields (`install_family`, `layout`, `chrome_needs`) so chrome shape filters instead of reading ~90 `use_when` prose lines: land it only if S53 shows the prose match failing.

- **The block sweep script that wrapped everything.** The 1.8.0 cycle unwrapped 82 stray `.nds-block` divs it had left across doc pages — around definition items, around demo cards, and inside `<table>`. All known instances are gone and the built site scans clean. If a future sweep touches markup in bulk, check its output against the parents it wraps, not just the count of files changed.
