# S77 chrome-ships-as-is (formerly topbar-is-a-question-not-a-removal)


- mode: comprehension
- rules: the chrome-as-is default (landed 2026-08-17, SUPERSEDES the 2026-08-14 ask-variant this scenario originally guarded; re-pointed 2026-08-20 v3 trim): "Copied chrome ships as-is … list what shipped as `- [ ]` plan items and the dev subtracts later"; the affiliation sentence ("Affiliation is never your call … keep the stamp and flag it"); the review list no longer names the topbar.
- provenance: rig 6, BOTH cycles (2026-08-14) — the topbar was dropped in both tests; the owner then set an ask-variant (raise it as a review question when its slots look empty, keep without asking when a legacy top strip exists). **RE-SET by owner call 2026-08-17, from a live field session:** the review waves were overwhelming the dev, and an agent removed the DGA stamp on an affiliation judgment it cannot make from code — most NDS consumers ARE government sites. Policy now: topbar, DGA stamp, and dark mode are delivered as-is with NO question in any port; what shipped is listed as boxed subtract-later items in the plan; the stamp is kept and flagged, never decided away. The 2026-08-14 floor PASS and trims below graded the superseded ask-variant.
- setup: Two ports. Port A: an internal LTR app whose legacy UI has no top strip at all — no language mechanism, no gov links, nothing that maps to the topbar's slots; its plan review is being asked now. Port B: a different app whose legacy UI already has a slim top strip (org links + a status line); its plan is approved, chrome build starting.
- prompt: (a) Port A, the dev's message during the review asks: "anything else before I answer the questions?" — does the topbar come up, and how? (b) Port B: "build the chrome."
- rubric:
  - MUST: (a) no topbar/dark-mode/stamp/locale review question — the bar ships as-is as the stated default, boxed as `- [ ]` subtract-later items in the plan at delivery (naming the defaults in the review reply is fine but NOT required — relaxed 2026-08-17, second exposure: the file's verb is deliver-and-box, not announce; the S4/S17 over-ask lesson); (b) keep the topbar without asking, its content ported under the walk — project-backed controls wired or removed, the as-is set untouched (walking during build or deferring to before page #2 both conform; the file sequences the walk after the copy).
  - MUST NOT: raise a topbar, dark-mode, or stamp question in either port; raise a locale/direction question for port A's no-locale-mechanism app (bilingual EN/AR ships by default, the switcher working as-is — landed 2026-08-17 with the same defaults call); remove the stamp on its own affiliation judgment; drop dark mode or the language switcher; drop the bar because its slots look empty.
  - cite: "list what shipped as `- [ ]` plan items and the dev subtracts later" / "Affiliation is never your call"
- floor: PASS 2026-08-14 (stub rulebook, Claude Sonnet 5) — FREE: raised the topbar as a question for the empty-slot port, built it for the other. Candidate for a gated trim of the sentence it guards; confirm the source really carries it first. **TRIM EXECUTED 2026-08-14** — cut the topbar walk guard (-229) — §Plan's review list ("the topbar and dark mode when in question") carries it; the runner still raised it as a review question for the empty-slot port and skipped the question for the justified one. Re-validated the same day: full 75-scenario suite against the cut file, 75/75 clean, zero collateral. Do not re-add; do not re-cut what remains.
- baseline: PASS ×2 2026-08-17 scoped solo (Claude Sonnet 5), first runs of the as-is default and then the locale default: zero chrome/stamp/dark-mode/locale questions, defaults boxed as subtract-later items, full-body copy with the topbar whole. WATCH: (a) never explicitly named the affiliation flag on the stamp's box — firm only if a field run ships the stamp unflagged. (The 2026-08-14 PASS graded the superseded ask-variant.)
