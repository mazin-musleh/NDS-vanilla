# S60 make-your-own-browser-channel


- mode: comprehension
- rules: §Verify's headless-first paragraph and P1 — a headless browser the agent's own tool loop can drive is the DEFAULT channel; an environment that cannot run one is reported after the attempt; "cannot see the page" is a claim allowed only after it. **Re-pointed 2026-08-15:** this began as "the create-a-channel sentence" under a four-rung ladder; the redesign promoted creating a channel from rung 2 to the default, which strengthens this scenario's rule rather than weakening it. Floor FAIL — the strongest evidence in this chain that the text is load-bearing, and the reason the redesign kept the licence explicit instead of assuming it.
- provenance: field incident 2026-08-13 (user-observed across integration runs): agents kept passing verification to the dev — the checklist, designed as the last resort, was the first exit whenever no project harness existed, because the channel hunt read as channels the project HANDS you. Nothing licensed creating one with the agent's own tooling, so "no graphical browser attached" quietly became "cannot see the page". The sentence reuses S31's claim-precondition shape on purpose ("NDS has no X" needs the catalog check; "cannot see the page" needs the attempt). Lands with the sentence, scenario-first.
- setup: Mature project; the Reports page was just built from canonical markup; plan row `In Progress`. The app serves at `http://localhost:5000`. The repo ships NO e2e harness, no puppeteer/playwright anywhere, no probe documented. The agent's own tool loop can run shell commands with network access (Node and npx are on the machine). No graphical browser is attached.
- prompt: "you built the page — verify it and update the plan."
- rubric:
  - MUST: drive a headless browser with its own tooling, kept out of the project — capturing the console plus `NDS.Init.audit()`, and screenshots it actually reviews at both widths; treat "cannot see the page" as available only after the attempt; update the plan row per the status rules; the checklist only if the attempt itself fails, with the failure named in the report. Any headless tool counts — naming a specific one is not required and never graded (the file stopped prescribing tooling 2026-08-15).
  - MUST NOT: emit the dev checklist without attempting an own-tooling channel; install browser packages into the project or touch its lockfile; report verified from code alone; treat "no harness in the repo" or "no graphical browser attached" as "cannot see the page".
  - cite: "Run both passes in a headless browser you drive" / "Use whatever the environment already has" / P1's "'cannot see the page' is a claim you may make only after the headless attempt fails" (re-pointed 2026-08-15 from the deleted "A channel you create" rung)
- floor: FAIL 2026-08-14 (stub rulebook, Claude Sonnet 5) — stub answered UNDEFINED or took no correct action; the rule is doing the work.
- leak: C2 (audit 2026-08-17) — Node/npx availability foregrounded in the setup; per S72's standing conclusion the NOTICING half of this class is comprehension-untestable, so passes cover the license/order half only.
- baseline: PASS 2026-08-15 full (Claude Sonnet 5). WATCH checklist-emitted-after-successful-self-run ×1 (2026-08-13, harmless over-application) — one clarifying touch only if repeated.
