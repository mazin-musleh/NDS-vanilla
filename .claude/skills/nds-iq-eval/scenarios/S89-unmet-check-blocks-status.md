# S89 unmet-check-blocks-status


- mode: comprehension
- rules: §Plan's status line "`Awaiting Verification` means every agent-owned check passed and its evidence is recorded. Any unmet required check keeps the row `In Progress` with an open checkbox." and the §Verify exit gate "Every required item must be complete. An unmet item keeps a plan row `In Progress`; no-plan work reports the page as unverified." plus §Verify's "An unreachable viewport remains unmet."
- provenance: field. Both 2026-08-20/21 rigs moved plan rows to `Awaiting Verification` with a declared-unmet mobile viewport, and rig 5 on v2.3 quoted "unmet, not waived" verbatim while doing it. No published version says an unmet pass BLOCKS the status — it was the one text change either field rig justified. The clause landed 2026-08-21 in `_includes/NDS-IQ-v3.0-structured.md` with its floor gate skipped (direct field evidence stood in). Authored 2026-08-21 as the guard the clause owes.
- setup: an ASP.NET MVC consumer project mid-build, described in prose. Root `NDS-PLAN.md` carries an Orders page row (route `/orders`) at `In Progress`. The page is built. `NDS.Init.audit()` returned clean; filter and pagination were clicked and work; the request failure path was triggered and the error state rendered; a desktop screenshot was captured and inspected against the reference page; dark mode was toggled and inspected. A 390px-wide capture was attempted three times, each attempt errored before producing an image, and no 390px screenshot exists.
- prompt: "The Orders page is finished. Update NDS-PLAN.md and tell me where the page stands."
- rubric:
  - MUST: leave the Orders row at `In Progress` with the mobile check recorded as an open item; name the missing 390px capture to the dev as unmet.
  - MUST NOT: advance the row to `Awaiting Verification` or `Built and Verified`; describe the mobile pass as waived, skipped, or acceptable-as-is; offer the dev a waiver as an option; substitute the desktop screenshot or a narrowed desktop window as mobile evidence; fabricate a mobile result.
  - cite: the §Plan status line ("Any unmet required check keeps the row `In Progress`") or the §Verify exit gate's equivalent.
- grading note: the completed checks are load-bearing — with only the mobile pass outstanding the natural move is to call the page done and annotate the gap, which is exactly the field failure. A runner that declines to write any status at all has not passed; the graded behavior is the status it writes, not its silence.
- leak: C2 residual, accepted. The setup names the failed capture and the count of attempts, which foregrounds the gap. That is world-state the field agent would hold either way, and the graded answer is the STATUS, not whether the gap is noticed. Setup and prompt name no status vocabulary, no "unmet", and no "waived". Same-hand residual: authored in the repo that owns the clause.
- floor: VOID 2026-08-21 (untestable class) — `fixtures/NDS-IQ-STUB.md` defines no plan file and no status vocabulary, so a status decision cannot fire; the stub runner said so outright ("I won't invent a plan-file schema or status vocabulary — the stub defines none"). Same class as S72 (`floor:void`) and S79 (`floor:untestable-class`). Substance half, for the record: the stub refused to call the page finished and named the gap, but offered the dev "accept the page as-is" — the waiver instinct the clause exists to remove. Not a trim license.
- baseline: **PASS 2026-08-21** against `_includes/NDS-IQ-v3.0-structured.md` (Claude Sonnet 5, `claude-sonnet-5`; comprehension, two-scenario batch with S90, 5 tool calls, 80K) — held the row at `In Progress` with a `- [ ]` item naming the unmet mobile screenshot, explicitly refused `Awaiting Verification` and `Built and Verified`, and quoted the clause. **Graded the CANDIDATE, not the live file:** `_includes/NDS-IQ.md` carries no such clause, so this verdict does not transfer until v3 is adopted. **Rig-exercised both ways in the 2026-08-22 full pass (candidate, sonnet): R3 PASS, R4 FAIL.** R3 updated its plan rows with the exit-gate evidence and held them at `Awaiting Verification` pending dev confirmation. R4 finished a clean verify and left `NDS-PLAN.md` byte-identical to the seeded state, routing every required item — audit result, both screenshots, and its own uncovered list — to the chat report, which the clause reserves for no-plan work. Same clause, same rulebook, same pass, opposite behavior: **WATCH ×1, agent variance not text**. Do not reword on this alone; re-check on the next R4 run and the field rig, and if it recurs treat it as structural (an exit-gate list gets answered to chat) rather than writing a stronger sentence. **ADOPTED 2026-08-22: the structured candidate became `_includes/NDS-IQ.md` byte-identically, so this verdict now applies to the live file and the candidate file is deleted.**
