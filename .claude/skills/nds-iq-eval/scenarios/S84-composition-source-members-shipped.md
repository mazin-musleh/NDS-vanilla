# S84 composition-source-members-shipped


- mode: comprehension
- rules: rule #3's ban row ("Dropping or reordering a copied unit's members to satisfy a project constraint") and P1's "this page is done" row (both §Verify passes + the icon sweep) against §Build step 4's parts inventory. **No self-check sentence exists — no text moves before this measures** (floor pair, stub AND real must both fail — the S80 correction).
- provenance: field report `nds-test-app-7` cycle 2, 2026-08-16, finding F6 — composing the Application-list toolbar from `manage-records.md` (the plan row's own citation), the agent silently dropped the `nds-search-btn` submit (`examples/manage-records.md:105`) and the full `nds-dropmenu nds-filter` control (`:111`), both present in the source, matching legacy which had neither; the agent's own admitted miss, filed in `NDS-REPORT.md`. Toolbar-adjacent recurrence #3 across rigs. **Distinct graded moment from its siblings:** S46 grades routing TO Toolbar at the parts inventory, S28 grades FINDING manage-records via `use_when`; this grades whether the shipped page still carries every member of the source the plan cites, at the before-done moment — completeness, not routing.
- setup: the S83 fixture (`fixtures/mini-spa/`, per-run assembly in `fixtures/README.md`) advanced to post-review state: the approved v2.1-format plan (decisions recorded, open items boxed, gate-by-gate pacing) in BOTH app copies; `src/pages/Records.jsx` is the parity trap the fixture now ships — a live-filter search input with NO submit button, a plain table, NO filter control. Copies byte-identical except the rulebook. Harness note: the prompt never names the toolbar, the source, or completeness (the S72/S79 tell). **Seeded-row spec (de-leaked 2026-08-17, harness-fidelity audit): the plan's Records row reads exactly the field plan's row — `Example match: "Manage Records" (_source/examples/manage-records.md), console/nds-full-width chrome shape` — NEVER "table + filter + pagination + export" or any member enumeration. The 2026-08-17 pair passed on a seeded list that enumerated the members under test, and the pass measured the leak.**
- prompt: "plan's approved — build the Records page."
- rubric:
  - MUST: the shipped NDS Records page's control bar carries every member of the source's toolbar block — the search field WITH its submit button, the filter dropmenu, and the rest of the source's control members — copied from the source; any member deliberately cut is named to the dev as a scope question before the page is called done, never dropped silently.
  - MUST NOT: trim the toolbar to legacy parity silently; hand-compose the control bar from primitives; invent `.nds-*` markup; call the page done with the drop unflagged.
  - cite: rule #3's "Dropping or reordering a copied unit's members to satisfy a project constraint" row
- grading note: the artifact carries the grade — diff the built page's control bar member-by-member against `manage-records.md`'s toolbar block; "I followed the source" as prose passes nothing. The stub cell grades on substance (no rule #3 exists there): a complete copy or a flagged cut on ordinary judgment passes; a silent parity trim fails.
- leak: C1 CONFIRMED (audit 2026-08-17) — the proven case that triggered the audit; see the seeded-row spec in `setup:` and the voided verdicts below.
- floor: see baseline — the de-leaked pair is the floor evidence (stub cell = flagged parity trim); the voided leaked PASS is recorded there.
- baseline: **soft-FAIL 2026-08-17, de-leaked pair (Claude Sonnet 5; stub 69 / real 76 tool calls) — OPEN, hands to R5.** History: the same-day leaked pair (seeded row enumerated the members) read PASS-PASS while the field failed twice — voided by the harness audit; the leak was the result. De-leaked (seeded row = the field plan's own text; both cells the same no-chrome post-review state with the logo decision recorded): STUB trimmed the bar to legacy parity (filter/export/column-visibility/per-page absent) but FLAGGED it — judgment discloses, it does not ship. REAL shipped 6/7 members incl. both field-dropped ones (nds-search-btn; nds-dropmenu nds-filter + nds-filter-applied), named its cuts (CRUD / row actions / bulk-select) — and silently dropped column visibility, justifying the named cuts with the parity rationale R5 proposes to ban ("legacy never had them"). The harness agrees with the field; R5's floor evidence is in hand. Side notes: the stub cell dropped the DGA stamp on its own affiliation judgment while the real cell kept it flagged (S77's clause carrying where present); run-1 lesson stands — a composition scenario must not sit behind an unresolved chrome gate. WATCH annotated-Status-cell ×2 (2026-08-17, both real-file runs of the leaked pair): the Status column shipped annotated ("Awaiting Verification — built (chrome + content); …") where the file says "Statuses, exactly" — parseable and honest, but off-canon; a third occurrence gets a scenario look.
