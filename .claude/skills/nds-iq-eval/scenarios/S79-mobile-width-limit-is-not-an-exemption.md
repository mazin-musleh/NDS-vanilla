# S79 mobile-width-limit-is-not-an-exemption


- mode: comprehension
- rules: §Verify's headless-first paragraph (landed 2026-08-15: "Run both passes in a headless browser you drive … desktop and mobile are the same run, not a fallback you have to reach"); the visual pass's "Look at the page at desktop and mobile width"; the fallback ladder's rung 1 and P1. Scenario written earlier the same day against the ladder this replaced — see `provenance:` and `floor:` for why the redesign happened.
- provenance: nds-test-app-7, 2026-08-15 (React SPA, template 1.7.2, NDS IQ v1.0 — the file with the resize sentence cut). The agent's harness browser could open and screenshot the page but had no viewport action. It verified desktop, reported the mobile check as unverifiable in this environment, and stopped. The dev's "check on other tools like Playwright/Puppeteer" produced correct behavior on the very next turn — capability present, trigger missing. **Written as the "untold twin" of S72 — a claim that did not survive the same day.** The intent: S72 TELLS its runner the window cannot be resized, so it measures the fallback only, never the noticing, and therefore passes whatever is cut from this chain. This setup was meant to withhold that tell by describing the tool's action list neutrally instead of flagging it as a problem. The cut probe disproved it (see `floor:`) — stating the gap at all is the tell, and the scenario passes without the sentence too. **Read this scenario for what it does test — that the agent defaults to headless rather than settling for the attached browser — and not as a trim gate for anything.**
- setup: Mature project; the Reports page was just built from canonical markup and its plan row reads `In Progress`. The app serves at `http://localhost:5000`. Your harness ships a browser tool: it opens a URL in the machine's browser, takes screenshots, clicks, and reads the console. Its action list has no window-size or viewport option. The repo ships no e2e harness. Node and npx are on the machine, and your tool loop can run shell commands.
- prompt: "you built the page — verify it and update the plan." Then, as the artifact: write out the plan row you would set, and the verification result you would report to the dev, in the words you would use. **Do not ask for an `NDS-REPORT.md` entry** — the first wording did (2026-08-15) and the runner correctly refused it, quoting "collects what adoption surfaces about NDS itself"; a clean verification run is not an entry, so the ask manufactured a soft on a right answer (the standing S4/S17 lesson). The dev-facing result line is what carries the grade: "mobile: unverifiable in this environment" is the field failure in the runner's own words.
- rubric:
  - MUST: drive a headless browser for both passes rather than settling for the harness tool, with the mobile width part of the SAME run (post-redesign the fallback need not be "reached" at all — reaching it via rung 1 and then correcting still passes, so long as mobile is actually seen); run both passes at both widths; move the row to `Awaiting Verification`. Any headless tool counts — naming a specific one is not required and never graded.
  - MUST NOT: report the page verified from desktop width alone; record mobile as "unverifiable" / "not testable in this environment" / "needs the dev" in the report, the plan, or the dev-facing result; emit the dev checklist while a headless browser is drivable; claim "cannot see the page" (the page WAS seen — only the width was out of reach); install browser packages into the project or touch its lockfile; self-mark `Built and Verified`.
  - cite: "Run both passes in a headless browser you drive — it sets its own viewport, so desktop and mobile are the same run, not a fallback you have to reach" / "a width it cannot reach is an UNMET pass, never a waived one" / P1's "'cannot see the page' is a claim you may make only after the headless attempt fails" (all re-pointed 2026-08-15 to the redesigned ladder)
- grading note: the artifact carries the grade — a result line reading "mobile: unverifiable" is the field failure verbatim, and it cannot hide behind a described route.
- floor: not stub-run. CUT PROBE 2026-08-15 PASSED — which is the finding: stating a capability gap in ANY setup is itself the tell, so the probe would have cleared the bad trim too. Do not re-propose a cut probe as a trim gate in this chain; the class (discovery-under-load) is untestable in comprehension mode — S72's floor carries the standing conclusion.
- leak: C2, self-documented (audit 2026-08-17) — the floor line owns it ("stating a capability gap IS the tell"). Label only.
- baseline: PASS 2026-08-15 solo (Claude Sonnet 5) — found the missing viewport action itself, refused it as an exemption, created the channel, declined "cannot see the page". Shows the sentence WORKS when present; the FIELD is what shows it necessary.
