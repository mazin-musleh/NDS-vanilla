# S82 legacy-sheet-vs-project-nds-layer


- mode: comprehension
- rules: rule #6's classification sentence ("Anything not NDS's own is legacy UI, not project canon") against rule #5's sanctioned project stylesheet (token rebinds at `:root` loaded AFTER `nds-main.min.css`, overrides scoped under the project's own class) and rule #3 edit 4's required output. The collision under test: the discriminator is CONTENT (element-level globals vs token rebinds/scoped knobs), not ownership, and rule #6's classification sentence does not use it.
- provenance: found 2026-08-15 while working B5 (nds-test-app-7): the withheld B5 sentence ("move that import into the legacy routes' own tree") is unsafe when the sheet is the project's own NDS layer, and the rules supply no discriminator. No run has deleted a legitimate layer because no setup carried one; this setup is the first with BOTH. Both sheets are deliberately described as "written by the team" with neutral names, so only the content can discriminate.
- setup: Porting under rule #6's sanctioned parallel files, server-rendered ASP.NET app; the NDS runtime is installed and the new NDS layout already carries the head unit. The legacy master layout loads two project sheets, both written by the team: `wwwroot/css/site.css` (sets `body { font-family; color }`, `h1`–`h3` sizes, `input` padding — written against the old UI) and `wwwroot/css/brand.css` (sets `:root { --background-primary: #0F7B4A; ... }` with the same set mirrored under `:root[data-theme~="dark"]`, plus one `.acme-form-width { --form-max-width: 560px; }` class the ported pages use; added last month when the theme work started).
- prompt: "build the NDS layout for the ported pages. List every stylesheet it loads, in order, and say what happens to each of site.css and brand.css."
- rubric:
  - MUST: the NDS layout loads the head unit's sheets plus `brand.css` AFTER `nds-main.min.css` — it is the project's own NDS layer (rule #5's sanctioned stylesheet: semantic rebinds with the dark mirror, a scoped knob class), kept and loaded; `site.css` is never carried onto NDS pages (element-level globals written against the old UI); the classification justified by what each sheet CONTAINS, not by ownership or filename; legacy pages keep both files, removal the dev's call.
  - MUST NOT: classify `brand.css` as legacy because it is not NDS's own and drop it from the NDS layout (the theming silently reverts); carry `site.css`; hand-merge either sheet's rules into new files; re-tint via `.nds-*` selector overrides instead of keeping the layer.
  - cite: rule #5's loaded-after sentence / rule #6's globals row or paragraph
- grading note: the per-file disposition is the artifact; "handle the stylesheets appropriately" as prose is ungradable.
- floor: PASS 2026-08-16 (stub rulebook, Claude Sonnet 5, 6 tool calls) — kept `brand.css` after the NDS bundles, dropped `site.css`, both justified by content; grepped the semantic tokens to confirm the rebind target is real, and flagged the fixture's `--form-max-width` as wired to nothing.
- baseline: PASS pair 2026-08-16 (stub 6 / real 8 tool calls; Claude Sonnet 5) — brand.css kept after the bundles, site.css dropped, both justified by CONTENT. Stub-pass + real-pass = the collision is LATENT at this tier; no text moves. Standing guard: a run that deletes a legitimate project layer reopens the discriminator question; B5's withheld sentence inherits the gap. Fixture blemish: the --form-max-width knob matches no NDS knob — swap for a plain property if it ever distracts a run.
