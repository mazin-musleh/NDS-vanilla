# S85 framework-view-lifecycle


- mode: comprehension
- rules: §JS wiring's banner/core routing and the Reference index's `_source/core/*.md` line, landing on `core/refresh.md`'s Framework Views block (landed 2026-08-17 with F2's decision): "Call `window.NDS?.Init.refresh(view)` after mount and `NDS.Init.destroy(view)` before unmount — that is the whole contract" / "Do not write a poll or retry helper for this; there is nothing to wait for." Guards a DOC note, not rules text — no floor pair owed; the real-file run is the gate.
- provenance: field report `nds-test-app-7` cycle 2 (F2/F7, 2026-08-16): the rig implemented only the MOUNT half of the documented pattern — a `src/nds/refresh.js` helper that polled for `window.NDS` and called `refresh()`, with no `destroy()` on unmount — so Filter's id-keyed registry kept the detached instance and a remount at the same id stayed skeleton-held (F7's symptom). F2 decided 2026-08-17 (owner call): doc note over a readiness API — `scripts/check-mount-timing.mjs` measured the loader's self-heal 13/13 (every mount-timing window covered without a consumer wait). This scenario guards the consumer-side contract the field run half-implemented.
- setup: React SPA mid-migration (the `mini-spa` shape), `<StrictMode>` on; a behavior variant can seed the broken hook into `fixtures/mini-spa`. The project's integration hook, written by a prior session and shown to the runner verbatim in the prompt, polls for `window.NDS` in a `useEffect` (`setInterval` until `NDS` exists, then `NDS.Init.refresh(ref.current)`) and returns no cleanup. Harness note: the prompt shows the symptom and the code only — never name destroy, teardown, or the missing half (the S72/S79 tell).
- prompt: "our NDS records page breaks when you navigate away and come back — the table stays stuck as skeletons until a full reload. Here's the hook every NDS view uses: [the code above]. What's wrong, and what's the fix?"
- rubric:
  - MUST: route to `core/refresh.md` (via the rules' JS-wiring/core index path) rather than answer from React instinct; name the missing `NDS.Init.destroy(view)` in the effect cleanup as the cause (the detached view's instances survive and squat their registry ids, so the remount is skipped); deliver the corrected hook — cleanup calls `destroy`, the poll replaced by the guarded one-line `window.NDS?.Init.refresh(view)` per the doc's no-poll sentence.
  - MUST NOT: blame or propose patching NDS source; keep or defend the poll/retry helper; work around via key-remount or full-reload hacks; invent a readiness event or promise the runtime does not ship.
  - cite: the Framework Views contract sentence, or Teardown's "Tear down what you remove before you remove it."
- grading note: the artifact is the corrected hook plus the named mechanism — "add cleanup" as prose without the destroy call and the squatting-registry cause is not a pass.
- baseline: PASS 2026-08-17 solo, same sitting the Framework Views block landed (Claude Sonnet 5, 3 tool calls) — routed to core/refresh.md, the missing destroy() named, the corrected hook delivered with the no-poll sentence quoted. WATCH mechanism-naming: cause named as stale markers/listeners rather than the registry-id squat — identical fix; firm only if a field run misdiagnoses because of it.
