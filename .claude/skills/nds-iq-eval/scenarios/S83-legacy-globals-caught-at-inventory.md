# S83 legacy-globals-caught-at-inventory


- mode: comprehension
- rules: §Plan step 1's inventory list and its response-header CSP sweep (the prompted-check precedent) against rule #6's globals clause ("element-level globals (`body`, `h1`, `input`) break it with no class at all"). **No text moves before this measures** — the floor pair decides whether a sentence is owed at all (stub AND real file must both fail — the S80 correction).
- provenance: field report `nds-test-app-7` cycle 2, 2026-08-16 (React SPA, template 1.8.0, IQ v2.0), finding F5 — and the **B5 REOPEN CONDITION MET** ("Reopen only if a rig ships the leak again on this text"): the rig ran on current text and shipped the leak anyway. It inventoried `src/styles.css` as rule #6's named legacy dependency and never opened it for the bare-element selectors the same rule names; the dev caught it as the dark-mode symptom, exactly as in the first incident. The rig's own ask is a step-1 sweep shaped like the CSP one. **Distinct graded moment from its siblings:** S80 grades noticing and remedy while building, S21 grades what a server-rendered NDS layout carries; this grades whether the leak is named in the INVENTORY, before a page is built — which is the only moment that reaches the dev's strategy decision.
- setup: `fixtures/mini-spa/` (checked in 2026-08-17; per-run assembly in `fixtures/README.md`), first NDS session, no plan written yet. `src/main.jsx` carries `import './styles.css'` beside the React mount; `src/styles.css` sets `body { color: #1f2933; font-family: … }`, `h1`/`h2` sizes and `a { color }`, written for the current UI. Six route components under `src/pages/`, one `index.html` serving every route, no CSP header. `NDS_ROOT` and `NDS_ASSETS` are set and the runtime is installed. Harness note: describe the project only — never state that the sheet is a risk, and never name dark mode (the S72/S79 tell).
- prompt: "we're moving this app over to NDS. Start."
- rubric:
  - MUST: the step-1 inventory OPENS `styles.css` rather than listing it by role, and records that its bare-element rules reach every NDS page through the one shared entry — as a risk or decision visible in `NDS-PLAN.md` before any page is built; the porting-strategy question reaches the dev with that fact attached.
  - MUST NOT: inventory the sheet by name/role and move on; defer it to the build or to the verify pass; delete or rewrite `styles.css` unasked; frame it as a token or dark-mode problem.
  - cite: rule #6's "element-level globals (`body`, `h1`, `input`) break it with no class at all", reached FROM the inventory step.
- grading note: the artifact is the plan's content — a run that greps the sheet and writes nothing about it in `NDS-PLAN.md` has NOT passed. Silence about what the sheet contains is the failure being measured, not whether the file was read. **The stub cell grades on substance, not the artifact name**: only the real file mandates `NDS-PLAN.md`, so the stub passes by surfacing the bare-element leak as a pre-build risk or decision that reaches the dev in whatever plan artifact it produces, and fails by building under the sheet in silence — graded literally, the stub auto-fails on a formality and biases the gate toward "sentence owed".
- floor: **FAIL 2026-08-16 (stub rulebook, Claude Sonnet 5, 35 tool calls — not a lazy runner).** The runner even EDITED `styles.css` (removed a dead `.card` rule, added `page-shell.md`'s `#root { display: contents }`) and still shipped canonical `.nds-card` markup on all six pages under the sheet's `body`/`h1`/`a` globals — no flag, no dev question. Seeing the sheet is not enough without the rule; the model does not do this for free. A first stub run the same day was VOIDED: that fixture had no `.nds/` on disk and the runner correctly stalled on the missing template — a fixture lie, not signal; the pair re-ran on the corrected harness (junction-backed `.nds/` in zip shape, `_source/` populated from the working tree). Harness notes for re-runs: the built runtime banners `1.8.x-dev`, which costs each real run one harmless P5 dev question; and one runner's directory listing showed the `_site` junction as empty while direct-path reads resolved fine — a runner reporting "no built pages" may be a link-traversal artifact, check before grading.
- baseline: PASS 2026-08-17 confirm on v2.1 (Claude Sonnet 5, 26 tool calls) — the plan opens with the stylesheet hazard naming the import, the sheet's REAL selectors (h2 proves a read, not an echo of the sentence's examples), and the shared-entry reach, pre-build, strategy-attached. Gate history: stub FAIL + two real-v2.0 FAILs (F5 reproduced exactly) cleared the sentence 2026-08-16. Soft, not a miss: the concrete escape mechanism is a dev-call area (S80's re-shaped MUST) — demanding one pre-review would repeat the over-ask.
