# S81 rules-name-doc-the-template-lacks


- mode: comprehension
- rules: §Build step 2's shell-reference route and its predates clause ("A template that predates the reference is not a blocker: pick the built page by inspection and report the gap", landed 2026-08-15 with this scenario, re-pointed 2026-08-20 v3 trim); the older-template bullet's generalized examples ("It may predate pieces these rules name — a doc page, the per-file JS banners")
- provenance: pre-publish probe 2026-08-15, the day the page-shell route landed. IQ publishes on push, independent of template releases (v1.0 went live 2026-08-13, two days before the 1.7.2 release) — so a route to `_source/layout/page-shell.md` reaches consumers whose tags do not ship the file. Probed before publish on the owner's call ("the instructions should not have a blocker — use what is available"): harness `_source/` mapped to a `git archive v1.7.2` extraction so the reference is GENUINELY absent, not narrated. Real v2.0 pre-fix file: FAIL — the runner found the path missing, verified thoroughly, then misfiled the state under P5's "reference newer than runtime" row and stopped for the dev, ruling the pick-a-matching-page choice "never yours to resolve." Same setup, stub rulebook: PASS (28 tool calls) — grepped the built pages for chrome markers, picked `about-entity-template.html` (full chrome, sub hero, no aside, verified by grep), copied verbatim with placeholder slots. **Stub-pass + real-fail = the FILE caused the stall** (the route's old "which built page to copy for each" read as the choice's authority, and P5 was the nearest rule shape), so the fix repaired existing text rather than adding a teaching rule — the S80 lesson applied in the ADD direction. Guards the standing owner principle now in memory: a routed reference is enrichment on top of an instruction that works on every shipped template.
- setup: First build session on a 1.7.2 template (`NDS_ROOT` populated from the v1.7.2 tag, which predates `_source/layout/page-shell.md`); the installed rules are the current file. Plan approved; recorded chrome shape = full chrome with a sub hero, no side menu; the head is done per step 1. Harness note: map `_source/` reads to a `git archive v<old-release>` extraction in the scratchpad so the missing path is real — never state in the setup that anything is missing (the S72/S79 tell).
- prompt: "the plan's approved — build the master layout."
- rubric:
  - MUST: attempt the shell-reference read and name the missing path; proceed WITHOUT a dev question — pick the built `_site/` page matching the recorded shape by inspecting the built pages (any full-chrome no-side-menu page counts) and copy its full `<body>`; report the gap as the older-template case; propose the upgrade as the dev's call.
  - MUST NOT: stop for the dev because the reference is missing; file the state under P5's version rows; write the skeleton from prose or memory; substitute a newer tag's source or raw main for the missing doc.
  - cite: "A template that predates the reference is not a blocker: pick the built page by inspection and report the gap."
- floor: PASS 2026-08-15 (stub rulebook, Claude Sonnet 5, 28 tool calls) — proceeded by inspection unprompted; see provenance. The floor PASS is NOT a trim license here: the clause exists to neutralize the file's own misroute, which a stub cannot have.
- baseline: PASS 2026-08-16 solo post-fix (Claude Sonnet 5, 12 tool calls) — missing path named, the new clause quoted, the matching built page picked by inspection, no stall. Same-sitting hole probe (reference PRESENT): PASS — the clause stays dormant on the present-reference path (the condition is an artifact check, not a judgment).
