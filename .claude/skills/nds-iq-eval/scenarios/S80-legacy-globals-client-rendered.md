# S80 legacy-globals-client-rendered


- mode: comprehension
- rules: rule #6's globals row (`your own global site.css / site.js` → rule #5's styling order + §JS wiring) and its paragraph ("Anything not NDS's own is legacy UI, not project canon"; "element-level globals (`body`, `h1`, `input`) break it with no class at all"); rule #6's parallel-files default. **No sentence covers the client-rendered shape yet — this scenario lands BEFORE any sentence, and the floor run decides whether one is owed at all.**
- provenance: nds-test-app-7, 2026-08-15 (React SPA, template 1.7.2). The ported page's topbar rendered dark-on-dark in dark mode: `.nds-topbar` paints `--background-topbar` and inherits its text from `body`, and the legacy `styles.css` sets `body { color: var(--ink) }` (#1f2933) after the NDS sheet, so it won. Measured on their page against ours in dark: text `rgb(31,41,51)` on `rgb(229,231,235)`, identical backgrounds and identical `--text-default` / `--background-topbar` tokens, so the token layer was never at fault. Their agent shipped the global `import './styles.css'` unprompted and moved it into the legacy `App.jsx` only after the dev saw the symptom. **The sibling of S21, and the reason it exists: S21's setup is server-rendered — a master layout with `<link>` tags — so its rubric ("`site.css` is never carried") is satisfiable in full while this bug ships.** Their agent never linked anything: the sheet arrived through one `import` in the shared JS entry, and one `index.html` serves every route, so "which assets does the NDS page load" has no per-page answer. Owner call the same day: fix it consumer-side, not in NDS — making chrome roots declare `color: var(--text-default)` was built and REVERTED as a measured visual no-op, so a runner proposing that is wrong, not clever.
- setup: A Vite + React app. One `index.html` serves every route. `src/main.jsx` is the single entry: it does `import './styles.css'` and then mounts `<App />`. `src/App.jsx` renders a shared `<Layout>` shell around all routes. `styles.css` is the team's own: `body { color: var(--ink) }`, heading sizes, input tweaks. The plan is approved and rule #6's parallel-files default is sanctioned, so the ported page is a NEW route and component beside the legacy one. Dark mode is in scope. You are building the first NDS route now.
- prompt: "build the Reports page as our first NDS route." Then, as the artifact: list every file you create or change, and for each changed file write the line you change and what it becomes. **Do not name `styles.css` in the prompt.** The first wording asked "What happens to styles.css?" and floor-PASSED on 2026-08-15 — but that handed the runner the question the field agent never asked itself, which is the S72/S79 defect repeating. The failure under test is NOTICING the global import mid-build, so the setup states `main.jsx`'s contents as neutral structure and the prompt asks only for the page. Unlike S79's tool-capability case this one IS testable, because the blocker is a fact in the codebase rather than a limit of the runner's own tools. The artifact is the whole changed-file list, so an untouched `main.jsx` is visible as an omission.
- rubric:
  - MUST: state that the entry's `import` reaches the NDS route too, because one entry and one bundle mean there is no per-page asset list to leave it out of; then take EITHER sanctioned remedy — (i) move the import out of `src/main.jsx` into the legacy tree so it loads only for legacy routes, with the dev informed, or (ii) leave the legacy file untouched, give the NDS route scoped protection (its own shell outside the shared `<Layout>` plus rule #5's override tier under the project's own class), and report the root cause to the dev as their call (re-shaped 2026-08-16 — the original relocate-only MUST encoded the withheld sentence, and run 5 showed the file's own ownership rules produce path (ii); the S4/S17 over-ask lesson); keep `styles.css` on disk and working for legacy routes; give the NDS route its own shell rather than the shared legacy `<Layout>`.
  - MUST NOT: leave the import in the shared entry; answer that the NDS page simply "does not link site.css" and stop there (S21's answer, which passes while the bug ships); delete `styles.css` or hand-port its rules into the NDS page; propose fixing it on the NDS side by declaring `color` on chrome roots or raising specificity against the legacy sheet; treat the leak as a token or dark-mode bug.
  - cite: rule #6's globals row / "Anything not NDS's own is legacy UI, not project canon" / "element-level globals (`body`, `h1`, `input`) break it with no class at all"
- grading note: the artifact carries the grade. "Scope the legacy CSS" as prose is not enough — the named file and the relocated `import` line are what show the runner understood that the entry, not the page, is where the sheet enters.
- floor: **FAIL 2026-08-14-style stub, run 2026-08-15 (Claude Sonnet 5), on the UNTOLD prompt.** The runner left the import in `src/main.jsx` and argued for it: "NDS ships as global `<script>`/`<link>` tags, not an ES import, and the legacy CSS stays parallel per the sanctioned-parallel-files plan." It noticed the conflict and reached for the wrong fix — load order in `index.html` — which does not hold, since Vite injects an imported sheet after the static tags, exactly how the rig ended up legacy-wins. **An earlier floor run the same day PASSED, on the TOLD prompt** ("What happens to styles.css?"): it deleted the import, relocated it, and reasoned about the dark-mode bleed unprompted. Same stub, same setup, only the prompt differed, opposite outcomes. **Keep that pair — it is the cleanest evidence in this file that a floor result is only as good as the prompt that produced it**, and the first time the tell was caught before it cost anything (S72 and S79 cost a shipped regression and a wasted trim).
- baseline: CLOSED 2026-08-16, no sentence owed — runs 5 and 6 (provenance and no-provenance cells, current file) both PASSED the noticing with sanctioned remedies; run 3's failure did not reproduce and the camouflage hypothesis found no support. The rubric accepts both remedies (relocate the import, or scoped shell + rule-#5 tier + dev report). The reopen condition fired 2026-08-16 at the INVENTORY moment — that moment is S83's and was worked there (the v2.1 sweep); S80's own graded moment stays closed. Reopen only if a rig ships the leak while BUILDING on current text.
- proposed-and-withheld: a sentence extending rule #6's globals paragraph was drafted ("In a client-rendered app the legacy sheet enters through an `import` in the shared JS entry, not a per-page link. Move that import into the legacy routes' own tree…") and **NOT landed**, for two reasons. The evidence is 1-1 on confounded setups. And the instruction is unsafe as written: applied to a project's legitimate NDS layer rather than a legacy sheet, "move that import into the legacy routes' own tree" breaks the NDS pages. It needs a discriminator the rules do not currently supply — see the rule #6 classification collision recorded in `TODO.md`.
