# S87 mount-root-layout-chain


- mode: comprehension
- rules: §Build step 2's mount-root sentence: "A client-rendered app mounts this copy inside a container element (`<div id=\"root\">` or the stack's equivalent). Give that element `display: contents` in the project's own stylesheet: `body`'s layout chain must reach `header`/`main`/`footer` directly, and the mount node otherwise breaks it — the footer rides up on short pages." Routed enrichment behind it: `_source/layout/page-shell.md`'s Framework Wrappers block, which adds the flex pass-through alternative.
- provenance: field report `nds-test-app-7` cycle 3 (R2, 2026-08-17) — the rig's ported page put the chrome inside `<div id="root">`, `body`'s flex column stopped reaching `main`, and the footer sat mid-page on short routes. The rig root-caused and fixed it itself, which is why R2 filed as the adapt-principle working; the owner's own later test found the rule needed in the file regardless. This scenario is the guard that text owes, not the evidence that licensed it.
- setup: A React single-page app with six routed pages and its own build. An NDS template is on disk in zip shape with `_source/` populated. `NDS-PLAN.md` exists with the inventory done and the dev's review answered; **no page has been ported yet and Contact is the first row.** That clause is load-bearing: the graded sentence lives in §Build step 2, the master-layout copy, so a setup seeding prior built rows puts the runner past the moment under test. Setup and prompt name neither the mount element, any stylesheet, the footer, the layout chain, `display: contents`, nor the Framework Wrappers block; the prompt stays canonical (~130 words, no be-specific-about-files nudge — it steers toward the enumeration where the fix lives).
- prompt: "port the Contact page to NDS."
- rubric:
  - MUST: give the app's mount element `display: contents` (or the flex pass-through — `flex: 1` plus a column) in the PROJECT's own stylesheet; carry it out as part of the port, before the row is called done, not after a dev reports a symptom.
  - MUST NOT: edit NDS source, bundles, or copied canonical markup to compensate; add wrapper elements or re-nest the chrome to work around the mount node; fake the pin with a fixed `height`/`min-height`/`margin-top` on the footer or mount node; call the row verified with the mount node left as a plain block.
  - cite: the §Build mount-root sentence, or `page-shell.md`'s "Give the mount root `display: contents`, or pass the same flex rules through it."
- grading note: Contact is deliberately the SHORT page (a heading and one card) — the only shape where the break is visible, so a runner that ports a tall page has not been tested. In behavior mode the artifact is the shipped CSS rule; in comprehension a described rule counts; both have now been observed.
- leak: **C2 CONFIRMED and FIXED 2026-08-18, by self-audit the same sitting it was authored.** The setup named the mount container and `src/styles.css` — the graded element and its destination — and the prompt ran off-canon with a be-specific-about-files nudge; the pass was voided and re-run de-leaked. Standing residual, unresolved by design: authored by the hand that landed the sentence, so a field run or second author is what would confirm it. Note for re-runs: "fixture re-used verbatim" is NOT a mitigation here — comprehension describes `mini-spa` in prose and never assembles it.
- floor: **FAIL 2026-08-18 (stub rulebook, Claude Sonnet 5) — three stub cells, same miss every time:** the card markup and asset wiring get done, the shell never does. No built-page copy, no mount element, no `display: contents`. The stub runner treats a page as its components and never treats the shell as a step, which is the sentence's whole job. Effort caveat: all three stubs were lazy (3, 1, 1 tool calls) and none wandered into `page-shell.md`, whereas S83's 35-call stub found `#root { display: contents }` there unprompted. Doc and rule are therefore not redundant but differ in RELIABILITY — the doc pays off for a wide reader, the sentence pays off at one tool call. That resolves the S83 datapoint and retires the pre-run expectation that this would land latent like S82.
- baseline: **PASS 2026-08-18, de-leaked floor pair (Claude Sonnet 5, 1 tool call).** Real cell recognised the chrome as unbuilt because no page was ported, copied head + `<body>` from a built `_site/` page, then "mount the React root inside it with `display: contents`" — with no mount container or stylesheet named anywhere in the setup. Two softs, both plausibly the 130-word budget, neither touching a MUST NOT: the mount sentence is absent from BASIS (four other rules are quoted), and the destination stylesheet goes unnamed. Two earlier pairs the same day were voided — one setup defect, one leak (see `setup:` and `leak:`). **Behavior half CLOSED 2026-08-18**: S84's run on `mini-spa` applied `#root { display: contents }` unprompted — the shipped-CSS artifact this rubric wants and comprehension cannot give.
