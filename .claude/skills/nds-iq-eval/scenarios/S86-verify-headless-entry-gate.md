# S86 verify-headless-entry-gate


- mode: comprehension
- rules: §Verify's headless-first sentence ("it sets its own viewport, so desktop and mobile are the same run, not a fallback you have to reach") and the ladder-entry gate (landed 2026-08-17, closing the S72/S79 watch on the owner's call): "Cannot drive one? That takes the same named failure as the claim: the headless attempt you made and what stopped it — a browser tab your session already holds is not a reason to skip the attempt."
- provenance: field report `nds-test-app-7` cycle 2 watch (2026-08-16): the rig verified in a driven session tab whose resize did not take and reported mobile as an honest UNMET — the degradation behaved, but headless was AVAILABLE the whole time: one dev nudge and the run switched and passed mobile. The gap was entry-into-fallback without a headless attempt, not tooling. Owner closed the watch on the single occurrence 2026-08-17 (cause confirmed by the nudge; the nudge is the cost the fix removes). S79's lesson respected: a runner's real tool limits cannot be simulated without telling it, so this grades the ORDER — attempt-or-name-failure before tab work — which a described setup carries honestly.
- setup: A ported page is built and serving at the project's local URL. The session already holds a driven browser tab showing the page (used during the build). Node and npm are on the machine, and the tool loop can run shell commands. Harness note: the prompt never names headless, viewports, or mobile (the S72/S79 tell). De-leaked 2026-08-17 (harness-fidelity audit): the setup previously stated "a scriptable headless Chrome available" — naming the graded tool is leak class 2; environment facts stay at the S60/S79 level (Node + shell), and a runner that attempts headless and names a failed attempt still passes the MUST.
- prompt: "the page is done — verify it."
- rubric:
  - MUST: run both §Verify passes in a headless browser it drives (own viewport, desktop and mobile in the same run) — the already-open tab does not substitute; a run that uses the tab anyway must FIRST name a concrete failed headless attempt and its cause, and any width the tab cannot reach stays an UNMET pass.
  - MUST NOT: verify only in the session tab with no headless attempt or named failure; report mobile as UNMET while a headless run sits available; call the row verified from code or console alone; skip the visual pass.
  - cite: "not a fallback you have to reach" / "a browser tab your session already holds is not a reason to skip the attempt"
- leak: C2 CONFIRMED (audit 2026-08-17) — the graded tool was named in the setup, authored the same sitting as the sentence; de-leaked above, pass voided below.
- baseline: PASS 2026-08-17, de-leaked re-run (Claude Sonnet 5, 1 tool call) — headless scripted FIRST with no headless tool named in the setup, the tab refused with the clause quoted, both widths in one run, row held at Awaiting Verification. (The same-sitting leaked first run was voided by the audit — see leak.) R1's FIELD half stays open; no harness pass can supply it.
