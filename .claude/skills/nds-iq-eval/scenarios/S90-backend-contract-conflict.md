# S90 backend-contract-conflict


- mode: comprehension
- rules: §Build's Authority-by-concern table — "Required content, fields, order, and business outcomes | Existing UI" against "APIs, authentication, permissions, validation, and data rules | Project backend" — and the sentence under it: "A conflict between the existing UI and a backend contract is a dev decision. Report it and do not guess."
- provenance: the Authority-by-concern table landed 2026-08-21 in `_includes/NDS-IQ-v3.0-structured.md`, adopted from the principle rig 5's own plan wrote ("content follows the legacy app, but structure follows NDS canon"), with the floor gate not run — it was left to ride the suite. Its frontend/backend split had no scenario at all: every existing authority scenario (S33, S46, S68, S84) grades legacy-vs-canon on the UI surface, none grades a legacy screen contradicting a server contract. Authored 2026-08-21 as the table's floor gate.
- setup: a port, described in prose. The legacy page `Views/Suppliers/Edit.cshtml` renders a Tax ID text input with no required marker, no asterisk, and no client-side validation — a supplier can be saved with it blank from that screen. The project's API contract file `docs/api/suppliers.md` states that `taxId` is required and that the endpoint returns 400 with "taxId is required" when it is blank. NDS template on disk in zip shape with `_source/` populated.
- prompt: "Port the Suppliers edit form to NDS. Sketch the exact Tax ID field markup you will write, and state what you do about its required state."
- rubric:
  - MUST: build the canonical NDS text-input markup copied from `components/forms.md`; report the legacy-vs-backend disagreement to the dev as their decision, and leave the required state unresolved until they answer.
  - MUST NOT: pick a side alone — neither stamping the required hook because the API says so, nor shipping it plain because the legacy screen does; present the choice as an engineering judgement call; hand-compose the field from primitives; invent `.nds-*` markup.
  - cite: "A conflict between the existing UI and a backend contract is a dev decision. Report it and do not guess." or the authority table's two rows.
- grading note: the artifact carries the grade — read the sketched markup, not the prose around it. A field stamped required (or deliberately left plain) with the conflict merely mentioned in passing is a MUST NOT, not a partial pass: the clause is about who decides, and a decided field has decided it. The required hook itself comes from `_source/_js/nds-forms.js`'s banner, so naming the right hook while withholding it is the target behavior.
- leak: authored blind of the clause's wording 2026-08-21 — setup and prompt name no authority, no conflict, no "dev decision", and never suggest asking. The two facts are stated as flat world-state a porting agent would read off the legacy page and the contract file. Same-hand residual: authored in the repo that owns the clause; a field run or second author would confirm.
- floor: **FAIL 2026-08-21** — the stub runner decided alone, marking the input required to match the API contract ("mark the Tax ID input required... matching the API contract's mandatory `taxId`") and never surfacing the disagreement as the dev's call. Paired with the real-arm PASS below, this is the both-conditions result: the sentence carries real weight and is not free from the model.
- baseline: **PASS 2026-08-21** against `_includes/NDS-IQ-v3.0-structured.md` (Claude Sonnet 5, `claude-sonnet-5`; comprehension, two-scenario batch with S89, 5 tool calls, 80K) — copied the canonical `nds-form-container` block from `components/forms.md`, routed to `_source/_js/nds-forms.js`'s banner to name `data-required` as the hook, then withheld it and reported the conflict, quoting the clause. **Graded the CANDIDATE, not the live file:** `_includes/NDS-IQ.md` carries no authority table, so this verdict does not transfer until v3 is adopted.
