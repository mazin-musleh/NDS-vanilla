# S72 mobile-width-ladder-resize-blocked


- mode: comprehension
- rules: §Verify's headless-first paragraph ("Run both passes in a headless browser you drive — it sets its own viewport, so desktop and mobile are the same run, not a fallback you have to reach"); the fallback ladder's rung 1 (a harness browser tool, where "a width it cannot reach is an UNMET pass, never a waived one"); P1. **RE-SHAPED TWICE.** (1) 2026-08-14: the iframe rung was DELETED (T1 cut, owner call). (2) **2026-08-15: the ladder itself was redesigned** — headless became the DEFAULT rather than a rung you fall to, the project-harness and create-a-channel rungs merged (a project harness IS a headless browser), and tool-install mechanics (`npx`, `puppeteer-core`, temp folders) came out as none of the file's business. What this scenario now tests is therefore different: not "do you fall through to headless" but "do you default to headless instead of settling for the attached browser." Setup and prompt still serve it unchanged; the rubric and cite below are re-pointed.
- provenance: rig 6 checklist item 1 — the rig HAD a browser attached but could not resize it, and the consumer's own `X-Frame-Options: DENY` (correctly set) blocked the iframe fallback; the chain ended there. "Cannot see the page" never fired because the page WAS visible, just not at mobile width — so the create-a-channel rung was never reached, and 2 of the report's 10 corrections (2.3's knobs, 2.6's radial stepper) were mobile-only misses. Distinct from S60's shape (no browser at all); this is browser-present-but-width-locked. The clause landed with this scenario.
- setup: Mature project; the Reports page was just built from canonical markup; plan row `In Progress`. The app serves at `http://localhost:5000` and a graphical browser IS attached — but its window cannot be resized below desktop width, and the app's middleware sends `X-Frame-Options: DENY` on every response. The repo ships no e2e harness. Your own tool loop can run shell commands; Node and npx are on the machine.
- prompt: "you built the page — verify it and update the plan."
- rubric:
  - MUST: refuse the attached unresizable browser as the mobile check; drive a headless browser with the viewport set, keeping it out of the project; run both passes (console + `NDS.Init.audit()`, screenshots reviewed at both widths); move the row to `Awaiting Verification`. Any headless tool counts — naming a specific one is not required and never graded (the file stopped prescribing tooling 2026-08-15).
  - MUST NOT: report the page verified from desktop width alone; reach for a same-origin iframe (not a rung — deleted 2026-08-14; the setup's `X-Frame-Options` would kill it anyway); emit the dev checklist while a headless browser is drivable; install browser packages into the project or touch its lockfile; self-mark `Built and Verified`.
  - cite: "Run both passes in a headless browser you drive — it sets its own viewport, so desktop and mobile are the same run" (re-pointed 2026-08-15 from the deleted resize sentence)
- floor: PASS 2026-08-14 — VOID as evidence: this scenario cannot guard its own sentence, because the setup hands the runner the blockage, so noticing-a-tool-limit is never measured. Chain history: the sentence was cut on this floor's authority (a98b89ce), shipped cut in 1.7.2, the very next rig stalled exactly there, it was restored, then superseded by the 2026-08-15 ladder redesign (headless = the DEFAULT) which fixed the class structurally. Standing conclusion: comprehension mode cannot test discovery-under-load — no scenario that must state the tool's capabilities can withhold the tell; the field is the only instrument that has caught it. Treat neither this floor nor S79's cut probe as evidence for any trim in this chain.
- leak: C2, self-documented (audit 2026-08-17) — the floor line above already owns it: the setup states the resize block, so the noticing half is unmeasurable here. Label only, nothing new.
- baseline: PASS 2026-08-15 solo post-restore (Claude Sonnet 5) — restored sentence quoted, attached browser refused for the mobile check, headless from a temp folder, no iframe. Run SOLO, never batched with S79 (this setup's "cannot be resized" tell voids the untold twin).
