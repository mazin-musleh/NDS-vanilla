# Changelog (fixture stub)

## 1.6.0

### Migrating from v1.5.0

- Replace `assets/js/*.min.js` and `assets/css/*.min.css` with the new bundles.
- BREAKING: `.nds-demo-widget` renamed to `.nds-sample-widget` — update markup.

### Changed

- Sample entry: tag component gains a `nds-compact` modifier.

## 1.5.0

### Migrating from v1.4.0

- Replace the bundles.
- BREAKING: sample table wrapper requires `data-table-scroll` on `.nds-table-wrap`.
