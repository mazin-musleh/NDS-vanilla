---
name: nds-js-audit
description: Audit NDS source JS for DRY/KISS violations, scalability and maintainability risks, performance issues, client-side security risks, architectural design (scheduling, memory, DOM API choice, complexity, in-file DRY, KISS), and cross-file pattern consistency. Covers listener/observer pooling via nds-core.js (NDS.onResize, NDS.onIntersect, NDS.onElementResize, NDS.onDOMAdd, NDS.onAttrChange, NDS.debounce), throttling of scroll/resize handlers, NDS.State and NDS.Status reuse, NDS.breakpoints reuse, cross-file detection of repeated helper functions that should be promoted to nds-core.js, component contract conformance (factory vs singleton shape via NDS.{Name}.init/reinit/create), dead defensive guards inside loader-registered components, common web-security sinks (innerHTML XSS, eval/new Function, target="_blank" tabnabbing, postMessage origin checks, untrusted URLs), and architectural review (JSA rules covering unbounded caches/maps, scheduling-primitive choice, polyfill fidelity, DOM API choice, mixed-concern functions, in-file repeated blocks, interaction-handler INP cost, and forced layout reads at init). Use this skill for "audit the JS", "audit performance", "audit security", "audit the architecture", "improve INP", "lower init cost", "optimize listeners", "check nds-core usage", "find functions that should live in core", "find unthrottled scroll handlers", "find heavy interaction handlers", "find forced layout reads in init", "find XSS risks", "find unbounded caches", "find mixed-concern functions", "make all components follow the same pattern", "check component consistency", or any JS source-code quality pass on `_js/`. NOT for SCSS/CSS audits, documentation page audits (use nds-doc), or UX copy review.
argument-hint: "[target] [optional: rule-group]"
---

# NDS JS Audit

Apply this skill to: `$ARGUMENTS`

This skill audits the NDS JavaScript source — the files in `_js/` — against the conventions already codified in `CLAUDE.md` and the shared-utility contract published by `_js/nds-core.js`. It reports real duplication, real performance risk, and real maintainability drag, then applies fixes only after the user approves the report. It is the code-quality counterpart to `nds-doc` (which audits documentation).

> **THIS SKILL WILL BURN YOUR TOKENS FAST.** A full-scope run reads every `_js/nds-*.js` file, applies all four catalogs against each, and spawns per-file review agents during Phase 5/6. Narrow the scope whenever you can:
> - `/nds-js-audit <filename> [rule-group]` — single-file pass against one component (e.g. `/nds-js-audit nds-modal.js architecture`). ~15–25k tokens — a dedicated deep-read agent runs per single-file pass (see Phase 3), which is where the architectural deep-reads pay off. Omit the rule-group to run all four. Best fit for a newly-added component before merging.
> - `/nds-js-audit js <rule-group>` — full-tree pass for one rule group (`performance` / `dry` / `security` / `architecture`). ~30–60k tokens. The release-prep / refactor-window shape. Full-tree `architecture` runs only the greppable JSA rules; deep-read JSA rules are skipped with a mandatory banner.

---

## Mindset

Audit like a staff engineer doing code review before a release. Favor concrete, actionable findings over theoretical cleanliness. Flag real duplication and real performance risk; do not manufacture issues to pad the report. When a rule is qualitative (JSD-05 promotion candidates), require at least two call sites and meaningful body overlap before flagging. If a finding cannot be explained in one sentence with a specific fix, it is not a finding. Prefer the highest-leverage recommendation over the longest list.

The goal is a report a developer can act on in the next hour, not a lint dump they will close and forget.

The rule catalog is a living document — surface gaps and dead rules during the audit (Phase 3 tracks them, Phase 7 applies catalog edits) so the skill sharpens rather than silently shrinks.

---

## Response style

Reports are scannable, not prose. **This section overrides any verbosity in the Phase 4 template examples below — when they differ, follow this.**

- **Recommendation-first, always.** Open every report with ONE line: `<N HIGH / N MED / N LOW, or "Clean">  →  Recommended: <action> (<≤12-word why>)`. Never bury the recommendation; never end a report without one.
- **Tables only for short text and comparisons — never for long findings.** A table is the right shape when every cell is a short phrase: the Per-group results grid, severity counts, a side-by-side comparison. The moment a cell needs more than one short line, a table becomes unreadable (wrapped paragraphs crammed into a column). So: findings whose whole `Finding → fix` fits on ONE short line may share a compact `| Sev | Rule | Location | Finding → fix |` table; but any finding that needs more — every HIGH, every `tradeoff` carrying `Proposed solution:` / `Benefit vs cost:`, anything multi-sentence, or a long location list — drops to a per-finding **block** instead: `### {file}` then `- L{n} [{rule}] {finding}` with indented `Type:` / `Fix:` / detail lines, exactly as the Phase 4 template shows. Don't force long text into a grid; pick the format that stays readable.
- **Disclosures are one line, not banner blocks.** Collapse every skip/coverage note to a single line, e.g. `Skipped: JSD-05 (cross-tree, single-file mode).`
- **Omit empty sections.** No "Gaps observed: none" headers. Drop "Why it matters" unless the risk is non-obvious in one glance.
- **End with a numbered Next Step** where item 1 IS the recommended action; close with "Reply with a number."
- **Numbered-reply discipline** (every numbered prompt — Phase 4 Next Step, Phase 6 per-file, Phase 7): every primary acceptance path gets a number, *including the recommended action itself* — never omit it on the grounds that the user already sees it as the recommendation. Renumber to drop options that are no-ops on this run. Literal filter commands (`fix HIGH`, `fix _js/nds-foo.js`) sit below the numbered block as filters, not inside it. Never close with "reply with the literal action or a number."

Target: a report a reader skims in ~15 seconds with the decision obvious. Prefer the shortest version that stays actionable. The same applies to fix-phase replies and the `## Catalog evolved` block — lead with what happened and the next step, not a recap.

---

## Auto-drive

Default to advancing, not asking. Run the obvious next step automatically and report what you did. Pause ONLY at these three points — everywhere else, proceed:

1. **Before applying any code fix** — Phase 5 needs ONE explicit go (`fix all` / `fix <scope>` / a number). After that go, the batch runs file-by-file to completion **without per-file `next` prompts**.
2. **On a regression** — an agent-review FAIL or a `regression in {file}` report stops the run for revert.
3. **Before a git commit** — never auto-commit; propose the message and wait.

Catalog/persona self-evolution is NOT a pause point — Phase 7 auto-applies concrete rule refinements at run end and reports the diff (the user reverts via git if they disagree). Read-only / reversible steps (running the audit, emitting the report, `save`-ing it on request) take the recommended action without an extra confirmation round-trip. When nothing is ambiguous, do it and say so — don't stop to ask permission for the step you already recommended.

---

## Phase 1: RESOLVE

Parse `$ARGUMENTS` into a mode and (optionally) a rule-group filter.

### No arguments — show menu and stop

If `$ARGUMENTS` is empty, **do not run the audit**. Instead, print this menu and wait for the user to reply with a choice:

```
NDS JS Audit — for best results run each group in separate sessions:

  1  js performance        Performance rules only (JSP)
  2  js dry                DRY/KISS rules only (JSD)
  3  js security           Security rules only (JSS)
  4  js architecture       Architecture rules only (JSA)
  0  exit

Reply with a number, or type the scope directly.

For a single-file audit (e.g. a newly-added component before merging),
type the filename directly — no menu needed. Omit the rule-group to run all four:
  /nds-js-audit nds-accessibility.js              (all four groups → one consolidated report)
  /nds-js-audit nds-accessibility.js performance  (one group)
  /nds-js-audit nds-accessibility.js architecture (all JSA rules, including deep-read sub-shapes)
```

Accept `0` or `exit` as "end here, nothing runs".

Do not begin Phase 2 or any file reads until the user responds.

### Mode selection (first argument)

| `$1` | Files in scope | Rule groups run |
|---|---|---|
| `js` | All `_js/nds-*.js` except `nds-core.js`, `nds-loader.js`, `nds-showcase.js` | Always paired with a rule-group filter (`performance` / `dry` / `security` / `architecture`); see below. In `architecture` mode, deep-read JSA rules are **skipped with a banner** — see "JSA deep-read rules in full-tree mode" below |
| **filename** (e.g. `nds-accessibility.js`, `_js/nds-accessibility.js`, `nds-accessibility`, `accessibility`) | One file: the resolved target | A rule-group filter (`performance` / `dry` / `security` / `architecture`), or `all`. **When the rule-group is omitted, default to `all`** — single-file `all` is cheap, so run it instead of prompting. In `dry` (and the dry portion of `all`), JSD-05 is **skipped with a banner**. `architecture` (and the architecture portion of `all`) runs every JSA rule, greppable AND deep-read. |

**File resolution.** When `$1` doesn't match a known mode token (`js`), treat it as a filename and try these in order: (1) literal `_js/$1` if `$1` already starts with `nds-` and ends in `.js`; (2) `_js/$1` if it ends in `.js`; (3) `_js/$1.js` if it starts with `nds-`; (4) `_js/nds-$1.js` for the bare-name form. Stop at the first existing file. If none exist, reply: *"Couldn't resolve `<filename>` to a file in `_js/`. Tried: `_js/<X>.js`, `_js/nds-<X>.js`. Pass the filename as it appears in `_js/`."* and stop.

**Excluded-file rejection.** If the resolved file is `nds-showcase.js` or any `.min.js`, reply: *"`<file>` is excluded from audits — see the 'Excluded files' section below for why."* and stop. Don't attempt a partial audit on a hard-excluded file.

**Direct-named core/loader advisory.** If the resolved file is `nds-core.js` or `nds-loader.js`, proceed with the single-file audit but print this one-line advisory at the top of the Phase 4 report (above the Summary block): *"Auditing `<file>` directly — note: the rule catalog is derived from core's patterns, so findings should be read as 'is this pattern still right for this file' rather than as automatic regressions."* These two files are excluded from full-tree `js` mode (the bulk run would treat the catalog's source-of-truth as a violation target), but single-file mode IS supported for deliberate review. For `architecture` single-file runs on these two files, **JSA's deep-read rules are the primary value** — the catalog's pattern rules typically pass cleanly on core/loader, but JSA surfaces design-tradeoff observations (scheduling choice, polyfill fidelity, unbounded caches) that the per-pattern catalog can't generalize.

Unfiltered four-group runs in **full-tree `js` mode** are not supported — sweeping all four catalogs across 40+ files in one pass is too expensive for routine use; run one rule group at a time. **Single-file `all` IS supported** (e.g. `/nds-js-audit nds-foo.js all`): four catalogs against one file is cheap (~15–25k tokens), and it emits one consolidated report. If the user passes `all` with `js` (full-tree), reject: *"Full-tree `all` isn't supported — run one rule group at a time (`js performance` / `js dry` / `js security` / `js architecture`). For an all-groups sweep, target a single file: `/nds-js-audit nds-<file>.js all`."* See the `all` row in the rule-group filter table below.

**SCSS is not supported.** If the user passes `scss` or any path ending in `.scss`, stop and reply: *"SCSS audit coverage was removed from this skill. Refactor SCSS manually — see `CLAUDE.md` for token, RTL, and scaffold conventions."* Do not run partial rules on an SCSS file. (`all` is no longer rejected here — in single-file mode it runs all four JS catalogs; see the `all` row below. Only `js all` is rejected, per the full-tree note above.)

**Strict scope constraint.** Only JSP, JSD, JSS, and JSA rules run. No SCSS-scoped rule groups exist in this catalog.

### Rule-group filter (second argument)

`$2` selects which rule group runs:

| `$2` | Rule groups included |
|---|---|
| `performance` | JSP |
| `dry` | JSD |
| `security` | JSS |
| `architecture` | JSA |
| `all` (**single-file only**) | JSP + JSD + JSS + JSA against the one file → one consolidated report |

**If `$2` is missing:** single-file mode defaults to `all` and runs immediately (single-file `all` is cheap — don't prompt for a group). Full-tree `js` mode has no default — show the Phase 1 menu and wait. `all` is valid in single-file mode only; in full-tree `js` it's rejected (see the full-tree note above).

**Single-file `all` — consolidated report.** Run each group's Phase 3 analysis against the one file, then emit ONE Phase 4 report (not four): a `## Per-group results` table (one row per group: clean / N findings), findings merged and grouped by severity exactly as the normal report does (rule IDs disambiguate the group), and every applicable single-file banner shown once (the JSD-05 single-file skip banner for the dry portion; no JSA deep-read banner since single-file runs them all). Compute the Recommended action over the merged finding set using the same Phase 4 table. Phase 5 fixes and Phase 7 evolve work identically on the merged set. This is the same shape as running the four groups back-to-back, collapsed into a single pass and a single report.

### Cross-tree rules in single-file mode

Single-file `dry` runs every JSD rule **except JSD-05** — JSD-05 compares helper bodies across all of `_js/` and needs the full tree; the report shows the JSD-05 skip banner (Phase 4) in its place. **JSD-15 runs in single-file mode** by reading the canonicals in `PERSONA.md` (sibling file) and applying each entry's "Audit behavior" check against the target — a deterministic per-file check with no corpus re-tally (see `Phase 3: ANALYZE → JSD-15`). Every other JSD rule is a per-file check and runs normally.

### JSA deep-read rules in full-tree mode

Full-tree `architecture` runs only the greppable JSA rules (JSA-01, -02, -04, -05, -06, -07, -16); the deep-read rules (JSA-03, -08 through -15, -17, -18) are skipped and the report shows the JSA deep-read banner (Phase 4). Single-file `architecture` runs every JSA rule — the per-file deep reads (complexity, in-file duplication, mixed-concern structure, INP cost, CSS-subsumption) are where deep-reads earn their keep, and the single-file budget allows them.

### Excluded files

Two tiers — direct-named single-file audits behave differently from full-tree `js` mode for the first tier.

**Excluded from full-tree `js` mode only (auditable when directly named in single-file mode):**

- `_js/nds-core.js` — not included in full-tree runs because its patterns ARE the rule catalog; bulk-running the catalog against its source-of-truth produces noise. Single-file audits ARE supported (e.g., `/nds-js-audit nds-core.js performance`) for deliberate review of the helper surface itself — read findings as "is this pattern still right for this file" rather than as automatic regressions. Phase 5 also edits core when applying a JSD-05 promotion candidate via the `promote <api-name>` command — see "Applying promotion candidates" in Phase 5. Plain fix batches against components (`fix all`, `fix HIGH`, `fix <file>`) never touch core.
- `_js/nds-loader.js` — not included in full-tree runs because the loader has its own ordering rules outside the per-component JSP/JSD/JSS catalog. Single-file audits ARE supported when the user is intentionally tuning the loader; same advisory applies.

**Excluded always (no direct-named carve-out):**

- `_js/nds-showcase.js` — demo-page wiring, not a shipped component.
- Any `.min.js` file.

---

## Phase 2: READ

Source files are the single source of truth. Read them fully, not by skimming.

### MUST read every run

- **`_js/nds-core.js`** — the full shared-utility surface. Re-read every run so the rule catalog reflects the current `NDS.*` API (names, arities, return values). Rules JSP-01 through JSP-08, JSD-01 through JSD-04, and JSS fixes that cite helpers (e.g., `NDS.escapeHtml` if/when promoted) all reference specific functions here; if core gains a utility, the skill's recommendation should route to it instead of flagging the pattern as unresolved.
- **At least one JS good-pattern exemplar** — `_js/nds-scroll-more.js` for RAF-throttled scroll + pooled ResizeObserver, `_js/nds-drawer.js` for NDS.State destructuring + reads-before-writes, `_js/nds-modal.js` for NDS.State and backdrop API usage. Recommendations cite these with `file:line` so the user can copy a working pattern rather than invent one. JSA rules reuse the same exemplar set (plus `_js/nds-autocomplete.js:290-312` for size-capped fetch + abort, and `_js/nds-loader.js:272-281` for `MessageChannel`-based microtask yielding).

### MUST read every target file

Read each target file top-to-bottom before running the catalog. Pattern-matching alone misses context (a `setTimeout` inside a `NDS.debounce` callback is not a JSP-07 violation). A full read is what separates this skill from a grep script.

---

## Phase 3: ANALYZE

Run the rule catalog file-by-file. Each match is recorded as: file, line, rule ID, offending snippet (≤120 characters), suggested fix pointing to the exact `NDS.*` call. Dedupe findings whose line ranges overlap so the same handler is not triple-flagged.

### Track observations for Phase 7

While analyzing, keep a parallel log of two meta-observations so Phase 7 has data to propose catalog edits from:

- **GAP**: a pattern in the current file that looks like a real violation (duplicated across files, obviously inefficient, breaks a CLAUDE.md convention, or opens a web-security sink) but does not match any existing rule ID. Record: file, line, snippet, one-sentence description of what looks wrong, and the rule group it would belong to (JSP, JSD, or JSS) if it existed.
- **SKIP**: a rule match that had to be discarded because the recommended fix does not apply in context. Record: rule ID, file, line, why the fix failed (e.g., "JSP-02 matched but the handler is already RAF-batched via an imported helper"). One-line rationale per skip — do not silently drop.

These logs are additive across the run and feed into the "Gaps observed" and "Dead-rule candidates" sections of the report.

### Rule catalog

Severity drives the order in which fixes are applied during Phase 5: HIGH (correctness and performance first), then MEDIUM, then LOW.

#### JS Performance (JSP)

| ID | What to detect | Severity | Fix |
|---|---|---|---|
| JSP-01 | `addEventListener(['"']resize['"']` on `window` | HIGH | Replace with `NDS.onResize(handler)` — it already debounces at 150ms and pools subscribers.<br><br>**Explicitly NOT a finding** (do NOT flag):<br>- `window.addEventListener('resize', handler)` calls that appear inside a named `bind*()`/`open()` method which is symmetrically paired with a `unbind*()`/`close()` method that calls `window.removeEventListener('resize', handler)`, where the handler is already rate-limited via `NDS.rafThrottle` or `NDS.debounce`. This is a per-open/close lifecycle pattern (the listener's lifetime is bounded by a UI state, not the component's lifetime); NDS.onResize's 150ms debounce would cause visible positioning lag on a live-open UI element. The current add/remove symmetry is the correct approach for this shape. Motivating example: `_js/nds-date-picker.js:945-953` `bindViewportTracking()` / `unbindViewportTracking()` pair. |
| JSP-02 | `addEventListener(['"']scroll['"']` where the handler body does NOT contain any of `requestAnimationFrame(`, `NDS.debounce(`, `NDS.rafThrottle(`, `NDS.onResize(` within ~10 lines of the handler's opening brace. Custom `throttle()` wrappers do not count as throttled unless they themselves use one of these primitives — trace into the wrapper to confirm.<br><br>**Explicitly NOT throttled** (these are findings, not passes):<br>- bare `setTimeout` inside the handler, even with a `clearTimeout`+rebind debounce shape — for scroll, that's JSP-07's job, not "throttled" for JSP-02.<br>- a custom `throttle()` / `rafThrottle()` wrapper whose implementation uses bare `setTimeout` instead of `requestAnimationFrame`.<br>- a handler-body early-return guard on scroll position (e.g., `if (window.scrollY < 200) return;`) — that's a threshold, not a rate limit; the handler still fires on every scroll event above the threshold.<br>- `requestIdleCallback` — it does not help scroll responsiveness and is not on the approved list.<br>- the `passive: true` flag on the listener — that improves scrolling smoothness but does not throttle the handler.<br><br>**Explicitly NOT a finding** (do NOT flag):<br>- Scroll handlers that remove their own listener as part of their work — detectable by a `removeEventListener` reference in the handler body, or in any function the handler calls within ~20 lines of traced call tree. These are single-shot dismiss handlers (e.g., close-on-external-scroll), not hot loops. RAF-throttling adds dismissal latency with no throughput benefit.<br>- Scroll listeners returning a stored unsubscribe handle to the caller, whose body is bounded by a single `Element.contains(...)` / `Element.matches(...)` / `Node.contains(...)` guard before invoking a caller-supplied callback. The dismissal callback typically invokes the returned unsubscribe, so the listener is effectively single-shot per open cycle. RAF throttling would add visible dismissal latency for a scroll-to-dismiss UX. Motivating example: `_js/nds-core.js:669-676` (`NDS.onOutsideScroll`) — handler is `if (scopeEl.contains(e.target)) return; onClose();`, and the returned function at L675 invokes `document.removeEventListener` so callers release the listener as part of their close flow.<br><br>If none of the approved primitives are present in the handler (tracing wrappers where needed), emit the finding. Do not rationalize. | HIGH | Wrap in RAF throttle using `NDS.rafThrottle(fn)`. Copy the pattern from `_js/nds-scroll-more.js:143`. |
| JSP-03 | `new IntersectionObserver`. **Not exempt**: singleton usage, single-element observation, proper `disconnect()`/`unobserve()` disposal, or manual pooling do NOT make this compliant. The rule is about routing through the shared `NDS.onIntersect` pool so observers are grouped by threshold/rootMargin across the whole app — not about leak prevention. A well-disposed bare `new IntersectionObserver` is still a finding. | MEDIUM | Replace with `NDS.onIntersect(el, handler, { threshold, rootMargin })`. Core pools observers by threshold/rootMargin automatically. Store the returned unsubscribe handle. |
| JSP-04 | Three sub-shapes:<br><br>**(a) Raw `new ResizeObserver`.** **Not exempt**: singleton usage, single-element observation, or local `disconnect()` do NOT make this compliant. The rule routes through the shared `NDS.onElementResize` pool; a local pool replicating the same logic is a duplication, not a pass.<br><br>**(b) Pool-routed subscription with discarded unsubscribe handle.** A call to any pool helper that returns an unsubscribe function — currently the full family `NDS.onResize`, `NDS.onElementResize`, `NDS.onIntersect`, `NDS.onOutsideScroll`, `NDS.onAttrChange`, `NDS.onDOMAdd`, `NDS.onDOMRemove`, `NDS.onChildrenChange` — used as a statement-expression where the returned unsubscribe function is not stored on any instance / closure / module-scope reference. When the call sits inside a per-instance or re-callable function (constructor, public `init()`, `setup*()` called per re-init) and the owning component lacks a teardown path that could have invoked the handle, the per-element callback in the pool's map leaks for the process lifetime; on re-init, fresh callbacks stack on top. The pool's selector+fn dedup (e.g., `nds-core.js:205`'s `some(s => s.sel === sel && s.fn === fn)`) does NOT catch fresh closures across instances, because each instance creates a new function reference. Detection: any pool-helper call NOT on the right-hand side of an assignment AND not the trailing expression of a function whose return value is captured by the caller. Mirrors how JSP-08 complements JSP-05 for MutationObserver teardown.<br><br>**Explicitly NOT a finding** (sub-shape (b) only):<br>- Calls inside a singleton-bootstrap IIFE that runs once and is intended to live for the page lifetime (e.g., process-level utility wiring to `document.body`).<br>- Calls inside a public `init()` / `setup*()` guarded by a one-shot latch (`if (_initDone) return; _initDone = true;` flag, `window.ndsXxxInitialized` sentinel, equivalent). The guard makes re-call a no-op so loader re-invocation cannot stack subscriptions. See `_js/nds-theme.js:67-70` (`_initDone`) and `_js/nds-tables.js:414-449` (`window.ndsXxxInitialized`).<br>- Calls inside a public `init()` / `setup*()` targeting ONLY the two dedup-capable pool helpers AND passing a stable, instance-independent function reference: `NDS.onElementResize` (Set-based per-element dedup) and `NDS.onIntersect` (`Map.set` replaces per element). Inline arrows do NOT qualify (each call creates a new reference). **The other six pool helpers** (`NDS.onResize`, `NDS.onDOMAdd`, `NDS.onDOMRemove`, `NDS.onChildrenChange`, `NDS.onAttrChange`, `NDS.onOutsideScroll`) **unconditionally push to their subs arrays with no identity check** — a stable reference does NOT save them; use the init-guard exemption above or the stored-handle pattern at `_js/nds-stepper.js:331-332,364-365`.<br>- Calls where the host element is statically known to outlive the page (`document.documentElement`, `document.body`).<br>- Calls inside an `NDS.onDOMAdd` callback paired with a matching `NDS.onDOMRemove` clean-up — element-lifecycle teardown is handled by the remove handler.<br>- Calls preceded within 3 lines by an annotation comment naming why the per-instance closure is acceptable (annotation-exemption shape mirrors JSP-05 / JSS-01).<br><br>**(c) Destroy bypasses the lifecycle method that releases stored `NDS.on*` handles.** When a component stores pooled-subscription handles inside an `open()` / `show()` / `enter()` lifecycle method (e.g., `this._offScroll = NDS.onOutsideScroll(scope, …)`, `this._unsubResize = NDS.onResize(…)`) AND releases them inside the matching `close()` / `hide()` / `exit()` method, but the component's `destroy()` / teardown path does NOT first call that lifecycle method when the component is in the open state, destroying an already-open component leaves the pooled subscription wired forever. The portal/state side effects (e.g., a menu still attached to `<body>` via `NDS.portal`) compound the leak: the closure is rooted by the pool, the portaled DOM is rooted by the closure. Sub-shape (b) doesn't fire (the handles ARE stored), and sub-shape (a) doesn't apply (no raw observer). Detection: identify a `destroy()` / `teardown()` / `dispose()` method that does NOT reference the close-lifecycle method by name, AND verify the open-state handle assignments live inside `open()` / `show()` rather than the constructor. Severity stays MEDIUM because the leak only fires on the rarer "destroy while open" path — but each occurrence permanently strands a pooled subscription.<br><br>**Raw-listener variant.** The same hazard applies when a component pairs `bindXxx()` / `unbindXxx()` methods around raw `window.addEventListener` / `document.addEventListener` calls (typically rate-limited via `NDS.rafThrottle` to satisfy the JSP-01 / JSP-02 live-positioning carve-outs), invoked from `open()` / `close()` respectively. If `destroy()` does not reference `unbindXxx()` (directly or via the close path), the raw listener leaks for the page lifetime when destroy fires while open. Detection: a `bindXxx()` / `unbindXxx()` pair attaching window/document listeners, AND `destroy()` does NOT call `unbindXxx()` or the close-lifecycle method.<br><br>**Explicitly NOT a finding** (sub-shape (c) only):<br>- Constructor-attached subscriptions released in `destroy()` directly (already covered by sub-shape (b)'s positive case).<br>- `destroy()` calls the close-lifecycle method (`if (this.isOpen) this.close();`) before teardown work — release path runs through the existing close handler.<br>- `destroy()` aborts an `AbortController` that scopes the open-state subscriptions (rare with pooled subs since `NDS.on*` doesn't accept `signal`). | MEDIUM | Sub-shape (a): replace with `NDS.onElementResize(el, handler)`. Returns an unsubscribe function; store it for teardown. Sub-shape (b): capture the returned handle on the instance / closure / module scope (e.g., `this._offResize = NDS.onElementResize(el, handler);` or `let _off; ... if (_off) _off(); _off = NDS.onAttrChange(...);`) and invoke it in `destroy()` / `cleanup()` / re-init. For module-level singletons whose `init()` may run more than once, either guard the registration behind a `let _wired = false;` flag, or capture the handle and release the prior one before re-registering. Motivating example: `_js/nds-stepper.js:329, :361` and `_js/nds-progress.js:59` register `NDS.onAttrChange` inside their public `init()` and discard the unsubscribe; if the loader re-invokes `init()`, the closures stack. Sub-shape (c): make `destroy()` drive the close-lifecycle method when the component is open — `if (this.isOpen) this.close();` as the first statement of `destroy()`. The close path already releases the handles and reverses any portal/state side effects, so destroy stays a thin wrapper. Alternatively, lift the open-state subscriptions to instance-lifetime (constructor) and gate them with an `if (!this.isOpen) return;` inside the handler — but that pays the always-on subscription cost for the rare-open case, so the lifecycle-driven path is preferred. Motivating example: `_js/nds-dropmenu.js:620-626` `destroy()` does `replaceWith(clone)` but does NOT call `close()` first; `this._offScroll` (an `NDS.onOutsideScroll` document-scroll subscription) and `this._unsubResize` (an `NDS.onResize` subscription), both set inside `open()` at L489-490, leak whenever destroy hits while the menu is open. Raw-listener variant: same fix — `destroy()` prepends `if (this.isOpen) this.close();` or calls the matching `unbindXxx()` directly before any teardown work. |
| JSP-05 | `new MutationObserver`.<br><br>**Not exempt**: observing a single element, attaching once at setup, or `observer.disconnect()` on teardown do NOT make this compliant. One MutationObserver per component scales linearly with component count; the NDS pool keeps one observer per configuration and dispatches by selector.<br><br>**Explicitly NOT a finding** (do NOT flag):<br>- MutationObserver instances preceded (within 3 lines above the `new MutationObserver` line) by an inline comment that explicitly rationalizes why the shared `NDS.onDOMAdd` / `NDS.onDOMRemove` / `NDS.onAttrChange` pool does not cover the use case — e.g., content-mutation semantics (`characterData`, text-node observation), cross-frame observation, or other mutation types not in the pool's API.<br>- A bare "// MutationObserver" or unrelated contextual comment does NOT count — the comment must reference the pool's limitation and explain why the local observer is necessary. This is an escape hatch for cases the pool genuinely doesn't cover; it is not an opt-out for "I didn't bother". | MEDIUM | Replace with `NDS.onDOMAdd(selector, handler)`, `NDS.onDOMRemove(selector, handler)`, or `NDS.onAttrChange(selector, attrs, handler)` depending on mutation type. |
| JSP-06 | `addEventListener` without a matching `removeEventListener` or a stored unsubscribe returned by `NDS.on*`.<br><br>**(a) Loop pattern**: listeners attached per-element inside `forEach`/`map`/`for`, no `AbortController` / stored-handle bookkeeping. Flag the whole loop as a single finding with the listener count (not one per line).<br><br>**(b) Dynamic re-attach pattern**: a function attaches per-element listeners, is called more than once per lifecycle (event handlers, re-init paths, data/dir/lang re-renders), AND does NOT detach prior listeners before re-attaching. Two sub-shapes:<br>  **(b.1) forEach/for-loop shape** — attachments live inside a `forEach`/`map`/`for` loop.<br>  **(b.2) Sequential addEventListener shape** — sequential `addEventListener` calls on freshly-rebuilt nodes (typically inside `render()`/`redraw()`/`rebuild()` that does `el.innerHTML = ''` then re-creates children). Listeners attached via `this._ac.signal` where the controller is aborted ONLY at `destroy()` are STILL a finding: the `AbortSignal` retains references across re-renders until destroy fires, so the abort-handler chain grows linearly. Detection: a method that (i) is called more than once per lifetime, (ii) attaches per-element listeners (with or without `this._ac.signal`), AND (iii) does NOT reset the controller (`this._ac.abort(); this._ac = new AbortController()`) before re-attaching.<br>In both sub-shapes, listeners stack per invocation.<br><br>**Explicitly NOT a finding** (sub-shape (b.2) only):<br>- Methods that reset their controller before re-attaching (e.g., `if (this._renderAC) this._renderAC.abort(); this._renderAC = new AbortController();` at the top).<br>- Per-render controllers stored on the rebuilt element (`overlay._ac = new AbortController()`) where the prior overlay's controller is aborted before `innerHTML = ''`.<br>- Single-render components — methods called exactly once per lifetime. If not re-callable, no stacking.<br><br>**(c) Per-instance document/window listener**: a single `document.addEventListener(...)` / `window.addEventListener(...)` inside a constructor or instance-`init()` / `setup*()`, where the handler is NOT wrapped with `{ signal }` AND no stored reference enables later `removeEventListener`. Scales O(n) with component count and leaks per reinit.<br><br>**Explicitly NOT a finding**:<br>- forEach/for loops whose parent container is cleared via `innerHTML = ''` / `replaceChildren()` earlier in the same chain — removed DOM takes its listeners with it.<br>- Sub-shape (c) inside a module-level IIFE / singleton-bootstrap guarded by `window.ndsXxxInitialized` or equivalent one-shot latch (see `_js/nds-tables.js:578, 591`). One-per-page listeners, not one-per-instance.<br>- Sub-shape (c) at the **top level of the module IIFE itself** — straight-line code that runs as the IIFE body. The IIFE is its own one-shot latch (cannot execute twice by construction). See `_js/nds-fontLoading.js:209`.<br>- Sub-shape (c) using `{ signal }` from a **module-scope singleton AbortController** created once at module load and never re-created — purely structural sugar to satisfy the "must use signal" form, even with no `.abort()` call. Detection signature: a module-scope `let`/`const` holding an `AbortController` (`new AbortController()`), created once (lazily or at load) and used in exactly one place to `{ signal }` a single document/window listener, with no `.abort()` anywhere. See `_js/nds-stepper.js:9` (`_stepperControlsAbortController` — declared module-scope, created once behind a one-shot latch in `setupControls()` at L256-257, signal-wired at L286). NOT covered: per-instance `this._ac` — those MUST be aborted in `destroy()`.<br><br>**Sub-shape (a) factory variant** — missing `destroy()` path: factory components with forEach/for listener loops in constructor/init, NO `this._ac`, NO public `destroy()` method. The init guard (e.g., `data-nds-foo-initialized`) prevents stacking per page load, but consumers who remove and recreate the component cannot clean up. Both conditions required: (1) listeners in a loop with no AbortController signal, (2) no `destroy()` anywhere in the file. Motivating example: `_js/nds-tooltip.js:141-152`.<br><br>**Explicitly NOT a finding** (factory-variant only): listeners bound DIRECTLY to content elements (`el.addEventListener(...)` where `el` is the swept element itself, not a stable parent or an outliving closure target) AND each element carries a per-element one-shot init guard (`el._ndsXxxInitialized` JS property or `data-nds-<name>-initialized` attribute set on first visit). Such listeners are reclaimed by GC when the element leaves the DOM, and the per-element guard prevents re-attachment on re-sweep — so there is no stacking and no leak, even without a `destroy()`. The factory-variant targets the case where teardown is genuinely impossible (listeners rooted by a surviving closure, or re-attached without a guard); element-bound + guarded listeners self-clean. Motivating exemplar: `_js/nds-forms.js` `initializeInput` attaches ~10 listeners per `.nds-form-control` input, each guarded by `input._ndsInitialized` — element-scoped, GC-reclaimed on removal, not a finding. | MEDIUM | Sub-shapes (a) and (b): store the unsubscribe handle (`wrapper._offResize = NDS.onResize(...)`), or attach an `AbortController.signal` to every listener so `.abort()` cleans atomically. For dynamic re-attach, abort the previous controller before creating a new one, or use delegated listeners on a stable parent. Sub-shape (c): add `this._ac = new AbortController()` to the constructor, pass `{ signal: this._ac.signal }` to the listener, abort in `destroy()`. See `_js/nds-tabs.js:42, 127, 320`. |
| JSP-07 | **Trailing-edge debounce only.** A wrapper of shape `(...args) => { clearTimeout(t); t = setTimeout(() => fn(...args), CONST_MS); }` (or its named-function equivalent) that is invoked from an event handler / DOM listener / repeated user-input path AND uses a constant ms argument. The intent is "fire `fn` once, after the last call settles" — exactly the shape `NDS.debounce(fn, ms)` wraps. Detection requires: (1) a `clearTimeout(<varName>)` immediately followed by an assignment of `setTimeout(...)` to the same `<varName>`, (2) a constant numeric ms literal as the setTimeout's second arg, AND (3) the enclosing function is callable per-event (not orchestration code).<br><br>**Explicitly NOT a finding** (do NOT flag — these match the surface pattern but the recommended fix doesn't apply):<br>- **Single-pending-action latches.** `clearTimeout` + `setTimeout` calls used as a manual one-shot scheduler — caller invokes `schedule()` and `cancel()` explicitly to orchestrate animations / timing-dependent state, not to throttle event-handler invocations. NDS.debounce returns a wrapper that callers invoke per event; replacing the latch's internal `setTimeout` with `NDS.debounce` would change call-site semantics. Recognized by: one explicit `cancelXxx()` / clear function paired with a `scheduleXxx()` / set function, with at least one caller invoking `cancelXxx()` separately from `scheduleXxx()`. Motivating example: `_js/nds-mainnav.js:88-92` (`scheduleToggleAction` / `cancelToggleAction` pair).<br>- **Priority-routed schedulers.** `setTimeout` whose ms argument is dispatched between values based on a priority/option arg, OR whose dispatch routes to `requestAnimationFrame` for one branch and `setTimeout(fn, ms)` for others. NDS.debounce models a single constant ms; wrapping the dispatcher in `NDS.debounce(fn, ms)` collapses the priority branching. Motivating example: `_js/nds-mainnav.js:304-316` (`overflow.schedule` with `'immediate'` → RAF, `'high'` → 10ms, `'low'` → 100ms).<br>- **Single-shot delay timers (no `clearTimeout` in the same flow).** `setTimeout(fn, ms)` used as a one-time delayed effect (e.g., voice-input timeout, hold-to-repeat acceleration, placeholder restore) where no preceding `clearTimeout` resets the timer. The detector's first-condition gate already excludes these; called out here so they're not flagged on a careless re-read.<br>- **Variable-ms intervals** — `setTimeout(check, nextCheckDelay)` / similar adaptive-polling shapes. The ms is not constant; not a debounce.<br><br>If detection ever uplifts to MEDIUM-style judgment, prefer a SKIP entry (with the specific orchestration shape named) over flagging the wrong pattern — JSP-07's value comes from being precise about what `NDS.debounce` actually replaces. | LOW | Use `NDS.debounce(fn, ms)` — store the wrapper once on the instance/closure and call it per event so the `clearTimeout` bookkeeping stays inside the helper. For single-pending-action latches and priority-routed schedulers, leave the inline pattern in place; consider extracting a richer scheduler primitive into core only if the same shape recurs in 3+ files. |
| JSP-08 | **MutationObserver without disconnect on lifecycle boundary.** A local `new MutationObserver(...)` bound to a specific element (per-element lifecycle, not `document.body` / `document.documentElement`) where EITHER (a) the owning component has a `cleanup()` / `destroy()` / `reinit()` / `teardown()` path that does NOT invoke `.disconnect()` on the observer, OR (b) the component has no teardown path at all — so the observer's lifetime is effectively unbounded despite its element binding. Complements JSP-05: JSP-05 covers pool-routing (is a local observer justified at all?); JSP-08 covers teardown hygiene (once local, is it cleaned up?).<br><br>**Explicitly NOT a finding** (do NOT flag):<br>- Observers stored in an instance array (e.g., `this.observers.push(observer)`) AND disconnected in the teardown path via a loop over that array.<br>- Observers on `document.body` or `document.documentElement` that are intentionally process-lifetime (singleton bootstrap in core or a root-level utility) — not tied to an element that can be re-rendered.<br>- Observers whose JSP-05 exemption comment additionally states that the observer must outlive the element (rare; require explicit rationale, not just absence of disconnect). | MEDIUM | Store the observer handle on the instance (e.g., `this.observers.push(observer)`) and invoke `.disconnect()` in the teardown path. If the owning component lacks a teardown path but creates observers per element, either add the teardown path or re-evaluate whether the observer should be pool-routed via `NDS.onAttrChange` / `NDS.onDOMAdd` / `NDS.onDOMRemove`. |
| JSP-09 | **`onDOMAdd`-wired listener without `onDOMRemove` teardown.** A per-element listener (click / keydown / change / input / pointer*) attached inside an `NDS.onDOMAdd('selector', callback)` handler, where no matching `NDS.onDOMRemove('selector', ...)` exists in the same module to unwire or neutralize the listener when a matched element is removed. Per-element re-wiring guards (e.g., `if (el.hasAttribute(WIRED_ATTR)) return; el.setAttribute(WIRED_ATTR, '')`) prevent double-wiring on re-add but do NOT detach the original listener when the element leaves the DOM — the GC reclaims the element, but if the same element is re-added to a different parent, the guard short-circuits re-wiring and the old closure state persists; if a fresh element takes the same selector, the old closure is still rooted via any captured references. Complements JSP-06: JSP-06 covers per-instance listener stacking; JSP-09 covers observer-wired listener stacking.<br><br>**Explicitly NOT a finding** (do NOT flag):<br>- `onDOMAdd` callbacks that attach via an `AbortController` tied to the element's lifecycle (e.g., `el._ac = new AbortController(); el.addEventListener(..., { signal: el._ac.signal })`) with a paired `onDOMRemove` that calls `el._ac.abort()`.<br>- `onDOMAdd` callbacks that only read or mutate attributes/styles on the matched element (no `addEventListener`) — nothing to tear down.<br>- Listeners attached to a stable parent (event delegation) from inside the callback, where the parent's lifetime is process-long.<br>- `onDOMAdd` callbacks whose body invokes a module-level refresh / re-init routine (not per-element wiring inline), where the invoked routine is idempotent via its own guards — a `_ndsFooInitialized` / `data-nds-foo-initialized` marker on touched elements, an instance-level observer flag (`element._ndsFilterObserver = true`), or a whole-scope setup function that re-queries and rebinds as needed (e.g., `initFormControlClasses()`, `initInputAutoFill()`). These patterns delegate to a refresh-rather-than-per-element shape; the callback body does not itself carry the `addEventListener` the rule targets. See `_js/nds-filter.js:1305` and `_js/nds-forms.js:1657–1658` for the pattern. NOTE: this exemption applies only when the invoked routine genuinely self-guards. If the routine re-attaches listeners unconditionally to the same elements on every invocation, the exemption does not apply and the finding stands. | LOW | Pair the `NDS.onDOMAdd('selector', ...)` subscription with an `NDS.onDOMRemove('selector', ...)` that removes or aborts the attached listener. If the element owns its own AbortController, abort it in the remove handler. If listeners are intrinsic to the element's content, prefer event delegation on a stable parent so no per-element wiring is needed. Motivating example: `_js/nds-cooldown-button.js:191–194` wires `click` on each `.nds-cooldown-button` in an `onDOMAdd` callback with no `onDOMRemove` counterpart. |
| JSP-10 | **Eager module-IIFE DOM work in loader-registered components.** A `_js/nds-*.js` file whose namespace (`NDS.<Name>`) appears in `_js/nds-loader.js`'s `COMPONENTS` array, AND whose outermost module IIFE body — straight-line code directly inside `(() => { ... })()` at module top — contains a DOM-walking query (`document.querySelectorAll(...)`, `getElementsByClassName(...)`, `getElementsByTagName(...)`, `NDS.queryAll(document, ...)`), AND iterates via `.forEach(...)`, `for...of`, or `for(let i = 0; i < ...; i++)`, where the body does at least one of: `.style.*` assignment, `addEventListener(...)`, `setAttribute(...)`, `getBoundingClientRect()`, `new IntersectionObserver(...)`, `new MutationObserver(...)`, `NDS.on*(...)` subscription, `appendChild/removeChild(...)`, `innerHTML/outerHTML = ...`, `.classList.add/remove/toggle(...)`, or CSS-triggering attribute toggle (`hidden` / `data-state` / `data-status`).<br><br>"Top-level of the IIFE body" means NOT nested inside a named function, an arrow assigned to a method, a method on an object literal, a class body method, or any callback passed to `addEventListener` / `requestAnimationFrame` / `requestIdleCallback` / `NDS.onIdle` / `setTimeout`.<br><br>Why it hurts TBT: bundle parse+execute is a single browser task. IIFE-top code runs INSIDE that task — before `_js/nds-loader.js`'s budget-slicer starts. A DOM walk + per-element loop at IIFE top pushes the task past TBT's 50ms long-task threshold. The loader's `initBudgetMs: 5` yields between components during the critical pass but cannot manage code that runs before it.<br><br>**Explicitly NOT a finding**:<br>- Queries inside `NDS.onIdle(...)` / `requestIdleCallback(...)` / `requestAnimationFrame(...)` callbacks at the top of the IIFE — walk runs off the bundle eval task.<br>- Single `document.querySelector(singleSelector)` (NOTE: not querySelectorAll) as a presence guard for early-exit (`if (!document.querySelector('.nds-foo')) return;`). One query is not a walk.<br>- Top-level constant/regex/lookup-table definitions (no DOM walking). Bundle-eval cost but out of this rule's scope.<br>- Files NOT in `_js/nds-loader.js`'s `COMPONENTS` array. Utility modules are exempt; self-attach-without-register problems route to JSP-06(c) / JSD-07.<br>- **Annotation exemption**: comment within 3 lines naming why the work must run at module load (e.g., `// FOUT-blocking — must run before next paint`). Vague `// safe` / `// fast` do NOT qualify. Mirrors JSP-05 / JSS-01 / JSD-08. | MEDIUM | **Primary**: move the DOM-walking work into the component's public `init()` body — the loader's budget-slicer then manages it. Loader registration is already in place (detection requires it), so this is a cut+paste from IIFE top to `init()`. **Secondary** (work genuinely must run as the DOM parses, e.g., FOUT-blocking font swap, BFCache scroll-restore): wrap in `NDS.onIdle(() => { ... }, { timeout })` so it runs in an idle slot rather than extending bundle eval. **Tertiary** (one-shot bootstrap on a known-singleton root): defer the DOM walk separately via `NDS.onIdle(...)`. Rule is prophylactic — current HEAD has no findings, but future PRs adding IIFE-body DOM work bypass the budget-slicer. |
| JSP-11 | **Forced layout read on the synchronous `init()` / constructor path.** A layout-reading API — `getBoundingClientRect()`, `.offsetWidth` / `.offsetHeight` / `.offsetTop` / `.offsetLeft`, `.clientWidth` / `.clientHeight`, `.scrollWidth` / `.scrollHeight` / `.scrollTop` / `.scrollLeft`, or `getComputedStyle(...)` — evaluated on the **synchronous path** of a component's public `init()` or its constructor, where the element being read is already in the live DOM so the read forces a reflow inside the loader's critical init task. "Synchronous path" means directly in `init()` / the constructor, or in a helper they call synchronously — NOT inside a callback passed to `requestAnimationFrame` / `requestIdleCallback` / `NDS.onIdle` / a `ResizeObserver` or `IntersectionObserver` callback / `setTimeout` / an event listener / a `Promise.then`.<br><br>Why it hurts init cost: `init()` runs in the loader's time-sliced critical pass; a forced reflow there extends the long task and delays the `data-nds-loaded` reveal. The project's cold-init contract is that `init()` does cheap registration only (refs, listeners, observers) and defers all measurement until the element is shown.<br><br>**Explicitly NOT a finding** (do NOT flag):<br>- Reads inside a deferred callback opened at the top of `init()` (`NDS.onElementResize` initial callback, `NDS.onIntersect`, `requestAnimationFrame`, `NDS.onIdle`) — the measurement runs off the critical task. This is the canonical cold-init pattern; it's the fix, not a finding.<br>- Reads in a method reached only on user interaction or on `show()` / `open()` (post-init, element already visible) — not on the init path.<br>- Reads on an element the component just created and has NOT yet inserted into the live DOM (measuring a detached / off-screen build) — no full-page reflow forced.<br>- Reads already caught by JSA-02 (≥3 read/write alternations in the same function) — route there; JSP-11 does not double-flag. JSP-11 is the single-read case JSA-02's alternation threshold misses.<br>- A read annotated within 3 lines with a comment naming why the measurement MUST run at init before paint (e.g. FOUT-blocking, BFCache scroll-restore). Vague `// measure` does not qualify. Mirrors JSP-10's annotation exemption.<br><br>**Cross-reference:** JSP-10 keeps DOM *walks* out of the module IIFE; JSA-01 covers >50-element *loops* at init; JSP-11 covers the single forced *layout read* that no loop-count or alternation-count rule catches. | MEDIUM | Move the measurement into the element's first `NDS.onElementResize(el, handler)` callback (it fires an initial callback when observation starts — measure there), or build-while-hidden and measure on `show()` / `open()`, or push the math into CSS `calc()` so no JS read is needed. Reference: the cold-init refactors of `_js/nds-scroll-more.js`, `_js/nds-pagination.js`, `_js/nds-drawer.js`, `_js/nds-swiper.js` — each moved eager `init()` measurement into a ResizeObserver initial callback to kill the eager-init forced-reflow. If the read genuinely must run at init before first paint, annotate with a comment naming the constraint. |

#### JS DRY/KISS (JSD)

| ID | What to detect | Severity | Fix |
|---|---|---|---|
| JSD-01 | Three sub-shapes. **(a) Direct `data-state` access** — `setAttribute('data-state', ...)`, `getAttribute('data-state')`, `removeAttribute('data-state')`, `hasAttribute('data-state')`, or assignment to `dataset.state`. The `hasAttribute` form bypasses `NDS.State` just like the others — existence reads still encode the same coupling.<br><br>**(b) `data-*-state` parallel state-machine attributes** — `setAttribute('data-<prefix>-state', ...)`, `getAttribute('data-<prefix>-state')`, `removeAttribute('data-<prefix>-state')`, `hasAttribute('data-<prefix>-state')`, or `dataset.<prefix>State` assignment, where the written/read values match a state-machine vocabulary: `open` / `closing` / `opening` / `closed` / `show` / `hide` / `shown` / `hidden` / `active` / `inactive` / `selected` / `expanded` / `collapsed`. A `data-<prefix>-state` attribute (e.g., `data-toast-state`, `data-menu-state`, `data-overlay-state`) is a parallel state machine that should consolidate onto `data-state` so a single `NDS.State` API serves the whole component — keeping multiple state attributes per element fragments the lifecycle and forces every reader to learn which attribute means what. Motivating example: `_js/nds-alert.js:189,261` writes `data-toast-state` with `'show'`/`'hide'`. Carve-out: `data-<prefix>` attributes whose values are NOT in the state-machine vocabulary (e.g., `data-position-vertical="top"`, `data-axis="x"`, `data-variant="primary"`) are configuration / positional / classification attributes, NOT state.<br><br>**(c) Attribute-selector queries against `data-state`** — `querySelector(...)`, `querySelectorAll(...)`, `matches(...)`, `closest(...)` whose selector string contains `[data-state~="<token>"]`, `[data-state="<token>"]`, `[data-state*="<token>"]`, or any other attribute-selector form targeting `data-state`, AND the embedded token matches the state-machine vocabulary listed under sub-shape (b) (`open` / `closing` / `opening` / `closed` / `show` / `hide` / `shown` / `hidden` / `active` / `inactive` / `selected` / `expanded` / `collapsed`). HTML-template writes that embed `data-state="<token>"` as a literal attribute inside an `innerHTML` string (e.g., `el.innerHTML = '<button data-state="active">'`) also count under (c) — the template bypasses `NDS.State.set` just like sub-shape (a)'s direct write. The selector form is the read-side analog of `hasAttribute('data-state')`: it queries by state token in a way that couples consumer code to the State vocabulary outside the State API, just as the `hasAttribute` form encodes existence-by-token outside the API. Motivating examples: `_js/nds-pagination.js:80` selector `[data-state~="active"]`, `_js/nds-pagination.js:491` template write `data-state="active"`, `_js/nds-mainnav.js:843` selector `[data-state~="open"]`, `_js/nds-tooltip.js:169` selector `[data-state~="open"]`, `_js/nds-multiselect.js:217` `.matches('[data-state~="open"]')`. Carve-out: selectors targeting custom `data-*` attributes outside the state vocabulary (e.g., `[data-no-auto-close]`, `[data-dropmenu-no-click]`, `[data-position-vertical]`, `[data-filter]`) are configuration markers, not state — NOT a finding. Carve-out: when a component already maintains its own canonical state-tracking data structure (e.g., a module-level `Set` of open instances, like `_js/nds-mainnav.js:_openDropdowns`), the selector form is doubly wrong (it duplicates the source of truth); when no such structure exists yet, the fix may require adding one.<br><br>**Carve-out: server-rendered state-vocab reads (sub-shape (c) only).** When the queried `[data-state~="<token>"]` is set in HTML template output (Liquid / Jekyll-rendered) AND no preceding `NDS.State.set(*, '<token>')` / `NDS.State.add(*, '<token>')` call in the same file writes that token to a matched element type, the selector is the canonical entry-point — it reads initial state from DOM, not duplicate state from a JS Set. NOT a finding. Detection refinement: for each `[data-state~="<token>"]` selector, grep the same file for `NDS.State.set` / `NDS.State.add` writes of that token; if zero writes of that token exist anywhere in the file, the read is server-rendered and exempt. The recommended fix (iterate + `NDS.State.has`) would replace a single browser-optimized selector with a JS-level filter — strictly worse, no behavior change. Motivating examples (both NOT findings under this carve-out): `_js/nds-drawer.js:250` (`initActiveStates` reads server-rendered `li[data-state~="active"]` descendants to mark parent buttons; no `NDS.State.set(*, 'active')` write of `'active'` exists in the file), and `_js/nds-sidemenu.js:170` (`updateToggleLabel` reads the server-rendered active item's label to display on the collapsed toggle; same shape). | HIGH | Sub-shape (a): use `NDS.State.add/remove/has/get/set/clear/apply`. For `hasAttribute('data-state')` reads, use `NDS.State.get(el) !== ''` when testing for any state, or `NDS.State.has(el, 'specific-token')` when a specific token is known. Sub-shape (b): migrate the parallel state machine onto `data-state` using component-namespaced tokens (e.g., `data-toast-state="show"` → `NDS.State.add(toast, 'toast-show')` or `NDS.State.set(toast, 'shown')`). If the component genuinely needs two orthogonal state axes (rare — usually a redesign smell), use space-separated tokens on a single `data-state` rather than two attributes. Sub-shape (c): when iterating a known set, swap the selector for `NDS.State.has(el, token)`; when querying by state across an unknown scope, either track an instance/module `Set` (mirroring `_js/nds-mainnav.js:_openDropdowns`) or omit the attribute from any HTML template and call `NDS.State.set(el, token)` after `innerHTML` is set. If three or more files independently re-implement attribute-selector queries by state, surface as a JSD-05 promotion candidate for a core `NDS.State.queryByToken(scope, sel, token)` helper — the recurring pattern would justify a shared abstraction over per-component Sets. |
| JSD-02 | Same patterns on `data-status` — `setAttribute`, `getAttribute`, `removeAttribute`, `hasAttribute`, or `dataset.status` assignment. The `hasAttribute('data-status')` form bypasses `NDS.Status` just like the others. | HIGH | Use `NDS.Status.set/get/clear`. Status is a single value, not a set. For `hasAttribute('data-status')` reads, use `NDS.Status.get(el) !== ''`. |
| JSD-03 | Hardcoded layout pixels bypassing the token layer, in three sub-shapes: **(a) viewport comparisons** — any integer literal in `[400, 2000]` compared (`<`, `<=`, `>`, `>=`, `===`, `!==`) against `window.innerWidth`, `document.documentElement.clientWidth`, or `document.body.clientWidth`. The range captures the realistic viewport-breakpoint space while excluding unrelated small/large magic numbers (counts, sizes, timeouts). Off-by-one literals like `958` (near-desktop) or `1439` (near-large-desktop) are the main reason for widening beyond the canonical 768/1024/1280/1440 list; **(b) fallback literals** — the canonical breakpoint values (768/1024/1280/1440) used as the right-hand side of `\|\|` after `getComputedStyle().getPropertyValue(...)` parses a CSS variable (e.g., `parseInt(styles.getPropertyValue('--nds-...')) \|\| 1280`); **(c) `matchMedia` breakpoint literals** — any integer literal in `[400, 2000]` embedded inside a string argument to `window.matchMedia(...)` as part of a `min-width` / `max-width` / `min-height` / `max-height` media feature (e.g., `window.matchMedia('(max-width: 600px)').matches`, `window.matchMedia('(min-width: 601px) and (max-width: 960px)')`). Off-by-one literals like `601` / `961` are the common shape (media queries use inclusive edges while `NDS.breakpoints` picks one side as canonical), and they silently drift from the tokens on every re-audit. All three shapes hardcode the token into JS; (b) also hides a missing-CSS-var bug when the fallback silently takes over.<br><br>**Explicitly NOT a finding** (do NOT flag):<br>- Numeric literals on the right-hand side of a module-scope `const` declaration whose identifier is SCREAMING_SNAKE_CASE (e.g., `CONTENT_MAX_WIDTH_FALLBACK`, `SLIDES_MAX_WIDTH`) AND which has a rationale comment within 3 lines above explaining why JS needs the numeric value. This IS the fix for sub-shape (b); re-flagging it on every audit run is noise. The detection fires on raw literals in comparisons or fallback positions, not on named-constant declarations that encode the token-layer shim.<br>- Integer comparisons against viewport width where the literal is OUTSIDE `[400, 2000]` — those are genuinely not breakpoint logic (thresholds for tiny embed widths, very-wide ultra-monitor layouts, etc.) and should be judged case-by-case rather than swept up by this rule.<br>- `matchMedia` strings embedded in feature-query syntax unrelated to viewport width/height (e.g., `(prefers-reduced-motion: reduce)`, `(hover: none)`, `(prefers-color-scheme: dark)`, `(pointer: coarse)`) — sub-shape (c) only catches literal integer pixel values inside `(min\|max)-(width\|height)` media features.<br>- `matchMedia` calls whose argument is `NDS.breakpoints.<token>` or a module-scope `const` with a rationale comment — the token layer is already in use, no finding. | MEDIUM | Viewport comparisons and `matchMedia` literals → use `window.matchMedia(NDS.breakpoints.mobile/tablet/'tablet-max'/desktop/'desktop-max'/'large-desktop').matches`. If the specific pixel threshold isn't in `NDS.breakpoints`, either add it to core or align the component to the closest existing token (e.g., `958` → `'desktop'` at 960px, `601` → `'tablet'` at 600px, `961` → `'desktop'` at 960px). Fallback literals → ensure the CSS variable is always defined in base styles so the fallback never fires, or extract the literal to a SCREAMING_SNAKE_CASE module-scope constant with a rationale comment explaining why JS needs the value. |
| JSD-04 | Re-implemented locale/direction check that duplicates `NDS.lang` / `NDS.isRTL` / `NDS.isArabic`. Two sub-shapes:<br><br>**(a) Direction/RTL checks:** `document.dir`, `document.documentElement.dir === 'rtl'`, `getAttribute('dir')` on `document.documentElement` or `document.body`, or equivalent reads of the `dir` attribute to derive RTL-ness.<br><br>**(b) Language checks:** `document.documentElement.lang`, `document.lang`, `document.documentElement.getAttribute('lang')`, or derivations that lowercase / prefix-split / compare-against-`'ar'` those values to produce a lang code (e.g., `(document.documentElement.lang \|\| 'en').split('-')[0].toLowerCase()`, `document.documentElement.lang.startsWith('ar')`).<br><br>**Explicitly NOT a finding** (do NOT flag):<br>- `NDS.lang` / `NDS.isRTL` / `NDS.isArabic` definitions inside `nds-core.js` — that's the source of truth, not a duplicate.<br>- Per-element `lang` reads: `el.getAttribute('lang')` / `el.lang` where `el` is NOT `document.documentElement` / `document.body`. Component code may legitimately read per-element language overrides (e.g., a widget switching mid-page); that's not a duplication of the document-level `NDS.lang`.<br>- Per-element `direction` reads: `getComputedStyle(el).direction === 'rtl'` (or `=== 'ltr'`) where `el` is NOT `document.documentElement` / `document.body`. `NDS.isRTL` is a document-level signal and cannot answer per-instance direction questions — a component nested inside a `dir="…"` wrapper or styled via CSS `direction:` (e.g., chart canvases, embedded RTL widgets inside an LTR shell) must resolve direction at the element level. Motivating example: `_js/nds-chart.js:404` reads `getComputedStyle(this.el).direction` for per-chart RTL handling — a legitimate use case that `NDS.isRTL` cannot satisfy. | MEDIUM | Use `NDS.isRTL` (live getter) or `NDS.lang` / `NDS.isArabic`. |
| JSD-05 | **Promotion candidate.** A helper function with near-identical signature and body appears in two or more component files and has no equivalent in `nds-core.js`. | MEDIUM | Propose an `NDS.*` API name and list every call site. Typical candidates: focus-trap, scroll-lock, `getComputedStyle` readers, `matches()` / `closest()` helpers, RAF batchers, event-delegation factories, z-index stackers, `aria-expanded` toggles. Surface the candidate in the Phase 4 report; editing `nds-core.js` to add the helper + migrating every call site is a Phase 5 action the user approves per-candidate via `promote <api-name>` (see "Applying promotion candidates" in Phase 5). |
| JSD-06 | DOM read after write in the same frame (e.g., `setProperty` then `getBoundingClientRect` in a loop).<br><br>**Explicitly NOT a finding** (do NOT flag):<br>- Standalone forced-reflow triggers (`void el.offsetHeight;` or `el.offsetHeight;` as the sole statement on its line) are deliberate transition-start patterns, not thrashing.<br>- Staged-measurement patterns: a write to `style.cssText` / `style.visibility` / `style.position` that puts an element into a measurable off-screen state, followed by `offsetHeight` / `offsetWidth` / `getBoundingClientRect()` reads. The read semantically depends on the write (the element must be staged before it can be measured), so "batch reads before writes" does not apply. Require an inline comment near the write confirming the staging intent to earn this exemption — otherwise it's indistinguishable from a thrash loop.<br><br>Thrashing is a LOOP pattern (N writes × N reads within one synchronous block). One-shot write-then-read sequences are not thrashing. | LOW | Batch reads before writes. See the pattern in `_js/nds-drawer.js:77-99`. |
| JSD-07 | **Component contract conformance.** A `_js/nds-{name}.js` file whose public surface does not match one of the two shapes the loader expects. Factory components must expose `NDS.{Name}.init()`, `NDS.{Name}.reinit()`, and `NDS.{Name}.create(el)`. Singletons must expose `NDS.{Name}.init()` plus their component-specific methods. The namespace `{Name}` must be the PascalCase form of the filename suffix.<br><br>**Explicitly NOT a finding** (utility-module exemption): files that are NOT registered in `_js/nds-loader.js` AND have no DOM-attachment surface (no top-level `document.querySelectorAll` / `document.addEventListener` to a content selector / `NDS.onDOMAdd` subscription) AND publish only pure-function helpers consumed by other components. To verify the exemption applies, grep `_js/nds-loader.js` for the module's namespace literal — if absent from the loader's registry AND the module's IIFE body contains no DOM-attachment calls, the contract does not apply. Example: `_js/nds-voice-recognition.js` exports `{audioFeedback, isSupported, getLanguage, create, startListening, stopListening, _loaded}` for `nds-forms.js` to consume; it is not loader-registered and does not self-attach.<br><br>**Also NOT a finding** (declarative-delegation library exemption): a module that (1) is NOT registered in `_js/nds-loader.js`, (2) publishes a documented programmatic API (`NDS.<Name>.<methods>`), AND (3) whose ONLY DOM-attachment surface is a single lazily-installed document-level *delegated* listener (a `document.addEventListener('click'\|'input'\|…, handler)` whose body resolves targets via `e.target.closest('[data-<name>]')`), optionally paired with an `NDS.onDOMAdd('[data-<name>]', …)` subscription that re-arms the same delegation for late-added triggers. Such modules drive declarative `[data-*]` attributes rather than per-element component instances; they need no `init()`/`reinit()`/`create()`, and adding one nothing calls would itself be a JSD-11 pretender. The defeating signal for the pure-utility exemption above (a single delegated listener + `onDOMAdd` re-arm) is exactly this exemption's *qualifying* signal — the distinction is that the DOM attachment is one document-level delegation, not per-element wiring the loader would need to manage. Motivating example: `_js/nds-export.js` exposes `NDS.Export.{export,csv,xls,pdf,collect,…}` and self-installs one delegated `[data-export]` click listener (L431) plus an `NDS.onDOMAdd('[data-export]', …)` re-arm (L435); not loader-registered. | MEDIUM | Restructure the module to match the shape of the closest peer (factory example: `_js/nds-modal.js`; singleton example: `_js/nds-theme.js`). Inconsistent shapes force every reader to relearn the API per component and block the loader from treating components uniformly. For utility modules covered by the exemption above, no fix is required — but a one-line comment near the namespace export naming the consuming components keeps future audits from re-flagging. For declarative-delegation libraries, a one-line comment near the namespace export naming the pattern (e.g. `// Library + declarative [data-export] delegation; no loader entry by design.`) serves the same purpose. |
| JSD-08 | **Dead defensive guards.** Inside a component registered with `nds-loader.js`, checks like `typeof NDS !== 'undefined'`, `typeof NDS.X !== 'undefined'`, `NDS && NDS.X`, the `window.`-prefixed variants (`window.NDS && window.NDS.X`, `window.NDS && NDS.X`, any mix), OR the optional-chaining variants (`NDS?.X?.method()`, `NDS.X?.method()`, `NDS.X?.subProp`) used as preconditions before calling core APIs. Detection covers bare-`NDS`, `window.NDS`, AND optional-chaining (`?.`) forms — earlier drafts of this rule only caught the bare-`&&` form and missed both fully-prefixed guards in defensive consumer-style code AND the modern `?.` collapse, which is the equivalent existence-guard expressed via optional chaining. The loader guarantees `NDS` and every registered namespace exist before any component's `init()` runs, so these guards are dead code that implies uncertainty the loader contract explicitly denies. Multi-part guards where ONLY the final method-existence check is load-bearing (e.g., `NDS && NDS.Forms && NDS.Forms.validateForm`) collapse to just the final call when the API is unconditionally assigned — verify by grepping `NDS.{Namespace} = {` in the owner module to see if the method lives inside an always-present object literal.<br><br>**Explicitly NOT a finding** (do NOT flag these as JSD-08):<br>- `typeof window !== 'undefined'` — this is a runtime-environment guard protecting against SSR/Node execution, not a loader-contract guard. Whether it's actually dead depends on the project's SSR posture, which is a separate architectural decision outside this rule's scope.<br>- `typeof document !== 'undefined'` — same reasoning.<br>- `NDS.Alert` / `NDS.X` checks in code that runs BEFORE the loader (module top level, or in a bootstrap script) — for those, the loader contract has not yet fired, so the guard is load-bearing.<br>- **Annotation exemption (documented soft dependency).** A multi-part guard preceded within 3 lines above by an inline comment that names a specific reason the API is treated as optional — typically an inter-component soft-dependency intentionally left optional for selective bundling or for compatibility with consumer environments that may exclude one component. The comment must (1) name the specific dependency by namespace (`NDS.Alert`, `NDS.Forms`, etc.) and (2) state the rationale for treating it as optional (e.g., "Soft dependency — component no-ops if X isn't bundled"). Vague "// safe", "// just in case", or "// defensive" comments do NOT qualify; the comment must explain *why* the guard is intentional. This mirrors the annotation-exemption shape used by JSP-05 (local MutationObservers with rationale) and JSS-01 (HTML-sink interpolations with rationale). Motivating example: `_js/nds-cooldown-button.js:98-99` — the line above the guard reads `// Soft dependency — component no-ops the toast if Alert isn't bundled.` That documents the exact intent the rule should not contradict.<br><br>Scope is deliberately narrow on two axes: **(1) where the guard appears** — only loader-contract-redundant guards inside method/function bodies called after the component's `init()` runs; AND **(2) what API the guard protects** — only guards on the `nds-core.js` utility surface, which the loader guarantees is loaded before any component's `init()` runs. The core-utility allowlist (the only namespaces in scope for JSD-08): `NDS.State`, `NDS.Status`, `NDS.breakpoints`, `NDS.lang`, `NDS.isRTL`, `NDS.isArabic`, `NDS.langKey`, `NDS.escapeHtml`, `NDS.debounce`, `NDS.rafThrottle`, `NDS.onResize`, `NDS.onElementResize`, `NDS.onIntersect`, `NDS.onDOMAdd`, `NDS.onDOMRemove`, `NDS.onChildrenChange`, `NDS.onAttrChange`, `NDS.onOutsideScroll`, `NDS.trapFocus`, `NDS.scrollLock`, `NDS.cache`, `NDS.i18n`, `NDS.portal`, `NDS.unportal`, `NDS.placeFixed`, `NDS.flipPosition`, `NDS.uniqueId`, `NDS.transitionSpeed`, `NDS.focusableSel`, `NDS.gridLastRow`, `NDS.closest`, `NDS.queryAll`, `NDS.querySelector`, `NDS.needsPortal`, `NDS.onIdle`.<br><br>**Also in scope of JSD-08 — self-referencing guards on the file's own namespace.** A third in-scope shape: optional-chaining (`NDS.<Same>?.<method>?.()`, `NDS.<Same>?.<prop>`) or `&&` (`NDS.<Same> && NDS.<Same>.<method>`) guards on the *file's own exported namespace* — where `<Same>` matches the file's own `NDS.<Same> = { ... }` definition earlier in the same module. By module-eval time, that assignment has already executed, so the guard is dead in exactly the shape JSD-08 catches for core utilities. Detection: identify the file's primary namespace via a grep for `^\s*NDS\.<Name>\s*=\s*\{` (or `NDS\.<Name>\s*=\s*\(?\s*\{`) at module top level, then flag any subsequent `NDS.<Name>?.<method>?.()` / `NDS.<Name> && NDS.<Name>.<method>` guards in the same file. Same fix (delete the guard) applies. **Carve-outs**: (a) guards inside code paths that run BEFORE the namespace assignment (rare — would require the guard to live above the `NDS.<Same> = { ... }` line); for those, the namespace genuinely doesn't exist yet and the guard is load-bearing. (b) annotation exemption — same comment-rationale shape as the core-utility annotation exemption above. Motivating example: `_js/nds-accessibility.js:1106, 1113` — `NDS.Accessibility?.init?.()` and `NDS.Accessibility?.toggle?.()` inside `bootAccessibility()` IIFE, with `NDS.Accessibility = { ... }` defined hundreds of lines earlier in the same file.<br><br>**Explicitly NOT in scope of JSD-08** (route to JSD-12 instead): guards on *sibling-component* namespaces (`NDS.Alert`, `NDS.Backdrop`, `NDS.Copy`, `NDS.Dropmenu`, `NDS.Feedback`, `NDS.Forms`, `NDS.Mainnav`, `NDS.Modal`, `NDS.Pagination`, `NDS.Sort`, `NDS.Tooltip`, `NDS.TimeDate`, `NDS.UserFeedback`, etc. — anything outside the core-utility allowlist above AND not the file's own namespace). The loader does NOT guarantee one sibling exists when another's `init()` runs — deferred components can be absent when the critical pass starts, optional components can be bundle-excluded by consumers, and load order across siblings is registry-order-dependent. A guard on a sibling component is therefore *potentially* load-bearing; it shouldn't be removed automatically, but it should be annotated. JSD-12 catches unannotated sibling guards. | LOW | Remove the guard. If the call site truly cannot assume the dependency (e.g., code that runs before the loader or outside the component lifecycle), route it through the loader or defer the call — do not hedge at the call site. |
| JSD-09 | **Teardown references properties never stored, or stored handles never invoked.** Four sub-shapes:<br><br>**(a) `this.handlers.{name}` map shape** — Inside any teardown path (function named `cleanup` / `destroy` / `teardown`, or any function containing `removeEventListener(..., this.handlers[name])` — either in a loop over named keys OR as straight-line `removeEventListener` calls referencing `this.handlers.{name}` directly), every handler key referenced must have a matching assignment `this.handlers.{name} = ...` somewhere else in the same file. A teardown that dereferences `this.handlers.foo` when `foo` was never stored is dead-code-pretending-to-clean-up: the `removeEventListener` call receives `undefined` and silently does nothing. Detection: for each `this.handlers.{name}` or `this.handlers[name]` reference inside a teardown path, grep the file for `this.handlers.{name} =` or `this.handlers[{name-literal}] =`.<br><br>**(b) Instance-property handler shape** — `removeEventListener(..., <owner>._handlerName)` or `removeEventListener(..., <owner>.<propName>)` (where `<owner>` is `this`, an instance reference, or a closure-scoped variable; `_handlerName` / `propName` is a single identifier, not a method call). Same detection as (a) but generalized: grep the file for `<owner>._handlerName =` / `<owner>.<propName> =`. If no assignment exists, the call receives `undefined` and silently noops. Canonical bad shape: a `removeEventListener('resize', el._someHandler)` line in teardown where the file never executes `el._someHandler = function(...) {...}` before the matching `addEventListener`. Also covers `<owner>.observer.disconnect()` / `<owner>.<prop>.disconnect()` / `<owner>.<prop>()` zero-arg call forms in teardown that depend on `<prop>` being assigned — e.g., `this.observer.disconnect()` where `this.observer` was never assigned in the constructor or setup methods.<br><br>**(c) Stored `NDS.on*` unsubscribe handle never invoked** — `<owner>._off* = NDS.on*(...)` stored on an instance/closure but no teardown path in the same file calls `<owner>._off*()`. The pooled subscriber leaks for the process lifetime; on re-init the same component stacks subscriptions. Detection: collect every `<owner>._off* = NDS.on*(...)` assignment, then grep for `<owner>._off*(` / `<owner>._off*?.()` call forms. If zero call sites exist in any teardown / re-init path, emit the finding.<br><br>**(d) Collection-shaped teardown stores never populated** — An instance field assigned to an empty collection (`<owner>.<prop> = []`, `<owner>.<prop> = new Set()`, `<owner>.<prop> = new Map()`) AND iterated in a teardown path (e.g., `this.observers.forEach(o => o.disconnect())`, `this._cleanups.forEach(fn => fn())`) BUT no `<owner>.<prop>.push(...)` / `<owner>.<prop>.add(...)` / `<owner>.<prop>.set(..., ...)` write to that field exists anywhere in the file. The forEach is a permanent no-op; the field is dead bookkeeping that pretends to release resources nothing ever stored. Sub-shapes (a)–(c) all check for assignment-presence; (d) checks for collection-population, since `this.observers = []` IS an assignment but writes nothing useful. Detection: collect every `<owner>.<prop> = (\[\]\|new (Set\|Map\|Array)\(\))` initializer, then grep the file for `<owner>.<prop>.push(` / `<owner>.<prop>.add(` / `<owner>.<prop>.set(`. If zero population sites exist AND the field is iterated in a teardown path, emit the finding. Motivating example: `_js/nds-date-picker.js:537` initializes `this.observers = []`; `cleanup()` at L1143-1148 and `destroy()` at L1904-1908 iterate it via `forEach(observer => observer.disconnect())`; no `this.observers.push` exists in the file, so both disconnect loops are dead.<br><br>**Explicitly NOT a finding**: teardown keys assigned via destructuring (`const { foo } = this.handlers`) or assigned in a branch the detector can't statically see (e.g., conditionally stored inside a framework callback). When in doubt, confirm the binder's shape first — don't emit a false positive on dynamic code. | LOW | Sub-shape (a): mirror the correct binder in the same file (e.g., `bindNavigationEvents` in `_js/nds-date-picker.js` uses `this.handlers.prevBtn = function...; element.addEventListener('click', this.handlers.prevBtn);`). Sub-shape (b): either assign `<owner>._handlerName = function...;` before the matching `addEventListener`, or replace with an `AbortController` + `{ signal }` pattern. Sub-shape (c): invoke the stored handle in the teardown path — e.g., `if (drawer._offResize) drawer._offResize();` then `delete drawer._offResize;`. Alternatively, delete the teardown entry if the listener's lifetime is managed differently (AbortController, innerHTML reset, component destruction). Sub-shape (d): delete the empty-collection field and its teardown forEach block — the resources the field pretends to track are managed elsewhere (or were never tracked at all). If the field is supposed to track something currently un-stored, fix the storage shape instead by adding the `.push` / `.add` writes at the call sites that create those resources. |
| JSD-10 | **File-local helper shadows an existing `NDS.*` utility.** The natural counterpart to JSD-05: JSD-05 watches for *promotion* candidates (duplicates across ≥2 component files with no equivalent in core); JSD-10 watches for *demotion* candidates (a single component file defines a helper that core already exposes). Once a helper is promoted to core, every file-local copy becomes dead duplication — each one is a correctness hazard because future bug fixes to the core implementation won't propagate to the shadowed version.<br><br>Detection: for any `function name(...)` or class-method `name(...)` declared inside a component file (NOT at module top level of core), whitespace-normalize its body and compare against the body of `NDS.<name>` (read from `_js/nds-core.js`). Fire the finding when EITHER (1) OR (2) holds:<br><br>**(1) Overlap path** — ≥65% token overlap AND comparable argument shapes. The lowered threshold (was 90% in the first draft of this rule) catches unhardened variants of a core helper: a local copy that lacks the null guard, the `String(...)` coerce, or other hardening lines core adds. Those hardening lines are typically the first 25–35% of core's body, so a strict 90% gate rejected exactly the cleanup targets the rule existed for.<br><br>**(2) Canonical-idiom path** — both bodies match the same canonical DOM idiom AND the signatures are comparable (same argument count, same return type). This path catches divergent implementations of the same pattern — e.g., one file uses `.textContent = x`, another uses `.appendChild(document.createTextNode(x))`, both produce identical output for string inputs but share <50% token overlap. Canonical idioms currently recognized:<br>- **HTML escape**: `createElement(tag)` → text-safe content assignment (`.textContent = x` OR `.appendChild(document.createTextNode(x))`) → read `.innerHTML`<br>- **Clamp**: `Math.min(Math.max(v, lo), hi)` (any argument order)<br>- **Debounce/throttle**: a stored timer/flag + `setTimeout`/`requestAnimationFrame` + inner function call<br><br>Canonical-idiom recognition can be extended as audit runs reveal new duplicate-with-divergent-syntax patterns (record as a gap observation when a pattern recurs in 3+ files).<br><br>Record both locations (component helper + core helper) in the finding so the user can verify the overlap visually before deleting.<br><br>**Explicitly NOT a finding** (do NOT flag):<br>- Helpers with the same name as a core utility but meaningfully different behavior — e.g., a component `escapeHtml` that strips tags rather than encoding them, or one that additionally trims whitespace. Name collision alone is not grounds for a finding; body equivalence is the gate.<br>- Helpers where core's version has a different arity or return type (e.g., core returns a Promise, component version is synchronous). Swapping would break callers — surface it as a gap, not a finding.<br>- Intentional forks of a core helper that a comment within 3 lines above explicitly justifies (e.g., `// Local copy with IE11 workaround; core version assumes modern API`). The annotation-exemption pattern mirrors JSS-01 / JSP-05.<br><br>JSD-10 fires at LOW severity because file-local duplicates are not dangerous on their own — they're cruft that grows confusing over time as core evolves and the forks diverge silently. | LOW | Delete the file-local helper definition. Rewrite every call site in the same file from `helperName(...)` / `this.helperName(...)` to `NDS.<helperName>(...)`. See `_js/nds-core.js` for the authoritative definition. If callers relied on a different method shape (e.g., instance-bound `this.helperName`), replace with the core call at the site and let the instance reference fall out of scope. |
| JSD-11 | **Loader-contract pretender — `init()` exposed but never invoked.** A module whose namespace literal (`NDS.<Name>`) does NOT appear in the `COMPONENTS` array of `_js/nds-loader.js` AND that exports a `NDS.<Name>.init` method, where EITHER **(a)** the file's `init` body executes meaningful DOM work (a `document.querySelectorAll` plus per-element setup, listener attachment, attribute writes other than a sentinel marker) but no other file in `_js/` calls `NDS.<Name>.init()` and the loader does not fire it, OR **(b)** an inline comment within 3 lines of the `init` definition claims the method exists to satisfy the loader's singleton contract while the namespace is not in the loader registry. Detection: grep `_js/nds-loader.js` for the namespace literal — if absent, then grep `_js/` for `NDS.<Name>.init(` call sites. If zero call sites exist outside the file's own definition, AND `init`'s body either does meaningful DOM work (sub-shape a) or carries the misleading "loader contract" annotation (sub-shape b), emit the finding. Both shapes are dead-code-pretending-to-conform: sub-shape (a) leaves a `querySelectorAll` walking the DOM and silently does nothing because no caller fires it; sub-shape (b) misleads the reader about why the method exists.<br><br>**Explicitly NOT a finding** (do NOT flag):<br>- Modules where `init` is genuinely a no-op AND the comment honestly describes lazy initialization (e.g., "Lazily initialized on first `show()` call; nothing to do at page load"). Honest no-op + truthful comment = working-as-intended utility shape.<br>- Modules where `init` is invoked by another module via documented composition (e.g., `NDS.Sort.create(...)` is called from `nds-filter.js` and `nds-tables.js`). The composition site IS the call site — the loader-registry absence is by design for composable widgets.<br>- Pure programmatic-API modules whose only DOM-attachment surface is wrapped behind `create({})` / `show()` / `dismiss()` style entry points; if their `init` is genuinely empty, the rule's "meaningful DOM work" gate (sub-shape a) is not met.<br>- Modules whose `init` is referenced from a documentation page or example that is NOT in `_js/` (e.g., snippet code in `components/*.md`). Detection covers `_js/` only — surface as a gap, not a finding, if the call site lives elsewhere.<br><br>JSD-11 fires LOW because the dead body isn't dangerous — it just trains future readers to expect loader-driven enhancement that never arrives. Complements JSD-07 (contract conformance for loader-registered modules) by covering the inverse. | LOW | Three options by the module's actual purpose: (1) **Register** in `_js/nds-loader.js` if `init` should actually run on every page (add `COMPONENTS` entry with selector or `universal: true`). (2) **Delete the `init` method** if the module is a programmatic API and `init` was vestigial — the file becomes a JSD-07-exempt utility shape. (3) **Replace the misleading comment** if the no-op is intentional (lazy init at first call) — drop any "satisfies the loader's singleton contract" language when the module is not in the loader. |
| JSD-12 | **Unannotated sibling-component soft-dependency guard.** Inside a loader-registered component, a guard on a *sibling-component* namespace (NOT a core utility — see the core-utility allowlist in JSD-08) lacks a leading comment naming the specific sibling AND stating why it's optional. Detection: a check of the form `if (NDS.<Sibling>) { … NDS.<Sibling>.<method>(…) }`, `if (!NDS.<Sibling>) return;`, `NDS.<Sibling> && NDS.<Sibling>.<method>(…)`, `NDS.<Sibling>?.<method>(…)`, where `<Sibling>` is any namespace outside the JSD-08 core-utility allowlist (e.g., `NDS.Alert`, `NDS.Backdrop`, `NDS.Copy`, `NDS.Dropmenu`, `NDS.Feedback`, `NDS.Forms`, `NDS.Mainnav`, `NDS.Modal`, `NDS.Pagination`, `NDS.Sort`, `NDS.Tooltip`, `NDS.TimeDate`, `NDS.UserFeedback`). Unlike core utilities (which the loader guarantees), sibling-component existence is contingent on bundling and load order, so the guard is *potentially* load-bearing — the problem is silence about whether it is. An unannotated guard leaves the next reader guessing whether the dependency is hard, soft, or vestigial; a future refactor can't tell whether removing the guard is safe.<br><br>Rule complements JSD-08: JSD-08 catches dead guards on the always-present core surface; JSD-12 catches *potentially-load-bearing* guards on the sibling-component surface without rationale.<br><br>**Explicitly NOT a finding** (do NOT flag):<br>- **Annotation exemption (documented soft dependency).** A guard preceded within 3 lines above by an inline comment that (1) names the specific sibling namespace AND (2) states the rationale for treating it as optional (e.g., "// Soft dependency — component no-ops the toast if Alert isn't bundled."). Mirrors the JSD-08 / JSP-05 / JSS-01 annotation-exemption shape. Vague "// safe" / "// defensive" / "// just in case" does NOT qualify; the comment must explain *why* the sibling is optional. Motivating exemplar: `_js/nds-cooldown-button.js:98-99` documents the exact intent the rule should not contradict.<br>- Guards on `NDS.<Sibling>` whose presence is in a code path that runs BEFORE the loader (module top level, bootstrap script) — for those, the loader contract has not yet fired, so the guard is load-bearing regardless.<br>- Raw method calls without any guard (`NDS.<Sibling>.<method>(…)` with no `if`/`?.`/`&&`) — those are NOT JSD-12. They might be JSP/JSS issues if the sibling is genuinely optional, but they don't fit this rule's "unannotated guard" shape. | MEDIUM | Add an annotation comment within 3 lines above the guard that names the sibling AND states why it's optional. Template: `// Soft dependency — <component> no-ops <feature> if <NDS.Sibling> isn't bundled.` If the dependency is actually hard (the sibling is always shipped with this component in every bundle), delete the guard outright — let the call throw if the contract is violated, so future drift is loud. If three or more components carry the same guard against the same sibling, surface as a JSD-05 promotion candidate (the cluster suggests the dependency should be lifted to a shared abstraction). |
| JSD-13 | **Hardcoded transition timing instead of `NDS.transitionSpeed()`.** A bare `setTimeout(fn, N)` (or `setTimeout(fn, N + delta)` where `delta` is a small numeric literal ≤ 100 — e.g., `NDS.transitionSpeed() + 50`-style offsets count toward the same intent) where `N` is a numeric literal in `{150, 200, 250, 300, 400}` — OR a module-scope `const` / object-property binding (e.g., `CONFIG.transitionDuration`, `TRANSITION_MS`) whose RHS resolves at parse time to one of those literals, in which case the binding form counts as the literal for this rule's purposes (the value lookup happens at module-eval time, identical to inlining the number). Detection: unwrap a `Name` / `OBJ.prop` reference to its declaration in the same file; if the declaration is `const Name = N;` / `const OBJ = { prop: N, ... };` with `N` matching the set above, treat the reference as if it were the literal. Motivating example: `_js/nds-drawer.js:143-146, 165-168` use `setTimeout(cleanup, CONFIG.transitionDuration + 50)` where `CONFIG.transitionDuration = 250` (in set); the binding form is functionally identical to inlining `250 + 50` and the rule fires. (the canonical CSS transition durations used in NDS), AND TWO contextual conditions both hold:<br><br>**(i) Same-function-body proximity to a State write of a transition-vocabulary token** — the `setTimeout` and a `NDS.State.set(...)` / `NDS.State.add(...)` / `NDS.State.remove(...)` call writing one of `closing` / `opening` / `show` / `hide` / `shown` / `hidden` / `entering` / `exiting` / `collapsing` / `expanding` live in the same function body, with no `return`, `throw`, or `break` between them. Function-body scope replaces the earlier "3 lines below" gate, which was too tight to catch even the motivating modal example (7 lines of transition setup — `setAttribute('aria-hidden', …)`, backdrop hide, etc. — legitimately sit between the State write and the timer).<br><br>**(ii) Callback-clears-token guardrail** — the `setTimeout`'s callback body must, somewhere along its synchronous path, EITHER call `NDS.State.clear(<sameTarget>)` / `NDS.State.remove(<sameTarget>, '<transitionToken>')` / `NDS.State.set(<sameTarget>, '<terminalState>')` on the same element targeted by the upstream State write, OR call `setAttribute('hidden', '')` / `removeAttribute('hidden')` on the same element (a common transition-end cleanup shape). This guardrail is what differentiates "transition-end timer" (the actual concern) from "unrelated delayed work that happens to live in the same function." Without it, the rule would over-fire on debounce-style timers, animation-step timers, or polling delays that share a function with State writes purely by coincidence.<br><br>Together, conditions (i) + (ii) capture the canonical shape: "set token → wait for CSS transition → clear token." The `setTimeout` is acting as a "wait for the CSS transition to finish" fallback that should read its duration from the same source the CSS uses — `--nds-transition-speed`, exposed via `NDS.transitionSpeed()`. Hardcoding the ms value couples the JS to whatever the CSS variable happens to be at write time; a future design-token refresh that bumps `--nds-transition-speed` from 200ms to 250ms silently breaks the JS-CSS alignment, leaving residual transition state visible.<br><br>Motivating example: `_js/nds-modal.js:83→90-94` — `NDS.State.set(modal, 'closing')` at L83 plus `setTimeout(() => { … NDS.State.clear(modal); … }, 300)` at L90 inside `closeModal()`. Same function body (L83-94), callback clears the upstream token at L92. Both conditions satisfied → finding.<br><br>**Explicitly NOT a finding** (do NOT flag):<br>- `setTimeout(fn, N)` where `N` is NOT in the canonical set above (`{150, 200, 250, 300, 400}`) — those are not transition durations and likely encode a different intent (debounce, animation step, retry backoff).<br>- `setTimeout` whose ms argument is a variable resolved via `NDS.transitionSpeed()` (or `NDS.transitionSpeed() + N`-style offsets) — that's the canonical fix, not a violation. Motivating example: `_js/nds-accordion.js:186-188` already uses `NDS.transitionSpeed() + 50`.<br>- `setTimeout` paired with `addEventListener('transitionend', …)` rather than a State write — that's a transitionend-fallback timer where the listener does the State cleanup. The State write happens earlier in a different function (open/close flow), so condition (i) fails by design. Motivating examples (NOT findings): `_js/nds-dropmenu.js:535-536` (transitionend listener + 200ms fallback), `_js/nds-sidemenu.js:160-161` (same shape, 300ms fallback). If the same shape recurs across 4+ components, surface as a JSD-05 promotion candidate (`NDS.onTransitionEnd(el, fn, fallbackMs?)`) rather than expanding JSD-13.<br>- `setTimeout` whose callback does NOT touch the transition token's owning element (condition (ii) fails) — e.g., `setTimeout(scheduleUpdate, 150)` for unrelated scheduling, even if a State write sits in the same function. The callback-clears-token guardrail is what keeps this rule honest.<br>- `setTimeout` inside an `NDS.debounce`-wrapped handler — that's a different concern (JSP-07's territory).<br>- `requestAnimationFrame(fn)` chains used for transition-start staging (single frame) — that's not a transition-end timer.<br>- Annotation exemption: a comment within 3 lines above the `setTimeout` that explicitly justifies why the ms value cannot be sourced from `NDS.transitionSpeed()` (e.g., "// Fixed 200ms — this fade is independent of --nds-transition-speed."). Vague "// 300ms" or "// wait" does NOT qualify. | LOW | Replace the literal with `setTimeout(fn, NDS.transitionSpeed())`. The helper at `_js/nds-core.js:533-541` reads `--nds-transition-speed` from `:root` once and memoizes the result, so per-call cost is a single property read. If the timer needs a small safety margin past the CSS duration (a common shape for ensuring the transition has fully settled before cleanup runs), use `NDS.transitionSpeed() + N` — the offset is fine; only the base duration needs sourcing from the token. If multiple transitions in the same component need different base durations (rare), keep one literal explicit-and-annotated, and route the others through `NDS.transitionSpeed()`. If three or more components reach for the same non-canonical timing constant, surface as a JSD-05 promotion candidate (probably a new `--nds-transition-speed-*` token + `NDS.transitionSpeed('slow')` accessor). |
| JSD-14 | **Code noise / readability.** A catch-all rule for maintainability drag the other JSD rules don't target. Five sub-shapes — each one a precise, detectable signature so the rule does not devolve into "I don't like this code".<br><br>**Mode-aware coverage.** Only sub-shape (c) is grep-detectable; sub-shapes (a), (b), (d), (e) require per-file deep reads (scope analysis, control-flow tracing, reachability). Coverage by mode:<br>- **Full-tree mode (`/nds-js-audit js dry`)**: sub-shape (c) only. Sub-shapes (a)(b)(d)(e) are SKIPPED — per-file deep reads across 40+ files exceed the documented 30-60k token budget. The Phase 4 report MUST surface this with a banner (see Phase 4 "JSD-14 full-tree coverage banner"); silently omitting the limit pretends to "clean across the tree" coverage the run did not actually perform.<br>- **Single-file mode (`/nds-js-audit nds-foo.js dry`)**: all five sub-shapes run. Per-file deep read is the budget for this mode, so the scope analysis sub-shapes (a)(b)(d)(e) are tractable.<br><br>This split is deliberate, not a bug: JSD-14 is the rule where per-file reads pay off the most (every other JSD rule has a primarily-greppable signature). Code-noise hunts naturally live in single-file dry runs alongside JSA-10 / JSA-11 / JSA-12 / JSA-13 (the architectural code-quality rules that also deep-read in single-file mode). The five sub-shapes:<br><br>**(a) Deep nesting** — a function body with more than 4 levels of `if` / `for` / `while` / `try` / nested-function-expression nesting (measured from the function's opening `{`). `switch` bodies count as one level, not per-case. Carve-out: nesting introduced by `Array.prototype` callbacks (`forEach`, `map`, `filter`) whose inline body is ≤ 3 statements does NOT count toward the depth — those are idiomatic, not nesting smell.<br><br>**(b) Redundant truthy/null guard** — two `if (x)` / `if (x != null)` / `if (x !== undefined)` / `if (!x) return` checks on the same identifier within the same function scope, where no intervening reassignment, `await`, or function call that could mutate `x` exists between them. The second check can never observe a different result than the first. Carve-out: guards separated by an `await` always pass — async re-entry can invalidate the prior check, so the second guard is load-bearing.<br><br>**(c) Nested ternary chains** — a ternary expression nested ≥ 3 levels deep (e.g., `a ? b : c ? d : e ? f : g`). Reads as a state machine glued together with `?` and `:`. Carve-out: chains where every branch is a primitive literal AND total expression length is < 60 chars (those read like a small inline lookup, e.g., `mode === 'A' ? 1 : mode === 'B' ? 2 : 0`).<br><br>**(d) Dead code after unconditional terminator** — statements after an unconditional `return` / `throw` / `break` / `continue` in straight-line flow within the same block, where no label / control-flow construct can reach the statement. Carve-out: code after a `throw` inside a `try` block (the catch path or editor type-narrowing tooling may rely on the unreachable line).<br><br>**(e) Unused local** — a `const` / `let` declaration whose binding is never read in any reachable scope, AND whose initializer has no observable side effect (not a function call, not `new X()`, not a getter access on a host object). Carve-out: destructuring rest patterns (`const { used, ...rest } = obj`) where `rest` is unused — the pattern is idiomatic for excluding fields. Carve-out: declarations preceded within 3 lines above by an inline comment naming a specific reason the binding is kept (e.g., `// kept to document the shape returned by foo()`). | LOW | Sub-shape (a): extract a helper, or invert guards to early-return so the happy path stays at the top level. Sub-shape (b): delete the redundant guard. Sub-shape (c): refactor to `if` / `else` blocks, or a lookup map when every branch is a value-from-key. Sub-shape (d): delete the unreachable code. Sub-shape (e): delete the unused declaration; if the initializer had a side effect the caller depended on, lift the expression out of the declaration. No core-helper reference — this rule is about subtraction, not API routing. |
| JSD-15 | **Code persona — project conventions per `PERSONA.md`.** The codebase has chosen canonicals for the patterns that recur across files: instance-lifetime AbortController name, teardown method name, lifecycle pair naming, component console prefix, init-guard sentinel, listener attachment shape. The canonicals live in `PERSONA.md` (sibling to this file) along with the principle defending each choice, the rejected alternatives, the documented carve-outs, and the literal "Audit behavior" check. JSD-15's job is to apply each entry's check to the target file(s) and flag divergence.<br><br>**Runs in both single-file and full-tree modes.** Single-file mode applies the checks to one file. Full-tree mode applies them across every component file AND additionally reports the per-entry "current adoption" tally as a refactor-progress signal.<br><br>**Detection: read `PERSONA.md`, walk each entry's "Audit behavior" field, apply it to the file.** Each PERSONA entry's check is concrete (e.g., entry 1 flags `this.<name> = new AbortController()` where `<name>` is ≤4 chars or matches `_[a-z]{1,3}` or equals `controller`; entry 4 flags `console.warn/error` whose first string arg doesn't match `/^NDS [A-Z][A-Za-z]*: /`). The rule does NOT re-derive canonicals from the corpus — the canonicals are the source; corpus state is the verification.<br><br>**Carve-outs are PERSONA-driven.** Each entry lists concept-different cases that share surface vocabulary but follow a different principle (e.g., secondary domain-named AbortControllers like `this.fetchAbortController`; the date-picker two-phase `cleanup()` + `destroy()` lifecycle; `window.nds<Name>Initialized` for cross-module observers in tables.js). The audit MUST honor these carve-outs by reading the cited file:line and confirming the carve-out's discriminator applies. Do NOT flag a carve-out as divergence.<br><br>**Phase 7 EVOLVE proposals.** When a file diverges from a canonical, the audit surfaces TWO outcomes for the user to pick from: (a) migrate the file to the canonical, OR (b) revise the canonical via Phase 7 EVOLVE proposal that names the file's reasoning. The canonical changes only through deliberate revision; drift never wins by accumulation. See `Phase 7: EVOLVE` below for how persona-drift proposals are routed alongside catalog rule changes.<br><br>**Annotation exemption.** A divergent pattern preceded within 3 lines by an inline comment naming the specific principle reason for the divergence is NOT flagged. Mirrors the annotation-exemption shape used by JSD-08 / JSD-12 / JSS-01. Vague "// safe" / "// chosen" do NOT qualify; the comment must explain *which* PERSONA principle the divergence answers to (e.g., `// Two-phase teardown — see PERSONA entry 6 carve-out`).<br><br>**If `PERSONA.md` is missing or unreadable**, surface this as a setup error in the report and skip JSD-15 entirely for that run. Do NOT fall back to corpus-tally heuristics — the persona is the source of truth; running the rule without it would re-introduce the snapshot-vs-principle failure mode the persona exists to fix. | LOW | Two outcomes per finding, user picks per file: (1) migrate the file to match the canonical (mechanical rename for fields/methods; restructure for listener attachment shape; concept-bucket switch for lifecycle pairs), OR (2) open a Phase 7 EVOLVE proposal to revise the canonical with the file's reasoning cited inline. Findings that span three or more PERSONA entries within the same file (e.g., divergent AbortController name AND divergent teardown method AND divergent console prefix) surface a broader observation: the file may be a candidate for a focused refactor rather than piecemeal fixes. |
| JSD-16 | **Falsy-constant + truthy-guard dead code (frozen disabled feature flag).** A module-scope `const NAME = '' \| 0 \| false \| null` declaration paired with an `if (NAME && <expr>)` guard somewhere later in the same file, where `NAME` is never reassigned anywhere in the file. The guard's left operand is permanently falsy, so the right operand AND any code in the guarded block are unreachable. The shape typically appears when a feature is deliberately disabled but the scaffolding is preserved — leaving a "frozen disabled feature flag" that misleads future readers into thinking the guard might fire under some condition.<br><br>**Mode-aware coverage.** Deep-read only — confirming "never reassigned anywhere in the file" requires whole-file scope analysis that is not greppable with a single signature. Coverage by mode:<br>- **Full-tree mode (`/nds-js-audit js dry`)**: JSD-16 is SKIPPED. The Phase 4 report MUST surface this with a banner (see Phase 4 "JSD-16 full-tree coverage banner"), parallel to JSD-14's full-tree disclosure. The banner converts a misleading "clean" into honest coverage disclosure.<br>- **Single-file mode (`/nds-js-audit nds-foo.js dry`)**: JSD-16 runs. Per-file deep read is the budget for this mode, so the scope analysis is tractable.<br><br>**Cross-references:** JSD-14(d) catches dead code after an unconditional terminator (`return` / `throw` / `break` / `continue`). JSD-16 catches dead code after a permanently-falsy guard — the dual of (d). JSD-14(e) catches unused locals; JSD-16's target IS used (read by the guard) but its read result is always falsy, making the use itself vestigial.<br><br>**Explicitly NOT a finding** (do NOT flag):<br>- The constant is reassigned anywhere in the file. Even one reassignment makes the guard load-bearing at runtime — the value can change.<br>- **Annotation exemption (intentional disabled state).** The guard is preceded within 3 lines by a comment that (1) names why the disabled state is intentional AND (2) names what would re-activate it (e.g., `// Disabled pending consumer migration to v3 — set NAV_EXCLUSIONS to '.nds-main-nav, .nds-sidemenu' to re-enable.`). Mirrors the JSD-08 / JSD-12 / JSS-01 / JSP-05 annotation-exemption shape. Vague "// disabled" / "// TODO" without the re-activation path do NOT qualify.<br>- The constant is exported (`export const ...`, `module.exports.NAME = ...`, or assignment to `NDS.<Namespace>.<Prop>`) — external consumers may set its value at runtime; the in-file reads aren't the full source-of-truth.<br>- The expression on the right side of `&&` is also a no-op — `if (NAME && doNothing())` is dead code with no side effect to clean up. Rare but harmless; surface as a SKIP rather than a finding.<br>- The constant is initialized to the falsy literal via a ternary or `&&`/`||` expression whose other branch is non-literal (e.g., `const NAME = window.someFlag ? '.foo' : '';`). The static read is falsy at parse time, but runtime resolution depends on an external signal — the guard is load-bearing.<br><br>**Motivating example:** `_js/nds-link.js:6,26` — `const NAV_EXCLUSIONS = '';` with a comment preserving the original selector list (`/* .nds-main-nav, .nds-sidemenu, .nds-footer */`), paired with `if (NAV_EXCLUSIONS && a.closest(NAV_EXCLUSIONS)) return;` 20 lines later. The guard short-circuits on every anchor; the `closest()` call is dead code. No reassignment anywhere in the 37-line file. The comment shows intent (preserve original values for re-activation) but does NOT satisfy the annotation exemption — the exemption requires naming WHY the disabled state is intentional AND the re-activation path, not just preserving the prior value. | LOW | Two options based on intent:<br><br>**(a) Delete the constant + guard** (cleaner shape). The guard never fires; removing it simplifies the read. If the feature might be re-enabled later, surface as a TODO comment at the deletion site or as a project-tracker ticket — not as frozen scaffolding. This is the right default when the original-value comment is the only record of intent.<br><br>**(b) Assign the constant a real value** (re-enables the feature). The guard becomes active; the original behavior is restored. Pick this when the disabled state was an oversight rather than a decision.<br><br>**(c) Add the annotation-exemption comment** when the disabled state is deliberate. The rationale lives with the code; future audits skip the finding without losing the context. This is the right option when the disabled state is bounded by an external condition (consumer migration, deprecated API window, etc.) and the re-activation path is concrete. |

JSD-05 is the most qualitative rule: require ≥2 distinct files with substantially-overlapping bodies (same shape, same side effects) before flagging, and name the proposed API naturally (`NDS.trapFocus(el)`, not `NDS.helperFn7`).

JSD-14 has the highest false-positive risk in JSD. Skip the finding if a sub-shape match has a defensible rationale in context (e.g., deep nesting that mirrors a state machine the reader needs to see linearly; a redundant guard that documents an invariant the next reader would otherwise miss). When in doubt, record as a Gap observation rather than a finding — the rule earns its keep by sharpening over time, not by padding the report.

JSD-15's false-positive defense is `PERSONA.md` itself. The persona's per-entry carve-outs name the concept-different cases that look like divergence but aren't (transient toasts using `show`/`hide` vs modals using `open`/`close`; the date-picker two-phase `cleanup()`+`destroy()` vs single-method instances; `window.nds<Name>Initialized` flags for tree-wide observers in tables.js). The audit MUST read these carve-outs before flagging — a file matching a documented carve-out's discriminator is NOT a finding. When divergence is real but the canonical itself feels wrong for the case at hand, the answer is a Phase 7 EVOLVE proposal to revise the canonical with the file's reasoning, not a quiet skip — the persona changes through deliberate revision, never through silent erosion.

#### JS Security (JSS)

Web-security sinks that commonly appear in vanilla-JS component code. Severity is calibrated to exploitability in a Jekyll static-site context: a reflected XSS through an NDS component is HIGH because the component ships everywhere; a same-origin `window.open` with a developer-controlled `data-*` attribute is MEDIUM. Each JSS finding must name the untrusted-input source in plain language — "this `${title}` comes from `NDSAlert.create()` options, which callers can fill with arbitrary strings" — so the fix cost is visible alongside the risk.

| ID | What to detect | Severity | Fix |
|---|---|---|---|
| JSS-01 | Direct HTML-sink assignment or method call where the RHS / argument is a template literal or `+` concatenation containing at least one non-literal substitution: `el.innerHTML = X`, `el.outerHTML = X`, `el.insertAdjacentHTML(pos, X)`, `document.write(X)`, `document.writeln(X)`, `<documentRef>.write(X)`, `<documentRef>.writeln(X)` — where `<documentRef>` resolves to a `Document` (most commonly `iframe.contentDocument`, `popup.document`, or `globalThis.document`). Classic XSS. Focus on sinks that interpolate caller-supplied arguments (component-options, `dataset` reads, API responses, `getAttribute`), not sinks where every substitution is compile-time-known. The `<documentRef>.write` shape covers the iframe-init idiom (build full `<!doctype html>` document, hand to `iframe.contentDocument.write`) where DOM construction via `createElement` is impractical — the same escape rules apply regardless of which sink delivers the markup. Motivating example for the doc-write shape: `_js/nds-export.js:284-294` builds a print iframe via `doc.write(html)` with every substitution routed through `NDS.escapeHtml` (currently safe; the rule's prior sink list missed this shape).<br><br>**Explicitly NOT a finding**:<br>- Template literals where every `${}` substitution matches the numeric/boolean allowlist. A substitution is allowlisted when, and ONLY when, it is one of:<br>  **(a)** identifier from a visible `for`-loop counter (`i`, `j`, `k`, `n`, `index`, `idx`, or the loop's declared counter name);<br>  **(b)** call expression `parseInt(...)` / `parseFloat(...)` / `Number(...)` / `Math.<any>(...)`, OR `.toString()` / `.padStart(n, '0')` / `.padEnd(n, '0')` / `.toFixed(n)` / `.toLocaleString(...)` on a numeric-by-allowlist receiver — these can only return digit-strings;<br>  **(c)** numeric literal (`42`, `3.14`, `0xFF`);<br>  **(d)** `.length` property of any expression;<br>  **(e)** boolean literal (`true`, `false`);<br>  **(f)** arithmetic / bitwise / comparison / logical expression (`+`, `-`, `*`, `/`, `%`, `**`, `<`, `>`, `&`, `\|`, `&&`, `\|\|`) whose operands all independently match (a)–(e) or (f) recursively;<br>  **(g)** a variable whose most recent assignment in the same function scope resolves to (a)–(f), OR is a template literal whose every `${}` substitution independently matches (a)–(j). One non-matching substitution forfeits the whole variable;<br>  **(h)** ternary `cond ? A : B` where BOTH `A` and `B` are string literals containing no `${}` substitutions — the value is one of two compile-time-known strings;<br>  **(i)** reference to a module-scope `const` declared in the same file with a compile-time-known literal initializer. Three sub-shapes:<br>    - **(i.1)** direct reference `${CONST}` where initializer is a single string/number literal;<br>    - **(i.2)** property access `${CONST.key}` / `${CONST[expr]}` where initializer is an object literal whose every leaf is a string/number literal (nested objects allowed);<br>    - **(i.3)** element access `${CONST[i]}` where initializer is an array literal whose every element is a string/number literal.<br>    Any non-literal leaf in the initializer (function call, dynamic import, template literal with substitutions, variable reference) disqualifies the exemption;<br>  **(j)** instance/object property read `<obj>.<prop>` (or chained `<obj>.<prop1>.<prop2>`) where EVERY assignment to `<prop>` anywhere in the same file resolves to (a)–(f) recursively. Whole-file scope. Covers by-construction numeric fields (`this.totalSteps`, `this.currentStep`, `this.activeIndex`). A single assignment outside (a)–(f) anywhere in the file disqualifies the property entirely — partial coverage doesn't count. Disqualifying chains: `this.opts.colors[i]`, `getAttribute` / `dataset` / `localStorage` reads, non-allowlisted function-call returns.<br>- Interpolations wrapped in the file's own escape helper (`${escapeHtml(x)}` / `${encodeEntities(x)}` / equivalent defined in the same file).<br>- Sinks where the RHS is a static string with no `${}` substitutions.<br>- Annotation exemption: comment within 3 lines naming the specific source of the value AND why it is safe (e.g., `// XSS-safe: icon class comes from TYPE_DEFAULTS keys, not user input`). Vague `// safe` does NOT qualify.<br>- Sinks on freshly-created elements where the value was already `.textContent`-assigned elsewhere (handled as a combined read).<br><br>**Critical override — taint-source escalation.** None of the exemptions above apply when the value's nearest assignment chain reads from a same-origin-writable store: `localStorage.getItem(...)` / `sessionStorage.getItem(...)` / `document.cookie` / a same-file helper wrapping any of these. Same-origin XSS in another page, malicious extensions with `storage` access, or dev-tools paste can plant attacker-controlled HTML; the next render injects it. The taint-source escalation is a HIGH finding regardless of which exemption otherwise applied. Detection: trace the RHS variable's most recent assignment; if it terminates at a writable-store read (direct or via same-file helper), fire. Fix is `NDS.escapeHtml(cached)`, OR migrate the producer to write `.textContent`-safe data only, OR an annotation naming the store AND why the cached payload is trusted (e.g., "// XSS-safe: cache writer at L88 escapes via NDS.escapeHtml before storing"). Motivating example: `_js/nds-cityWeather.js:43, L133`. | HIGH | **Primary**: wrap every caller-supplied substitution in `${NDS.escapeHtml(x)}` — escapes via the browser's textContent serializer, catches edge cases (quotes, backticks, entities, attribute-context bypasses) a regex escape misses. One-line per substitution. **Attribute-context values** still need `setAttribute` rather than interpolation — `escapeHtml` doesn't close quote-breakout vectors in attribute values; lift those out of the template. **Secondary** (mixed-chrome templates / avoid innerHTML entirely): build DOM via `document.createElement` and assign dynamic values through `.textContent` / `.setAttribute`. **Never use regex-based escapes.** |
| JSS-02 | Dynamic code execution sinks: `eval(x)`, `new Function(...args, body)`, `setTimeout(x, ...)` / `setInterval(x, ...)` where the first argument is a string literal or a string-valued variable (not a function reference, arrow, or function expression). The browser will parse and execute the argument as JavaScript, bypassing every escape upstream and skating past any CSP `script-src` restriction that doesn't include `'unsafe-eval'`. There is no legitimate use case for any of these in NDS component code. | HIGH | Replace with a direct function call. `setTimeout('foo()', 100)` → `setTimeout(foo, 100)`. For dynamic dispatch (action name → handler), use a lookup object: `({ next: goNext, prev: goPrev }[action] ?? noop)()`. If you genuinely need to parse user-supplied expressions (extremely rare for UI code), sandbox it in a Worker with a dedicated parser — not in the main realm via `eval`. |
| JSS-03 | Anchor element with `target="_blank"` (or a template-literal expression that could resolve to `_blank` at runtime) and NO `rel="noopener"` or `rel="noreferrer"`, in three sub-shapes: **(a)** HTML string literals containing `<a ... target="_blank" ...>` where no `rel=` attribute in the same tag includes `noopener` or `noreferrer`; **(b)** DOM construction — `document.createElement('a')` followed by `.target = '_blank'` or `.setAttribute('target', '_blank')` with no matching `.rel =` / `setAttribute('rel', ...)` in the same setup block; **(c)** HTML string templates where the `target` attribute value is set via any `${...}` substitution — including conditional spread patterns like `${cond ? \` target="${value}"\` : ''}` — with no `rel=` attribute (literal or interpolated) emitted in the same tag. Sub-shape (c) exists because callers routinely pass `target: '_blank'` through component-options APIs; without a paired `rel` interpolation the template stays vulnerable whenever the condition is true. Without `noopener`, the new tab can reach back through `window.opener` and redirect the original page (reverse-tabnabbing).<br><br>**Explicitly NOT a finding** (do NOT flag):<br>- `window.open(url, '_blank', featureString)` — when a feature string is passed, modern browsers treat the new window as noopener by default; this is a different API from the `<a target="_blank">` attribute pattern.<br>- Anchors whose `href` is a static same-origin path (starts with `/`, `#`, `./`, or the current origin) AND is visible at the rule-detection site (i.e., not a template-literal `${url}` whose destination is dynamic). Same-origin known-safe destinations don't grant cross-origin capability to the new tab.<br>- `rel="noreferrer"` alone is sufficient (it implies `noopener` in the modern spec). | MEDIUM | Add `rel="noopener noreferrer"` to every `target="_blank"` anchor. HTML strings: `<a href="..." target="_blank" rel="noopener noreferrer">`. DOM construction: after setting `target`, also set `link.rel = 'noopener noreferrer';`. Making this the project-wide default in a link-factory helper is ideal. |
| JSS-04 | `addEventListener('message', handler)` on `window`, `self`, or any EventTarget where the handler body does NOT read `event.origin` (destructured `origin` also counts) and compare it against an expected value before touching `event.data`. Any frame on the page — including an attacker-controlled one in an iframe or a tab that holds a window reference — can send a forged `postMessage` with data that drives the handler's actions.<br><br>**Explicitly NOT a finding** (do NOT flag):<br>- `onmessage` / `addEventListener('message', …)` handlers on a `MessageChannel` **port** (`new MessageChannel().port1` / `.port2`) created in the SAME realm, where neither port is transferred to another realm/document — i.e. NOT a `MessagePort` received via a `window` / `self` `'message'` event's `event.ports`, and neither port is handed to a worker / iframe / `window.postMessage`. A same-realm `MessageChannel` is a closed two-party channel (no external frame can reach it) and `MessagePort` message events carry no security-meaningful `origin` (it's always the empty string), so an origin check is impossible and pointless. Reference safe shape: `_js/nds-loader.js:355-363` (`_yieldChannel` — `port2` is closure-private, used only for `postMessage(null)` as a yield signal; the handler ignores `event.data`).<br>**Still a finding** (do NOT exempt): a `MessagePort` obtained from `event.ports` of a `window` / `self` `'message'` event (that port came from an untrusted sender — validate the originating message's `event.origin` first); any `BroadcastChannel` `onmessage`; a worker / iframe `onmessage` reachable cross-context; or any `MessageChannel` where EITHER port is exposed cross-realm (transferred via `postMessage` or a public NDS API) — once a port leaves the realm the channel is no longer closed, so the handler on the other port is attacker-reachable. | HIGH | Validate origin as the first check in the handler. For a fixed-origin setup: `if (event.origin !== 'https://trusted.example') return;`. For an allowlist: keep a `Set` of allowed origins and early-return on miss. Never compare against `'*'` (no-op), never rely on `event.source` alone (it proves the sender, not the origin), and never skip the check for same-origin-only deployments — same-site subdomains and intermediary frames still cross the origin boundary. |
| JSS-05 | Navigation sinks where the destination is an expression, not a literal: `window.open(x, ...)` / `window.location = x` / `location.href = x` / `location.assign(x)` / `location.replace(x)` / `a.href = x` / `a.setAttribute('href', x)` (the last two when `a` is a newly-created anchor) where `x` is a variable, template literal, or expression whose value at runtime can come from any of these sources: `getAttribute('data-*')`, `dataset.*`, URL query parameters, form-input values, server-response fields, `postMessage` data, caller-supplied component-options object fields (e.g., an `actions: [{ href }]` entry passed to a `create()` API). Without scheme validation, a `data-share-url="javascript:alert(1)"` attribute becomes script execution when the user clicks.<br><br>The rule has two independent concerns. (1) **noopener dimension** — does the new context retain a back-reference to the opener? (2) **scheme dimension** — does the URL parser execute a `javascript:` / `data:` / `vbscript:` payload? Carve-outs apply per dimension; an exemption from one does NOT exempt the other.<br><br>**Explicitly NOT a finding for the noopener dimension** (still subject to the scheme dimension):<br>- `window.open(url, '_blank', featureString)` — when a non-empty feature string is passed, modern browsers default the new context to noopener.<br><br>**Explicitly NOT a finding for either dimension** (do NOT flag):<br>- Navigation to `window.location.href` / `location.pathname` / `location.origin` composed with static paths (e.g., `window.location.origin + '/contact'`). Those values are browser-managed; they can't be injected with `javascript:`.<br>- Values already passed through `encodeURIComponent` for use as query-string parameters (the scheme is whatever the base URL sets, not what the encoded component is).<br>- Values that flow through a same-file scheme-validation helper (`new URL(x, location.href)` checked against an `http:`/`https:`/`mailto:`/`tel:` allowlist) before the navigation sink.<br><br>**Critical**: when `x` traces back to `dataset.*` / `getAttribute('data-*')` / `postMessage.data` / form-input / server-response, the scheme dimension fires regardless of whether the noopener dimension is satisfied by a feature string. Motivating example: `_js/nds-share.js:24-26` calls `window.open(url, '_blank', POPUP_FEATURES)` with `url` from `data-share-url`; noopener is exempt (feature string passed) but scheme is not — `data-share-url="javascript:alert(1)"` flows through and executes. | MEDIUM | Validate the URL's scheme before navigating. Compact form: `try { const u = new URL(x, location.href); if (!['http:', 'https:', 'mailto:', 'tel:'].includes(u.protocol)) return; window.open(u.href, '_blank', 'noopener'); } catch { return; }`. For internal navigation, assert the value begins with `/` or `#` before assigning. For share/copy flows (see `_js/nds-share.js:52–61`), apply the scheme check inside the URL-reading helper so every caller benefits. |
| JSS-06 | **Prototype pollution via JSON-from-writable-store fed into `Object.assign` / object-spread.** A value whose nearest assignment chain reads from a same-origin-writable JSON store — `JSON.parse(localStorage.getItem(...))` / `JSON.parse(sessionStorage.getItem(...))` / `JSON.parse(<cookie-derived>)` / a same-file helper that wraps any of those (e.g. `loadCache(key)` whose body returns `JSON.parse(localStorage.getItem(...))`) — used as a source argument to `Object.assign(target, ..., parsed, ...)` (any source position past the first), or as the source of an object-spread (`{...parsed}`, `{...parsed.settings}`), without a preceding own-key-only filter. Reason: `JSON.parse('{"__proto__":{"polluted":1}}')` produces an own data property named `__proto__`. `Object.assign` (and the spread runtime) calls `[[Set]]` for each source own key, which for `__proto__` triggers `Object.prototype.__proto__`'s setter on the target, replacing the target's `[[Prototype]]` with the attacker-controlled object. Same-origin XSS in any other page on the origin, a malicious browser extension with `storage` access, or a dev-tools paste can plant the poisoned payload; the next page's rehydration takes the prototype assignment.<br><br>Practical impact varies. If the rehydrated object is read only by known-key access (`state.modes`, `state.settings.font-step`) and never spread / iterated via `for...in`, the planted prototype barely changes the component's behavior — the immediate damage is bounded to the rehydrated object. If the object is iterated, spread back into another assign, or used as a key map (`if (key in state)`), the planted properties leak into legitimate logic. Either way, a same-file safe-merge is the cheap defense: it costs one helper call and removes the entire vector.<br><br>**Explicitly NOT a finding** (do NOT flag):<br>- Targets created via `Object.create(null)` — the null prototype means `[[Set]]` for `__proto__` falls through to a regular own data property assignment; the prototype chain is never mutated.<br>- Sources that pass through an own-key filter before the assign — `NDS.i18n.safeMerge(target, parsed)`, an explicit `for (const k of Object.keys(parsed)) { if (k === '__proto__' \|\| k === 'constructor' \|\| k === 'prototype') continue; ... }` loop, or an explicit `delete parsed.__proto__; delete parsed.constructor;` chain immediately preceding the assign.<br>- Annotation exemption: an inline comment within 3 lines above the `Object.assign` call that names the writable store AND why the rehydrated payload is trusted (e.g., `// safe: cache writer at L88 only stores values from TYPED_VALUES`). Vague "// safe" / "// trusted" without a specific rationale do NOT qualify.<br>- Multi-source `Object.assign(target, literal1, parsed, literal2, ...)` where every source past the first that comes from a writable store has independently passed the filter exemption above. The rule fires per untrusted source, not per call.<br>- Sources where the writable-store value never reaches `Object.assign` / spread — e.g., parsed payloads consumed only via known-key reads (`if (parsed.foo === 'bar') ...`) and discarded. The vulnerability requires the assign-or-spread step.<br><br>Motivating example: `_js/nds-accessibility.js:153-155` does `const parsed = JSON.parse(raw)` from `localStorage.getItem(STORAGE_KEY)`, then `Object.assign(defaultState(), parsed, { settings: Object.assign(defaultState().settings, parsed.settings \|\| {}) })`. Both Object.assigns take the parsed JSON as a source past the first position with no key filter. Same shape was hardened in the same file at L907 via `NDS.i18n.safeMerge(A11Y_I18N, data.js)` — the load() rehydrator is the remaining gap. | MEDIUM | Route the parsed source through `NDS.i18n.safeMerge(target, parsed)`. The helper at `_js/nds-core.js:118-125` copies own enumerable keys via `Object.keys` and skips `__proto__` / `constructor` / `prototype` — equivalent to `Object.assign` for legitimate keys, hardened against the prototype-key vector. For the multi-source Object.assign shape, replace each writable-store source individually: `Object.assign(target, literal, NDS.i18n.safeMerge({}, parsed), other)` — pass through a fresh own-keys copy. If the consuming object should never have a non-default prototype, switch the target to `Object.create(null)` for full immunity (works whether or not any source is filtered). The helper currently lives under `NDS.i18n` for historical reasons; if non-i18n callers proliferate, surface a JSD-05 promotion candidate to lift it to top-level `NDS.safeMerge`. |

JSS-01 is the highest-false-positive rule in the catalog — static-analysis can't prove that a component-options value is safe at the caller level. When the codebase legitimately trusts a value (e.g., icon class names resolved from a `TYPE_DEFAULTS` enum in the same file), prefer the annotation-exemption comment over widening the rule's numeric/helper allowlist. If the same "safe by construction" rationale repeats across three or more sites, that is a JSD-05 promotion candidate (the rationale becomes a typed enum or helper).

#### JS Architecture (JSA)

JSA covers per-file design decisions that the per-pattern catalog (JSP/JSD/JSS) cannot generalize — scheduling-primitive choice, unbounded data-structure growth, DOM-API tradeoffs, in-file DRY/KISS, code organization. JSA findings carry plain HIGH/MEDIUM/LOW severity (symmetrical with JSP/JSD/JSS) PLUS a `Type:` field that's unique to this catalog — either `actionable` (concrete change, bounded risk; include a `Fix:` line) or `tradeoff` (a deliberate design choice that no mechanical fix resolves). `fix all` and `fix JSA` apply actionable findings only; tradeoffs are never auto-fixed. But a tradeoff is NOT a dead-end: when a real, higher-leverage way to overcome it exists, the finding carries a `Proposed solution:` line (the lever) plus a `Benefit vs cost:` one-liner, and — when the benefit clearly outweighs the cost — is marked `Type: tradeoff → recommend solving` so the report's Recommended action surfaces it as a focused-refactor candidate (a solution too large for an inline fix can be saved as a plan under `~/.claude/plans/` and linked). Tradeoffs with no better option (or where cost ≫ benefit) carry only `Rationale for accepting:` — never manufacture a `Proposed solution:` to fill the slot.

Mode coverage is split: **greppable** rules run in both full-tree and single-file modes; **deep-read** rules run in single-file mode only (full-tree skips with the mandatory banner described in "JSA deep-read rules in full-tree mode" above). The Mode column documents per-rule coverage.

| ID | What to detect | Severity | Mode | Fix |
|---|---|---|---|---|
| JSA-01 | **Blocking sync loops at module-eval / `init()` over >50 elements without yielding.** A `for` / `forEach` / `for...of` / `for...in` loop body that executes at module-eval time (IIFE top level) OR inside the public `init()` method, where the iterable comes from `document.querySelectorAll` / `getElementsByClassName` / `getElementsByTagName` / `NDS.queryAll(document, ...)`, OR from an instance/closure array whose elements were collected via an **element-scoped** `el.querySelectorAll(...)` at init and looped later (e.g. `this.items = container.querySelectorAll(sel)` in the constructor, then `this.items.forEach(...)` inside an init-reachable method) — the document-vs-element distinction does not change the in-`init()` long-task hazard, so trace the array's assignment in the same file to confirm its source selector, AND no yielding primitive (`NDS.onIdle`, `requestAnimationFrame`, `requestIdleCallback`, `MessageChannel.port.postMessage`) breaks the iteration into batches. Element-count heuristic: flag when the selector's worst-case match across the page exceeds ~50. Known-bounded collections (loop's `.length <= 5` from a documented constraint) are NOT findings.<br><br>**Explicitly NOT an actionable finding — classify as `Type: tradeoff`** (do NOT recommend chunking): an init-reachable full-collection pass whose result must be settled **before first paint** to avoid a visible reflow (e.g. a URL-active filter that must compute its filtered/sorted view synchronously so unfiltered items don't flash then re-hide), where chunking via a yielding primitive would either reintroduce the flash OR break a synchronous post-loop contract (a count / no-results / pagination step that reads the settled set in the same tick). Both conditions required: (1) the loop is reachable at init only on a pre-activated-state path (URL param, server-rendered state), OR its result renders into a **visible control** where deferral would cause pop-in / CLS (e.g. building a visible auto-populated filter's option DOM — hidden/dropmenu controls should still defer), AND (2) a synchronous consumer reads the loop's result later in the same method. Motivating exemplars: `_js/nds-filter.js` `applyFilters()` + `_buildItemCache()` (the pre-activated-state path — the full-item pass runs in `init()` only when URL params pre-activate a filter, `visibleCount` / `updateNoResultsAlert` / `updatePagination` consume the settled set synchronously, the cache is built once + memoized); and `collectFilterValues()` (the visible-control path — a visible auto-populated filter scans all items to build its option DOM, which must paint with the control, while dropmenu-hidden auto-filters defer to first open via `_deferredFilters`). The eager cost is bounded to the must-render-now case.<br><br>**Second sufficient trigger (write-only first-paint visibility render) — also `Type: tradeoff`, do NOT recommend chunking:** an init-reachable pass whose body is a **write-only visibility toggle** (`item.hidden = …`, `el.style.display = …`, or `classList.add/remove/toggle` of a purely display-controlling class) that paints the surface's **final paged/visible first-paint state**, where deferring it via a yielding primitive would render all items visible for a frame and then collapse them — a visible all-items → final flash (CLS). This trigger does **not** require URL pre-activation **nor** a synchronous post-loop consumer (it stands alone), but BOTH hard discriminators are required to qualify: (a) the loop body performs **zero per-iteration layout reads** — no `getBoundingClientRect` / `offset*` / `clientWidth/Height` / `scrollWidth/Height` / `getComputedStyle` inside the loop (a measurement-reading loop is JSA-02/JSD-06 territory, not this exemption), AND (b) the loop produces the **first visible paint** of the surface, so deferral causes a real flash/CLS — not mere bookkeeping. **Stays ACTIONABLE (chunk per Default fix):** loops that do per-iteration measurement, network/IO (`fetch` / storage), heavy DOM construction (`createElement` / `innerHTML` per item), or any non-visibility side effect; and visibility loops whose result is NOT first-paint-visible (off-screen / dropmenu-hidden state that can defer without a flash). Motivating exemplar: `_js/nds-pagination.js` `showPage()` (driven by `paginate()` → `updatePagination()` at critical init) — a write-only `items.forEach(item => item.hidden = …)` over an element-scoped `.nds-page-item` collection (worst case >50) that has no synchronous consumer after it, yet must settle synchronously to render the paged view without an all-items → paginated flash; the grid path paginates synchronously and the table path defers exactly one frame, both for the no-flash rationale documented at `_js/nds-pagination.js:498-518`. The critical cost is bounded to the must-render-now case.<br><br>**Cross-reference:** JSP-10 covers the narrow IIFE-body DOM walk case for loader-registered components. JSA-01 extends to in-`init()` walks AND to non-loader-registered utility modules. | MEDIUM | greppable | **Default fix**: chunk the loop via `NDS.onIdle(() => { ... })` per batch, or yield between batches with `MessageChannel.port.postMessage` (see `_js/nds-loader.js:272-281` for the loader's canonical microtask-yield pattern). If the loop must complete before first paint (rare — typically only FOUT-blocking work qualifies), annotate with a comment naming the constraint.<br><br>**CSS-visible inline-decoration variant**: when each iteration adds a class/attribute that triggers `::after`/`::before { content: ... }` badges, inline-width icons, or any `classList` toggle that causes layout-on-add to inline-flow content, the default chunk-defer fix CLSes every element past the synchronous first batch (the decoration lands one+ frames after first paint and shifts surrounding inline text). Use a **viewport partition** instead: in one tight-loop pass, read `getBoundingClientRect()` for each element and partition by `rect.bottom > 0 && rect.top < window.innerHeight`; sync-tag the visible partition (hits first paint, no CLS); register `NDS.onIntersect(el, fn, { rootMargin: '200px' })` for the off-screen partition so each tags just before scrolling into view (no CLS on any scroll speed; the 200px buffer absorbs typical scroll velocity). The component's loader-registry entry MUST stay `critical: true` so the visible-portion sync work hits first paint — see JSA-05's matching CSS-visible variant. To recognize the variant during detection, grep `_sass/` for the class the loop adds — if the class definition includes `::after { content: ... }`, an icon mask, or any inline-flow-affecting rule, the variant applies. Add a comment above `init()` naming the CSS surface and the partition intent (e.g., `// Viewport-aware: .nds-external triggers a CSS ::after badge — visible-portion sync + onIntersect for off-screen avoids CLS.`). Motivating exemplar: `_js/nds-link.js` (`.nds-external` adds a CSS `::after` badge); migrated 2026-05-29 to the viewport partition + `NDS.onIntersect` shape after a plain chunked-sweep fix caused visible CLS on anchors past the first 100. |
| JSA-02 | **Layout thrashing in straight-line code (outside a loop).** A straight-line sequence of ≥3 alternations between style reads (`offsetHeight`, `offsetTop`, `scrollHeight`, `clientHeight`, `getBoundingClientRect()`, `getComputedStyle(...)`) and style writes (`el.style.* = ...`, `classList.add/remove/toggle`, `setAttribute('class', ...)`, `setAttribute('style', ...)`) in the same function body. Each alternation forces a synchronous layout; the linear-sequence form is invisible to JSD-06 but pays the same per-event cost.<br><br>**Cross-reference:** JSD-06 catches the loop form (N writes × N reads). JSA-02 catches the linear sequence form — write, read, write, read in straight-line code that runs per-event or per-render.<br><br>**Explicitly NOT a finding:** staged-measurement patterns (one write that puts an element into a measurable off-screen state, followed by one read) — the read depends on the write; not thrashing. | MEDIUM | greppable | Batch every read into local vars at the top of the function, then apply all writes after. See `_js/nds-drawer.js:77-99` for the canonical batched pattern. |
| JSA-03 | **Unbounded caches / Sets / Maps without LRU / TTL / disposal.** A `Map`, `Set`, or plain object (`const cache = {};`) used as a cache — written to via `.set(...)` / `.add(...)` / `obj[key] = ...` inside a function called per-event / per-render / per-component-creation — where no `.delete(...)` / `.clear()` / LRU eviction / TTL expiry / max-size guard exists in the same file. The cache grows for the page lifetime; on long-lived SPA navigation or repeated component lifecycles, memory is recovered only on full page unload.<br><br>**Cross-reference:** JSP-04(b)/(c) and JSD-09 catch listener-pool leaks. JSA-03 catches data-structure leaks the per-pattern catalog misses.<br><br>**Explicitly NOT a finding:** `WeakMap` / `WeakSet` containers — GC reclaims unreachable keys; bounded by the element's own lifetime. Caches with a documented page-lifetime intent (e.g., memoized breakpoint values) annotated with a comment naming the intent. Caches whose write site fires at most O(1) times per page load.<br><br>**Sub-shape — no-disposal public subscriber registry (latent-leak / API-consistency, NOT a growth check).** A PUBLIC registration API on an `NDS.*` namespace (a method whose documented purpose is "register a hook / subscriber / callback", including methods returned from a namespace IIFE such as `NDS.State`'s `onAdd` / `onRemove`) that PUSHES a per-call subscriber record onto a module-scoped collection (`arr.push(...)`, `set.add(...)`, `map.set(...)`, or the keyed `(obj[key] \|\| (obj[key] = [])).push(...)` shape) AND returns `undefined` — its reachable return is bare (no `return () => {...}`, no returned token the caller could pass to a removal method) AND no separately-exported method in the same module removes an individual registration from that collection — WHILE at least one SIBLING subscriber-pool registration API in the SAME file DOES return a callable unsubscribe handle (`return () => { ...indexOf/splice/delete/unobserve... }`). The detection signature is the in-file asymmetry, three conjoined parts: (1) the function is reachable as a public `NDS.<Ns>.<verb>` (or `NDS.<verb>`) surface and its net effect is a subscriber-record push onto a module-scoped collection; (2) its reachable return is `undefined` AND no paired removal method exists in the file; (3) somewhere in the SAME file, a peer registrar returns a teardown closure, establishing return-a-handle as the file's own convention that THIS registry breaks. **Unlike the base JSA-03 growth check, this sub-shape fires REGARDLESS of how often the current writer fires** — it is a latent-leak + API-consistency defect, not "the collection is observably growing right now": a future per-instance or `init()`-time caller of the registry would stack subscriber records with no way to release them.<br><br>**Severity: this latent sub-shape is LOW** (the base growth check stays MEDIUM); tag accordingly so the latent finding is never reported at MEDIUM.<br><br>**Reconciliation with the base growth check (complementary, never double-counted).** Base JSA-03 already carves out "Caches whose write site fires at most O(1) times per page load," so a no-disposal registry whose ONLY current caller registers at module-eval correctly silences the growth check — there is no live leak. This sub-shape is the carve-out-reversal: the same registry is STILL flagged here for the structural asymmetry, because the O(1)-per-page exemption is a statement about the present caller, not about the API's safety. The two never double-fire: when the writer IS already per-instance / per-render (the collection is observably growing), report the LIVE leak under the base growth check at MEDIUM and do NOT also emit this sub-shape — the live finding subsumes the latent one. This sub-shape's distinct value is exactly the O(1)-writer registry the growth check waves through.<br><br>**Type framing.** `Type: tradeoff → recommend solving` — there is a real, low-cost lever (mirror the sibling pools: return an unsubscribe closure, or add paired `off*` removal methods) whose benefit (closes a latent leak + restores the file-wide teardown symmetry core's own DOM-bus comment already promises) clearly outweighs the cost (a few lines, no change to existing O(1) call sites). Include `Proposed solution:` and `Benefit vs cost:`. Like every JSA tradeoff finding it is NOT auto-fixed by `fix all` / `fix JSA`; it surfaces in the Phase 4 Recommended action.<br><br>**Explicitly NOT a finding (sub-shape):**<br>- Sibling subscriber pools that ALREADY return a callable unsubscribe handle — they satisfy the contract. In `nds-core.js` this exempts `NDS.onResize` (:221-230), `NDS.onElementResize` (handle closure at :251-254, IIFE block :235-256), `NDS.onIntersect` (:261-287), `NDS.onDOMAdd`/`onDOMRemove`/`onChildrenChange` (:365-382), `NDS.onAttrChange` (:393-415), `NDS.onOutsideScroll` (:792-799).<br>- Ordinary one-shot `addEventListener` / `NDS.on*` CALL sites in component files — those CONSUME a registration API, they do not DEFINE a public registry (call-site listener-pool leaks are JSP-04(b)/(c) / JSD-09 territory, not this sub-shape).<br>- Module-scoped arrays / Maps / Sets that are purely INTERNAL plumbing (no public `NDS.*` registration verb writes to them — e.g. domBus's private `addSubs`/`removeSubs` before they are wrapped, or the private `attrSubs` array). Fires only on a PUBLIC registration surface.<br>- `WeakMap` / `WeakSet`-backed registries keyed by the subscriber's element — GC reclaims the record, so a missing handle does not strand the subscription (same reasoning as base JSA-03's WeakMap/WeakSet carve-out).<br>- Collections the BASE growth check already governs because their writer fires per-event / per-render / per-component-creation — report those as a LIVE JSA-03 leak (MEDIUM); this latent sub-shape does NOT additionally fire (no double-count).<br>- A registry that is genuinely process-lifetime (module-eval registrants only) AND is annotated within 3 lines with a comment naming the deliberate no-teardown contract — e.g. `// Process-lifetime registry — module-eval registrants only; no per-instance callers, no teardown by design.` Mirrors the JSA-04 / JSA-09 / JSA-15 annotation-exemption shape; vague `// no teardown` / `// safe` does NOT qualify.<br>- A LONE asymmetric registry — a file where NO sibling subscriber-pool registrar returns a handle — has no in-file convention to break, so record a Gap observation (possible cross-file JSD-15 persona question), NOT a finding. A deliberate precision-over-recall choice so the rule never manufactures a finding without the same-file anchor. | MEDIUM (base growth) / LOW (latent no-disposal-registry sub-shape) | deep-read | Add an LRU eviction policy (cap by entry count or memory), a TTL (remove entries older than N ms), or an explicit `.clear()` in a teardown path. If the cache is intentionally process-lifetime, annotate with `// Process-lifetime cache — bounded by <reason>`.<br><br>**Sub-shape fix (no-disposal public subscriber registry):** Mirror the file's sibling pools — have the registrar `return` a release closure that removes the pushed record. For `NDS.State.onAdd` / `onRemove`, push a `sub` record and return its remover — `const onAdd = (state, scope, fn) => { const sub = { scope, fn }; const list = (_onAdd[state] \|\| (_onAdd[state] = [])); list.push(sub); return () => { const i = list.indexOf(sub); if (i !== -1) list.splice(i, 1); }; };` (same shape for `onRemove`) — matching the `_js/nds-core.js:365-382` return-a-handle pattern; OR add paired `NDS.State.offAdd(state, fn)` / `offRemove(state, fn)` removal methods. The change is ADDITIVE and backward-compatible: existing module-eval callers that ignore the return value (e.g. `nds-forms.js:14-48`) are unaffected, while any future per-instance / `init()`-time caller gains a teardown path and the in-file pattern becomes uniform. Because the present sole caller registers at module-eval and never needs to unsubscribe, this is `Type: tradeoff → recommend solving`, NOT auto-fixed by `fix all` / `fix JSA`. `Proposed solution:` return a handle from `onAdd`/`onRemove` matching the `:365-382` shape so the registry honors the file-wide convention its own DOM-bus comment (`:289-304`) defends. `Benefit vs cost:` closes a latent per-instance leak and restores uniform teardown across every public `NDS.*` registry vs ~4 lines and zero change to existing O(1) call sites — the handle is NOT a new feature, it makes one outlier conform to core's already-documented contract. If the no-teardown contract is genuinely intentional (registry is module-eval-only by design), annotate within 3 lines instead and the sub-shape is annotation-exempt. |
| JSA-04 | **Scheduling primitive mismatch — `setTimeout(fn, 0)` where a more appropriate primitive exists.** A `setTimeout(fn, 0)` call where the developer's intent is to yield (defer to the next task) rather than to schedule at a specific point. Microtask-level yields are better served by `queueMicrotask(fn)` (faster, no paint between); paint-aligned yields by `requestAnimationFrame(fn)`; macrotask-level yields with no rendering pressure by `MessageChannel.port.postMessage` (see `_js/nds-loader.js:272-281`).<br><br>**Explicitly NOT a finding:** `setTimeout(fn, N)` where `N > 0` (delay, not yield). `setTimeout(fn, 0)` inside an `NDS.debounce` wrapper (debounce semantics). `setTimeout(fn, 0)` annotated with a comment naming why the macrotask-level yield is intentional (e.g., "// Macrotask yield — must paint between this and the next step"). | LOW | greppable | Rewrite per intent — `queueMicrotask(fn)` for "next microtask", `requestAnimationFrame(fn)` for "next paint", `MessageChannel.port.postMessage` for "next macrotask without rendering pressure". The loader's `MessageChannel`-based yielder at `_js/nds-loader.js:272-281` is the canonical exemplar; reuse it rather than re-implementing. |
| JSA-05 | **Loader-registered non-critical chrome marked `critical: true` (should default to deferred).** A `_js/nds-*.js` file registered in `_js/nds-loader.js`'s `COMPONENTS` array **with `critical: true`**, AND the component's role is non-critical chrome (tooltip, popover, autocomplete dropdown, copy-button, share-menu — anything not on the LCP / FCP / above-the-fold critical path). `critical: true` puts a component on the **reveal checklist** — it runs in the time-sliced critical pass before the loader stamps `data-nds-loaded`, gating the reveal. Non-critical chrome should NOT be on the checklist; leaving it unflagged makes it **deferred** (runs in idle slots after the reveal) so the critical budget stays available for what genuinely gates the reveal. (Default flipped 2026-06-01: unflagged = deferred, `critical: true` is the explicit checklist opt-in; the old `idle: true` flag is gone.)<br><br>**Registry-hygiene sub-shape (single-file loader audits only — `/nds-js-audit nds-loader.js architecture`).** When the audited file IS `_js/nds-loader.js`, the cross-file "is this component non-critical chrome" test cannot be answered from a loader-only read (it needs each component's role), so the rule otherwise silently passes on the file that *owns* the registry. The loader-local, observable signal instead: a **`critical: true` `COMPONENTS` entry** whose object literal lacks an inline rationale comment, while sibling critical entries carry one (e.g. Accordion `// Critical: auto-expand state would CLS…`, CooldownButton `// Critical: enforces single-submit…`, Drawer / Toc / Pagination / Filter / Link all defended in-line). Flag each comment-less `critical: true` entry as a **LOW `Type: tradeoff → verify`** registry-hygiene observation — `critical: true` is the expensive opt-in (it gates the reveal), so every checklist member should be justified in-line. Do NOT assert the component must drop `critical` from a loader-only read (some critical components read no layout yet must settle before the reveal to avoid CLS — dropping the wrong one reintroduces a post-reveal flush / CLS; see the critical-tier carve-outs and JSA-01's exemptions). The strongest auto-surfaced candidates are `critical: true` entries whose sibling overlays are unflagged/deferred (e.g. Multiselect critical while Modal / Dropmenu / Tooltip / Alert are deferred). This sub-shape stays greppable (scan the loader for `critical: true` entries lacking a leading `//` comment in the object literal).<br><br>**Cross-reference:** JSP-10 prevents IIFE-body DOM walks in loader-registered components; JSA-05 keeps non-reveal-gating components OFF the critical checklist (deferred) so the critical budget isn't spent where it isn't needed. | MEDIUM | greppable cross-file | **Default fix**: **drop `critical: true`** from the component's COMPONENTS entry in `_js/nds-loader.js` (unflagged defaults to deferred). If the component genuinely gates the reveal (participates in LCP / first paint / first interaction, or would CLS if it settled after the reveal), keep `critical: true` and document the reason with a one-line comment near the registry entry so future audits don't re-flag.<br><br>**CSS-visible inline-decoration variant**: when the component's `init()` adds a class/attribute that triggers `::after`/`::before { content: ... }` badges, inline-width icons, or any `classList` toggle that causes layout-on-add to inline-flow content, plain deferral would CLS every decorated element (the class lands one+ frames after the reveal and shifts surrounding inline text). Keep the registry entry **`critical: true`** AND migrate `init()` to the **viewport-partition pattern** documented under JSA-01's Fix column: sync-tag the visible partition (hits first paint, no CLS); register `NDS.onIntersect(el, fn, { rootMargin: '200px' })` for the off-screen partition (tags before user scrolls there, no CLS on any scroll speed). Detection requires reading the SCSS: grep `_sass/` for the class the component's `init()` adds (e.g., `.nds-external`); if the class definition includes a `::after`/`::before { content: ... }` rule, an icon-mask declaration, or otherwise consumes inline space, the variant applies. Add a rationale comment to the loader entry naming the critical requirement (e.g., `// Critical: tags CSS-visible inline decoration; deferral would CLS visible elements past first paint.`). Motivating exemplar: `_js/nds-link.js` adds `.nds-external` to outbound anchors (CSS `::after` badge); migrated 2026-05-29 to viewport-partition + critical-load after deferral caused visible CLS on every external anchor.<br><br>**Explicitly NOT a finding** (do NOT flag):<br>- Components that participate in LCP / FCP (hero slider, sticky topbar, mainnav skeleton).<br>- Components whose registry entry already carries an explanatory comment defending its `critical: true`.<br><br>**Registry-hygiene sub-shape fix**: emit one LOW `Type: tradeoff → verify` finding per comment-less `critical: true` entry; recommend a per-component `/nds-js-audit nds-<name>.js architecture` run to confirm deferral-safety; then drop `critical: true` from each verified deferral-safe, non-reveal-gating component, and add the missing one-line rationale comment to those that must stay critical. NEVER auto-drop `critical: true` from a loader-only read — confirming deferral-safety is a per-component deep-read. |
| JSA-06 | **Unaborted fetch in superseded contexts.** A `fetch(...)` call inside a debounced handler / input handler / scroll handler / typeahead loop where no `AbortController.signal` is passed AND no prior `AbortController.abort()` is called to cancel any in-flight request. Each new event fires a fresh request before the previous one resolves; under typeahead, the user sees N requests racing where the last response should win but the order isn't guaranteed.<br><br>**Explicitly NOT a finding:** `fetch` calls inside a singleton-bootstrap that fires exactly once per page lifetime. `fetch` calls inside a `Promise.all([...])` batch where every call must complete (no superseding semantics). | MEDIUM | greppable | Create an `AbortController` per debounced cycle, pass `{ signal: ctrl.signal }` to `fetch`, and abort the previous controller before kicking off a new request. See `_js/nds-autocomplete.js:290-312` for the canonical pattern — it ALSO size-caps the response via `Content-Length` to prevent runaway downloads, which is worth copying alongside the abort plumbing. |
| JSA-07 | **DOM API choice — `innerHTML = '<static template>'` where `replaceChildren(...nodes)` is safer/faster.** An `el.innerHTML = '<...>...</...>'` write where the template is a static string with no `${}` substitutions AND replaces structured content (multiple sibling nodes). The browser parses the string into a DocumentFragment internally; building nodes via `document.createElement` + `replaceChildren(...nodes)` skips the parser pass entirely.<br><br>**Explicitly NOT a finding:** single-text-node content (`el.innerHTML = 'hello'`) — that's covered by a separate `textContent = ...` recommendation. Templates with `${}` substitutions — those belong to JSS-01's escape audit. Sites where `innerHTML = ''` is used purely to clear children — `replaceChildren()` with no args is the cleaner form but the perf delta is negligible (LOW-worthy gap observation, not a finding under JSA-07). | LOW | greppable | Construct the nodes via `document.createElement` + `replaceChildren(...nodes)`. Avoids the parser pass and removes the XSS surface entirely (no string to escape). If the template is genuinely static and the perf delta doesn't matter, consider this a `Type: tradeoff` finding — the rule surfaces the choice so the reader understands what was traded. |
| JSA-08 | **Algorithmic complexity — nested loops over DOM collections degrading to O(n²).** A `forEach` / `for` / `for...of` loop over a DOM collection (NodeList, HTMLCollection, or array of elements) whose body contains another `forEach` / `for` / `for...of` over the same or another DOM collection AND each inner iteration does a comparison / attribute read / DOM walk. Worst-case is O(n²); on large lists (>100 elements) the cost compounds visibly. Also covers `.find` / `.some` / `.filter` calls inside hot loops over the same collection where a `Map` lookup would be O(1).<br><br>**Explicitly NOT a finding:** collections of known small fixed size (4 viewport edges, 5 breakpoints). Inner loops over collections of strictly different shape (an outer loop over rows, inner loop over the row's own children — typically O(n×k) where k is small and bounded). | MEDIUM | deep-read | Build an index `Map` once (O(n)) before the outer loop, then look up in the inner loop (O(1)). For `.find` / `.some` patterns, hoist the predicate-keyed Map outside the loop. |
| JSA-09 | **Polyfill / fallback fidelity — fallback secretly defeats the optimization.** A polyfill for a perf-critical primitive (`requestIdleCallback`, `IntersectionObserver`, `ResizeObserver`, Scheduler API, Web Animations API) whose fallback does NOT model the perf characteristic the native API provides — e.g., `requestIdleCallback` polyfilled as `setTimeout(cb, 1)` runs as a near-immediate macrotask, not in actual browser-managed idle time. The fallback silently changes the perf contract; callers that asked for "do this only when the browser is idle" get "do this almost-immediately" on unsupported browsers.<br><br>**Explicitly NOT a finding:** pure-functional polyfills with identical perf characteristics (e.g., a `flat()` polyfill that calls itself recursively). Polyfills annotated within 3 lines with a comment naming the perf-contract divergence so callers understand what they're getting (see `_js/nds-loader.js:285-286` for the canonical pattern). | LOW | deep-read | Add an annotation comment naming the fallback's actual behavior so callers can reason about the contract. If the divergence is large enough that the polyfilled API should NOT be relied on for perf-critical paths, surface as a tradeoff observation rather than a fix candidate. |
| JSA-10 | **Outlier cyclomatic complexity.** A single function in the file whose cyclomatic complexity (sum of `if` / `else if` / `case` / `&&` / `\|\|` / ternary branches inside the body) is 4–5× higher than the median complexity of every other function in the same file. The outlier is typically a state-machine glued together with branches; readers must hold the entire decision tree to understand any one path.<br><br>**Explicitly NOT a finding:** the file's only meaningful function (the cluster size is 1, so "outlier" has no meaning). State-machine functions where the branching is the documented architectural choice (every branch is named via inline comments mapping to a state). | LOW | deep-read | Extract sub-functions per branch family. Replace nested `if` / `else if` chains with lookup maps where the predicate is a stable key. Invert guards to early-return so the happy path stays at the top level. |
| JSA-11 | **In-file repeated block pattern ≥10 lines.** Two or more code blocks within the same file ≥10 lines each that share token structure (same call sequence, same shape, same side effects) but with different identifiers / different ambient values. The blocks would collapse cleanly to a local helper but currently force the reader to compare them visually.<br><br>**Cross-reference:** JSD-05 covers cross-file promotion to core. JSA-11 covers in-file consolidation — the step before a pattern is mature enough for core. If the same shape escalates to 2+ files, JSD-05 catches it in the next full-tree `dry` run. | LOW | deep-read | Extract a local helper (typically `function _helperName(args) { ... }` at the bottom of the file or near peers). If the shape generalizes (no caller-specific surface), surface as a JSD-05 gap observation on the next full-tree `dry` run so other files can adopt the same helper if it's promoted to core. |
| JSA-12 | **KISS / over-engineering.** Three sub-shapes:<br><br>**(a) Single-call-site local helper** — a `function _helper() { ... }` private to the module with exactly one call site in the same file, where inlining the helper's body would not significantly hurt readability of the call site.<br><br>**(b) Single-value option default** — a configurable option (`function open({ animate = true } = {})`) where every caller in the file either passes the default value or omits the option entirely. The configurability is unused.<br><br>**(c) Multi-strategy abstraction with single strategy** — a lookup table / factory dispatch / strategy map where only one strategy is ever populated or invoked. The abstraction adds indirection without adding flexibility.<br><br>**Cross-reference:** Project rule (CLAUDE.md): "Don't add features, refactor, or introduce abstractions beyond what the task requires." JSA-12 catches abstractions that grew past what the codebase still uses.<br><br>**Explicitly NOT a finding:** helpers extracted explicitly for testability (annotated). Helpers whose name materially aids readability of the call site (the name is the documentation). Options that are part of a public API surface where future callers might want the configurability — even if no current caller exercises it. A **listener-teardown `AbortController`** whose signal scopes `addEventListener` calls (PERSONA entry 6 conformance) — even one with no current `.abort()` call — is NEVER an over-engineering / dead-mechanism strip candidate: it is the project's chosen listener idiom plus atomic-teardown insurance, and removing it diverges from PERSONA 6 and the JSP-06 contract that promoted it. See the matching JSA-14 carve-out. | LOW | deep-read | Sub-shape (a): inline the helper; delete its declaration. Sub-shape (b): remove the option, hardcode the value at every call site. Sub-shape (c): delete the unused strategy slots, collapse to a single code path. The reduction makes future readers' jobs easier without losing functionality. |
| JSA-13 | **Mixed-concern function over 80 lines.** A function whose body is >80 lines AND interleaves four+ concerns — render (DOM construction / mutation), state (local variable assignments / instance-field updates), IO (fetch / localStorage / cookie / dataset reads), and event-binding (`addEventListener`, `NDS.on*` calls) — in straight-line code without sub-step section comments or extracted sub-functions. Future edits to one concern carry regression risk in the others because they sit in the same scope.<br><br>**Explicitly NOT a finding:** state-machine functions where the concerns are deliberately interleaved per state (e.g., a setup function whose lifecycle moves through "create DOM → measure → bind handlers" as a contiguous flow that wouldn't simplify under extraction). Annotate the function with a comment naming the deliberate interleaving and JSA-13 ignores it. | MEDIUM | deep-read | Extract concern-specific sub-methods (`_render()`, `_persistState()`, `_fetchPayload()`, `_bindEvents()`) and keep the outer function as a thin orchestrator that calls each in sequence. If the function's length is structural rather than concern-mixing (e.g., a long sequence of similar same-concern operations), section-comment banners (`// ── Render ─────`) are a lighter-weight alternative to extraction. |
| JSA-14 | **Leaner-architecture candidate: same behavior & perf, less code, no new abstraction.** A working code structure (a method, a class section, or a cluster of cooperating functions) that could achieve **observably identical behavior** AND **equivalent-or-better runtime cost** with materially less source — by switching to a better-fit native/idiomatic construct, or by dropping redundant bookkeeping — where the replacement is **net-simpler** (removes concepts, state, or indirection), not merely shorter. The constructive complement to JSA-11 (mechanical in-file dedup) and JSA-12 (subtract unused flexibility): JSA-14 re-shapes code that *works* but is bulkier than its job requires.<br><br>**Three invariants — ALL must hold, or it is a Gap observation, not a finding:**<br>**(1) Behavior-identical** — every branch and edge case (null/empty, RTL/LTR, first/last, reduced-motion, etc.) produces the same DOM mutations, return values, and side-effect ordering as the current code. If concrete equivalence can't be shown → gap, not finding.<br>**(2) Perf-neutral-or-better** — no new forced layout read, no hotter loop, no larger allocation on a per-event / per-frame path, no extra DOM pass. Trading code size for runtime cost is out of scope (route to the relevant JSP / JSA perf rule instead).<br>**(3) Net-simpler, no new abstraction** — the replacement removes more conceptual surface than it adds.<br><br>**Explicitly NOT a finding (do NOT flag):**<br>- Code-golf / clever density that shortens lines while raising cognitive load (chained ternaries, dense one-liners, bit-twiddling in place of a clear branch). Fewer lines ≠ simpler.<br>- Adding a helper / option / parameter / layer / strategy to save lines — that is the over-engineering this rule exists to avoid, and conflicts with CLAUDE.md "don't introduce abstractions beyond what the task requires." Merging two methods by adding a parameter callers don't otherwise need is NOT a finding.<br>- A reduction already covered more specifically by JSA-11 (in-file repeated block), JSA-12 (single-call helper / unused option / dead strategy), JSD-05 (cross-file promotion), or JSD-10 (file-local shadow of a core helper) — route there; JSA-14 does not double-flag.<br>- "I would write this differently" with no concrete named replacement construct. The finding MUST name the specific lever and the approximate lines removed.<br>- A **listener-teardown `AbortController`** (or its `{ signal }` plumbing) that scopes `addEventListener` calls per PERSONA entry 6 — even when it has no current `.abort()` call. "Never aborted yet" does NOT make it redundant bookkeeping: the value is convention-conformance (PERSONA 6, the project's chosen listener idiom) plus atomic-teardown insurance for any future `destroy()`. Stripping it diverges from PERSONA 6 and reverts the JSP-06 fix that typically promoted it. If the project ever decides singleton components should carry no teardown scaffolding, that is a PERSONA-6 revision via Phase 7 EVOLVE — not an ad-hoc JSA-14 strip. (This is the exact misfire that motivated the carve-out: a never-aborted but PERSONA-6-conforming controller in `_js/nds-mainnav.js` `setupInteractions` was wrongly flagged as vestigial.)<br><br>**Recognized levers (extensible via gap observations):**<br>- Hand-rolled imperative loop that is exactly one native call with identical semantics AND perf (a bounded-list `for`-accumulate → `map` / `filter`; a manual min/max scan → `Math.min(...)` / `Math.max(...)` over a small array; an imperative add/remove-class flip → `classList.toggle(name, cond)`).<br>- Redundant parallel bookkeeping — a variable / flag / array / Set kept in sync with a source of truth that is already cheaply queryable (the DOM, an existing instance field, a core pool); delete the mirror and read the source.<br>- Local reimplementation of something an existing `NDS.*` helper or a standard platform API already provides (broader than JSD-10's strict body-equivalence demotion — JSA-14 covers divergent-shape reimplementations too).<br>- Near-identical branches / methods that collapse to a single path *only when the merge subtracts net concepts* (no new option surface introduced).<br><br>This is a deep-read rule: it requires whole-file design comprehension, so it runs in single-file `architecture` only (full-tree skips it with the deep-read banner). | LOW | deep-read | Name the concrete lever (the specific construct / API / structure that replaces the verbose one) and the approximate lines removed, then verify all three invariants before flagging. Most section / class re-shapes are `Type: tradeoff → recommend solving` (include `Proposed solution:` + `Benefit vs cost:`; a large restructure is saved as a plan under `~/.claude/plans/` and linked). A small, contained single-construct swap (e.g. one loop → one native call) is `Type: actionable` with a direct `Fix:` line. Elevate severity to MEDIUM when the reduction also removes a real maintenance hazard (a bookkeeping mirror that can drift out of sync, a reimplementation that won't get core's future bug fixes). |
| JSA-15 | **Cross-method scroll/animation behavior-keyword fidelity.** Two or more sibling methods in the same component that drive `Element.scrollTo` / `scrollBy` / `scrollIntoView` / `Element.animate` (or equivalent) AND pass DIFFERENT, non-uniform `behavior` / easing keyword sets — e.g. one method passes `behavior: 'smooth'` only while another branches `behavior: cond ? 'instant' : 'smooth'` — where at least one path relies on a keyword NEWER than the broadly-supported `'auto'` / `'smooth'` set (notably `'instant'`, added to the CSSOM-View scroll options later than `'auto'`/`'smooth'`) that silently no-ops to the element's CSS `scroll-behavior` on older engines instead of throwing. The divergence is the smell: the two methods scroll the same element with different cross-engine guarantees, so behavior drifts by which method the caller reached for. Complements JSA-09 (a polyfill fallback you wrote secretly defeating the optimization) — JSA-15 covers a platform keyword you *rely on* that degrades silently AND inconsistently across sibling call paths.<br><br>**Explicitly NOT a finding** (do NOT flag):<br>- Both methods route through ONE shared internal scroll primitive — the keyword choice has a single home, uniform by construction.<br>- Uniform keyword sets across the sibling methods (both use the same `behavior` values), even if one value is newer — uniform degradation is a project-wide choice, not per-method drift.<br>- A divergence preceded within 3 lines by a comment naming the compat tradeoff (e.g. `// 'instant' degrades to CSS scroll-behavior on older WebKit — acceptable for the non-animated jump`). Mirrors the JSA-09 / JSS-01 annotation-exemption shape.<br>- Methods that pass no `behavior` keyword at all (rely entirely on CSS `scroll-behavior`) — no JS-level keyword to drift. | LOW | deep-read | Normalize both methods to a shared, broadly-supported keyword set, OR route both through one internal scroll helper so the keyword choice has a single source of truth. If a newer keyword (`'instant'`) is genuinely needed for one path, keep it but add the annotation comment naming the older-engine degradation so the divergence is documented, not accidental. Motivating shape (the bad pattern this rule catches): a `slideTo()` that passes `behavior: animate ? 'smooth' : 'instant'` while a sibling `goTo()` uses `behavior: 'smooth'` only — the `'instant'` path silently falls back to CSS `scroll-behavior` on older WebKit, so the non-animated jump behaves differently across engines than the smooth path. NOTE: `_js/nds-swiper.js` originally exhibited exactly this at `slideTo()`/`goTo()` (the case that motivated this rule); it was remediated in commit `03ee00d` — the instant path now passes NO `behavior` keyword and toggles inline `scroll-behavior: auto` around the `scrollBy`, with a comment naming the older-engine degradation. Treat that remediation as the reference good shape, not a live finding (the file is now JSA-15-clean and annotation-exempt). |
| JSA-16 | **Lone `requestAnimationFrame` gating a module's entire init — starves in hidden documents.** A `_js/nds-*.js` module whose ENTIRE initialization / orchestration is scheduled inside a single `requestAnimationFrame(cb)` callback — the rAF is the sole entry into the module's detection / setup / first render / lifecycle wiring — AND no `setTimeout` (or other non-rAF macrotask) fallback races it in the same kickoff path. Browsers do NOT invoke rAF callbacks while a document is hidden (they queue until the page next becomes visible), so a page loaded in a background tab (middle-click / "open in new tab") never runs that init until the user focuses the tab: components stay unwired, programmatic API calls made before focus fail, and any critical-CSS skeleton → real-component handoff performed inside the rAF is deferred into a visible flash precisely when the user arrives. Detection: the module's kickoff path (module-eval top level, the public `init()`, or an auto-init handler) wraps ALL of its init work in one `requestAnimationFrame(...)` with no sibling `setTimeout(sameWork, N)` racing it.<br><br>**Explicitly NOT a finding** (do NOT flag):<br>- rAF that defers a SUB-STEP, not the whole init — the module's init runs synchronously (or via the loader) and the rAF only delays one piece (an animation-suppression class stamp, a single deferred `paginate()` / `render()`), ESPECIALLY when a synchronous alternative path also exists (`if (cond) requestAnimationFrame(step); else step();`). Motivating non-findings: `_js/nds-accordion.js:296` (defers only the `data-nds-accordion-initialized` stamp), `_js/nds-pagination.js:452` (table path defers `paginate` one frame; the grid path runs it synchronously).<br>- rAF used for animation (`_js/nds-numbers.js`, `_js/nds-tabs.js`, `_js/nds-mainnav.js` smooth-scroll step), transition-start staging (single or double rAF before a style/state change — `_js/nds-modal.js:54-55`, `_js/nds-dropmenu.js:599`, `_js/nds-mainnav.js:206`), or layout measurement (`_js/nds-mainnav.js:246`) — animation/measurement work SHOULD pause in hidden tabs.<br>- `NDS.rafThrottle` / single-in-flight rAF scroll throttles (`_js/nds-core.js:200`, `_js/nds-tables.js:341`) — event-handler rate limiters, not init.<br>- rAF scheduled from an event handler / observer callback / post-init method (`_js/nds-mainnav.js:423,490,869,958`, `_js/nds-swiper.js:200`) — not the init gate.<br>- An rAF-driven settle/poll loop that ALREADY pairs a `setTimeout` fallback (`_js/nds-sidemenu.js:66-69` — `requestAnimationFrame(checkSettled)` + `setTimeout(doLock, 500)`); the fallback is exactly the fix this rule asks for.<br>- Annotation exemption: a comment within 3 lines naming why the rAF-only gate is intentional (e.g. `// rAF-only: purely visual init; deferring in hidden tabs is desired`). Vague `// next frame` / `// raf` do NOT qualify. Mirrors the JSA-09 / JSA-15 / JSS-01 annotation-exemption shape.<br><br>**Cross-reference:** JSA-04 covers `setTimeout(fn, 0)` scheduling-primitive choice; JSA-01 covers must-settle-before-first-paint loops. JSA-16 is narrower — hidden-document starvation of a whole-module init gated solely by rAF. | MEDIUM | greppable | Two resolutions, chosen by what the rAF actually guards.<br><br>**(A) The rAF guards a paint the user sees** (content is visible at first paint and the rAF commits a frame before mutating it): keep it, but **race** a `setTimeout` fallback behind a one-shot latch so a foreground tab keeps the "wait one frame" behavior (rAF wins; the timer no-ops) while a hidden tab still initializes (the timer fires; rAF is paused): `let _kicked = false; const kick = () => { if (_kicked) return; _kicked = true; /* existing rAF body */ }; requestAnimationFrame(kick); setTimeout(kick, 100);` (pick a fallback delay long enough that rAF reliably wins in a contended foreground tab). Reference idiom in-tree: `_js/nds-sidemenu.js:66-69`.<br><br>**(B) The rAF guards nothing visible** (the content is CSS-hidden behind a skeleton until init completes and reveals it, so "commit a frame before touching the DOM" protects a paint that's hidden anyway): **remove the rAF entirely** and run init directly off the existing macrotask kickoff — simpler, reveals a frame sooner, and the hidden-tab starvation disappears with the rAF. This is the cleaner resolution when it applies.<br><br>Motivating finding — now **remediated** via (B): `_js/nds-loader.js` formerly gated detection + the critical pass + the idle drain + the `data-nds-loaded` skeleton stamp behind one rAF; the rAF was removed because the page stays skeleton-hidden until the critical pass stamps the reveal, so there was no visible paint to protect. Treat that as the reference good shape, not a live finding (the loader is now JSA-16-clean). Instances are `Type: actionable` when the mechanical race (A) applies. |
| JSA-17 | **Heavy synchronous work on the interaction handler path (INP processing time).** A function attached as the listener for a user-interaction event — `click`, `input`, `change`, `keydown`, `keyup`, `pointerdown` / `pointerup`, `submit` (delegated handlers that branch on `e.target` count) — whose synchronous body, before it returns or yields, does at least one of: **(a)** iterates a DOM collection whose worst-case match exceeds ~50 (`forEach` / `for` / `for...of` over `document.querySelectorAll` / `getElementsBy*` / `NDS.queryAll(document, ...)`, or an instance / closure array traced in the same file to such a selector) doing per-item DOM mutation, attribute write, measurement, or comparison; **(b)** constructs DOM per-item via `createElement` / `innerHTML` / `insertAdjacentHTML` inside such a loop; or **(c)** runs an unbounded `sort` / `filter` / `reduce` over such a collection — AND no yield / deferral breaks the work off the interaction tick within the handler's traced call tree: none of `scheduler.yield()`, `scheduler.postTask`, an `await` of a real async boundary, `requestAnimationFrame`, `NDS.onIdle` / `requestIdleCallback`, or `MessageChannel.port.postMessage` (the loader's microtask-yield at `_js/nds-loader.js:272-281`), and the handler is not itself a `NDS.debounce` wrapper.<br><br>Why it hurts INP: the handler's synchronous run time IS the processing-time component of Interaction to Next Paint; a >50-item DOM pass or per-item construction on the interaction tick blocks the next paint and pushes the interaction past the 200ms "good" INP threshold.<br><br>**Explicitly NOT a finding** (do NOT flag):<br>- The handler is itself wrapped by `NDS.debounce(fn, ms)` at the attach site (`el.addEventListener('input', NDS.debounce(handler, 150))`) — the heavy work already runs once after input settles, off the per-keystroke tick. Detection MUST trace the attach site, not just the handler body.<br>- The collection is known-bounded ≤50 by a documented constraint (a fixed tab set, ≤N pagination controls).<br>- The heavy branch sits behind a one-shot guard that fires at most once (`if (this._built) return; this._built = true;` first-open lazy build) — the cost is paid once, not per interaction. Mirrors JSA-01's memoized-cache carve-out.<br>- The loop body is a **write-only visibility toggle** (`item.hidden = …`, `el.style.display = …`, display-only `classList` flip) that MUST settle synchronously to avoid an all-items → final flash on the same interaction — classify `Type: tradeoff`, do NOT recommend chunking (same rationale as JSA-01's write-only first-paint trigger; rarer on the interaction tick but possible, e.g. a filter re-applied on a control click). Requires zero per-iteration layout reads in the loop, exactly as JSA-01's write-only trigger does.<br><br>**Cross-reference:** JSA-01 covers the same >50-loop hazard at module-eval / `init()`; JSA-17 covers it on the post-init, INP-scored interaction tick. JSP-07 converts an *existing* trailing-edge debounce shape; JSA-17 flags heavy interaction work that has *no* debounce or yield at all. JSA-02 / JSD-06 cover layout-thrash specifically; JSA-17 covers heavy compute / per-item construction regardless of layout reads. | MEDIUM | deep-read | For input-driven recompute, debounce the handler — `el.addEventListener('input', NDS.debounce(fn, ms))` — so the heavy pass runs once after typing settles. For click-driven heavy passes, yield-to-main between batches with `MessageChannel.port.postMessage` (`_js/nds-loader.js:272-281`) or `NDS.onIdle` per batch and render incrementally, OR move the heavy compute behind a one-shot lazy build so it runs once rather than per interaction. Most instances are `Type: actionable`; the write-only-visibility-flash carve-out is `Type: tradeoff` (synchronous settle is required to avoid the flash — leave it, document the no-flash rationale inline). |
| JSA-18 | **JS re-implements behavior CSS expresses natively (wrong-layer UI logic).** A `_js/nds-*.js` component whose JS performs UI work a native CSS/HTML primitive expresses directly — measuring boxes then writing layout, toggling classes/attributes off `scroll`/`resize`/hover to drive a *visual* effect, or forking on viewport/motion conditions read from the environment. The cheapest UI work is the work the browser does in CSS: off the main thread, no forced reflow, correct in hidden tabs — so JS that re-implements it loses on BOTH perf (a forced read/write + a listener + a state mirror per event) AND code size. Recognized "CSS subsumes this" patterns (extensible via gap observations), each with its support floor:<br>- **Measure-then-animate-height** — JS reads `scrollHeight` / `getBoundingClientRect` and writes `style.height` / `maxHeight` to animate expand/collapse → CSS `grid-template-rows: 0fr → 1fr` (broad support), or `interpolate-size: allow-keywords` / `calc-size()` (Chromial 2024+).<br>- **Scroll-position class toggling** — a `scroll` handler adding/removing a class purely to drive a visual (reveal, parallax, progress, scroll-shadow) → CSS scroll-driven animations `animation-timeline: scroll()` / `view()` (Chromial 2024+; Firefox/Safari lagging — gate on baseline).<br>- **Sticky / affix tracking** — JS toggling a "stuck"/"pinned" class from scroll offset → `position: sticky` (+ an `IntersectionObserver` sentinel ONLY if a JS state hook is genuinely needed).<br>- **Viewport-condition forking** — JS reading `innerWidth` / `matchMedia` to branch *layout* → `@media` / `@container` queries (pairs with JSD-03's token rule).<br>- **Reduced-motion forking** — JS reading `prefers-reduced-motion` to fork *purely visual* animation values → `@media (prefers-reduced-motion: reduce)` in CSS; JS keeps only cases where it must skip *logic*, not just animation.<br>- **Derived sizing** — JS computing one element's width/height from siblings' measured boxes and writing it back → `flex` / `grid` intrinsic sizing + `min-width: 0` / `calc()`.<br><br>**Type framing.** ALWAYS `Type: tradeoff → recommend solving` — NEVER `actionable`. The lever (the CSS primitive) lives in a layer this skill deliberately does NOT edit (SCSS coverage was removed — see Non-goals), so JSA-18 NAMES the CSS primitive + its support floor in a `Proposed solution:` and routes the actual change to a manual `_sass/` pass (a large migration saved as a plan under `~/.claude/plans/` and linked). `fix all` / `fix JSA` NEVER auto-apply it. Include `Proposed solution:` (named primitive + support floor) and `Benefit vs cost:` (the JS the move deletes — measurement, listener, state mirror — vs the support/rewrite cost).<br><br>**Explicitly NOT a finding** (do NOT flag):<br>- **JS is genuinely load-bearing** — behavior CSS cannot express today: overflow *detection* (CSS has no "is this overflowing" selector), focus trapping, measurement feeding *non-visual* logic (counts, pagination math), scroll position read into JS state consumed elsewhere. Motivating non-finding: `_js/nds-mainnav.js` `overflow.check()` (L434-510) must measure `scrollWidth` / `clientWidth` in JS — the downstream `has-more` class IS CSS-driven once set, but the *detection* is irreducibly JS.<br>- **The CSS primitive's support floor is below the project baseline** — if the named primitive isn't supported across target browsers, the JS is the correct fallback. The rule MUST name the floor; if it sits above baseline, record a Gap observation (revisit when support lands), not a finding.<br>- **The only CSS route needs `:has()` on a critical state path** — the project restricts `:has()` to last-resort, non-critical use; JSA-18 MUST NOT push critical-path `:has()`. If that's the only route, the JS stays.<br>- **JS that orchestrates CSS** (flips a state token CSS then reacts to) — that IS the correct division of labor (JS owns state, CSS owns the visual). JSA-18 targets JS doing the *visual/layout work itself*, not JS stamping a `data-state` token. Motivating non-finding: the `animate` system in `nds-mainnav.js` stamps `open` / `opening` / `closing` and lets CSS run the transition.<br>- **Annotation exemption** — a comment within 3 lines naming why the JS layer is deliberate despite a CSS alternative (e.g. `// JS height-animate: grid 0fr/1fr drops the measured-height easing the product wants`). Vague `// needed` does NOT qualify. Mirrors the JSA-09 / JSA-15 / JSA-16 shape.<br><br>**Cross-reference:** JSD-03 (hardcoded breakpoints → tokens/queries) and JSD-13 (timing → `--nds-transition-speed`) are the narrow, greppable, already-codified slices of this lens; JSA-18 is the broad deep-read complement. JSA-14 covers leaner *JS* re-shapes; JSA-18 covers moving the work *out of JS entirely*. | LOW | deep-read | Name the CSS primitive AND its browser-support floor; frame as `Type: tradeoff → recommend solving` with `Proposed solution:` + `Benefit vs cost:`. NEVER auto-edit SCSS (the skill dropped SCSS coverage) — route the change to a manual pass against the `_sass/` conventions in CLAUDE.md, or save a larger migration as a plan under `~/.claude/plans/` and link it. Verify the support floor AND the no-critical-`:has()` constraint before recommending. If CSS cannot fully subsume the JS (detection + non-visual consumers remain), keep the JS and record a Gap observation instead. Elevate to MEDIUM when the JS being subsumed is a measured forced-reflow on a hot (per-event / per-frame) path — moving it to CSS removes a real INP cost, not just code. |

JSA findings carry the unique `Type:` field — `actionable` or `tradeoff`. Architecture observations frequently surface deliberate design choices the report should acknowledge without forcing a fix; `Type: tradeoff` is the explicit acknowledgment, and Phase 5 batches skip every tradeoff finding regardless of severity. When in doubt, prefer `Type: tradeoff` and explain what was traded — the user can re-classify if they disagree.

**Don't let `tradeoff` become a graveyard.** Prefer `tradeoff` over inventing a fix, but for every tradeoff ask: is there a real, high-leverage way to overcome it? If yes, name it in a `Proposed solution:` line and weigh it in `Benefit vs cost:` — then let the user decide; do not silently bury a solvable cost under "accepted." If the solution's benefit clearly outweighs its cost, mark the finding `Type: tradeoff → recommend solving` and call it out in the Phase 4 Recommended action. A solution too large to apply inline (e.g. a component-wide refactor) should be captured as a plan under `~/.claude/plans/` and linked from the finding, so it isn't lost. Only when there is genuinely no better option, or the cost dwarfs the benefit, does a tradeoff stay a plain `Rationale for accepting:` with no proposed solution.

**Mode coverage discipline.** Greppable JSA rules (JSA-01, JSA-02, JSA-04, JSA-05, JSA-06, JSA-07, JSA-16) run in both full-tree and single-file modes. Deep-read JSA rules (JSA-03, JSA-08, JSA-09, JSA-10, JSA-11, JSA-12, JSA-13, JSA-14, JSA-15, JSA-17, JSA-18) run in single-file mode only — full-tree runs skip them and MUST print the banner from the "JSA deep-read rules in full-tree mode" section above. Silent omission is forbidden; the banner is the report's honest disclosure of what wasn't covered.

JSA-10, JSA-11, JSA-12, JSA-14 have the highest false-positive risk in JSA. Skip the finding if a sub-shape match has a defensible rationale in context (e.g., an outlier-complexity function whose branching IS the documented architectural choice; a "single-strategy abstraction" whose other strategies are deliberately reserved for an upcoming feature; a JSA-14 "leaner" rewrite that can't prove all three invariants, or whose verbosity is load-bearing clarity the reader needs). JSA-14 in particular must NEVER reward code-golf or a new abstraction — when its three invariants (behavior-identical, perf-neutral-or-better, net-simpler-no-new-abstraction) can't all be shown concretely, record a Gap observation, not a finding. When in doubt, record as a Gap observation rather than a finding — JSA earns its keep by sharpening over time, not by padding the report. Two-to-five JSA observations is the typical single-file output; ten+ is a sign of padding.

### Single-file deep-read pass (dedicated agent)

**Single-file mode only.** After the inline catalog pass completes, spawn ONE dedicated deep-read agent (`Agent` tool, `general-purpose` subagent) whose sole job is to re-read the target file end-to-end with fresh context and hunt the rules that reward undivided attention. This is what makes single-file the deepest mode — the inline pass owns the fast greppable rules, the agent owns the architectural deep reads. Full-tree mode does NOT spawn this agent: its budget is committed to breadth, and deep-read rules are banner-skipped there.

**Deep-read scope for the agent:** JSA-03, JSA-08, JSA-09, JSA-10, JSA-11, JSA-12, JSA-13, JSA-14, JSA-15, JSA-17, JSA-18; JSD-14 sub-shapes (a) deep nesting, (b) redundant guards, (d) dead code, (e) unused locals; JSD-16 (frozen disabled-flag dead code); and the context-dependent confirmations for JSS-01 (innerHTML XSS vector) and JSS-04 (postMessage origin allowlist). Restrict the agent to the rule group(s) in scope for this run — on a single-group run with no deep-read rules (e.g. `performance`), skip the agent entirely; on `architecture`, `dry`, `security`, or `all`, run the in-scope deep-read subset.

**Brief template** (self-contained — the agent has no audit context):
1. **Target** — the resolved `_js/nds-<file>.js` path. "Read it top-to-bottom before judging; pattern-matching without the full read misses context."
2. **Rules to apply** — paste the in-scope subset of the deep-read scope above, each with its one-line detection signature and fix (copy from the Phase 3 catalog rows). Include the JSA-14 three-invariant bar and the "record a Gap, not a finding, when in doubt" discipline verbatim.
3. **Reference anchors** — the Phase 2 good-pattern exemplars (`_js/nds-scroll-more.js`, `_js/nds-drawer.js`, `_js/nds-loader.js:272-281`, `_js/nds-autocomplete.js:290-312`) so fixes cite a real pattern.
4. **Output contract** — return ONLY a findings table: `| Sev | Rule | Location (file:line) | Finding → one-line fix | Type (JSA: actionable/tradeoff) |`, plus a short "Gaps observed" list for shapes no rule covers. No prose, no fabricated findings; two-to-five JSA observations is typical, ten+ signals padding.

**Merge.** Fold the agent's findings into the Phase 4 report, deduped against the inline pass (inline keeps greppable hits; the agent owns the deep-read rows). Tag agent-sourced rows so the merge is auditable (a trailing `(deep-read agent)` marker on the location cell). Apply the same Type/tradeoff and Gap-vs-finding discipline as the inline pass. If the agent returns nothing, say so — a clean deep-read pass is a real result.

---

## Phase 4: REPORT

**STOP. Emit the report. Do not edit any files yet.**

**Skip-banner rule (governs all four banners below).** Show a banner only on the run mode where its rule was actually skipped — JSD-14, JSD-16, and the JSA deep-read rules → full-tree only; JSD-05 → single-file `dry` only. Omit it on any run where the rule was in scope and ran. When a rule WAS skipped, its banner is mandatory — never silently omit it; it converts a misleading "clean" into honest coverage disclosure.

Use this structure verbatim:

```
# Code Audit — {target} ({mode})

**{X HIGH / Y MED / Z LOW, or "Clean"}  →  Recommended: {action}** ({≤12-word why})

## Summary
- Files scanned: N
- Findings: X HIGH / Y MEDIUM / Z LOW
- Rule groups hit: JSP, JSD, JSA (list every group whose rules fired; omit groups with zero findings)

> **JSD-05 (cross-tree promotion candidates) was skipped in single-file mode.**
> To surface helpers in `<target>` that duplicate code in other files, periodically
> run `/nds-js-audit js dry` against the full tree.

> **JSD-14 sub-shapes (a)(b)(d)(e) were not scanned in full-tree mode.**
> Only sub-shape (c) (nested ternary chains ≥ 3 deep) is grep-detectable across
> the tree. Deep nesting, redundant guards, dead code, and unused locals
> require per-file deep reads that exceed the full-tree token budget. To
> surface those sub-shapes in a specific component, run
> `/nds-js-audit nds-<file>.js dry`.

> **JSD-16 (frozen disabled feature flag dead code) was not scanned in full-tree mode.**
> JSD-16 requires whole-file scope analysis (confirming a module-scope const is
> never reassigned anywhere in the file) that is not greppable with a single
> signature. To surface frozen-flag dead code in a specific component, run
> `/nds-js-audit nds-<file>.js dry`.

> **JSA deep-read rules (JSA-03, JSA-08, JSA-09, JSA-10, JSA-11, JSA-12, JSA-13, JSA-14, JSA-15, JSA-17, JSA-18)
> were not scanned in full-tree mode.**
> These rules require per-file analysis (cyclomatic outlier detection, in-file
> repeated-block matching, mixed-concern function structure, unbounded-collection
> scope tracing, leaner-architecture re-shaping, interaction-handler INP-cost tracing) that exceeds the full-tree token budget. To surface those in a
> specific component, run `/nds-js-audit nds-<file>.js architecture`.

## HIGH
### _js/nds-alert.js
- L219 [JSP-01] Raw window scroll listener
  Fix: Replace with RAF-throttled handler; store unsubscribe. See _js/nds-scroll-more.js:140-157.

### _js/nds-accordion.js
- L42 [JSD-01] dataset.state assignment clobbers other state tokens
  Fix: Use NDS.State.set/add/clear instead.

## MEDIUM
### _js/nds-foo.js
- L42 [JSA-13] Mixed-concern function over 150 lines interleaves render, state, IO, event-binding
  Type: actionable
  Why it matters: Future edits to one concern carry regression risk in the others.
  Fix: Extract `_render()`, `_persistState()`, `_fetchPayload()`, `_bindEvents()` and keep the outer function as a thin orchestrator.

- L120 [JSA-09] `requestIdleCallback` polyfill runs idle work as a near-immediate macrotask on Safari <18
  Type: tradeoff
  Why it matters: Idle-tier components don't actually defer on older Safari; they run via `setTimeout(1)` with a fake 50ms deadline.
  Proposed solution: replace the `setTimeout(1)` fallback with a `MessageChannel`-based idle shim that estimates a real deadline (pattern at `_js/nds-loader.js:272-281`).
  Benefit vs cost: LOW benefit — Safari 18 (Sept 2024) ships native rIC, so this only touches shrinking older-Safari traffic, and the shim adds maintenance surface. Cost > benefit → stay put.
  Rationale for accepting: older-Safari degradation is the documented constraint; the polyfill degrades quietly there.

- L42 [JSA-13] Mixed-concern function over 180 lines wires every per-element listener inline
  Type: tradeoff → recommend solving
  Why it matters: ~10 listeners attached per element scale O(n) and the function is hard to edit safely.
  Proposed solution: event-delegation — one document-level handler set resolving `e.target.closest('.nds-foo')`, installed once; per-element init shrinks to state-sync.
  Benefit vs cost: HIGH benefit — O(n×10) listeners → O(1), faster init, dissolves the long-function smell; cost is a careful refactor of a critical component. Worth solving → saved as a plan and linked.
  Rationale for accepting: only until the refactor lands; tracked at `~/.claude/plans/<name>.md`.

(JSA findings always carry a `Type:` field — `actionable` (include a `Fix:` line) or
`tradeoff`. A tradeoff includes `Rationale for accepting:`, and — when a real higher-leverage
fix exists — also `Proposed solution:` + `Benefit vs cost:`; if the benefit clearly wins it's
marked `tradeoff → recommend solving`. JSP / JSD / JSS findings do NOT use the Type field;
only catalog findings from JSA do. `fix all` and `fix JSA` apply actionable findings only;
tradeoffs (including `→ recommend solving`) are never auto-fixed — they're surfaced for the
user to decide, and a large solution may be saved as a plan and linked.)

## LOW
(same grouping)

## Promotion candidates (JSD-05)
- NDS.trapFocus(el, opts)
  Sites: _js/nds-modal.js:42-88, _js/nds-drawer.js:131-177
  Rationale: both files implement focus containment with near-identical logic; a shared helper would remove ~90 lines of duplication and let future components opt in trivially.

## Gaps observed (no rule matched)
- {file-a}:{line} — {short description of the pattern that looks wrong}. The same shape also appears at {file-b}:{line} and {file-c}:{line}. No existing rule covers it. Proposed: new rule {group}-{next-id} "{short rule name}".
- {file}:{line} — {different gap}. Proposed: {extension of existing rule or brand-new rule}.

## Dead-rule candidates (rule matched but fix did not apply)
- {rule-id} — N matches in {file(s)}, all skipped because {specific reason the recommended fix does not apply in context}. Proposed: {narrow the detection / add an allowlist / delete the rule}.
- {rule-id} — N matches where {different context the rule did not account for}. Proposed: {concrete refinement}.

## Next Step

**Recommended: {action}** — {one-line reason tied to findings}

Other options:

1. `fix all` — apply every catalog finding marked `Type: actionable` (for JSA findings) plus every JSP/JSD/JSS finding in the report. Excludes JSD-05 promotion candidates (those go through `promote <api-name>` with per-candidate approval) and every JSA finding marked `Type: tradeoff` (those are acknowledged, not auto-fixed) — tradeoffs are skipped regardless of severity.
2. `promote <api-name>` — apply one JSD-05 promotion: edit `_js/nds-core.js` to add the proposed helper, migrate every call site listed in the report, and follow the file-by-file rhythm (rebundle + agent review + pause between files). Only offered when the report has "Promotion candidates (JSD-05)" entries.
3. `save` — persist this report to `.claude/audit-reports/` (see "Saving the report" subsection below).
4. `skip` — end the audit here; nothing is written, nothing is changed.

(Catalog/persona evolution is no longer a numbered choice — Phase 7 auto-applies any concrete Gap / Dead-rule / Persona-drift refinement at run end and appends a `## Catalog evolved` block to the report. The user reverts via git if they disagree.)

Finer-grained fix filters (type the literal, no number):
- `fix HIGH` / `fix MEDIUM` / `fix LOW` — catalog findings by severity (JSA findings of that severity are included only when `Type: actionable`)
- `fix JSP` / `fix JSD` / `fix JSS` / `fix JSA` — by rule group (`fix JSA` applies every JSA actionable finding regardless of severity; tradeoff findings always skipped)
- `fix <file>` (e.g., `fix _js/nds-alert.js`) — one file only (includes every actionable finding in that file across all four rule groups)
- `fix all then promote <api-name>` — chain a plain fix batch and a promotion application in one session (each still follows its own file-by-file rhythm and user pauses)
```

When emitting the Next Step block at the end of a real report, include only options available for THIS run: omit `promote <api-name>` when there are no JSD-05 promotion candidates; omit `fix all` on a clean run. Renumber so the user never sees a no-op choice.

Follow **Numbered-reply discipline** (see Response style): every available primary action gets a number including the recommended one, renumber to drop no-op options, and literal filter commands (`fix HIGH`, `fix _js/nds-foo.js`) sit below the numbered block.

### Computing the Recommended action

Recommendation is `save` in essentially every state — evolution auto-applies and fixes need an explicit go. Pick the matching reason by walking top-to-bottom and stopping at the first match:

| Run state | Recommended | One-line reason template |
|---|---|---|
| Phase 7 auto-applied catalog/persona refinements this run | `save` | "persists the report and the `## Catalog evolved` record of the {N} refinement(s) auto-applied this run" |
| Has "Promotion candidates (JSD-05)" entries | `save` | "persists the snapshot; reply `promote <api>` next turn to apply the {N} promotion(s)" |
| Has catalog findings with ≥1 `Type: actionable` (JSA) or any JSP / JSD / JSS finding | `save` | "persists the report; reply `fix all` next turn to apply the {N} actionable finding(s) file-by-file" |
| Has a `tradeoff → recommend solving` finding (any severity) | `save` | "persists the report; the {N} `recommend solving` tradeoff(s) have a high-leverage `Proposed solution:` — reply to apply it inline, or `save the plan` if it's a larger refactor" |
| JSA tradeoff-only findings (all plain-accepted), no other findings | `save` | "persists the report; the {N} JSA tradeoff finding(s) document deliberate design choices — no action needed unless you want to reconsider a tradeoff" |
| Clean run — zero findings | `save` | "keeps the diff trail. Even clean runs are worth saving — Run N+1's diff vs. this one shows whether the codebase stayed clean" |

Phrase the reason in plain language tied to the actual numbers from THIS run so the user can read the recommendation without scrolling back through the report.

### Saving the report (optional, user-approved)

When the user replies `save` — alone, or combined with another action like `fix HIGH then save` — write the current report verbatim to `.claude/audit-reports/` under one of these patterns:

**Full-tree filename:** `YYYY-MM-DD-nds-js-{category}-audit-run-N.md`
**Single-file filename:** `YYYY-MM-DD-nds-js-{component}-{category}-audit-run-N.md`

- `YYYY-MM-DD` is today's date (the run date, not the save date — they're the same in 99% of cases, but use the date of the audit if they diverge).
- `{category}` is the rule-group filter the run used: `performance` / `dry` / `security` / `architecture`. The Phase 1 rule-group filter maps 1:1 to this slug — `performance` → `performance`, `dry` → `dry`, `security` → `security`, `architecture` → `architecture`. Always lowercased.
- `{component}` (single-file mode only) is the resolved target's stem with the `nds-` prefix stripped: `_js/nds-accessibility.js` → `accessibility`. Lowercased, hyphens preserved (`nds-cooldown-button.js` → `cooldown-button`).
- `N` is the **per-scope save-order index**:
  - **Full-tree:** `(count of existing `*-{category}-audit-run-*.md` files at the full-tree pattern in `.claude/audit-reports/`) + 1`.
  - **Single-file:** `(count of existing `*-{component}-{category}-audit-run-*.md` files in `.claude/audit-reports/`) + 1`. Indexed per-(component, category) so "Run 2 vs Run 1 of accessibility performance" stays diffable independently of any full-tree run that landed between them.
- Index is per-category (and per-component for single-file), not global — comparing apples to apples is what makes "Diff vs. Run (N−1)" useful. Do NOT count `/nds-js-audit` invocations from the conversation that didn't get saved; the index reflects persisted artifacts only.

**Always include a frontmatter-style header** in the saved file so later audits can be diffed meaningfully:

```markdown
# Code Audit — {scope-line} — Run N

**Date:** YYYY-MM-DD
**Rule catalog version:** {single group e.g. "JSA" | combined e.g. "JSP + JSD + JSS + JSA"} (note any post-evolution refinements: "post-evolution: JSD-03 [400,2000] range, JSS-01 allowlist v3, JSA-13 80-line threshold")
**Invocation:** `/nds-js-audit {args}`

## Summary
…
```

`{scope-line}` is `` `_js/` (js mode) `` for full-tree runs, or `` `_js/nds-accessibility.js` (single-file mode) `` (use the actual file path) for single-file runs. Single-file reports MUST also surface the JSD-05 skip banner from Phase 1 verbatim when the run was `dry`.

**Diff-vs-prior section.** If `N ≥ 2`, append a "Diff vs. Run (N−1)" section at the bottom of the file listing what changed between this run and the last saved one: new findings, closed findings, re-classified findings, and any rule evolutions applied between runs. This is what makes multi-run refinement legible across time.

**Never overwrite.** Before writing, `ls .claude/audit-reports/` to confirm the target filename doesn't exist. If it does (e.g., two saves on the same day), bump the `-N` index.

**Writes go only to `.claude/audit-reports/`.** Never save reports inside `_js/`, the project root, or other Jekyll-tracked directories — the `.claude/` tree is outside the site build.

The "Gaps observed" and "Dead-rule candidates" sections are omitted when both logs are empty. Each proposal must be concrete: a specific rule ID to add/narrow/remove, with at least one example finding that motivates it. Hand-wavy proposals ("maybe add a rule for ...") do not belong here.

Findings are grouped by severity first (action priority), then by file (navigation), then by line. Rule IDs in brackets enable filter commands like "fix JSP".

If the run surfaces zero findings in a rule group, omit that section rather than printing an empty header.

---

## Phase 5: FIX (optional, user-approved)

Only enter this phase when the user replies with an explicit fix instruction. Recognized forms:

- `fix HIGH` / `fix MEDIUM` / `fix LOW` — catalog findings by severity (JSA findings at that severity are included only when `Type: actionable`)
- `fix JSP` / `fix JSD` / `fix JSS` / `fix JSA` — catalog findings by rule group. `fix JSA` applies every JSA actionable finding regardless of severity; tradeoff findings are always skipped.
- `fix <file>` — every actionable finding in one file (across JSP/JSD/JSS/JSA; JSA tradeoffs still skipped)
- `fix all` — every catalog finding marked actionable. Excludes JSD-05 promotion candidates (those go through `promote <api-name>`) and every JSA finding marked `Type: tradeoff` (those are acknowledged in the report but never auto-fixed).
- `promote <api-name>` — apply one JSD-05 promotion candidate (edits `_js/nds-core.js` to add the helper, then migrates every listed call site). Covered separately in "Applying promotion candidates" below — the flow diverges from plain fix batches because it spans core plus N files.
- `fix all then promote <api-name>` — chain a plain fix batch and one promotion in a single session. Each segment still follows its own file-by-file rhythm; the user `next`s through both.

**JSA findings follow the same file-by-file rhythm as JSP/JSD/JSS findings.** Each actionable JSA finding has a proposed `Fix:` line in the Phase 4 report; applying it goes through the same Apply → Rebundle → Agent review → Per-file summary → STOP-for-`next` cycle. When a file has findings across multiple rule groups, all in-scope findings (per the user's filter) are addressed in that file's slot before advancing. JSA tradeoff findings are never auto-fixed; they exist in the report to be acknowledged.

### Before applying (safety checkpoint)

Automated refactors carry regression risk. Before starting the first fix batch, tell the user:

> Recommended: create a git checkpoint now so any fix batch can be reverted cleanly — for example `git add -A && git commit -m "checkpoint before nds-js-audit fixes"`. The skill will NOT run git commands; this is your call.

If the user declines or ignores the reminder, proceed — they know their workflow.

### Application order (file-by-file with checkpoints)

Apply fixes **one file at a time**, not one rule group at a time. For each file in the user's batch:

0. **Perf baseline (only when the file has a measurable perf finding).** Before editing, capture a "before" measurement of the current (pre-fix) bundle following the "Performance measurement (before/after)" section of `PUPPETEER.md`. This step is mandatory for actionable JSP findings and for JSA perf findings whose metric is headless-measurable (`LayoutCount`/`RecalcStyleCount` for JSA-02/JSD-06, JS-heap-across-cycles for JSP-06/JSP-08/JSA-03, handler-invocation-count for JSP-01/JSP-02/JSP-07, longtask/init-time for JSP-10/JSA-01, in-flight-fetch-count for JSA-06). The baseline MUST be taken before the edit — once the fix lands and rebundles, the pre-fix number is gone. Record the numbers for the Phase 6 delta. Skip this step for non-perf findings and for perf fixes whose benefit isn't reliably measurable headless (note which, don't fabricate a baseline).
1. Apply every in-scope finding in that file (HIGH → MEDIUM → LOW within the file).
2. Re-run Phase 3 on that file: confirm every targeted finding is resolved and no new findings appeared.
3. Emit the Phase 6 per-file report — including the before/after perf delta for any baseline captured in step 0.
4. **Auto-advance when clean** (Auto-drive rule). If Phase 6's agent review passed with no regression, emit the one-line per-file summary and move straight to the next file — do NOT stop for `next`. **STOP only** on a regression (agent-review FAIL or a `regression in {file}` report) or an explicit `stop`.

A regression is the rollback grain — that's where the user checkpoints and reverts a single file cleanly. Filters (`fix HIGH`, `fix JSP`, etc.) decide which findings are in-scope; the rhythm is the same.

### Out-of-scope modifications

- Fix batches MAY edit `_js/nds-core.js` and `_js/nds-loader.js` when a finding's `Fix:` recommendation routes there. The common case is JSA-05 (drop `critical: true` from a component's registry entry in `_js/nds-loader.js`); JSD-05 promotion candidates also edit core, but they go through the separate `promote <api-name>` flow (see "Applying promotion candidates" below) because they coordinate core + N call sites and need a signature-confirmation step. When a fix touches core or loader as part of a component-file batch, the per-file closing summary MUST list every file edited under the `Cross-file edits` line — the user must never be surprised by a loader/core change. Do NOT make incidental core/loader edits outside a finding's recommendation: a JSP-01 fix in `_js/nds-foo.js` may only touch `_js/nds-foo.js` unless the rule's `Fix:` column explicitly routes elsewhere. Direct-named single-file audits of `_js/nds-core.js` or `_js/nds-loader.js` (e.g. `/nds-js-audit nds-core.js performance`) remain the right shape when the AUDITED file is core/loader itself — the carve-out is for deliberate review of those files, not the only path to fixing them.
- Do not run `bundle exec jekyll build` unless the user explicitly asks. The Phase 6 Puppeteer behavior test MAY start `bundle exec jekyll serve` (port 4002) when no server is already running — that test requires a live server, so running the audit's fix phase carries that authorization. Reuse an existing server; don't restart per file. See `PUPPETEER.md`.
- Do not reformat code, adjust whitespace, or rename unrelated identifiers. Keep diffs minimal so the user can verify each change at a glance.

### Applying promotion candidates (JSD-05)

When the user replies `promote <api-name>`, take the named JSD-05 candidate through a mini-batch that spans `_js/nds-core.js` + every call site listed in the Phase 4 report. The scope is larger than a normal fix batch (touches multiple files plus core) and the risk profile is different — a bug in a new core helper breaks every migrated call site at once — so the per-file rhythm is slightly adapted:

1. **Confirm the helper's signature** with the user before touching core. Summarize the API name, parameters, return shape, and any divergence from the original duplicated bodies (callers often have slightly different needs — e.g., one caller wants `respectNav: false`, another needs the raw measurements vs a pre-computed `flipUp`). Proceed only on explicit user approval.
2. **Edit `_js/nds-core.js`** to add the helper. Follow core's existing style: IIFE-scoped section, section-header comment banner (`// ── Name ─────`), usage example inside the comment block, same indent as peer helpers, positioned near structurally-similar helpers (observer-pool helpers near each other, utility fns near each other). Rebundle. Spawn an agent review focused on: (a) does the helper's body cover every caller's needs without forcing behavior change, (b) what migration-time constraints does each caller inherit (ordering, opts, staging), (c) edge cases in the helper itself (null inputs, detached elements, missing ambient state). Pause for user `next` before advancing. This pause is important — it's the cheapest point to revise the signature before N call sites commit to it.
3. **Migrate each call site one at a time.** For each file in the candidate's call-site list: apply the migration, rebundle, run agent review, emit the per-file closing summary (same template as plain fix batches), pause for `next`. Migration-specific agent-review questions to include: (i) semantic equivalence with the pre-migration inline code (produce identical values for every branch — with / without ambient DOM, RTL / LTR, etc.), (ii) any caller-specific parameters the helper requires (e.g., `respectNav: false`), (iii) ordering constraints the caller must preserve around the helper call (e.g., pre-staging DOM via `visibility:hidden;position:fixed` so the helper's measurements are non-zero), (iv) stale references / renamed instance fields from the refactor.
4. **Emit the batch summary** when the last call site migrates. Include: helper added (name + one-line description), call sites migrated, findings resolved (usually none — promotion is a refactor, not a fix; if a JSD-05-adjacent JSD-02/JSP-06 finding on one of the migrated files was folded into the same batch, list that too), and any follow-up the agent review surfaced that was out-of-scope for this batch.

Rollback grain: since promotions span core + N call sites, a regression in any one call site doesn't require reverting core. The user reverts the broken file only; the helper stays in core and the remaining migrated call sites stay migrated. Retry the broken migration later once the user and skill agree on what went wrong.

### Auto-rebundle after each file

Run `ruby _plugins/js_processor.rb` automatically at the end of each file fix — required after any `_js/` change per `CLAUDE.md`. Surface any rebundle errors in the per-file summary; on failure, stop the batch (do not proceed to the next file). Per-file rebundle matches the per-file fix rhythm so the user can test each file before the next lands.

---

## Phase 6: VERIFY

After every file's fixes land, confirm the following on that file before emitting the per-file summary:

- Phase 3 re-runs clean: every targeted finding is gone and no new findings appeared.
- Structural invariants hold: every new `NDS.on*` call stores its unsubscribe handle where teardown can reach it, and the loader registration still works.
- Identifier sanity: any fix that INJECTS new setup code (`new AbortController()`, new declarations, observer scaffolding) must use identifiers that exist in the enclosing scope. A typo (`selectElement._ac` vs `selectInput._ac`) throws at runtime BEFORE the listener attachments that follow — earlier code still ran, but half the component's interactions silently stop responding. After inserting such a block, verify every identifier against the function's parameter list and enclosing `const`/`let`/`var` declarations.
- Rebundle: run `ruby _plugins/js_processor.rb` automatically. Surface its output (bundle sizes or errors) in the per-file summary. If rebundle fails, stop the batch — do not proceed to the next file.
- **Agent review (mandatory):** after rebundle succeeds, spawn a review agent via the Agent tool with a self-contained brief. See the "Agent review" subsection below for the brief template. Wait for its report before emitting the per-file closing summary. Fold the result into the summary. If the agent flags a real regression, stop — do not produce a clean summary or advance to the next file.

### Agent review (mandatory per-file step)

After rebundle succeeds but before the per-file closing summary, spawn a review agent (`Agent` tool, `general-purpose` subagent) that does TWO things in one spawn: (1) a **static review** to catch silent failures the rule catalog can't see (semantic drift, typos in injected setup, call-site breakage, escape-context mismatches), and (2) a **Puppeteer behavior test** that actually drives the fixed component in headless Chrome to prove it still works — following the playbook in `PUPPETEER.md` (sibling to this file). Mandatory per fix batch — one-line changes are where typos hide best, and re-running the catalog proves only that the bad pattern is gone, never that the component still functions. Do not ask the user; always spawn. Verifying the fix is the skill's job, not the user's.

**Brief template:**

1. **What changed** — concise before/after snippets with file paths + line numbers.
2. **Reference anchors** — cite `_js/nds-core.js:<line>` for any core helper invoked.
3. **What to verify statically (under 200 words)** — 3–5 specific questions: semantic equivalence, call-site impact, edge cases (null/empty/missing options), escape/boundary semantics, typos / scope issues, plus a one-line catch-all.
4. **Behavior test (Puppeteer, mandatory)** — follow `PUPPETEER.md`: ensure the dev server is up on `http://localhost:4002`, launch the component in headless Chrome via `puppeteer-core`, reproduce the interaction for every rule that fired in this file, and assert the fix works with no console errors. Report each rule as `PASS` / `FAIL` / `not-headless-testable` with the exercised steps. The per-rule scenarios live in the "Behavior regression verification (per rule)" table below — automate the matching row, don't print it. **For perf findings**, also run the "after" measurement from `PUPPETEER.md`'s "Performance measurement (before/after)" section against the same scenario as the pre-edit baseline (the baseline numbers are supplied below in this brief), and report the before/after delta per measured rule — or the honest "structural / within-noise" result when there's no clean delta.
5. **Response format** — direct quoted `file:line` references for static findings; for the behavior test, the per-rule verdict block from `PUPPETEER.md`. Under-200-word cap on the static prose. No vague "looks fine".

**On report:**
- **Clean** → requires BOTH: static review found nothing AND every Puppeteer behavior test returned `PASS` (or a justified `not-headless-testable`). Add a one-line recap of each to the per-file summary's "Agent review" and "Puppeteer test" lines.
- **Regression found** — a static finding OR any Puppeteer `FAIL` (broken assertion or non-empty console errors) → do NOT emit a clean summary, do NOT advance. Report verbatim (quote the failing assertion / error text), propose follow-up or revert, wait. Overrides auto-`next`.

**Follow-up fixes.** Executable-code follow-ups re-run agent review on the new change. Documentation-only follow-ups (comments, docstrings, naming-in-comments) skip the re-test. Note the follow-up briefly in the summary's `Agent review:` line.

### Behavior regression verification (per rule)

Re-running the rule catalog proves the offending pattern is gone. It does NOT prove the component still works. NDS has no JS unit-test suite, so the review agent **drives the component itself** in headless Chrome via Puppeteer (`PUPPETEER.md`) rather than handing the user a checklist — verification is the skill's job. The per-rule table below is the **scenario source the agent automates**: for each rule that fired, the agent reproduces the "What to verify in the browser" interaction in a real browser and asserts the outcome (plus zero console errors).

Fall back to giving the user the checklist row **only** when the rule is `not-headless-testable` — a UI-shell piece with no demo page (`header`, `footer`, `mainnav`, theme, font-loading) or an interaction gated behind a browser-blocked user gesture. In that case name why it couldn't be driven and emit just that rule's row for the user to confirm; never present a `not-headless-testable` rule as if it passed.

For every rule ID that fired in the current file, emit the matching checklist row below.

| Rule | Regression risk | What to verify in the browser |
|---|---|---|
| JSP-01 (pooled resize) | Low — `NDS.onResize` is a transparent wrapper over window resize | Resize the window across breakpoints; the component must still adapt (layout mode switches, recomputed widths, etc.). |
| JSP-02 (RAF scroll throttle) | Low — RAF wrapping is idempotent when the handler is pure | Scroll fast with keyboard and wheel; confirm sticky/reveal behavior still triggers at the right scroll position with no visible lag or missed frames. |
| JSP-03 (NDS.onIntersect) | Low-Medium — pool groups by threshold/rootMargin; behavior is identical for same config | Scroll the element into and out of the viewport at different speeds; the intersection-driven action (counter animation, lazy-reveal, etc.) fires once and does not re-trigger on minor movements. |
| JSP-04 (NDS.onElementResize) | Low-Medium — pooled ResizeObserver fires on same events | Resize the host container (drag splitter, toggle sidebar, rotate device emulator); layout recalculates consistently. |
| JSP-05 (NDS.onDOMAdd/Remove/AttrChange) | **Medium-High** — MutationObserver semantics differ subtly when selector-scoped; fire timing may change | Dynamically add/remove/mutate the observed selector in devtools or via component API (add row to table, inject option into select, toggle attribute); the handler must fire exactly as before. Test edge cases: bulk insertions, removal during iteration. |
| JSP-06 (stored unsubscribe / AbortController) | **Medium** — this rule's fixes INJECT new setup code (`new AbortController()`, per-element guards, etc.) BEFORE the existing `addEventListener` loop. A typo in the injected setup (wrong identifier, wrong property name) throws at runtime and silently detaches nothing — but all subsequent `addEventListener` calls in that function are skipped, and the component's click/keyboard handlers never bind. Symptom: the component opens/closes normally (earlier listeners still work) but keyboard navigation or click-handled actions stop responding. | Two-part check. **(1) Functional smoke test**: exercise at least one primary interaction that flows through the loop — click one of the listener-attached elements AND press one key the handler listens for. If either silently does nothing, open devtools console and look for a `ReferenceError`/`TypeError` thrown during init. **(2) Leak test**: open and close/destroy the component repeatedly (open-close a modal 20x, navigate away and back); no listener leaks (check devtools Memory or listener count in Event Listeners panel). Part 1 catches silent-throw regressions in the injected setup; Part 2 catches the original pooling/teardown bug. |
| JSP-07 (NDS.debounce) | Low — exact delay preserved | Trigger the debounced event rapidly (type fast in a search field, resize rapidly); final callback fires once after the debounce window. |
| JSP-08 (observer teardown) | **Medium** — injecting `.disconnect()` into a teardown path is usually safe, but placement matters: disconnecting BEFORE the rest of teardown runs can race with synchronous DOM reads that depend on observer-driven state. | Re-init or destroy the owning component at least 10x while watching for retained-memory growth in devtools (Performance/Memory tab). Confirm the component still reacts to its observed mutations BEFORE teardown runs (e.g., for a forms msgElement observer, type into the field and verify the success/error placeholder still toggles). |
| JSD-09 (teardown handler storage) | **Low-Medium** — the fix adds new `this.handlers.{name} = ...` assignments; a typo in the key name silently re-creates the same dead-teardown shape. | Confirm the key used in teardown's array EXACTLY matches the key assigned in the binder (copy-paste safe). Exercise the listener's action at least once (click the button, fire the event) and confirm it still fires. Then destroy/re-init the component and confirm the old listener is actually removed (devtools Event Listeners panel). |
| JSD-01 (NDS.State) | **Medium** — `dataset.state = 'open'` clobbers other tokens; `NDS.State.add('open')` merges with existing tokens. Wrong helper silently breaks multi-state components | Exercise every state transition on the demo page (default → hover → pressed → disabled, open → closed → focused, etc.). Confirm the `data-state` attribute holds the right token set at each step (inspect in devtools). Keyboard and pointer paths both. |
| JSD-02 (NDS.Status) | Low — single-value attribute, helper is a direct replacement | Trigger the status-changing action (copy button, form submit); status pill/indicator appears and clears on the expected timeline. |
| JSD-03 (NDS.breakpoints) | Low — pixel values match the tokens | Resize across all breakpoints (mobile / tablet / desktop / large-desktop); responsive behavior switches at the same viewport widths. |
| JSD-04 (NDS.isRTL / NDS.lang) | **Medium** — live getter vs cached read. If a component cached the value and relied on cache, switching to `NDS.isRTL` makes it reactive, which may be the intent or an unintended change | Toggle the direction switcher on a page that uses the component; confirm layout flips cleanly and does not leak into other direction-unrelated features. |
| JSD-05 (promotion candidate) | **Medium-High** — a bug in the new core helper breaks every migrated call site at once. A wrong signature, a missing caller-specific opt (e.g., `respectNav: false`), or a subtle off-by-one in the returned measurements produces semantic drift that only shows up in specific caller contexts. Skipping the "confirm signature before editing core" step compounds this: once N call sites commit to the signature, changing it means migrating them again. | Exercise every migrated component's primary interaction under both ambient conditions the helper's opts cover. For a flip-position promotion: open dropmenu, tooltip, date-picker, and custom-select at both the top and bottom of the viewport; each should flip (or not) identically to the pre-promotion behavior. Test with AND without the ambient element the helper can respect (e.g., with AND without `.nds-main-nav` on the page for nav-aware helpers). For a scroll-close helper: scroll inside and outside the scope element for each component — inside must not close, outside must close. Leak test: open/close each migrated component 20+ times and confirm no document/window listener count growth in devtools Event Listeners panel. |
| JSD-06 (batch reads-before-writes) | Low — refactor preserves behavior when done correctly, but easy to get wrong | Component's animated transitions and measurements must still be visually correct: height animations expand to the right size, position calculations land in the right place. Compare before/after with reduced-motion on and off. |
| JSD-07 (component contract) | **Medium** — refactoring the public shape can break documentation-page JS tabs and consumer integrations | Open the component's doc page and exercise every snippet in the "JS API" tab. Confirm `NDS.{Name}.init()`, `.reinit()`, `.create(el)` all work as documented. |
| JSD-08 (dead defensive guards) | Low — removing `typeof NDS !== 'undefined'` hedges only prunes dead code since the loader guarantees NDS exists. Risk is a missed case where the component actually runs before the loader (module top-level code). | Confirm the guard you removed was inside a method or function invoked after init — not at module top level. Then load any page using the component; no ReferenceError should appear in devtools console. |
| JSS-01 (innerHTML XSS) | **Medium-High** — replacing `innerHTML = template` with `createElement` + `textContent` changes the DOM-construction path. Whitespace inside templates disappears; child-element structure can subtly reorder; event-listener attachment points move. | Exercise every visual variant the affected component supports — every `variant`/`status`/`display` option in one of its factory methods (e.g., `NDS.Alert.create({ variant: 'success' | 'error' | ... })`). Inspect the rendered DOM in devtools and confirm class names, aria attributes, and text content all match the pre-fix version. Then paste an XSS probe string (`<img src=x onerror=alert(1)>`) as the user-facing value: it must render as literal text, not execute. |
| JSS-02 (eval/Function removal) | Low — dynamic code paths are rare in UI; replacements should behave identically. | Trigger the code path that previously built the string: call the feature from the browser console with both a typical value and an unusual one (symbols, empty string, very long string). No syntax errors, no silent noops. |
| JSS-03 (target="_blank" + rel="noopener") | Low — pure attribute addition; no behavior change. | Click the affected link, confirm new tab still opens. In devtools, inspect the rendered `<a>` — the `rel` attribute must contain both `noopener` and `noreferrer`. Open the new tab from the console: `console.log(window.opener)` must return `null`. |
| JSS-04 (postMessage origin check) | **Medium** — if the expected origin list is wrong, legitimate messages get dropped silently. | Identify every page/iframe that sends messages to this handler and confirm its origin is in the allowlist. Trigger one known-good message and verify the handler fires. Then send a message from a different origin (open devtools on a different site and `window.postMessage(...)` to this page) — it must be silently dropped. |
| JSS-05 (URL scheme validation) | **Medium** — wrapping `window.open`/`location.assign` in scheme checks can reject URLs the user legitimately expected to work if the allowlist is too narrow. | Test every share/navigate path the component supports (`http`, `https`, `mailto`, `tel` if applicable) end-to-end. Then try `javascript:alert(1)` as the `data-*` attribute — it must be silently dropped with no script execution and no alert. Confirm no console errors for the blocked case. |
| JSS-06 (prototype-pollution safe-merge) | Low — `safeMerge` is a thin wrapper around `Object.assign` that filters three specific keys (`__proto__`, `constructor`, `prototype`) which never appear in legitimate state shapes. Behavior on any real-world payload is identical to the original Object.assign. | Two-part check. **(1) Rehydration smoke test**: with valid existing state in the writable store, reload the page — every persisted field (modes, settings, etc.) must rehydrate exactly as before; the component looks unchanged. **(2) Poison test**: in devtools, plant `localStorage.setItem('<key>', '{"__proto__":{"polluted":1},"<a-real-field>":"<a-real-value>"}')` (or the equivalent for sessionStorage / cookies depending on the source) and reload — the real-field value still rehydrates AND `({}).polluted` returns `undefined` in the console (no global pollution). If `({}).polluted` returns `1`, the filter didn't run. |
| JSA-* (generic architectural change) | **Medium-High** — JSA fixes are architectural (extract function, replace innerHTML structure, add LRU disposal, reroute scheduling primitive). Regression surface is the affected component as a whole, not just the pattern site. A structural refactor can shift call-site semantics, drop whitespace nodes, change initialization order, or alter event-binding timing in ways the rule's catalog signature doesn't reveal. | Exercise every primary interaction and visible variant of the component. For DOM-structure changes (JSA-07): inspect the rendered DOM in devtools and compare class names, ARIA attributes, child ordering, and text content against the pre-fix version. For caching/disposal fixes (JSA-03): exercise the create→destroy→create cycle 20+ times and confirm no memory growth in devtools Memory panel. For function extraction (JSA-13): confirm every extracted sub-method is reachable on the orchestrator's primary code path; nothing is dead-code'd by the split. |
| JSA-07 (innerHTML → replaceChildren) | **Medium-High** — same risk profile as JSS-01: swapping the DOM-construction path can change whitespace handling, child-element ordering, and event-listener attachment points. The browser's HTML parser normalizes whitespace and creates implicit nodes (e.g., text nodes around block elements) that `createElement` does not. | Inspect the rendered DOM in devtools before and after the fix. Confirm class names, ARIA attributes, child node count, and text content match. If the original template inlined event-listener attributes (`onclick=`), confirm event-binding still fires — `replaceChildren` does not run inline-attribute event handlers the way `innerHTML` parsing does. |
| JSA-13 (mixed-concern function extraction) | **Medium-High** — extracting sub-methods from a >80-line function changes the call graph. A typo in one of the new method names throws at runtime; a missed `this` rebind in an extracted method silently breaks state mutation. The orchestrator's outer flow must still drive every concern in the right order. | Exercise the component's primary interaction that flows through the extracted function end-to-end. Confirm each extracted sub-method is invoked on the right code path (set a breakpoint in each `_render()` / `_persistState()` / etc. and confirm they all hit). Then exercise an edge-case path (error branch, empty state, hidden state) and confirm the same orchestrator flow still works — extraction can accidentally remove a guard or a side effect that was implicit in the straight-line code. |

Include only rows for rules that fired in this fix batch. Do not dump the whole table every time — that trains the user to skip it. For JSA findings, the generic JSA-* row covers any rule without a more specific row; use the specific row when it exists.

### Per-file closing summary

After each file's fixes are applied and the catalog is re-run, emit this and STOP:

```
## {file} — applied {N} fixes
- Findings resolved: {list rule IDs with line numbers}
- Cross-file edits: {list every additional file edited as part of this slot's fixes, with the rule ID that drove each edit — e.g., "`_js/nds-loader.js:158–163` (JSA-05 dropped `critical: true`)". If only `{file}` was touched, write "none". MANDATORY when a fix routes into `_js/nds-core.js` or `_js/nds-loader.js` — the user must see exactly what landed outside the audited file.}
- Findings remaining in this file: {M} (list rule IDs, or "none")
- Regressions introduced: 0 (or list them)
- Rebundle: ran `ruby _plugins/js_processor.rb` — {bundle size line from output}
- Agent review (static): {one-line recap — "clean" OR concise summary of any non-blocking notes; if a regression was found this block wouldn't be emitted at all}
- Puppeteer test: {surface driven (e.g. `http://localhost:4002/components/alert.html`) + per-rule verdict, e.g. "JSP-02 PASS, JSD-01 PASS; 0 console errors"}
- Perf delta: {ONLY for files with a perf finding — the before/after measurement per measured rule, e.g. "JSP-02: scroll-handler invocations over 100 events — before 100, after 7 (−93%)". Use "structural — not micro-benchmarkable headless" or "no significant change (within noise)" when honest measurement yields no clean delta. Omit this line entirely for files with no perf findings.}

## Behavior checks for this file (automated via Puppeteer)
Rules fixed: {JSP-02, JSD-01}
The review agent drove these in headless Chrome — results above. Listed here so you can re-confirm in your own environment if you want:
- JSP-02: {what the agent reproduced + asserted, from PUPPETEER.md}
- JSD-01: {what the agent reproduced + asserted}
{If any rule was `not-headless-testable`, list it here with the reason and the manual checklist row for the user to confirm — do NOT mark it passed.}

Reply with a number OR the literal command:

1. `next` — move on to the next file in the batch.
2. `regression in {file}` (type the literal with what broke) — pause, revert with your IDE's undo/local-history (not git), and re-examine the finding together.
3. `stop` — end the batch here; remaining files keep their existing findings unfixed.
```

Auto-chain to the next file when the agent review is clean — emit the compact one-line summary and continue without waiting for `next` (Auto-drive rule). Show the numbered `next`/`regression`/`stop` prompt ONLY when paused: on a regression, or at end-of-batch. A regression is the checkpoint-and-revert window. If the user reports a regression, do not try to patch around the broken fix. Ask them to revert the file using whichever workflow they use (their IDE's local-history or undo stack, not `git checkout`/`git restore` — their editor version can differ from HEAD), then re-examine the finding together before retrying.

### End-of-batch summary

When the user replies `stop`, or when every file in the batch has been processed, emit:

```
## Code audit batch complete
- Files touched: N
- Total findings resolved: X
- Findings remaining (all files): Y (list rule IDs per file)
- Regressions reported: 0 (or list them)
```

Do not mark the audit complete while findings remain open or regressions exist.

---

## Phase 7: EVOLVE (autonomous, runs at run end)

**Runs automatically — no approval prompt.** At the end of any run that surfaced concrete "Gaps observed", "Dead-rule candidates", or "Persona drift" entries, turn them into edits to this `SKILL.md` and/or `PERSONA.md`, apply them, and append a `## Catalog evolved` diff block to the report. The user reverts via git if they disagree (the diff block makes that one command). Self-evolution is not a pause point (see Auto-drive).

**Three sources, two target files:**
- **Gaps observed** → SKILL.md (new catalog rule, or extension of an existing rule).
- **Dead-rule candidates** → SKILL.md (narrow / delete an existing catalog rule).
- **Persona drift** → PERSONA.md (revise a canonical, add a carve-out, add a new entry). Surfaced when a JSD-15 finding's outcome is "revise the canonical" rather than "migrate the file."

**Quality bars (the safety floor that replaces the old approval gate — apply only edits that clear them):**
- **ADD rule** — requires a motivating finding with ≥2 sites or meaningful body overlap. A one-off shape stays a Gap observation, not a new rule.
- **NARROW / SEVERITY** — requires a specific cited reason (e.g. N consecutive skips in a named file).
- **DELETE rule** — requires the dead-rule evidence bar: zero true-positive matches across the session's emitted/saved runs. Rare by design; when unproven, narrow rather than delete.

A run with only hand-wavy or single-instance candidates auto-applies nothing — it records them in the report's Gaps/Dead-rule sections for a future run to confirm, and omits the `## Catalog evolved` block.

### What each applied edit records

For every edit applied, record the exact change in the `## Catalog evolved` block using these shapes:

```
Edit: ADD rule
  Group: JSP
  ID: JSP-11 (next free ID in group)
  Severity: MEDIUM
  Detect: IntersectionObserver created with `root: null` via a locally-pooled helper — duplicates NDS.onIntersect's viewport pool
  Fix: Route through NDS.onIntersect with matching threshold/rootMargin
  Motivating finding: _js/nds-foo.js:88 and _js/nds-bar.js:142 both wrap viewport IntersectionObservers in a local helper
```

```
Edit: NARROW rule
  ID: JSD-08
  Change: Exempt methods that run at module top level (before the loader lifecycle fires)
  Reason: 5 consecutive skips in _js/nds-foo.js — the guards protect bootstrap code that genuinely runs before nds-loader.js. Keeping the rule as-is trains the skill to ignore JSD-08 wholesale.
```

```
Edit: ADD rule
  Group: JSA
  ID: JSA-14 (next free ID in group)
  Severity: LOW
  Mode: deep-read
  Detect: Modules whose top-level state (module-scope `let`/`const` declarations) exceeds 20 mutable bindings, indicating module-level state machine that should be encapsulated in a singleton or class
  Fix: Encapsulate module state in an object literal or class instance; expose only the documented API surface; private state behind a closure or `#private` field
  Motivating finding: _js/nds-foo.js has 27 module-scope `let` declarations driving cross-method behavior — readers can't tell which functions read or mutate which variables without a top-to-bottom file scan
```

```
Edit: DELETE rule
  ID: (example) JSD-06
  Reason: Zero true-positive matches across the session's runs; read-after-write pattern is already caught by the browser devtools performance panel and does not warrant its own rule.
```

(JSA proposals always include the `Mode:` field — `greppable` or `deep-read` — since JSA rules carry mode-aware coverage that JSP/JSD/JSS don't. JSP/JSD/JSS proposals omit the Mode field.)

### Application

For each applied edit, modify the appropriate file under `.claude/skills/nds-js-audit/` and only that file:

**Catalog edits → SKILL.md:**
- **ADD**: insert the new rule in the correct rule-group table (JSP / JSD / JSS / JSA), keeping IDs sequential. JSA additions also populate the `Mode` column (`greppable` or `deep-read`); if the new rule is deep-read, also add its ID to the JSA deep-read banner in Phase 4 and the "JSA deep-read rules in full-tree mode" section in Phase 1.
- **NARROW / REFINE**: edit the "What to detect" column, not the rule's identity. Do not change its ID. For JSA rules, also update the `Mode` column if the refinement changes detection method.
- **DELETE**: remove the row and renumber the remaining rules in the group. If JSD-06 is deleted, JSD-07 becomes JSD-06 across the file (update the Phase 4 example references accordingly). For JSA deletions, also remove the ID from the deep-read banner in Phase 4 and the "JSA deep-read rules in full-tree mode" section in Phase 1 if it appeared there.
- **SEVERITY CHANGE**: update the Severity column only; do not touch detection or fix text.

**Persona edits → PERSONA.md:**
- **REVISE CANONICAL**: edit the entry's "Canonical" field; update "Why this canonical" if the revised choice has a different principle defense; update "Why not the alternatives" if the rejected forms changed; touch "Audit behavior" to match the new canonical. Do NOT touch "Current adoption" — it updates from corpus tally on the next full-tree run.
- **ADD CARVE-OUT**: append to the entry's "Carve-outs" list with the file:line citation that motivated it.
- **NEW ENTRY**: append a new entry to PERSONA.md using the six-field shape (Canonical, Why this canonical, Why not the alternatives, Carve-outs, Audit behavior, Current adoption). Renumber subsequent entries if added mid-document.

After applying, append the diff to the report as the `## Catalog evolved` block (covering both files when both were touched):

```
## Catalog evolved (auto-applied this run)
- SKILL.md added: JSP-08 "viewport IntersectionObserver duplication" (MEDIUM)
- SKILL.md narrowed: JSD-08 now exempts module-top-level bootstrap
- PERSONA.md revised: entry 4 canonical changed from "NDS <Name>:" to "[NDS.<Name>]"
- PERSONA.md added carve-out: entry 1, secondary controllers for streaming responses
- Rejected (conflicts with CLAUDE.md): {proposal} — {one-line reason}
- No deletions this round
Revert with `git checkout -- .claude/skills/nds-js-audit/` if you disagree. The next audit run uses the updated rules and canonicals.
```

### Guardrails

- Apply only edits that clear the quality bars above; everything else stays a recorded Gap/Dead-rule candidate for a future run to confirm. The bars — not a user prompt — are the gate. Every applied edit MUST appear in the `## Catalog evolved` block, so the change is never silent.
- Phase 7 edits SKILL.md and PERSONA.md only. Phase 5 handles source-code fixes; Phase 7 handles the rule catalog and the persona. Never edit other files during Phase 7.
- If a proposed evolution would conflict with the conventions in `CLAUDE.md` (e.g., "weaken JSP-01 to allow raw window resize listeners"), do NOT apply it — record it on the `Rejected (conflicts with CLAUDE.md)` line of the `## Catalog evolved` block with the reason. `CLAUDE.md` is upstream of this skill.
- Preserve each file's structure: SKILL.md keeps frontmatter, mindset, phases, rule tables, report format, non-goals; PERSONA.md keeps the six-field per-entry shape. Don't invent new top-level sections during an evolution.
- Persona-drift proposals MUST include the divergent file's reasoning (cited inline) so the user can argue the canonical change from data, not from authorial preference. A proposal that says only "the corpus moved" is rejected — corpus movement is a migration target, not a canonical revision.

---

## Non-goals

This skill deliberately does not cover:

- SCSS audits. SCSS coverage was removed because refactor blast radius (specificity, nesting, state/variant cascade) and token-family gaps made automated fixes too risky. Refactor SCSS manually against the conventions in `CLAUDE.md`.
- Documentation page audits — use `nds-doc`.
- Icon, placeholder-text, or sidemenu coverage.
- `_data/content/` YAML demo data.
- UX copy, hero descriptions, or visual design review.
- Replacing eslint. No lint config is authored; the rules are NDS-specific conventions enforced through an agent loop.
- Profiler-driven *detection*. The JSP/JSA catalogs are static-read heuristics — they surface candidates, not hotspots, and don't replace a DevTools Performance / Lighthouse pass. (The Phase 6 perf *verification* is different: a bounded before/after measurement of one applied fix, not a profiler sweep.)
- Including `_js/nds-core.js` or `_js/nds-loader.js` in full-tree `js` runs. Single-file audits on either ARE supported — see the "Excluded files" carve-out in Phase 1.
- Running `bundle exec jekyll build` unless the user explicitly asks. `ruby _plugins/js_processor.rb` runs automatically per-file in Phase 5/6, and the Phase 6 Puppeteer test MAY start/reuse `bundle exec jekyll serve` (port 4002) — see `PUPPETEER.md`.
- Editing files outside the Phase 5 / Phase 7 / audit-reports paths. Phase 5 edits `_js/nds-*.js`, plus `_js/nds-loader.js` / `_js/nds-core.js` only when a finding's `Fix:` routes there (JSA-05 registry edits; `promote <api-name>` candidates). Phase 7 auto-edits this `SKILL.md` and `PERSONA.md`. Phase 4 `save` writes (never overwrites) reports in `.claude/audit-reports/`. Nothing else.
