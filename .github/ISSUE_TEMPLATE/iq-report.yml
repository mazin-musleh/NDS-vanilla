name: IQ report
description: Consumer findings from installing NDS IQ. Attach your NDS-REPORT.md and add anything else worth flagging.
title: "[IQ]: "
labels: ["iq", "triage"]
body:
  - type: textarea
    id: report
    attributes:
      label: Report
      description: Drag your `NDS-REPORT.md` into this area. It already names the NDS version and the instructions version. Add any extra context above or below the attachment.
      placeholder: (drag NDS-REPORT.md here)
    validations:
      required: true
